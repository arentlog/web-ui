'use strict';

/**
 * Provides global access to the PvUser record. This does not have an API, but simply gives access to the record itself.
 */
app.factory('PvUser', function PvUser() {
   return {};
});
