'use strict';

/**
 * Mapping file from menu function to angular state. This is how links are generated inside the navigation menu.
 */
app.factory('MenuStateMap', function MenuStateMap() {

   return {
      // TODO: if needed, we can do specialized states via this map
      //"arsc": "arsc.some-custom-state"
   };

});
