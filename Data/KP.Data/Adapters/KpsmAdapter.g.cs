//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.KP.Data.Adapters
{
   using com.infor.sxproxy.kpproxy;
   using com.infor.sxproxy.kpproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdskpsm;
   using Models.Pdskptallymixlookup;

   public partial class KpsmAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdskpsm";
      private KPProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> kpsmTableControlKey;
		private readonly Tuple<string, string> icspTableControlKey;
      private pdskpsmDataSet dataSet;
        
      public KpsmAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new KPProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdskpsmDataSet() { DataSetName = DataSetName };
            this.kpsmTableControlKey = this.dataSet.ttblkpsm.GetTableControlParametersKey();
            this.icspTableControlKey = this.dataSet.ttblicsp.GetTableControlParametersKey();
            if (!this.tempTableControlParameters.ContainsKey(this.kpsmTableControlKey))
            {
               this.CreateTableControlParameters(this.kpsmTableControlKey);
            }
            if (!this.tempTableControlParameters.ContainsKey(this.icspTableControlKey))
            {
               this.CreateTableControlParameters(this.icspTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in KpsmAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, bool fillMode, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Kpsmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, fillMode, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poKpsmproxy = this.proxyAppObject.CreatePO_kpsmproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poKpsmproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Kpsmproxy - After Call");
      }
      
      private void SetAndFetch(string where, bool fillMode, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.kpsmTableControlKey, where, batchsize, fldList);
         this.SetFetchWhereParameters(this.icspTableControlKey, fillMode);
         this.SetTableParametersOnContext(this.kpsmTableControlKey, true);
         this.SetTableParametersOnContext(this.icspTableControlKey);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Kpsmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poKpsmproxy = this.proxyAppObject.CreatePO_kpsmproxy())
               {
                  this.SetRequiredContextParameters();
                  poKpsmproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Kpsmproxy - After Call");
      }
   

      public Kpsm GetByRowId(string rowId, bool fillMode, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fillMode, fldList);
         Kpsm kpsm = null;
         if (row != null)
         {
             kpsm = this.BuildFromRow(row);
         }
         return kpsm;
      }
   
      private DataRow GetRowByRowId(string rowId, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(kpsm) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), fillMode, 1, fldList);
         return this.dataSet.ttblkpsm.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Kpsm> GetListByRowIdList(List<string> rowIds, bool fillMode, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(kpsm)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), fillMode, batchsize, fldList);
      }

      protected Kpsm Fetch(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         var row = this.dataSet.ttblkpsm.AsEnumerable().SingleOrDefault();
         Kpsm kpsm = null;
         if (row != null)
         {
             kpsm = this.BuildFromRow(row);
         }
         return kpsm;
      }
	  
	  
      public IEnumerable<Kpsm> GetListAllByCono(int cono, bool fillMode, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("kpsm.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<Kpsm> GetList(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblkpsm.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Kpsm Get(int cono, string prod, string whse, string comprod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("kpsm.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND kpsm.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND kpsm.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(comprod)) 
         {
             sb.AppendFormatWithEscape(" AND kpsm.comprod = '{0}'", comprod);
         }
         var where = sb.ToString();
         return this.Fetch(where, fillMode, batchsize, fldList);
      }
  
      public IEnumerable<Kpsm> GetListByCompprod(int cono, string comprod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("kpsm.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(comprod)) 
         {
             sb.AppendFormatWithEscape(" AND kpsm.comprod = '{0}'", comprod);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public Kpsm BuildFromRow(DataRow row)
      {
         var returnRecord = Kpsm.BuildKpsmFromRow(row);
         returnRecord = this.BuildExtraFromRow<Kpsm>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Kpsm record)
      {
         Kpsm.UpdateRowFromKpsm(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Kpsm Insert(Kpsm record)
      {
         DataRow row = this.dataSet.ttblkpsm.NewttblkpsmRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblkpsm.AddttblkpsmRow((pdskpsmDataSet.ttblkpsmRow)row);
         this.SaveChanges();
         return this.dataSet.ttblkpsm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkpsm.Rows[0]) : null;
      }
  

      public Kpsm Update(Kpsm record)
      {
        var row = this.GetRowByRowId(record.rowID, false, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblkpsm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkpsm.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Kpsm record)
      {
         var row = this.GetRowByRowId(record.rowID, false, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblkpsm.NewttblkpsmRow();
            Kpsm.BuildMinimalRow(ref row, record);
            this.dataSet.ttblkpsm.AddttblkpsmRow((pdskpsmDataSet.ttblkpsmRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Kpsm() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public IEnumerable<Kptallymixlookupresults> Lookup(Kptallymixlookupcriteria kptallymixlookupcriteria)
      {   
         var results = new List<Kptallymixlookupresults>();
         
         var pdskptallymixlookup = new pdskptallymixlookupDataSet();
            
         DataRow ttblkptallymixlookupcriteriaCriteria = pdskptallymixlookup.ttblkptallymixlookupcriteria.NewttblkptallymixlookupcriteriaRow();
         Kptallymixlookupcriteria.UpdateRowFromKptallymixlookupcriteria(ref ttblkptallymixlookupcriteriaCriteria, kptallymixlookupcriteria);
         pdskptallymixlookup.ttblkptallymixlookupcriteria.AddttblkptallymixlookupcriteriaRow((pdskptallymixlookupDataSet.ttblkptallymixlookupcriteriaRow)ttblkptallymixlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
   
         
         NLogLoggerP.Trace("Lookup - Kpsm - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poKpsmproxy = this.proxyAppObject.CreatePO_kpsmproxy())
               {
                   this.SetRequiredContextParameters();
                   poKpsmproxy.Lookup(ref pdsContext, ref pdskptallymixlookup, out cErrorMessage);
               }
            });
         NLogLoggerP.Info("Lookup - Kpsm - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdskptallymixlookup); 
    
            foreach (DataRow row in pdskptallymixlookup.ttblkptallymixlookupresults)
            {
                results.Add(Kptallymixlookupresults.BuildKptallymixlookupresultsFromRow(row));
            }
            return results;
        
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  