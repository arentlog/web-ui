//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.KP.Data.Adapters
{
   using com.infor.sxproxy.kpproxy;
   using com.infor.sxproxy.kpproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdskpso;
   using Models.Pdskpoptionlookup;

   public partial class KpsoAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdskpso";
      private KPProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> kpsoTableControlKey;
		private readonly Tuple<string, string> icspTableControlKey;
      private pdskpsoDataSet dataSet;
        
      public KpsoAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new KPProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdskpsoDataSet() { DataSetName = DataSetName };
            this.kpsoTableControlKey = this.dataSet.ttblkpso.GetTableControlParametersKey();
            this.icspTableControlKey = this.dataSet.ttblicsp.GetTableControlParametersKey();
            if (!this.tempTableControlParameters.ContainsKey(this.kpsoTableControlKey))
            {
               this.CreateTableControlParameters(this.kpsoTableControlKey);
            }
            if (!this.tempTableControlParameters.ContainsKey(this.icspTableControlKey))
            {
               this.CreateTableControlParameters(this.icspTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in KpsoAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, bool fillMode, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Kpsoproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, fillMode, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poKpsoproxy = this.proxyAppObject.CreatePO_kpsoproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poKpsoproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Kpsoproxy - After Call");
      }
      
      private void SetAndFetch(string where, bool fillMode, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.kpsoTableControlKey, where, batchsize, fldList);
         this.SetFetchWhereParameters(this.icspTableControlKey, fillMode);
         this.SetTableParametersOnContext(this.kpsoTableControlKey, true);
         this.SetTableParametersOnContext(this.icspTableControlKey);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Kpsoproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poKpsoproxy = this.proxyAppObject.CreatePO_kpsoproxy())
               {
                  this.SetRequiredContextParameters();
                  poKpsoproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Kpsoproxy - After Call");
      }
   

      public Kpso GetByRowId(string rowId, bool fillMode, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fillMode, fldList);
         Kpso kpso = null;
         if (row != null)
         {
             kpso = this.BuildFromRow(row);
         }
         return kpso;
      }
   
      private DataRow GetRowByRowId(string rowId, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(kpso) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), fillMode, 1, fldList);
         return this.dataSet.ttblkpso.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Kpso> GetListByRowIdList(List<string> rowIds, bool fillMode, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(kpso)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), fillMode, batchsize, fldList);
      }

      protected Kpso Fetch(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         var row = this.dataSet.ttblkpso.AsEnumerable().SingleOrDefault();
         Kpso kpso = null;
         if (row != null)
         {
             kpso = this.BuildFromRow(row);
         }
         return kpso;
      }
	  
	  
      public IEnumerable<Kpso> GetListAllByCono(int cono, bool fillMode, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("kpso.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<Kpso> GetList(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblkpso.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Kpso Get(int cono, string optionname, int seqno, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("kpso.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(optionname)) 
         {
             sb.AppendFormatWithEscape(" AND kpso.optionname = '{0}'", optionname);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND kpso.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, fillMode, batchsize, fldList);
      }
  
      public IEnumerable<Kpso> GetListByComprod(int cono, string comprod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("kpso.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(comprod)) 
         {
             sb.AppendFormatWithEscape(" AND kpso.comprod = '{0}'", comprod);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public Kpso BuildFromRow(DataRow row)
      {
         var returnRecord = Kpso.BuildKpsoFromRow(row);
         returnRecord = this.BuildExtraFromRow<Kpso>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Kpso record)
      {
         Kpso.UpdateRowFromKpso(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Kpso Insert(Kpso record)
      {
         DataRow row = this.dataSet.ttblkpso.NewttblkpsoRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblkpso.AddttblkpsoRow((pdskpsoDataSet.ttblkpsoRow)row);
         this.SaveChanges();
         return this.dataSet.ttblkpso.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkpso.Rows[0]) : null;
      }
  

      public Kpso Update(Kpso record)
      {
        var row = this.GetRowByRowId(record.rowID, false, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblkpso.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkpso.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Kpso record)
      {
         var row = this.GetRowByRowId(record.rowID, false, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblkpso.NewttblkpsoRow();
            Kpso.BuildMinimalRow(ref row, record);
            this.dataSet.ttblkpso.AddttblkpsoRow((pdskpsoDataSet.ttblkpsoRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Kpso() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public IEnumerable<Kpoptionlookupresults> Lookup(Kpoptionlookupcriteria kpoptionlookupcriteria)
      {   
         var results = new List<Kpoptionlookupresults>();
         
         var pdskpoptionlookup = new pdskpoptionlookupDataSet();
            
         DataRow ttblkpoptionlookupcriteriaCriteria = pdskpoptionlookup.ttblkpoptionlookupcriteria.NewttblkpoptionlookupcriteriaRow();
         Kpoptionlookupcriteria.UpdateRowFromKpoptionlookupcriteria(ref ttblkpoptionlookupcriteriaCriteria, kpoptionlookupcriteria);
         pdskpoptionlookup.ttblkpoptionlookupcriteria.AddttblkpoptionlookupcriteriaRow((pdskpoptionlookupDataSet.ttblkpoptionlookupcriteriaRow)ttblkpoptionlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
   
         
         NLogLoggerP.Trace("Lookup - Kpso - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poKpsoproxy = this.proxyAppObject.CreatePO_kpsoproxy())
               {
                   this.SetRequiredContextParameters();
                   poKpsoproxy.Lookup(ref pdsContext, ref pdskpoptionlookup, out cErrorMessage);
               }
            });
         NLogLoggerP.Info("Lookup - Kpso - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdskpoptionlookup); 
    
            foreach (DataRow row in pdskpoptionlookup.ttblkpoptionlookupresults)
            {
                results.Add(Kpoptionlookupresults.BuildKpoptionlookupresultsFromRow(row));
            }
            return results;
        
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  