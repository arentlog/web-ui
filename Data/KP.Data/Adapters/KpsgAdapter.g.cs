//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.KP.Data.Adapters
{
   using com.infor.sxproxy.kpproxy;
   using com.infor.sxproxy.kpproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdskpsg;
   using Models.Pdskpgrouplookup;

   public partial class KpsgAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdskpsg";
      private KPProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> kpsgTableControlKey;
		private readonly Tuple<string, string> icspTableControlKey;
      private pdskpsgDataSet dataSet;
        
      public KpsgAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new KPProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdskpsgDataSet() { DataSetName = DataSetName };
            this.kpsgTableControlKey = this.dataSet.ttblkpsg.GetTableControlParametersKey();
            this.icspTableControlKey = this.dataSet.ttblicsp.GetTableControlParametersKey();
            if (!this.tempTableControlParameters.ContainsKey(this.kpsgTableControlKey))
            {
               this.CreateTableControlParameters(this.kpsgTableControlKey);
            }
            if (!this.tempTableControlParameters.ContainsKey(this.icspTableControlKey))
            {
               this.CreateTableControlParameters(this.icspTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in KpsgAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, bool fillMode, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Kpsgproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, fillMode, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poKpsgproxy = this.proxyAppObject.CreatePO_kpsgproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poKpsgproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Kpsgproxy - After Call");
      }
      
      private void SetAndFetch(string where, bool fillMode, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.kpsgTableControlKey, where, batchsize, fldList);
         this.SetFetchWhereParameters(this.icspTableControlKey, fillMode);
         this.SetTableParametersOnContext(this.kpsgTableControlKey, true);
         this.SetTableParametersOnContext(this.icspTableControlKey);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Kpsgproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poKpsgproxy = this.proxyAppObject.CreatePO_kpsgproxy())
               {
                  this.SetRequiredContextParameters();
                  poKpsgproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Kpsgproxy - After Call");
      }
   

      public Kpsg GetByRowId(string rowId, bool fillMode, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fillMode, fldList);
         Kpsg kpsg = null;
         if (row != null)
         {
             kpsg = this.BuildFromRow(row);
         }
         return kpsg;
      }
   
      private DataRow GetRowByRowId(string rowId, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(kpsg) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), fillMode, 1, fldList);
         return this.dataSet.ttblkpsg.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Kpsg> GetListByRowIdList(List<string> rowIds, bool fillMode, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(kpsg)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), fillMode, batchsize, fldList);
      }

      protected Kpsg Fetch(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         var row = this.dataSet.ttblkpsg.AsEnumerable().SingleOrDefault();
         Kpsg kpsg = null;
         if (row != null)
         {
             kpsg = this.BuildFromRow(row);
         }
         return kpsg;
      }
	  
	  
      public IEnumerable<Kpsg> GetListAllByCono(int cono, bool fillMode, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("kpsg.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<Kpsg> GetList(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblkpsg.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Kpsg Get(int cono, string groupname, int seqno, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("kpsg.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(groupname)) 
         {
             sb.AppendFormatWithEscape(" AND kpsg.groupname = '{0}'", groupname);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND kpsg.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, fillMode, batchsize, fldList);
      }
  
      public IEnumerable<Kpsg> GetListByComprod(int cono, string comprod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("kpsg.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(comprod)) 
         {
             sb.AppendFormatWithEscape(" AND kpsg.comprod = '{0}'", comprod);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public Kpsg BuildFromRow(DataRow row)
      {
         var returnRecord = Kpsg.BuildKpsgFromRow(row);
         returnRecord = this.BuildExtraFromRow<Kpsg>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Kpsg record)
      {
         Kpsg.UpdateRowFromKpsg(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Kpsg Insert(Kpsg record)
      {
         DataRow row = this.dataSet.ttblkpsg.NewttblkpsgRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblkpsg.AddttblkpsgRow((pdskpsgDataSet.ttblkpsgRow)row);
         this.SaveChanges();
         return this.dataSet.ttblkpsg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkpsg.Rows[0]) : null;
      }
  

      public Kpsg Update(Kpsg record)
      {
        var row = this.GetRowByRowId(record.rowID, false, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblkpsg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkpsg.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Kpsg record)
      {
         var row = this.GetRowByRowId(record.rowID, false, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblkpsg.NewttblkpsgRow();
            Kpsg.BuildMinimalRow(ref row, record);
            this.dataSet.ttblkpsg.AddttblkpsgRow((pdskpsgDataSet.ttblkpsgRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Kpsg() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public IEnumerable<Kpgrouplookupresults> Lookup(Kpgrouplookupcriteria kpgrouplookupcriteria)
      {   
         var results = new List<Kpgrouplookupresults>();
         
         var pdskpgrouplookup = new pdskpgrouplookupDataSet();
            
         DataRow ttblkpgrouplookupcriteriaCriteria = pdskpgrouplookup.ttblkpgrouplookupcriteria.NewttblkpgrouplookupcriteriaRow();
         Kpgrouplookupcriteria.UpdateRowFromKpgrouplookupcriteria(ref ttblkpgrouplookupcriteriaCriteria, kpgrouplookupcriteria);
         pdskpgrouplookup.ttblkpgrouplookupcriteria.AddttblkpgrouplookupcriteriaRow((pdskpgrouplookupDataSet.ttblkpgrouplookupcriteriaRow)ttblkpgrouplookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
   
         
         NLogLoggerP.Trace("Lookup - Kpsg - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poKpsgproxy = this.proxyAppObject.CreatePO_kpsgproxy())
               {
                   this.SetRequiredContextParameters();
                   poKpsgproxy.Lookup(ref pdsContext, ref pdskpgrouplookup, out cErrorMessage);
               }
            });
         NLogLoggerP.Info("Lookup - Kpsg - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdskpgrouplookup); 
    
            foreach (DataRow row in pdskpgrouplookup.ttblkpgrouplookupresults)
            {
                results.Add(Kpgrouplookupresults.BuildKpgrouplookupresultsFromRow(row));
            }
            return results;
        
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  