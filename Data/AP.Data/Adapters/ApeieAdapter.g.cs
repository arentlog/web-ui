//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.AP.Data.Adapters
{
   using com.infor.sxproxy.approxy;
   using com.infor.sxproxy.approxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsapeie;

   public partial class ApeieAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsapeie";
      private APProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> apeieTableControlKey;
		
      private pdsapeieDataSet dataSet;
        
      public ApeieAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new APProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsapeieDataSet() { DataSetName = DataSetName };
            this.apeieTableControlKey = this.dataSet.ttblapeie.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.apeieTableControlKey))
            {
               this.CreateTableControlParameters(this.apeieTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ApeieAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Apeieproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poApeieproxy = this.proxyAppObject.CreatePO_apeieproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poApeieproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Apeieproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.apeieTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.apeieTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Apeieproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poApeieproxy = this.proxyAppObject.CreatePO_apeieproxy())
               {
                  this.SetRequiredContextParameters();
                  poApeieproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Apeieproxy - After Call");
      }
   

      public Apeie GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Apeie apeie = null;
         if (row != null)
         {
             apeie = this.BuildFromRow(row);
         }
         return apeie;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(apeie) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblapeie.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Apeie> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(apeie)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Apeie Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblapeie.AsEnumerable().SingleOrDefault();
         Apeie apeie = null;
         if (row != null)
         {
             apeie = this.BuildFromRow(row);
         }
         return apeie;
      }
	  
	  
      public IEnumerable<Apeie> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("apeie.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Apeie> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblapeie.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Apeie Get(int cono, string groupnm, DateTime? createddt, int groupseqno, string tablenm, int invseqno, int errorseqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("apeie.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(groupnm)) 
         {
             sb.AppendFormatWithEscape(" AND apeie.groupnm = '{0}'", groupnm);
         }
         if (createddt != null) 
         {
             sb.AppendFormatWithEscape(" AND apeie.createddt = '{0}'", createddt);
         }
         if (groupseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND apeie.groupseqno = {0}", groupseqno);
         }
         if (!string.IsNullOrEmpty(tablenm)) 
         {
             sb.AppendFormatWithEscape(" AND apeie.tablenm = '{0}'", tablenm);
         }
         if (invseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND apeie.invseqno = {0}", invseqno);
         }
         if (errorseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND apeie.errorseqno = {0}", errorseqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Apeie> GetListByApinvno(int cono, decimal vendno, string apinvno, string tablenm, int invseqno, int errorseqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("apeie.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND apeie.vendno = {0}", vendno);
         }
         if (!string.IsNullOrEmpty(apinvno)) 
         {
             sb.AppendFormatWithEscape(" AND apeie.apinvno = '{0}'", apinvno);
         }
         if (!string.IsNullOrEmpty(tablenm)) 
         {
             sb.AppendFormatWithEscape(" AND apeie.tablenm = '{0}'", tablenm);
         }
         if (invseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND apeie.invseqno = {0}", invseqno);
         }
         if (errorseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND apeie.errorseqno = {0}", errorseqno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Apeie> GetListByBuyer(int cono, string buyer, string tablenm, int pono, decimal posuf, decimal lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("apeie.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(buyer)) 
         {
             sb.AppendFormatWithEscape(" AND apeie.buyer = '{0}'", buyer);
         }
         if (!string.IsNullOrEmpty(tablenm)) 
         {
             sb.AppendFormatWithEscape(" AND apeie.tablenm = '{0}'", tablenm);
         }
         if (pono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND apeie.pono = {0}", pono);
         }
         if (posuf != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND apeie.posuf = {0}", posuf);
         }
         if (lineno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND apeie.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Apeie BuildFromRow(DataRow row)
      {
         var returnRecord = Apeie.BuildApeieFromRow(row);
         returnRecord = this.BuildExtraFromRow<Apeie>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Apeie record)
      {
         Apeie.UpdateRowFromApeie(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Apeie Insert(Apeie record)
      {
         DataRow row = this.dataSet.ttblapeie.NewttblapeieRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblapeie.AddttblapeieRow((pdsapeieDataSet.ttblapeieRow)row);
         this.SaveChanges();
         return this.dataSet.ttblapeie.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblapeie.Rows[0]) : null;
      }
  

      public Apeie Update(Apeie record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblapeie.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblapeie.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Apeie record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblapeie.NewttblapeieRow();
            Apeie.BuildMinimalRow(ref row, record);
            this.dataSet.ttblapeie.AddttblapeieRow((pdsapeieDataSet.ttblapeieRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Apeie() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  