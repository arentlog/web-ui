//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsswert;

   public partial class SwertAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsswert";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swertTableControlKey;
		
      private pdsswertDataSet dataSet;
        
      public SwertAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswertDataSet() { DataSetName = DataSetName };
            this.swertTableControlKey = this.dataSet.ttblswert.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swertTableControlKey))
            {
               this.CreateTableControlParameters(this.swertTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwertAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swertproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwertproxy = this.proxyAppObject.CreatePO_swertproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwertproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swertproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swertTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swertTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swertproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwertproxy = this.proxyAppObject.CreatePO_swertproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwertproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swertproxy - After Call");
      }
   

      public Swert GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Swert swert = null;
         if (row != null)
         {
             swert = this.BuildFromRow(row);
         }
         return swert;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(swert) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblswert.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Swert> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(swert)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Swert Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblswert.AsEnumerable().SingleOrDefault();
         Swert swert = null;
         if (row != null)
         {
             swert = this.BuildFromRow(row);
         }
         return swert;
      }
	  
	  
      public IEnumerable<Swert> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("swert.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swert> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblswert.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Swert Get(int cono, int repairordno, int repairordsuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swert.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swert.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swert.repairordsuf = {0}", repairordsuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swert.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Swert BuildFromRow(DataRow row)
      {
         var returnRecord = Swert.BuildSwertFromRow(row);
         returnRecord = this.BuildExtraFromRow<Swert>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Swert record)
      {
         Swert.UpdateRowFromSwert(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Swert Insert(Swert record)
      {
         DataRow row = this.dataSet.ttblswert.NewttblswertRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblswert.AddttblswertRow((pdsswertDataSet.ttblswertRow)row);
         this.SaveChanges();
         return this.dataSet.ttblswert.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswert.Rows[0]) : null;
      }
  

      public Swert Update(Swert record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblswert.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswert.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Swert record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblswert.NewttblswertRow();
            Swert.BuildMinimalRow(ref row, record);
            this.dataSet.ttblswert.AddttblswertRow((pdsswertDataSet.ttblswertRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Swert() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  