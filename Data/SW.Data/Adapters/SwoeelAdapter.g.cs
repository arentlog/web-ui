//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsswoeel;

   public partial class SwoeelAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsswoeel";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swoeelTableControlKey;
		
      private pdsswoeelDataSet dataSet;
        
      public SwoeelAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswoeelDataSet() { DataSetName = DataSetName };
            this.swoeelTableControlKey = this.dataSet.ttblswoeel.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swoeelTableControlKey))
            {
               this.CreateTableControlParameters(this.swoeelTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwoeelAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swoeelproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwoeelproxy = this.proxyAppObject.CreatePO_swoeelproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwoeelproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swoeelproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swoeelTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swoeelTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swoeelproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwoeelproxy = this.proxyAppObject.CreatePO_swoeelproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwoeelproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swoeelproxy - After Call");
      }
   

      public Swoeel GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Swoeel swoeel = null;
         if (row != null)
         {
             swoeel = this.BuildFromRow(row);
         }
         return swoeel;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(swoeel) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblswoeel.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Swoeel> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(swoeel)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Swoeel Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblswoeel.AsEnumerable().SingleOrDefault();
         Swoeel swoeel = null;
         if (row != null)
         {
             swoeel = this.BuildFromRow(row);
         }
         return swoeel;
      }
	  
	  
      public IEnumerable<Swoeel> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("swoeel.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swoeel> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblswoeel.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Swoeel Get(int cono, int orderno, int ordersuf, int solineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swoeel.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swoeel.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swoeel.ordersuf = {0}", ordersuf);
         }
         if (solineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swoeel.solineno = {0}", solineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Swoeel> GetListBySwert(int cono, int repairordno, int repairordsuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swoeel.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swoeel.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swoeel.repairordsuf = {0}", repairordsuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swoeel.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Swoeel BuildFromRow(DataRow row)
      {
         var returnRecord = Swoeel.BuildSwoeelFromRow(row);
         returnRecord = this.BuildExtraFromRow<Swoeel>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Swoeel record)
      {
         Swoeel.UpdateRowFromSwoeel(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Swoeel Insert(Swoeel record)
      {
         DataRow row = this.dataSet.ttblswoeel.NewttblswoeelRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblswoeel.AddttblswoeelRow((pdsswoeelDataSet.ttblswoeelRow)row);
         this.SaveChanges();
         return this.dataSet.ttblswoeel.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswoeel.Rows[0]) : null;
      }
  

      public Swoeel Update(Swoeel record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblswoeel.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswoeel.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Swoeel record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblswoeel.NewttblswoeelRow();
            Swoeel.BuildMinimalRow(ref row, record);
            this.dataSet.ttblswoeel.AddttblswoeelRow((pdsswoeelDataSet.ttblswoeelRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Swoeel() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  