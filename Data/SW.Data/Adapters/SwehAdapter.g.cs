//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssweh;
   using Models.Pdssworderlookup;
   using Models.Complex;

   public partial class SwehAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssweh";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swehTableControlKey;
		
      private pdsswehDataSet dataSet;
        
      public SwehAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswehDataSet() { DataSetName = DataSetName };
            this.swehTableControlKey = this.dataSet.ttblsweh.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swehTableControlKey))
            {
               this.CreateTableControlParameters(this.swehTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwehAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwehproxy = this.proxyAppObject.CreatePO_swehproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwehproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swehproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swehTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swehTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwehproxy = this.proxyAppObject.CreatePO_swehproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwehproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swehproxy - After Call");
      }
   

      public Sweh GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sweh sweh = null;
         if (row != null)
         {
             sweh = this.BuildFromRow(row);
         }
         return sweh;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sweh) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsweh.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sweh> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sweh)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sweh Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsweh.AsEnumerable().SingleOrDefault();
         Sweh sweh = null;
         if (row != null)
         {
             sweh = this.BuildFromRow(row);
         }
         return sweh;
      }
	  
	  
      public IEnumerable<Sweh> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sweh.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sweh> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsweh.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sweh Get(int cono, int repairordno, int repairordsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sweh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordsuf = {0}", repairordsuf);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sweh> GetListByCustno(int cono, decimal custno, int stagecd, int repairordno, int repairordsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sweh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.custno = {0}", custno);
         }
         if (stagecd != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.stagecd = {0}", stagecd);
         }
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordsuf = {0}", repairordsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sweh> GetListByDeliverby(int cono, string delivertech, string whse, DateTime? schddeliverdt, int repairordno, int repairordsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sweh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(delivertech)) 
         {
             sb.AppendFormatWithEscape(" AND sweh.delivertech = '{0}'", delivertech);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND sweh.whse = '{0}'", whse);
         }
         if (schddeliverdt != null) 
         {
             sb.AppendFormatWithEscape(" AND sweh.schddeliverdt = '{0}'", schddeliverdt);
         }
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordsuf = {0}", repairordsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sweh> GetListByOrderno(int cono, int orderno, int ordersuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sweh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.ordersuf = {0}", ordersuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sweh> GetListByPickupby(int cono, string pickuptech, string whse, DateTime? schdpickupdt, int repairordno, int repairordsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sweh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(pickuptech)) 
         {
             sb.AppendFormatWithEscape(" AND sweh.pickuptech = '{0}'", pickuptech);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND sweh.whse = '{0}'", whse);
         }
         if (schdpickupdt != null) 
         {
             sb.AppendFormatWithEscape(" AND sweh.schdpickupdt = '{0}'", schdpickupdt);
         }
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordsuf = {0}", repairordsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sweh> GetListBySched(int cono, decimal custno, int repairordno, int repairordsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sweh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.custno = {0}", custno);
         }
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordsuf = {0}", repairordsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sweh> GetListByWhse(int cono, string whse, string leadtech, int repairordno, int repairordsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sweh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND sweh.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(leadtech)) 
         {
             sb.AppendFormatWithEscape(" AND sweh.leadtech = '{0}'", leadtech);
         }
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweh.repairordsuf = {0}", repairordsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sweh BuildFromRow(DataRow row)
      {
         var returnRecord = Sweh.BuildSwehFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sweh>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sweh record)
      {
         Sweh.UpdateRowFromSweh(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sweh Insert(Sweh record)
      {
         DataRow row = this.dataSet.ttblsweh.NewttblswehRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsweh.AddttblswehRow((pdsswehDataSet.ttblswehRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsweh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsweh.Rows[0]) : null;
      }
  

      public Sweh Update(Sweh record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsweh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsweh.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sweh record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsweh.NewttblswehRow();
            Sweh.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsweh.AddttblswehRow((pdsswehDataSet.ttblswehRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sweh() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public SwehLookupResponseAPI Lookup(Sworderlookupcriteria sworderlookupcriteria)
      {   
         var result = new SwehLookupResponseAPI();
         
         var pdssworderlookup = new pdssworderlookupDataSet();
            
         DataRow ttblsworderlookupcriteriaCriteria = pdssworderlookup.ttblsworderlookupcriteria.NewttblsworderlookupcriteriaRow();
         Sworderlookupcriteria.UpdateRowFromSworderlookupcriteria(ref ttblsworderlookupcriteriaCriteria, sworderlookupcriteria);
         pdssworderlookup.ttblsworderlookupcriteria.AddttblsworderlookupcriteriaRow((pdssworderlookupDataSet.ttblsworderlookupcriteriaRow)ttblsworderlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Sweh - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwehproxy = this.proxyAppObject.CreatePO_swehproxy())
               {
                   this.SetRequiredContextParameters();
                   poSwehproxy.Lookup(ref pdsContext, ref pdssworderlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Sweh - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdssworderlookup); 
    
         foreach (DataRow row in pdssworderlookup.ttblsworderlookupresults)
         {
            result.sworderlookupresults.Add(Sworderlookupresults.BuildSworderlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  