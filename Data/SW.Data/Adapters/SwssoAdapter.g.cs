//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsswsso;

   public partial class SwssoAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsswsso";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swssoTableControlKey;
		
      private pdsswssoDataSet dataSet;
        
      public SwssoAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswssoDataSet() { DataSetName = DataSetName };
            this.swssoTableControlKey = this.dataSet.ttblswsso.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swssoTableControlKey))
            {
               this.CreateTableControlParameters(this.swssoTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwssoAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swssoproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwssoproxy = this.proxyAppObject.CreatePO_swssoproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwssoproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swssoproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swssoTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swssoTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swssoproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwssoproxy = this.proxyAppObject.CreatePO_swssoproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwssoproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swssoproxy - After Call");
      }
   

      public Swsso GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Swsso swsso = null;
         if (row != null)
         {
             swsso = this.BuildFromRow(row);
         }
         return swsso;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(swsso) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblswsso.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Swsso> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(swsso)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Swsso Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblswsso.AsEnumerable().SingleOrDefault();
         Swsso swsso = null;
         if (row != null)
         {
             swsso = this.BuildFromRow(row);
         }
         return swsso;
      }
	  
	  
      public IEnumerable<Swsso> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("swsso.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swsso> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblswsso.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Swsso Get(int cono, string whse, string tech, DateTime? startdt, string schedopttype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swsso.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND swsso.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND swsso.tech = '{0}'", tech);
         }
         if (startdt != null) 
         {
             sb.AppendFormatWithEscape(" AND swsso.startdt = '{0}'", startdt);
         }
         if (!string.IsNullOrEmpty(schedopttype)) 
         {
             sb.AppendFormatWithEscape(" AND swsso.schedopttype = '{0}'", schedopttype);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Swsso> GetListByStartdt(int cono, DateTime? startdt, string whse, string tech, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swsso.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (startdt != null) 
         {
             sb.AppendFormatWithEscape(" AND swsso.startdt = '{0}'", startdt);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND swsso.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND swsso.tech = '{0}'", tech);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swsso> GetListByTech(int cono, DateTime? startdt, string tech, string whse, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swsso.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (startdt != null) 
         {
             sb.AppendFormatWithEscape(" AND swsso.startdt = '{0}'", startdt);
         }
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND swsso.tech = '{0}'", tech);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND swsso.whse = '{0}'", whse);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Swsso BuildFromRow(DataRow row)
      {
         var returnRecord = Swsso.BuildSwssoFromRow(row);
         returnRecord = this.BuildExtraFromRow<Swsso>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Swsso record)
      {
         Swsso.UpdateRowFromSwsso(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Swsso Insert(Swsso record)
      {
         DataRow row = this.dataSet.ttblswsso.NewttblswssoRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblswsso.AddttblswssoRow((pdsswssoDataSet.ttblswssoRow)row);
         this.SaveChanges();
         return this.dataSet.ttblswsso.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswsso.Rows[0]) : null;
      }
  

      public Swsso Update(Swsso record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblswsso.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswsso.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Swsso record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblswsso.NewttblswssoRow();
            Swsso.BuildMinimalRow(ref row, record);
            this.dataSet.ttblswsso.AddttblswssoRow((pdsswssoDataSet.ttblswssoRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Swsso() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  