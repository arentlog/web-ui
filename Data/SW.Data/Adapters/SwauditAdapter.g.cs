//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsswaudit;

   public partial class SwauditAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsswaudit";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swauditTableControlKey;
		
      private pdsswauditDataSet dataSet;
        
      public SwauditAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswauditDataSet() { DataSetName = DataSetName };
            this.swauditTableControlKey = this.dataSet.ttblswaudit.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swauditTableControlKey))
            {
               this.CreateTableControlParameters(this.swauditTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwauditAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swauditproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwauditproxy = this.proxyAppObject.CreatePO_swauditproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwauditproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swauditproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swauditTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swauditTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swauditproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwauditproxy = this.proxyAppObject.CreatePO_swauditproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwauditproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swauditproxy - After Call");
      }
   

      public Swaudit GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Swaudit swaudit = null;
         if (row != null)
         {
             swaudit = this.BuildFromRow(row);
         }
         return swaudit;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(swaudit) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblswaudit.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Swaudit> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(swaudit)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Swaudit Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblswaudit.AsEnumerable().SingleOrDefault();
         Swaudit swaudit = null;
         if (row != null)
         {
             swaudit = this.BuildFromRow(row);
         }
         return swaudit;
      }
	  
	  
      public IEnumerable<Swaudit> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("swaudit.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swaudit> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblswaudit.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Swaudit Get(int cono, string whse, int repairordno, int repairordsuf, int rolineno, int solineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swaudit.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND swaudit.whse = '{0}'", whse);
         }
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swaudit.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swaudit.repairordsuf = {0}", repairordsuf);
         }
         if (rolineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swaudit.rolineno = {0}", rolineno);
         }
         if (solineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swaudit.solineno = {0}", solineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Swaudit> GetListByDelete(int cono, string whse, int repairordno, DateTime? transdt, string operinit, bool printfl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swaudit.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND swaudit.whse = '{0}'", whse);
         }
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swaudit.repairordno = {0}", repairordno);
         }
         if (transdt != null) 
         {
             sb.AppendFormatWithEscape(" AND swaudit.transdt = '{0}'", transdt);
         }
         if (!string.IsNullOrEmpty(operinit)) 
         {
             sb.AppendFormatWithEscape(" AND swaudit.operinit = '{0}'", operinit);
         }
         sb.AppendFormatWithEscape(" AND swaudit.printfl = {0}", printfl);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Swaudit BuildFromRow(DataRow row)
      {
         var returnRecord = Swaudit.BuildSwauditFromRow(row);
         returnRecord = this.BuildExtraFromRow<Swaudit>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Swaudit record)
      {
         Swaudit.UpdateRowFromSwaudit(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Swaudit Insert(Swaudit record)
      {
         DataRow row = this.dataSet.ttblswaudit.NewttblswauditRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblswaudit.AddttblswauditRow((pdsswauditDataSet.ttblswauditRow)row);
         this.SaveChanges();
         return this.dataSet.ttblswaudit.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswaudit.Rows[0]) : null;
      }
  

      public Swaudit Update(Swaudit record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblswaudit.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswaudit.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Swaudit record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblswaudit.NewttblswauditRow();
            Swaudit.BuildMinimalRow(ref row, record);
            this.dataSet.ttblswaudit.AddttblswauditRow((pdsswauditDataSet.ttblswauditRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Swaudit() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  