//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsswewh;

   public partial class SwewhAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsswewh";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swewhTableControlKey;
		
      private pdsswewhDataSet dataSet;
        
      public SwewhAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswewhDataSet() { DataSetName = DataSetName };
            this.swewhTableControlKey = this.dataSet.ttblswewh.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swewhTableControlKey))
            {
               this.CreateTableControlParameters(this.swewhTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwewhAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swewhproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwewhproxy = this.proxyAppObject.CreatePO_swewhproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwewhproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swewhproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swewhTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swewhTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swewhproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwewhproxy = this.proxyAppObject.CreatePO_swewhproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwewhproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swewhproxy - After Call");
      }
   

      public Swewh GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Swewh swewh = null;
         if (row != null)
         {
             swewh = this.BuildFromRow(row);
         }
         return swewh;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(swewh) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblswewh.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Swewh> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(swewh)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Swewh Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblswewh.AsEnumerable().SingleOrDefault();
         Swewh swewh = null;
         if (row != null)
         {
             swewh = this.BuildFromRow(row);
         }
         return swewh;
      }
	  
	  
      public IEnumerable<Swewh> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("swewh.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swewh> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblswewh.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Swewh Get(int cono, int intclaimno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swewh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (intclaimno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swewh.intclaimno = {0}", intclaimno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Swewh> GetListByVendno(int cono, decimal vendno, int intclaimno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swewh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swewh.vendno = {0}", vendno);
         }
         if (intclaimno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swewh.intclaimno = {0}", intclaimno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Swewh BuildFromRow(DataRow row)
      {
         var returnRecord = Swewh.BuildSwewhFromRow(row);
         returnRecord = this.BuildExtraFromRow<Swewh>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Swewh record)
      {
         Swewh.UpdateRowFromSwewh(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Swewh Insert(Swewh record)
      {
         DataRow row = this.dataSet.ttblswewh.NewttblswewhRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblswewh.AddttblswewhRow((pdsswewhDataSet.ttblswewhRow)row);
         this.SaveChanges();
         return this.dataSet.ttblswewh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswewh.Rows[0]) : null;
      }
  

      public Swewh Update(Swewh record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblswewh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswewh.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Swewh record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblswewh.NewttblswewhRow();
            Swewh.BuildMinimalRow(ref row, record);
            this.dataSet.ttblswewh.AddttblswewhRow((pdsswewhDataSet.ttblswewhRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Swewh() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  