//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsswelj;

   public partial class SweljAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsswelj";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sweljTableControlKey;
		
      private pdssweljDataSet dataSet;
        
      public SweljAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssweljDataSet() { DataSetName = DataSetName };
            this.sweljTableControlKey = this.dataSet.ttblswelj.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sweljTableControlKey))
            {
               this.CreateTableControlParameters(this.sweljTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SweljAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sweljproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSweljproxy = this.proxyAppObject.CreatePO_sweljproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSweljproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sweljproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sweljTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sweljTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sweljproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSweljproxy = this.proxyAppObject.CreatePO_sweljproxy())
               {
                  this.SetRequiredContextParameters();
                  poSweljproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sweljproxy - After Call");
      }
   

      public Swelj GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Swelj swelj = null;
         if (row != null)
         {
             swelj = this.BuildFromRow(row);
         }
         return swelj;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(swelj) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblswelj.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Swelj> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(swelj)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Swelj Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblswelj.AsEnumerable().SingleOrDefault();
         Swelj swelj = null;
         if (row != null)
         {
             swelj = this.BuildFromRow(row);
         }
         return swelj;
      }
	  
	  
      public IEnumerable<Swelj> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("swelj.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblswelj.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Swelj Get(int cono, string origtech, DateTime? systemindt, string systemintm, int seqno, int transseqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(origtech)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.origtech = '{0}'", origtech);
         }
         if (systemindt != null) 
         {
             sb.AppendFormatWithEscape(" AND swelj.systemindt = '{0}'", systemindt);
         }
         if (!string.IsNullOrEmpty(systemintm)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.systemintm = '{0}'", systemintm);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.seqno = {0}", seqno);
         }
         if (transseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.transseqno = {0}", transseqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Swelj> GetListByEmptype(int cono, bool activefl, string emptype, int repairordno, int repairordsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (!string.IsNullOrEmpty(emptype)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.emptype = '{0}'", emptype);
         }
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.repairordsuf = {0}", repairordsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetListByInwhse(int cono, bool activefl, DateTime? indt, string whse, string tech, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (indt != null) 
         {
             sb.AppendFormatWithEscape(" AND swelj.indt = '{0}'", indt);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.tech = '{0}'", tech);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetListBySchedopt(int cono, bool activefl, string schedopttype, DateTime? indt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (!string.IsNullOrEmpty(schedopttype)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.schedopttype = '{0}'", schedopttype);
         }
         if (indt != null) 
         {
             sb.AppendFormatWithEscape(" AND swelj.indt = '{0}'", indt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetListBySro(int cono, bool activefl, int repairordno, int repairordsuf, int lineno, DateTime? indt, string intm, string tech, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.repairordsuf = {0}", repairordsuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.lineno = {0}", lineno);
         }
         if (indt != null) 
         {
             sb.AppendFormatWithEscape(" AND swelj.indt = '{0}'", indt);
         }
         if (!string.IsNullOrEmpty(intm)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.intm = '{0}'", intm);
         }
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.tech = '{0}'", tech);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetListBySroclose(int cono, bool activefl, int repairordno, int repairordsuf, bool closedfl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.repairordsuf = {0}", repairordsuf);
         }
         sb.AppendFormatWithEscape(" AND swelj.closedfl = {0}", closedfl);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetListByStatus(int cono, bool activefl, string statustype, DateTime? indt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (!string.IsNullOrEmpty(statustype)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.statustype = '{0}'", statustype);
         }
         if (indt != null) 
         {
             sb.AppendFormatWithEscape(" AND swelj.indt = '{0}'", indt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetListByTechclose(int cono, bool activefl, string tech, bool closedfl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.tech = '{0}'", tech);
         }
         sb.AppendFormatWithEscape(" AND swelj.closedfl = {0}", closedfl);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetListByTechin(int cono, bool activefl, string tech, DateTime? indt, string intm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.tech = '{0}'", tech);
         }
         if (indt != null) 
         {
             sb.AppendFormatWithEscape(" AND swelj.indt = '{0}'", indt);
         }
         if (!string.IsNullOrEmpty(intm)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.intm = '{0}'", intm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetListByTechout(int cono, bool activefl, string tech, DateTime? outdt, string outtm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.tech = '{0}'", tech);
         }
         if (outdt != null) 
         {
             sb.AppendFormatWithEscape(" AND swelj.outdt = '{0}'", outdt);
         }
         if (!string.IsNullOrEmpty(outtm)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.outtm = '{0}'", outtm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swelj> GetListByWhse(int cono, bool activefl, string whse, int deptno, string tech, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swelj.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND swelj.activefl = {0}", activefl);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.whse = '{0}'", whse);
         }
         if (deptno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swelj.deptno = {0}", deptno);
         }
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND swelj.tech = '{0}'", tech);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Swelj BuildFromRow(DataRow row)
      {
         var returnRecord = Swelj.BuildSweljFromRow(row);
         returnRecord = this.BuildExtraFromRow<Swelj>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Swelj record)
      {
         Swelj.UpdateRowFromSwelj(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Swelj Insert(Swelj record)
      {
         DataRow row = this.dataSet.ttblswelj.NewttblsweljRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblswelj.AddttblsweljRow((pdssweljDataSet.ttblsweljRow)row);
         this.SaveChanges();
         return this.dataSet.ttblswelj.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswelj.Rows[0]) : null;
      }
  

      public Swelj Update(Swelj record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblswelj.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswelj.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Swelj record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblswelj.NewttblsweljRow();
            Swelj.BuildMinimalRow(ref row, record);
            this.dataSet.ttblswelj.AddttblsweljRow((pdssweljDataSet.ttblsweljRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Swelj() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  