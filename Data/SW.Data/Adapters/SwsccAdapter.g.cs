//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsswscc;

   public partial class SwsccAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsswscc";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swsccTableControlKey;
		
      private pdsswsccDataSet dataSet;
        
      public SwsccAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswsccDataSet() { DataSetName = DataSetName };
            this.swsccTableControlKey = this.dataSet.ttblswscc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swsccTableControlKey))
            {
               this.CreateTableControlParameters(this.swsccTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwsccAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swsccproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwsccproxy = this.proxyAppObject.CreatePO_swsccproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwsccproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swsccproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swsccTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swsccTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swsccproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwsccproxy = this.proxyAppObject.CreatePO_swsccproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwsccproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swsccproxy - After Call");
      }
   

      public Swscc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Swscc swscc = null;
         if (row != null)
         {
             swscc = this.BuildFromRow(row);
         }
         return swscc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(swscc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblswscc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Swscc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(swscc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Swscc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblswscc.AsEnumerable().SingleOrDefault();
         Swscc swscc = null;
         if (row != null)
         {
             swscc = this.BuildFromRow(row);
         }
         return swscc;
      }
	  
	  
      public IEnumerable<Swscc> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("swscc.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swscc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblswscc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Swscc Get(int cono, string cancelcd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swscc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(cancelcd)) 
         {
             sb.AppendFormatWithEscape(" AND swscc.cancelcd = '{0}'", cancelcd);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Swscc BuildFromRow(DataRow row)
      {
         var returnRecord = Swscc.BuildSwsccFromRow(row);
         returnRecord = this.BuildExtraFromRow<Swscc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Swscc record)
      {
         Swscc.UpdateRowFromSwscc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Swscc Insert(Swscc record)
      {
         DataRow row = this.dataSet.ttblswscc.NewttblswsccRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblswscc.AddttblswsccRow((pdsswsccDataSet.ttblswsccRow)row);
         this.SaveChanges();
         return this.dataSet.ttblswscc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswscc.Rows[0]) : null;
      }
  

      public Swscc Update(Swscc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblswscc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswscc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Swscc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblswscc.NewttblswsccRow();
            Swscc.BuildMinimalRow(ref row, record);
            this.dataSet.ttblswscc.AddttblswsccRow((pdsswsccDataSet.ttblswsccRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Swscc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  