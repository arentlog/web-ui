//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsswewl;

   public partial class SwewlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsswewl";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swewlTableControlKey;
		
      private pdsswewlDataSet dataSet;
        
      public SwewlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswewlDataSet() { DataSetName = DataSetName };
            this.swewlTableControlKey = this.dataSet.ttblswewl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swewlTableControlKey))
            {
               this.CreateTableControlParameters(this.swewlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwewlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swewlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwewlproxy = this.proxyAppObject.CreatePO_swewlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwewlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swewlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swewlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swewlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swewlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwewlproxy = this.proxyAppObject.CreatePO_swewlproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwewlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swewlproxy - After Call");
      }
   

      public Swewl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Swewl swewl = null;
         if (row != null)
         {
             swewl = this.BuildFromRow(row);
         }
         return swewl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(swewl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblswewl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Swewl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(swewl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Swewl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblswewl.AsEnumerable().SingleOrDefault();
         Swewl swewl = null;
         if (row != null)
         {
             swewl = this.BuildFromRow(row);
         }
         return swewl;
      }
	  
	  
      public IEnumerable<Swewl> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("swewl.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swewl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblswewl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Swewl Get(int cono, int intclaimno, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swewl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (intclaimno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swewl.intclaimno = {0}", intclaimno);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swewl.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Swewl BuildFromRow(DataRow row)
      {
         var returnRecord = Swewl.BuildSwewlFromRow(row);
         returnRecord = this.BuildExtraFromRow<Swewl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Swewl record)
      {
         Swewl.UpdateRowFromSwewl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Swewl Insert(Swewl record)
      {
         DataRow row = this.dataSet.ttblswewl.NewttblswewlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblswewl.AddttblswewlRow((pdsswewlDataSet.ttblswewlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblswewl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswewl.Rows[0]) : null;
      }
  

      public Swewl Update(Swewl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblswewl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswewl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Swewl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblswewl.NewttblswewlRow();
            Swewl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblswewl.AddttblswewlRow((pdsswewlDataSet.ttblswewlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Swewl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  