//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsswsct;

   public partial class SwsctAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsswsct";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swsctTableControlKey;
		
      private pdsswsctDataSet dataSet;
        
      public SwsctAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswsctDataSet() { DataSetName = DataSetName };
            this.swsctTableControlKey = this.dataSet.ttblswsct.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swsctTableControlKey))
            {
               this.CreateTableControlParameters(this.swsctTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwsctAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swsctproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwsctproxy = this.proxyAppObject.CreatePO_swsctproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwsctproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swsctproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swsctTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swsctTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swsctproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwsctproxy = this.proxyAppObject.CreatePO_swsctproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwsctproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swsctproxy - After Call");
      }
   

      public Swsct GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Swsct swsct = null;
         if (row != null)
         {
             swsct = this.BuildFromRow(row);
         }
         return swsct;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(swsct) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblswsct.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Swsct> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(swsct)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Swsct Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblswsct.AsEnumerable().SingleOrDefault();
         Swsct swsct = null;
         if (row != null)
         {
             swsct = this.BuildFromRow(row);
         }
         return swsct;
      }
	  
	  
      public IEnumerable<Swsct> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("swsct.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Swsct> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblswsct.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Swsct Get(int cono, int textno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swsct.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (textno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swsct.textno = {0}", textno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Swsct> GetListByGrouping(int cono, string grouping, int textno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("swsct.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(grouping)) 
         {
             sb.AppendFormatWithEscape(" AND swsct.grouping = '{0}'", grouping);
         }
         if (textno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND swsct.textno = {0}", textno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Swsct BuildFromRow(DataRow row)
      {
         var returnRecord = Swsct.BuildSwsctFromRow(row);
         returnRecord = this.BuildExtraFromRow<Swsct>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Swsct record)
      {
         Swsct.UpdateRowFromSwsct(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Swsct Insert(Swsct record)
      {
         DataRow row = this.dataSet.ttblswsct.NewttblswsctRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblswsct.AddttblswsctRow((pdsswsctDataSet.ttblswsctRow)row);
         this.SaveChanges();
         return this.dataSet.ttblswsct.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswsct.Rows[0]) : null;
      }
  

      public Swsct Update(Swsct record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblswsct.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblswsct.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Swsct record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblswsct.NewttblswsctRow();
            Swsct.BuildMinimalRow(ref row, record);
            this.dataSet.ttblswsct.AddttblswsctRow((pdsswsctDataSet.ttblswsctRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Swsct() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  