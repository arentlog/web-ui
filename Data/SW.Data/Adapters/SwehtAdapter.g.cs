//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SW.Data.Adapters
{
   using com.infor.sxproxy.swproxy;
   using com.infor.sxproxy.swproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssweht;

   public partial class SwehtAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssweht";
      private SWProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> swehtTableControlKey;
		
      private pdsswehtDataSet dataSet;
        
      public SwehtAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SWProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsswehtDataSet() { DataSetName = DataSetName };
            this.swehtTableControlKey = this.dataSet.ttblsweht.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.swehtTableControlKey))
            {
               this.CreateTableControlParameters(this.swehtTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SwehtAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Swehtproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSwehtproxy = this.proxyAppObject.CreatePO_swehtproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSwehtproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Swehtproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.swehtTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.swehtTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Swehtproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSwehtproxy = this.proxyAppObject.CreatePO_swehtproxy())
               {
                  this.SetRequiredContextParameters();
                  poSwehtproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Swehtproxy - After Call");
      }
   

      public Sweht GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sweht sweht = null;
         if (row != null)
         {
             sweht = this.BuildFromRow(row);
         }
         return sweht;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sweht) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsweht.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sweht> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sweht)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sweht Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsweht.AsEnumerable().SingleOrDefault();
         Sweht sweht = null;
         if (row != null)
         {
             sweht = this.BuildFromRow(row);
         }
         return sweht;
      }
	  
	  
      public IEnumerable<Sweht> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sweht.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sweht> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsweht.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sweht Get(int cono, int repairordno, int repairordsuf, string tech, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sweht.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (repairordno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweht.repairordno = {0}", repairordno);
         }
         if (repairordsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sweht.repairordsuf = {0}", repairordsuf);
         }
         if (!string.IsNullOrEmpty(tech)) 
         {
             sb.AppendFormatWithEscape(" AND sweht.tech = '{0}'", tech);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sweht BuildFromRow(DataRow row)
      {
         var returnRecord = Sweht.BuildSwehtFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sweht>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sweht record)
      {
         Sweht.UpdateRowFromSweht(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sweht Insert(Sweht record)
      {
         DataRow row = this.dataSet.ttblsweht.NewttblswehtRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsweht.AddttblswehtRow((pdsswehtDataSet.ttblswehtRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsweht.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsweht.Rows[0]) : null;
      }
  

      public Sweht Update(Sweht record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsweht.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsweht.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sweht record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsweht.NewttblswehtRow();
            Sweht.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsweht.AddttblswehtRow((pdsswehtDataSet.ttblswehtRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sweht() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  