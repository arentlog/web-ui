//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.EDI.Data.Adapters
{
   using com.infor.sxproxy.ediproxy;
   using com.infor.sxproxy.ediproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsedsc;

   public partial class EdscAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsedsc";
      private EDIProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> edscTableControlKey;
		
      private pdsedscDataSet dataSet;
        
      public EdscAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new EDIProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsedscDataSet() { DataSetName = DataSetName };
            this.edscTableControlKey = this.dataSet.ttbledsc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.edscTableControlKey))
            {
               this.CreateTableControlParameters(this.edscTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EdscAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Edscproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEdscproxy = this.proxyAppObject.CreatePO_edscproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEdscproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Edscproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.edscTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.edscTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Edscproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEdscproxy = this.proxyAppObject.CreatePO_edscproxy())
               {
                  this.SetRequiredContextParameters();
                  poEdscproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Edscproxy - After Call");
      }
   

      public Edsc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Edsc edsc = null;
         if (row != null)
         {
             edsc = this.BuildFromRow(row);
         }
         return edsc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(edsc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbledsc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Edsc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(edsc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Edsc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbledsc.AsEnumerable().SingleOrDefault();
         Edsc edsc = null;
         if (row != null)
         {
             edsc = this.BuildFromRow(row);
         }
         return edsc;
      }
	  
	  

      public IEnumerable<Edsc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbledsc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Edsc Get(bool transmitty, string partnerid, decimal docno, string version, string segment, decimal element, string loop, string condvalue, string condfield, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("edsc.transmitty = {0}", transmitty);
         if (!string.IsNullOrEmpty(partnerid)) 
         {
             sb.AppendFormatWithEscape(" AND edsc.partnerid = '{0}'", partnerid);
         }
         if (docno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND edsc.docno = {0}", docno);
         }
         if (!string.IsNullOrEmpty(version)) 
         {
             sb.AppendFormatWithEscape(" AND edsc.version = '{0}'", version);
         }
         if (!string.IsNullOrEmpty(segment)) 
         {
             sb.AppendFormatWithEscape(" AND edsc.segment = '{0}'", segment);
         }
         if (element != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND edsc.element = {0}", element);
         }
         if (!string.IsNullOrEmpty(loop)) 
         {
             sb.AppendFormatWithEscape(" AND edsc.loop = '{0}'", loop);
         }
         if (!string.IsNullOrEmpty(condvalue)) 
         {
             sb.AppendFormatWithEscape(" AND edsc.condvalue = '{0}'", condvalue);
         }
         if (!string.IsNullOrEmpty(condfield)) 
         {
             sb.AppendFormatWithEscape(" AND edsc.condfield = '{0}'", condfield);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Edsc> GetListByOut(bool transmitty, decimal docno, string version, string segment, decimal element, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("edsc.transmitty = {0}", transmitty);
         if (docno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND edsc.docno = {0}", docno);
         }
         if (!string.IsNullOrEmpty(version)) 
         {
             sb.AppendFormatWithEscape(" AND edsc.version = '{0}'", version);
         }
         if (!string.IsNullOrEmpty(segment)) 
         {
             sb.AppendFormatWithEscape(" AND edsc.segment = '{0}'", segment);
         }
         if (element != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND edsc.element = {0}", element);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Edsc BuildFromRow(DataRow row)
      {
         var returnRecord = Edsc.BuildEdscFromRow(row);
         returnRecord = this.BuildExtraFromRow<Edsc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Edsc record)
      {
         Edsc.UpdateRowFromEdsc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Edsc Insert(Edsc record)
      {
         DataRow row = this.dataSet.ttbledsc.NewttbledscRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbledsc.AddttbledscRow((pdsedscDataSet.ttbledscRow)row);
         this.SaveChanges();
         return this.dataSet.ttbledsc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbledsc.Rows[0]) : null;
      }
  

      public Edsc Update(Edsc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbledsc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbledsc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Edsc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbledsc.NewttbledscRow();
            Edsc.BuildMinimalRow(ref row, record);
            this.dataSet.ttbledsc.AddttbledscRow((pdsedscDataSet.ttbledscRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Edsc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  