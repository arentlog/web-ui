//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PO.Data.Adapters
{
   using com.infor.sxproxy.poproxy;
   using com.infor.sxproxy.poproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspoela;

   public partial class PoelaAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspoela";
      private POProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> poelaTableControlKey;
		
      private pdspoelaDataSet dataSet;
        
      public PoelaAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new POProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspoelaDataSet() { DataSetName = DataSetName };
            this.poelaTableControlKey = this.dataSet.ttblpoela.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.poelaTableControlKey))
            {
               this.CreateTableControlParameters(this.poelaTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PoelaAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Poelaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPoelaproxy = this.proxyAppObject.CreatePO_poelaproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPoelaproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Poelaproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.poelaTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.poelaTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Poelaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPoelaproxy = this.proxyAppObject.CreatePO_poelaproxy())
               {
                  this.SetRequiredContextParameters();
                  poPoelaproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Poelaproxy - After Call");
      }
   

      public Poela GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Poela poela = null;
         if (row != null)
         {
             poela = this.BuildFromRow(row);
         }
         return poela;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(poela) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpoela.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Poela> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(poela)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Poela Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpoela.AsEnumerable().SingleOrDefault();
         Poela poela = null;
         if (row != null)
         {
             poela = this.BuildFromRow(row);
         }
         return poela;
      }
	  
	  
      public IEnumerable<Poela> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("poela.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Poela> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpoela.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Poela Get(int cono, int jrnlno, int setno, int addonno, int pono, int posuf, int lineno, int compseqno, string bundleid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("poela.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (jrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.jrnlno = {0}", jrnlno);
         }
         if (setno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.setno = {0}", setno);
         }
         if (addonno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.addonno = {0}", addonno);
         }
         if (pono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.pono = {0}", pono);
         }
         if (posuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.posuf = {0}", posuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.lineno = {0}", lineno);
         }
         if (compseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.compseqno = {0}", compseqno);
         }
         if (!string.IsNullOrEmpty(bundleid)) 
         {
             sb.AppendFormatWithEscape(" AND poela.bundleid = '{0}'", bundleid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Poela> GetListByPono(int cono, int pono, int posuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("poela.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (pono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.pono = {0}", pono);
         }
         if (posuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.posuf = {0}", posuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poela.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Poela BuildFromRow(DataRow row)
      {
         var returnRecord = Poela.BuildPoelaFromRow(row);
         returnRecord = this.BuildExtraFromRow<Poela>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Poela record)
      {
         Poela.UpdateRowFromPoela(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Poela Insert(Poela record)
      {
         DataRow row = this.dataSet.ttblpoela.NewttblpoelaRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpoela.AddttblpoelaRow((pdspoelaDataSet.ttblpoelaRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpoela.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpoela.Rows[0]) : null;
      }
  

      public Poela Update(Poela record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpoela.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpoela.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Poela record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpoela.NewttblpoelaRow();
            Poela.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpoela.AddttblpoelaRow((pdspoelaDataSet.ttblpoelaRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Poela() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  