//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PO.Data.Adapters
{
   using com.infor.sxproxy.poproxy;
   using com.infor.sxproxy.poproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspoehe;

   public partial class PoeheAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspoehe";
      private POProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> poeheTableControlKey;
		
      private pdspoeheDataSet dataSet;
        
      public PoeheAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new POProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspoeheDataSet() { DataSetName = DataSetName };
            this.poeheTableControlKey = this.dataSet.ttblpoehe.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.poeheTableControlKey))
            {
               this.CreateTableControlParameters(this.poeheTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PoeheAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Poeheproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPoeheproxy = this.proxyAppObject.CreatePO_poeheproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPoeheproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Poeheproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.poeheTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.poeheTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Poeheproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPoeheproxy = this.proxyAppObject.CreatePO_poeheproxy())
               {
                  this.SetRequiredContextParameters();
                  poPoeheproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Poeheproxy - After Call");
      }
   

      public Poehe GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Poehe poehe = null;
         if (row != null)
         {
             poehe = this.BuildFromRow(row);
         }
         return poehe;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(poehe) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpoehe.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Poehe> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(poehe)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Poehe Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpoehe.AsEnumerable().SingleOrDefault();
         Poehe poehe = null;
         if (row != null)
         {
             poehe = this.BuildFromRow(row);
         }
         return poehe;
      }
	  
	  
      public IEnumerable<Poehe> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("poehe.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Poehe> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpoehe.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Poehe Get(int cono, int pono, int posuf, string edicd, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("poehe.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (pono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poehe.pono = {0}", pono);
         }
         if (posuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poehe.posuf = {0}", posuf);
         }
         if (!string.IsNullOrEmpty(edicd)) 
         {
             sb.AppendFormatWithEscape(" AND poehe.edicd = '{0}'", edicd);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poehe.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Poehe> GetListByVendno(int cono, decimal vendno, int pono, int posuf, string edicd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("poehe.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poehe.vendno = {0}", vendno);
         }
         if (pono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poehe.pono = {0}", pono);
         }
         if (posuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poehe.posuf = {0}", posuf);
         }
         if (!string.IsNullOrEmpty(edicd)) 
         {
             sb.AppendFormatWithEscape(" AND poehe.edicd = '{0}'", edicd);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Poehe> GetListByVendpo(int cono, decimal vendno, int vendpono, int vendposuf, string edicd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("poehe.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poehe.vendno = {0}", vendno);
         }
         if (vendpono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poehe.vendpono = {0}", vendpono);
         }
         if (vendposuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND poehe.vendposuf = {0}", vendposuf);
         }
         if (!string.IsNullOrEmpty(edicd)) 
         {
             sb.AppendFormatWithEscape(" AND poehe.edicd = '{0}'", edicd);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Poehe BuildFromRow(DataRow row)
      {
         var returnRecord = Poehe.BuildPoeheFromRow(row);
         returnRecord = this.BuildExtraFromRow<Poehe>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Poehe record)
      {
         Poehe.UpdateRowFromPoehe(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Poehe Insert(Poehe record)
      {
         DataRow row = this.dataSet.ttblpoehe.NewttblpoeheRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpoehe.AddttblpoeheRow((pdspoeheDataSet.ttblpoeheRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpoehe.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpoehe.Rows[0]) : null;
      }
  

      public Poehe Update(Poehe record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpoehe.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpoehe.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Poehe record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpoehe.NewttblpoeheRow();
            Poehe.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpoehe.AddttblpoeheRow((pdspoeheDataSet.ttblpoeheRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Poehe() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  