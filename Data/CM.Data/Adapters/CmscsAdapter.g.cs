//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.CM.Data.Adapters
{
   using com.infor.sxproxy.cmproxy;
   using com.infor.sxproxy.cmproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscmscs;

   public partial class CmscsAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscmscs";
      private CMProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cmscsTableControlKey;
		
      private pdscmscsDataSet dataSet;
        
      public CmscsAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new CMProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscmscsDataSet() { DataSetName = DataSetName };
            this.cmscsTableControlKey = this.dataSet.ttblcmscs.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cmscsTableControlKey))
            {
               this.CreateTableControlParameters(this.cmscsTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CmscsAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Cmscsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCmscsproxy = this.proxyAppObject.CreatePO_cmscsproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCmscsproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Cmscsproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cmscsTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cmscsTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Cmscsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCmscsproxy = this.proxyAppObject.CreatePO_cmscsproxy())
               {
                  this.SetRequiredContextParameters();
                  poCmscsproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Cmscsproxy - After Call");
      }
   

      public Cmscs GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Cmscs cmscs = null;
         if (row != null)
         {
             cmscs = this.BuildFromRow(row);
         }
         return cmscs;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(cmscs) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcmscs.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Cmscs> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(cmscs)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Cmscs Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcmscs.AsEnumerable().SingleOrDefault();
         Cmscs cmscs = null;
         if (row != null)
         {
             cmscs = this.BuildFromRow(row);
         }
         return cmscs;
      }
	  
	  

      public IEnumerable<Cmscs> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcmscs.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Cmscs Get(string campaigncd, string scriptcd, int pageno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(campaigncd)) 
         {
             sb.AppendFormatWithEscape("cmscs.campaigncd = '{0}'", campaigncd);
         }
         if (!string.IsNullOrEmpty(scriptcd)) 
         {
             sb.AppendFormatWithEscape(" AND cmscs.scriptcd = '{0}'", scriptcd);
         }
         if (pageno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND cmscs.pageno = {0}", pageno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Cmscs BuildFromRow(DataRow row)
      {
         var returnRecord = Cmscs.BuildCmscsFromRow(row);
         returnRecord = this.BuildExtraFromRow<Cmscs>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Cmscs record)
      {
         Cmscs.UpdateRowFromCmscs(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Cmscs Insert(Cmscs record)
      {
         DataRow row = this.dataSet.ttblcmscs.NewttblcmscsRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcmscs.AddttblcmscsRow((pdscmscsDataSet.ttblcmscsRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcmscs.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmscs.Rows[0]) : null;
      }
  

      public Cmscs Update(Cmscs record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcmscs.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmscs.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Cmscs record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcmscs.NewttblcmscsRow();
            Cmscs.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcmscs.AddttblcmscsRow((pdscmscsDataSet.ttblcmscsRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Cmscs() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  