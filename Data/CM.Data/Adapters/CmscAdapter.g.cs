//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.CM.Data.Adapters
{
   using com.infor.sxproxy.cmproxy;
   using com.infor.sxproxy.cmproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscmsc;

   public partial class CmscAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscmsc";
      private CMProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cmscTableControlKey;
		
      private pdscmscDataSet dataSet;
        
      public CmscAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new CMProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscmscDataSet() { DataSetName = DataSetName };
            this.cmscTableControlKey = this.dataSet.ttblcmsc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cmscTableControlKey))
            {
               this.CreateTableControlParameters(this.cmscTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CmscAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Cmscproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCmscproxy = this.proxyAppObject.CreatePO_cmscproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCmscproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Cmscproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cmscTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cmscTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Cmscproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCmscproxy = this.proxyAppObject.CreatePO_cmscproxy())
               {
                  this.SetRequiredContextParameters();
                  poCmscproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Cmscproxy - After Call");
      }
   

      public Cmsc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Cmsc cmsc = null;
         if (row != null)
         {
             cmsc = this.BuildFromRow(row);
         }
         return cmsc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(cmsc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcmsc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Cmsc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(cmsc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Cmsc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcmsc.AsEnumerable().SingleOrDefault();
         Cmsc cmsc = null;
         if (row != null)
         {
             cmsc = this.BuildFromRow(row);
         }
         return cmsc;
      }
	  
	  

      public IEnumerable<Cmsc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcmsc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Cmsc Get(string campaigncd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(campaigncd)) 
         {
             sb.AppendFormatWithEscape("cmsc.campaigncd = '{0}'", campaigncd);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Cmsc BuildFromRow(DataRow row)
      {
         var returnRecord = Cmsc.BuildCmscFromRow(row);
         returnRecord = this.BuildExtraFromRow<Cmsc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Cmsc record)
      {
         Cmsc.UpdateRowFromCmsc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Cmsc Insert(Cmsc record)
      {
         DataRow row = this.dataSet.ttblcmsc.NewttblcmscRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcmsc.AddttblcmscRow((pdscmscDataSet.ttblcmscRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcmsc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmsc.Rows[0]) : null;
      }
  

      public Cmsc Update(Cmsc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcmsc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmsc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Cmsc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcmsc.NewttblcmscRow();
            Cmsc.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcmsc.AddttblcmscRow((pdscmscDataSet.ttblcmscRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Cmsc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  