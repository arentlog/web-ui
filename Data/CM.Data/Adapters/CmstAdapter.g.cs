//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.CM.Data.Adapters
{
   using com.infor.sxproxy.cmproxy;
   using com.infor.sxproxy.cmproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscmst;

   public partial class CmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscmst";
      private CMProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cmstTableControlKey;
		
      private pdscmstDataSet dataSet;
        
      public CmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new CMProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscmstDataSet() { DataSetName = DataSetName };
            this.cmstTableControlKey = this.dataSet.ttblcmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cmstTableControlKey))
            {
               this.CreateTableControlParameters(this.cmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Cmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCmstproxy = this.proxyAppObject.CreatePO_cmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Cmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Cmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCmstproxy = this.proxyAppObject.CreatePO_cmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poCmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Cmstproxy - After Call");
      }
   

      public Cmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Cmst cmst = null;
         if (row != null)
         {
             cmst = this.BuildFromRow(row);
         }
         return cmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(cmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Cmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(cmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Cmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcmst.AsEnumerable().SingleOrDefault();
         Cmst cmst = null;
         if (row != null)
         {
             cmst = this.BuildFromRow(row);
         }
         return cmst;
      }
	  
	  

      public IEnumerable<Cmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Cmst Get(string codeiden, string codeval, string slsrep, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(codeiden)) 
         {
             sb.AppendFormatWithEscape("cmst.codeiden = '{0}'", codeiden);
         }
         if (!string.IsNullOrEmpty(codeval)) 
         {
             sb.AppendFormatWithEscape(" AND cmst.codeval = '{0}'", codeval);
         }
         if (!string.IsNullOrEmpty(slsrep)) 
         {
             sb.AppendFormatWithEscape(" AND cmst.slsrep = '{0}'", slsrep);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Cmst> GetListByHeading(string codeiden, string slsrep, string codeval, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(codeiden)) 
         {
             sb.AppendFormatWithEscape("cmst.codeiden = '{0}'", codeiden);
         }
         if (!string.IsNullOrEmpty(slsrep)) 
         {
             sb.AppendFormatWithEscape(" AND cmst.slsrep = '{0}'", slsrep);
         }
         if (!string.IsNullOrEmpty(codeval)) 
         {
             sb.AppendFormatWithEscape(" AND cmst.codeval = '{0}'", codeval);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Cmst BuildFromRow(DataRow row)
      {
         var returnRecord = Cmst.BuildCmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Cmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Cmst record)
      {
         Cmst.UpdateRowFromCmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Cmst Insert(Cmst record)
      {
         DataRow row = this.dataSet.ttblcmst.NewttblcmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcmst.AddttblcmstRow((pdscmstDataSet.ttblcmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmst.Rows[0]) : null;
      }
  

      public Cmst Update(Cmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Cmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcmst.NewttblcmstRow();
            Cmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcmst.AddttblcmstRow((pdscmstDataSet.ttblcmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Cmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  