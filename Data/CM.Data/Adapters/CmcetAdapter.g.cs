//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.CM.Data.Adapters
{
   using com.infor.sxproxy.cmproxy;
   using com.infor.sxproxy.cmproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscmcet;

   public partial class CmcetAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscmcet";
      private CMProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cmcetTableControlKey;
		
      private pdscmcetDataSet dataSet;
        
      public CmcetAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new CMProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscmcetDataSet() { DataSetName = DataSetName };
            this.cmcetTableControlKey = this.dataSet.ttblcmcet.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cmcetTableControlKey))
            {
               this.CreateTableControlParameters(this.cmcetTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CmcetAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Cmcetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCmcetproxy = this.proxyAppObject.CreatePO_cmcetproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCmcetproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Cmcetproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cmcetTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cmcetTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Cmcetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCmcetproxy = this.proxyAppObject.CreatePO_cmcetproxy())
               {
                  this.SetRequiredContextParameters();
                  poCmcetproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Cmcetproxy - After Call");
      }
   

      public Cmcet GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Cmcet cmcet = null;
         if (row != null)
         {
             cmcet = this.BuildFromRow(row);
         }
         return cmcet;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(cmcet) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcmcet.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Cmcet> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(cmcet)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Cmcet Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcmcet.AsEnumerable().SingleOrDefault();
         Cmcet cmcet = null;
         if (row != null)
         {
             cmcet = this.BuildFromRow(row);
         }
         return cmcet;
      }
	  
	  

      public IEnumerable<Cmcet> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcmcet.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Cmcet Get(string campaigncd, DateTime? schstartdt, string schstarttm, bool mustdo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(campaigncd)) 
         {
             sb.AppendFormatWithEscape("cmcet.campaigncd = '{0}'", campaigncd);
         }
         if (schstartdt != null) 
         {
             sb.AppendFormatWithEscape(" AND cmcet.schstartdt = '{0}'", schstartdt);
         }
         if (!string.IsNullOrEmpty(schstarttm)) 
         {
             sb.AppendFormatWithEscape(" AND cmcet.schstarttm = '{0}'", schstarttm);
         }
         sb.AppendFormatWithEscape(" AND cmcet.mustdo = {0}", mustdo);
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Cmcet> GetListByCampdate(string campaigncd, DateTime? schstartdt, string schstarttm, bool mustdo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(campaigncd)) 
         {
             sb.AppendFormatWithEscape("cmcet.campaigncd = '{0}'", campaigncd);
         }
         if (schstartdt != null) 
         {
             sb.AppendFormatWithEscape(" AND cmcet.schstartdt = '{0}'", schstartdt);
         }
         if (!string.IsNullOrEmpty(schstarttm)) 
         {
             sb.AppendFormatWithEscape(" AND cmcet.schstarttm = '{0}'", schstarttm);
         }
         sb.AppendFormatWithEscape(" AND cmcet.mustdo = {0}", mustdo);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Cmcet BuildFromRow(DataRow row)
      {
         var returnRecord = Cmcet.BuildCmcetFromRow(row);
         returnRecord = this.BuildExtraFromRow<Cmcet>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Cmcet record)
      {
         Cmcet.UpdateRowFromCmcet(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Cmcet Insert(Cmcet record)
      {
         DataRow row = this.dataSet.ttblcmcet.NewttblcmcetRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcmcet.AddttblcmcetRow((pdscmcetDataSet.ttblcmcetRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcmcet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmcet.Rows[0]) : null;
      }
  

      public Cmcet Update(Cmcet record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcmcet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmcet.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Cmcet record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcmcet.NewttblcmcetRow();
            Cmcet.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcmcet.AddttblcmcetRow((pdscmcetDataSet.ttblcmcetRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Cmcet() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  