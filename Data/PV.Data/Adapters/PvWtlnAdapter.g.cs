//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvWtln;

   public partial class PvWtlnAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_wtln";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvWtlnTableControlKey;
		
      private pdspv_wtlnDataSet dataSet;
        
      public PvWtlnAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_wtlnDataSet() { DataSetName = DataSetName };
            this.pvWtlnTableControlKey = this.dataSet.ttblpv_wtln.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvWtlnTableControlKey))
            {
               this.CreateTableControlParameters(this.pvWtlnTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvWtlnAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_wtlnproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_wtlnproxy = this.proxyAppObject.CreatePO_pv_wtlnproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_wtlnproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_wtlnproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvWtlnTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvWtlnTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_wtlnproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_wtlnproxy = this.proxyAppObject.CreatePO_pv_wtlnproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_wtlnproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_wtlnproxy - After Call");
      }
   

      public PvWtln GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvWtln pvWtln = null;
         if (row != null)
         {
             pvWtln = this.BuildFromRow(row);
         }
         return pvWtln;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_wtln) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_wtln.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvWtln> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_wtln)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvWtln Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_wtln.AsEnumerable().SingleOrDefault();
         PvWtln pvWtln = null;
         if (row != null)
         {
             pvWtln = this.BuildFromRow(row);
         }
         return pvWtln;
      }
	  
	  

      public IEnumerable<PvWtln> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_wtln.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvWtln Get(int wtno, int wtsuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("pv_wtln.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_wtln.wtsuf = {0}", wtsuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_wtln.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public PvWtln BuildFromRow(DataRow row)
      {
         var returnRecord = PvWtln.BuildPvWtlnFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvWtln>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvWtln record)
      {
         PvWtln.UpdateRowFromPvWtln(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvWtln Insert(PvWtln record)
      {
         DataRow row = this.dataSet.ttblpv_wtln.Newttblpv_wtlnRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_wtln.Addttblpv_wtlnRow((pdspv_wtlnDataSet.ttblpv_wtlnRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_wtln.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_wtln.Rows[0]) : null;
      }
  

      public PvWtln Update(PvWtln record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_wtln.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_wtln.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvWtln record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_wtln.Newttblpv_wtlnRow();
            PvWtln.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_wtln.Addttblpv_wtlnRow((pdspv_wtlnDataSet.ttblpv_wtlnRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvWtln() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  