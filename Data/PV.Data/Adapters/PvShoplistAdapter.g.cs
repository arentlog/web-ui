//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvShoplist;

   public partial class PvShoplistAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_shoplist";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvShoplistTableControlKey;
		private readonly Tuple<string, string> icspTableControlKey;private readonly Tuple<string, string> icswTableControlKey;
      private pdspv_shoplistDataSet dataSet;
        
      public PvShoplistAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_shoplistDataSet() { DataSetName = DataSetName };
            this.pvShoplistTableControlKey = this.dataSet.ttblpv_shoplist.GetTableControlParametersKey();
            this.icspTableControlKey = this.dataSet.ttblicsp.GetTableControlParametersKey();this.icswTableControlKey = this.dataSet.ttblicsw.GetTableControlParametersKey();
            if (!this.tempTableControlParameters.ContainsKey(this.pvShoplistTableControlKey))
            {
               this.CreateTableControlParameters(this.pvShoplistTableControlKey);
            }
            if (!this.tempTableControlParameters.ContainsKey(this.icspTableControlKey))
            {
               this.CreateTableControlParameters(this.icspTableControlKey);
            }
            if (!this.tempTableControlParameters.ContainsKey(this.icswTableControlKey))
            {
               this.CreateTableControlParameters(this.icswTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvShoplistAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, bool fillMode, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_shoplistproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, fillMode, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_shoplistproxy = this.proxyAppObject.CreatePO_pv_shoplistproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_shoplistproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_shoplistproxy - After Call");
      }
      
      private void SetAndFetch(string where, bool fillMode, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvShoplistTableControlKey, where, batchsize, fldList);
         this.SetFetchWhereParameters(this.icspTableControlKey, fillMode);
         this.SetFetchWhereParameters(this.icswTableControlKey, fillMode);
         this.SetTableParametersOnContext(this.pvShoplistTableControlKey, true);
         this.SetTableParametersOnContext(this.icspTableControlKey);
         this.SetTableParametersOnContext(this.icswTableControlKey);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_shoplistproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_shoplistproxy = this.proxyAppObject.CreatePO_pv_shoplistproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_shoplistproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_shoplistproxy - After Call");
      }
   

      public PvShoplist GetByRowId(string rowId, bool fillMode, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fillMode, fldList);
         PvShoplist pvShoplist = null;
         if (row != null)
         {
             pvShoplist = this.BuildFromRow(row);
         }
         return pvShoplist;
      }
   
      private DataRow GetRowByRowId(string rowId, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_shoplist) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), fillMode, 1, fldList);
         return this.dataSet.ttblpv_shoplist.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvShoplist> GetListByRowIdList(List<string> rowIds, bool fillMode, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_shoplist)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), fillMode, batchsize, fldList);
      }

      protected PvShoplist Fetch(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         var row = this.dataSet.ttblpv_shoplist.AsEnumerable().SingleOrDefault();
         PvShoplist pvShoplist = null;
         if (row != null)
         {
             pvShoplist = this.BuildFromRow(row);
         }
         return pvShoplist;
      }
	  
	  
      public IEnumerable<PvShoplist> GetListAllByCono(int cono, bool fillMode, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pv_shoplist.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<PvShoplist> GetList(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_shoplist.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvShoplist Get(int cono, string oper2, string prod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_shoplist.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND pv_shoplist.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND pv_shoplist.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.Fetch(where, fillMode, batchsize, fldList);
      }
  
      public IEnumerable<PvShoplist> GetListByDsplprod(int cono, string oper2, string dsplprod, int seqno, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_shoplist.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND pv_shoplist.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(dsplprod)) 
         {
             sb.AppendFormatWithEscape(" AND pv_shoplist.dsplprod = '{0}'", dsplprod);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_shoplist.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<PvShoplist> GetListByProd(int cono, string prod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_shoplist.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND pv_shoplist.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public PvShoplist BuildFromRow(DataRow row)
      {
         var returnRecord = PvShoplist.BuildPvShoplistFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvShoplist>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvShoplist record)
      {
         PvShoplist.UpdateRowFromPvShoplist(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvShoplist Insert(PvShoplist record)
      {
         DataRow row = this.dataSet.ttblpv_shoplist.Newttblpv_shoplistRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_shoplist.Addttblpv_shoplistRow((pdspv_shoplistDataSet.ttblpv_shoplistRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_shoplist.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_shoplist.Rows[0]) : null;
      }
  

      public PvShoplist Update(PvShoplist record)
      {
        var row = this.GetRowByRowId(record.rowID, false, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_shoplist.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_shoplist.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvShoplist record)
      {
         var row = this.GetRowByRowId(record.rowID, false, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_shoplist.Newttblpv_shoplistRow();
            PvShoplist.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_shoplist.Addttblpv_shoplistRow((pdspv_shoplistDataSet.ttblpv_shoplistRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvShoplist() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  