//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspvsassmobj;

   public partial class PvsassmobjAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspvsassmobj";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvsassmobjTableControlKey;
		
      private pdspvsassmobjDataSet dataSet;
        
      public PvsassmobjAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspvsassmobjDataSet() { DataSetName = DataSetName };
            this.pvsassmobjTableControlKey = this.dataSet.ttblpvsassmobj.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvsassmobjTableControlKey))
            {
               this.CreateTableControlParameters(this.pvsassmobjTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvsassmobjAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pvsassmobjproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPvsassmobjproxy = this.proxyAppObject.CreatePO_pvsassmobjproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPvsassmobjproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pvsassmobjproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvsassmobjTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvsassmobjTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pvsassmobjproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPvsassmobjproxy = this.proxyAppObject.CreatePO_pvsassmobjproxy())
               {
                  this.SetRequiredContextParameters();
                  poPvsassmobjproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pvsassmobjproxy - After Call");
      }
   

      public Pvsassmobj GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Pvsassmobj pvsassmobj = null;
         if (row != null)
         {
             pvsassmobj = this.BuildFromRow(row);
         }
         return pvsassmobj;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pvsassmobj) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpvsassmobj.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Pvsassmobj> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pvsassmobj)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Pvsassmobj Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpvsassmobj.AsEnumerable().SingleOrDefault();
         Pvsassmobj pvsassmobj = null;
         if (row != null)
         {
             pvsassmobj = this.BuildFromRow(row);
         }
         return pvsassmobj;
      }
	  
	  

      public IEnumerable<Pvsassmobj> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpvsassmobj.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Pvsassmobj Get(string menuset, string functionname, string trmgrlang, int parentid, int objid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuset)) 
         {
             sb.AppendFormatWithEscape("pvsassmobj.menuset = '{0}'", menuset);
         }
         if (!string.IsNullOrEmpty(functionname)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmobj.functionname = '{0}'", functionname);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmobj.trmgrlang = '{0}'", trmgrlang);
         }
         if (parentid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmobj.parentid = {0}", parentid);
         }
         if (objid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmobj.objid = {0}", objid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Pvsassmobj> GetListByPage(string menuset, string functionname, string trmgrlang, int parentid, int objpage, int objid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuset)) 
         {
             sb.AppendFormatWithEscape("pvsassmobj.menuset = '{0}'", menuset);
         }
         if (!string.IsNullOrEmpty(functionname)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmobj.functionname = '{0}'", functionname);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmobj.trmgrlang = '{0}'", trmgrlang);
         }
         if (parentid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmobj.parentid = {0}", parentid);
         }
         if (objpage != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmobj.objpage = {0}", objpage);
         }
         if (objid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmobj.objid = {0}", objid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Pvsassmobj BuildFromRow(DataRow row)
      {
         var returnRecord = Pvsassmobj.BuildPvsassmobjFromRow(row);
         returnRecord = this.BuildExtraFromRow<Pvsassmobj>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Pvsassmobj record)
      {
         Pvsassmobj.UpdateRowFromPvsassmobj(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Pvsassmobj Insert(Pvsassmobj record)
      {
         DataRow row = this.dataSet.ttblpvsassmobj.NewttblpvsassmobjRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpvsassmobj.AddttblpvsassmobjRow((pdspvsassmobjDataSet.ttblpvsassmobjRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpvsassmobj.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvsassmobj.Rows[0]) : null;
      }
  

      public Pvsassmobj Update(Pvsassmobj record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpvsassmobj.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvsassmobj.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Pvsassmobj record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpvsassmobj.NewttblpvsassmobjRow();
            Pvsassmobj.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpvsassmobj.AddttblpvsassmobjRow((pdspvsassmobjDataSet.ttblpvsassmobjRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Pvsassmobj() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  