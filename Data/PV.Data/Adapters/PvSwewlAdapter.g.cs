//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvSwewl;

   public partial class PvSwewlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_swewl";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvSwewlTableControlKey;
		
      private pdspv_swewlDataSet dataSet;
        
      public PvSwewlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_swewlDataSet() { DataSetName = DataSetName };
            this.pvSwewlTableControlKey = this.dataSet.ttblpv_swewl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvSwewlTableControlKey))
            {
               this.CreateTableControlParameters(this.pvSwewlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvSwewlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_swewlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_swewlproxy = this.proxyAppObject.CreatePO_pv_swewlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_swewlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_swewlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvSwewlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvSwewlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_swewlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_swewlproxy = this.proxyAppObject.CreatePO_pv_swewlproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_swewlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_swewlproxy - After Call");
      }
   

      public PvSwewl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvSwewl pvSwewl = null;
         if (row != null)
         {
             pvSwewl = this.BuildFromRow(row);
         }
         return pvSwewl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_swewl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_swewl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvSwewl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_swewl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvSwewl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_swewl.AsEnumerable().SingleOrDefault();
         PvSwewl pvSwewl = null;
         if (row != null)
         {
             pvSwewl = this.BuildFromRow(row);
         }
         return pvSwewl;
      }
	  
	  
      public IEnumerable<PvSwewl> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pv_swewl.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvSwewl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_swewl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvSwewl Get(int cono, int recjrnlno, int intclaimno, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_swewl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (recjrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_swewl.recjrnlno = {0}", recjrnlno);
         }
         if (intclaimno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_swewl.intclaimno = {0}", intclaimno);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_swewl.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<PvSwewl> GetListBySwewlprod(int cono, int recjrnlno, int intclaimno, string prod, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_swewl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (recjrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_swewl.recjrnlno = {0}", recjrnlno);
         }
         if (intclaimno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_swewl.intclaimno = {0}", intclaimno);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND pv_swewl.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public PvSwewl BuildFromRow(DataRow row)
      {
         var returnRecord = PvSwewl.BuildPvSwewlFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvSwewl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvSwewl record)
      {
         PvSwewl.UpdateRowFromPvSwewl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvSwewl Insert(PvSwewl record)
      {
         DataRow row = this.dataSet.ttblpv_swewl.Newttblpv_swewlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_swewl.Addttblpv_swewlRow((pdspv_swewlDataSet.ttblpv_swewlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_swewl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_swewl.Rows[0]) : null;
      }
  

      public PvSwewl Update(PvSwewl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_swewl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_swewl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvSwewl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_swewl.Newttblpv_swewlRow();
            PvSwewl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_swewl.Addttblpv_swewlRow((pdspv_swewlDataSet.ttblpv_swewlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvSwewl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  