//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspvregistry;

   public partial class PvregistryAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspvregistry";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvregistryTableControlKey;
		
      private pdspvregistryDataSet dataSet;
        
      public PvregistryAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspvregistryDataSet() { DataSetName = DataSetName };
            this.pvregistryTableControlKey = this.dataSet.ttblpvregistry.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvregistryTableControlKey))
            {
               this.CreateTableControlParameters(this.pvregistryTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvregistryAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pvregistryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPvregistryproxy = this.proxyAppObject.CreatePO_pvregistryproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPvregistryproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pvregistryproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvregistryTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvregistryTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pvregistryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPvregistryproxy = this.proxyAppObject.CreatePO_pvregistryproxy())
               {
                  this.SetRequiredContextParameters();
                  poPvregistryproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pvregistryproxy - After Call");
      }
   

      public Pvregistry GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Pvregistry pvregistry = null;
         if (row != null)
         {
             pvregistry = this.BuildFromRow(row);
         }
         return pvregistry;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pvregistry) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpvregistry.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Pvregistry> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pvregistry)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Pvregistry Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpvregistry.AsEnumerable().SingleOrDefault();
         Pvregistry pvregistry = null;
         if (row != null)
         {
             pvregistry = this.BuildFromRow(row);
         }
         return pvregistry;
      }
	  
	  
      public IEnumerable<Pvregistry> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pvregistry.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Pvregistry> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpvregistry.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Pvregistry Get(int cono, string oper, string pvfunction, string section, string subSection, string @object, string keyname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pvregistry.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper)) 
         {
             sb.AppendFormatWithEscape(" AND pvregistry.Oper = '{0}'", oper);
         }
         if (!string.IsNullOrEmpty(pvfunction)) 
         {
             sb.AppendFormatWithEscape(" AND pvregistry.pvfunction = '{0}'", pvfunction);
         }
         if (!string.IsNullOrEmpty(section)) 
         {
             sb.AppendFormatWithEscape(" AND pvregistry.Section = '{0}'", section);
         }
         if (!string.IsNullOrEmpty(subSection)) 
         {
             sb.AppendFormatWithEscape(" AND pvregistry.SubSection = '{0}'", subSection);
         }
         if (!string.IsNullOrEmpty(@object)) 
         {
             sb.AppendFormatWithEscape(" AND pvregistry.Object = '{0}'", @object);
         }
         if (!string.IsNullOrEmpty(keyname)) 
         {
             sb.AppendFormatWithEscape(" AND pvregistry.Keyname = '{0}'", keyname);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Pvregistry BuildFromRow(DataRow row)
      {
         var returnRecord = Pvregistry.BuildPvregistryFromRow(row);
         returnRecord = this.BuildExtraFromRow<Pvregistry>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Pvregistry record)
      {
         Pvregistry.UpdateRowFromPvregistry(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Pvregistry Insert(Pvregistry record)
      {
         DataRow row = this.dataSet.ttblpvregistry.NewttblpvregistryRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpvregistry.AddttblpvregistryRow((pdspvregistryDataSet.ttblpvregistryRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpvregistry.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvregistry.Rows[0]) : null;
      }
  

      public Pvregistry Update(Pvregistry record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpvregistry.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvregistry.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Pvregistry record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpvregistry.NewttblpvregistryRow();
            Pvregistry.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpvregistry.AddttblpvregistryRow((pdspvregistryDataSet.ttblpvregistryRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Pvregistry() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  