//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvImages;

   public partial class PvImagesAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_images";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvImagesTableControlKey;
		
      private pdspv_imagesDataSet dataSet;
        
      public PvImagesAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_imagesDataSet() { DataSetName = DataSetName };
            this.pvImagesTableControlKey = this.dataSet.ttblpv_images.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvImagesTableControlKey))
            {
               this.CreateTableControlParameters(this.pvImagesTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvImagesAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_imagesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_imagesproxy = this.proxyAppObject.CreatePO_pv_imagesproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_imagesproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_imagesproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvImagesTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvImagesTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_imagesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_imagesproxy = this.proxyAppObject.CreatePO_pv_imagesproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_imagesproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_imagesproxy - After Call");
      }
   

      public PvImages GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvImages pvImages = null;
         if (row != null)
         {
             pvImages = this.BuildFromRow(row);
         }
         return pvImages;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_images) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_images.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvImages> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_images)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvImages Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_images.AsEnumerable().SingleOrDefault();
         PvImages pvImages = null;
         if (row != null)
         {
             pvImages = this.BuildFromRow(row);
         }
         return pvImages;
      }
	  
	  
      public IEnumerable<PvImages> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pv_images.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvImages> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_images.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvImages Get(int cono, string keytype, string keyvalue1, string keyvalue2, int chunk, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_images.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(keytype)) 
         {
             sb.AppendFormatWithEscape(" AND pv_images.keytype = '{0}'", keytype);
         }
         if (!string.IsNullOrEmpty(keyvalue1)) 
         {
             sb.AppendFormatWithEscape(" AND pv_images.keyvalue1 = '{0}'", keyvalue1);
         }
         if (!string.IsNullOrEmpty(keyvalue2)) 
         {
             sb.AppendFormatWithEscape(" AND pv_images.keyvalue2 = '{0}'", keyvalue2);
         }
         if (chunk != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_images.chunk = {0}", chunk);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public PvImages BuildFromRow(DataRow row)
      {
         var returnRecord = PvImages.BuildPvImagesFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvImages>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvImages record)
      {
         PvImages.UpdateRowFromPvImages(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvImages Insert(PvImages record)
      {
         DataRow row = this.dataSet.ttblpv_images.Newttblpv_imagesRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_images.Addttblpv_imagesRow((pdspv_imagesDataSet.ttblpv_imagesRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_images.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_images.Rows[0]) : null;
      }
  

      public PvImages Update(PvImages record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_images.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_images.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvImages record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_images.Newttblpv_imagesRow();
            PvImages.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_images.Addttblpv_imagesRow((pdspv_imagesDataSet.ttblpv_imagesRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvImages() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  