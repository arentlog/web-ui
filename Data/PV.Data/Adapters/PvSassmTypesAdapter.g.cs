//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvSassmTypes;

   public partial class PvSassmTypesAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_sassm_types";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvSassmTypesTableControlKey;
		
      private pdspv_sassm_typesDataSet dataSet;
        
      public PvSassmTypesAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_sassm_typesDataSet() { DataSetName = DataSetName };
            this.pvSassmTypesTableControlKey = this.dataSet.ttblpv_sassm_types.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvSassmTypesTableControlKey))
            {
               this.CreateTableControlParameters(this.pvSassmTypesTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvSassmTypesAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_sassm_typesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_sassm_typesproxy = this.proxyAppObject.CreatePO_pv_sassm_typesproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_sassm_typesproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_sassm_typesproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvSassmTypesTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvSassmTypesTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_sassm_typesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_sassm_typesproxy = this.proxyAppObject.CreatePO_pv_sassm_typesproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_sassm_typesproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_sassm_typesproxy - After Call");
      }
   

      public PvSassmTypes GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvSassmTypes pvSassmTypes = null;
         if (row != null)
         {
             pvSassmTypes = this.BuildFromRow(row);
         }
         return pvSassmTypes;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_sassm_types) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_sassm_types.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvSassmTypes> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_sassm_types)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvSassmTypes Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_sassm_types.AsEnumerable().SingleOrDefault();
         PvSassmTypes pvSassmTypes = null;
         if (row != null)
         {
             pvSassmTypes = this.BuildFromRow(row);
         }
         return pvSassmTypes;
      }
	  
	  

      public IEnumerable<PvSassmTypes> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_sassm_types.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvSassmTypes Get(string typeCode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(typeCode)) 
         {
             sb.AppendFormatWithEscape("pv_sassm_types.type_code = '{0}'", typeCode);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public PvSassmTypes BuildFromRow(DataRow row)
      {
         var returnRecord = PvSassmTypes.BuildPvSassmTypesFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvSassmTypes>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvSassmTypes record)
      {
         PvSassmTypes.UpdateRowFromPvSassmTypes(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvSassmTypes Insert(PvSassmTypes record)
      {
         DataRow row = this.dataSet.ttblpv_sassm_types.Newttblpv_sassm_typesRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_sassm_types.Addttblpv_sassm_typesRow((pdspv_sassm_typesDataSet.ttblpv_sassm_typesRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_sassm_types.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_sassm_types.Rows[0]) : null;
      }
  

      public PvSassmTypes Update(PvSassmTypes record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_sassm_types.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_sassm_types.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvSassmTypes record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_sassm_types.Newttblpv_sassm_typesRow();
            PvSassmTypes.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_sassm_types.Addttblpv_sassm_typesRow((pdspv_sassm_typesDataSet.ttblpv_sassm_typesRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvSassmTypes() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  