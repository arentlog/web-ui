//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvAttachments;

   public partial class PvAttachmentsAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_attachments";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvAttachmentsTableControlKey;
		
      private pdspv_attachmentsDataSet dataSet;
        
      public PvAttachmentsAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_attachmentsDataSet() { DataSetName = DataSetName };
            this.pvAttachmentsTableControlKey = this.dataSet.ttblpv_attachments.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvAttachmentsTableControlKey))
            {
               this.CreateTableControlParameters(this.pvAttachmentsTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvAttachmentsAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_attachmentsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_attachmentsproxy = this.proxyAppObject.CreatePO_pv_attachmentsproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_attachmentsproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_attachmentsproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvAttachmentsTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvAttachmentsTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_attachmentsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_attachmentsproxy = this.proxyAppObject.CreatePO_pv_attachmentsproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_attachmentsproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_attachmentsproxy - After Call");
      }
   

      public PvAttachments GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvAttachments pvAttachments = null;
         if (row != null)
         {
             pvAttachments = this.BuildFromRow(row);
         }
         return pvAttachments;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_attachments) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_attachments.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvAttachments> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_attachments)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvAttachments Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_attachments.AsEnumerable().SingleOrDefault();
         PvAttachments pvAttachments = null;
         if (row != null)
         {
             pvAttachments = this.BuildFromRow(row);
         }
         return pvAttachments;
      }
	  
	  

      public IEnumerable<PvAttachments> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_attachments.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvAttachments Get(int docID, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (docID != int.MinValue) 
         {
             sb.AppendFormatWithEscape("pv_attachments.DocID = {0}", docID);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public PvAttachments BuildFromRow(DataRow row)
      {
         var returnRecord = PvAttachments.BuildPvAttachmentsFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvAttachments>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvAttachments record)
      {
         PvAttachments.UpdateRowFromPvAttachments(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvAttachments Insert(PvAttachments record)
      {
         DataRow row = this.dataSet.ttblpv_attachments.Newttblpv_attachmentsRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_attachments.Addttblpv_attachmentsRow((pdspv_attachmentsDataSet.ttblpv_attachmentsRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_attachments.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_attachments.Rows[0]) : null;
      }
  

      public PvAttachments Update(PvAttachments record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_attachments.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_attachments.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvAttachments record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_attachments.Newttblpv_attachmentsRow();
            PvAttachments.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_attachments.Addttblpv_attachmentsRow((pdspv_attachmentsDataSet.ttblpv_attachmentsRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvAttachments() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  