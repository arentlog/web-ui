//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvOeln;

   public partial class PvOelnAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_oeln";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvOelnTableControlKey;
		
      private pdspv_oelnDataSet dataSet;
        
      public PvOelnAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_oelnDataSet() { DataSetName = DataSetName };
            this.pvOelnTableControlKey = this.dataSet.ttblpv_oeln.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvOelnTableControlKey))
            {
               this.CreateTableControlParameters(this.pvOelnTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvOelnAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_oelnproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_oelnproxy = this.proxyAppObject.CreatePO_pv_oelnproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_oelnproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_oelnproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvOelnTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvOelnTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_oelnproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_oelnproxy = this.proxyAppObject.CreatePO_pv_oelnproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_oelnproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_oelnproxy - After Call");
      }
   

      public PvOeln GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvOeln pvOeln = null;
         if (row != null)
         {
             pvOeln = this.BuildFromRow(row);
         }
         return pvOeln;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_oeln) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_oeln.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvOeln> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_oeln)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvOeln Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_oeln.AsEnumerable().SingleOrDefault();
         PvOeln pvOeln = null;
         if (row != null)
         {
             pvOeln = this.BuildFromRow(row);
         }
         return pvOeln;
      }
	  
	  
      public IEnumerable<PvOeln> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pv_oeln.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvOeln> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_oeln.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvOeln Get(int cono, string batchnm, int orderno, int ordersuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_oeln.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(batchnm)) 
         {
             sb.AppendFormatWithEscape(" AND pv_oeln.batchnm = '{0}'", batchnm);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_oeln.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_oeln.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_oeln.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public PvOeln BuildFromRow(DataRow row)
      {
         var returnRecord = PvOeln.BuildPvOelnFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvOeln>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvOeln record)
      {
         PvOeln.UpdateRowFromPvOeln(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvOeln Insert(PvOeln record)
      {
         DataRow row = this.dataSet.ttblpv_oeln.Newttblpv_oelnRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_oeln.Addttblpv_oelnRow((pdspv_oelnDataSet.ttblpv_oelnRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_oeln.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_oeln.Rows[0]) : null;
      }
  

      public PvOeln Update(PvOeln record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_oeln.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_oeln.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvOeln record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_oeln.Newttblpv_oelnRow();
            PvOeln.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_oeln.Addttblpv_oelnRow((pdspv_oelnDataSet.ttblpv_oelnRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvOeln() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  