//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvSamb;

   public partial class PvSambAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_samb";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvSambTableControlKey;
		
      private pdspv_sambDataSet dataSet;
        
      public PvSambAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_sambDataSet() { DataSetName = DataSetName };
            this.pvSambTableControlKey = this.dataSet.ttblpv_samb.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvSambTableControlKey))
            {
               this.CreateTableControlParameters(this.pvSambTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvSambAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_sambproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_sambproxy = this.proxyAppObject.CreatePO_pv_sambproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_sambproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_sambproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvSambTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvSambTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_sambproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_sambproxy = this.proxyAppObject.CreatePO_pv_sambproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_sambproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_sambproxy - After Call");
      }
   

      public PvSamb GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvSamb pvSamb = null;
         if (row != null)
         {
             pvSamb = this.BuildFromRow(row);
         }
         return pvSamb;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_samb) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_samb.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvSamb> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_samb)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvSamb Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_samb.AsEnumerable().SingleOrDefault();
         PvSamb pvSamb = null;
         if (row != null)
         {
             pvSamb = this.BuildFromRow(row);
         }
         return pvSamb;
      }
	  
	  
      public IEnumerable<PvSamb> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pv_samb.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvSamb> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_samb.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvSamb Get(int cono, string functionname, string primarykey, string secondarykey, DateTime? enterdt, string entertm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_samb.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(functionname)) 
         {
             sb.AppendFormatWithEscape(" AND pv_samb.functionname = '{0}'", functionname);
         }
         if (!string.IsNullOrEmpty(primarykey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_samb.primarykey = '{0}'", primarykey);
         }
         if (!string.IsNullOrEmpty(secondarykey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_samb.secondarykey = '{0}'", secondarykey);
         }
         if (enterdt != null) 
         {
             sb.AppendFormatWithEscape(" AND pv_samb.enterdt = '{0}'", enterdt);
         }
         if (!string.IsNullOrEmpty(entertm)) 
         {
             sb.AppendFormatWithEscape(" AND pv_samb.entertm = '{0}'", entertm);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<PvSamb> GetListByPrimary(int cono, string primarykey, string secondarykey, DateTime? enterdt, string entertm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_samb.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(primarykey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_samb.primarykey = '{0}'", primarykey);
         }
         if (!string.IsNullOrEmpty(secondarykey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_samb.secondarykey = '{0}'", secondarykey);
         }
         if (enterdt != null) 
         {
             sb.AppendFormatWithEscape(" AND pv_samb.enterdt = '{0}'", enterdt);
         }
         if (!string.IsNullOrEmpty(entertm)) 
         {
             sb.AppendFormatWithEscape(" AND pv_samb.entertm = '{0}'", entertm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public PvSamb BuildFromRow(DataRow row)
      {
         var returnRecord = PvSamb.BuildPvSambFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvSamb>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvSamb record)
      {
         PvSamb.UpdateRowFromPvSamb(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvSamb Insert(PvSamb record)
      {
         DataRow row = this.dataSet.ttblpv_samb.Newttblpv_sambRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_samb.Addttblpv_sambRow((pdspv_sambDataSet.ttblpv_sambRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_samb.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_samb.Rows[0]) : null;
      }
  

      public PvSamb Update(PvSamb record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_samb.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_samb.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvSamb record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_samb.Newttblpv_sambRow();
            PvSamb.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_samb.Addttblpv_sambRow((pdspv_sambDataSet.ttblpv_sambRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvSamb() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  