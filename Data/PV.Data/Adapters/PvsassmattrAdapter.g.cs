//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspvsassmattr;

   public partial class PvsassmattrAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspvsassmattr";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvsassmattrTableControlKey;
		
      private pdspvsassmattrDataSet dataSet;
        
      public PvsassmattrAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspvsassmattrDataSet() { DataSetName = DataSetName };
            this.pvsassmattrTableControlKey = this.dataSet.ttblpvsassmattr.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvsassmattrTableControlKey))
            {
               this.CreateTableControlParameters(this.pvsassmattrTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvsassmattrAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pvsassmattrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPvsassmattrproxy = this.proxyAppObject.CreatePO_pvsassmattrproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPvsassmattrproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pvsassmattrproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvsassmattrTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvsassmattrTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pvsassmattrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPvsassmattrproxy = this.proxyAppObject.CreatePO_pvsassmattrproxy())
               {
                  this.SetRequiredContextParameters();
                  poPvsassmattrproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pvsassmattrproxy - After Call");
      }
   

      public Pvsassmattr GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Pvsassmattr pvsassmattr = null;
         if (row != null)
         {
             pvsassmattr = this.BuildFromRow(row);
         }
         return pvsassmattr;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pvsassmattr) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpvsassmattr.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Pvsassmattr> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pvsassmattr)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Pvsassmattr Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpvsassmattr.AsEnumerable().SingleOrDefault();
         Pvsassmattr pvsassmattr = null;
         if (row != null)
         {
             pvsassmattr = this.BuildFromRow(row);
         }
         return pvsassmattr;
      }
	  
	  

      public IEnumerable<Pvsassmattr> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpvsassmattr.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Pvsassmattr Get(string menuset, string functionname, string trmgrlang, int objid, int attrid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuset)) 
         {
             sb.AppendFormatWithEscape("pvsassmattr.menuset = '{0}'", menuset);
         }
         if (!string.IsNullOrEmpty(functionname)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.functionname = '{0}'", functionname);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.trmgrlang = '{0}'", trmgrlang);
         }
         if (objid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.objid = {0}", objid);
         }
         if (attrid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.attrid = {0}", attrid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Pvsassmattr> GetListByIdname(string menuset, string functionname, string trmgrlang, int objid, string attrname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuset)) 
         {
             sb.AppendFormatWithEscape("pvsassmattr.menuset = '{0}'", menuset);
         }
         if (!string.IsNullOrEmpty(functionname)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.functionname = '{0}'", functionname);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.trmgrlang = '{0}'", trmgrlang);
         }
         if (objid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.objid = {0}", objid);
         }
         if (!string.IsNullOrEmpty(attrname)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.attrname = '{0}'", attrname);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Pvsassmattr> GetListByName(string menuset, string functionname, string trmgrlang, string attrname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuset)) 
         {
             sb.AppendFormatWithEscape("pvsassmattr.menuset = '{0}'", menuset);
         }
         if (!string.IsNullOrEmpty(functionname)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.functionname = '{0}'", functionname);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.trmgrlang = '{0}'", trmgrlang);
         }
         if (!string.IsNullOrEmpty(attrname)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmattr.attrname = '{0}'", attrname);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Pvsassmattr BuildFromRow(DataRow row)
      {
         var returnRecord = Pvsassmattr.BuildPvsassmattrFromRow(row);
         returnRecord = this.BuildExtraFromRow<Pvsassmattr>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Pvsassmattr record)
      {
         Pvsassmattr.UpdateRowFromPvsassmattr(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Pvsassmattr Insert(Pvsassmattr record)
      {
         DataRow row = this.dataSet.ttblpvsassmattr.NewttblpvsassmattrRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpvsassmattr.AddttblpvsassmattrRow((pdspvsassmattrDataSet.ttblpvsassmattrRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpvsassmattr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvsassmattr.Rows[0]) : null;
      }
  

      public Pvsassmattr Update(Pvsassmattr record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpvsassmattr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvsassmattr.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Pvsassmattr record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpvsassmattr.NewttblpvsassmattrRow();
            Pvsassmattr.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpvsassmattr.AddttblpvsassmattrRow((pdspvsassmattrDataSet.ttblpvsassmattrRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Pvsassmattr() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  