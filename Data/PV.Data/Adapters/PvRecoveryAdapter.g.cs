//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvRecovery;

   public partial class PvRecoveryAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_recovery";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvRecoveryTableControlKey;
		
      private pdspv_recoveryDataSet dataSet;
        
      public PvRecoveryAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_recoveryDataSet() { DataSetName = DataSetName };
            this.pvRecoveryTableControlKey = this.dataSet.ttblpv_recovery.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvRecoveryTableControlKey))
            {
               this.CreateTableControlParameters(this.pvRecoveryTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvRecoveryAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_recoveryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_recoveryproxy = this.proxyAppObject.CreatePO_pv_recoveryproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_recoveryproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_recoveryproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvRecoveryTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvRecoveryTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_recoveryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_recoveryproxy = this.proxyAppObject.CreatePO_pv_recoveryproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_recoveryproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_recoveryproxy - After Call");
      }
   

      public PvRecovery GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvRecovery pvRecovery = null;
         if (row != null)
         {
             pvRecovery = this.BuildFromRow(row);
         }
         return pvRecovery;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_recovery) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_recovery.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvRecovery> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_recovery)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvRecovery Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_recovery.AsEnumerable().SingleOrDefault();
         PvRecovery pvRecovery = null;
         if (row != null)
         {
             pvRecovery = this.BuildFromRow(row);
         }
         return pvRecovery;
      }
	  
	  
      public IEnumerable<PvRecovery> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pv_recovery.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvRecovery> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_recovery.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvRecovery Get(int cono, string oper2, string functionname, int jrnlno, string recoverydata, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_recovery.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND pv_recovery.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(functionname)) 
         {
             sb.AppendFormatWithEscape(" AND pv_recovery.functionname = '{0}'", functionname);
         }
         if (jrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_recovery.jrnlno = {0}", jrnlno);
         }
         if (!string.IsNullOrEmpty(recoverydata)) 
         {
             sb.AppendFormatWithEscape(" AND pv_recovery.recoverydata = '{0}'", recoverydata);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public PvRecovery BuildFromRow(DataRow row)
      {
         var returnRecord = PvRecovery.BuildPvRecoveryFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvRecovery>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvRecovery record)
      {
         PvRecovery.UpdateRowFromPvRecovery(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvRecovery Insert(PvRecovery record)
      {
         DataRow row = this.dataSet.ttblpv_recovery.Newttblpv_recoveryRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_recovery.Addttblpv_recoveryRow((pdspv_recoveryDataSet.ttblpv_recoveryRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_recovery.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_recovery.Rows[0]) : null;
      }
  

      public PvRecovery Update(PvRecovery record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_recovery.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_recovery.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvRecovery record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_recovery.Newttblpv_recoveryRow();
            PvRecovery.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_recovery.Addttblpv_recoveryRow((pdspv_recoveryDataSet.ttblpv_recoveryRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvRecovery() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  