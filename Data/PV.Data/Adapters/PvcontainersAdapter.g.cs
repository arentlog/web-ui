//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspvcontainers;

   public partial class PvcontainersAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspvcontainers";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvcontainersTableControlKey;
		
      private pdspvcontainersDataSet dataSet;
        
      public PvcontainersAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspvcontainersDataSet() { DataSetName = DataSetName };
            this.pvcontainersTableControlKey = this.dataSet.ttblpvcontainers.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvcontainersTableControlKey))
            {
               this.CreateTableControlParameters(this.pvcontainersTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvcontainersAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pvcontainersproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPvcontainersproxy = this.proxyAppObject.CreatePO_pvcontainersproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPvcontainersproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pvcontainersproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvcontainersTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvcontainersTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pvcontainersproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPvcontainersproxy = this.proxyAppObject.CreatePO_pvcontainersproxy())
               {
                  this.SetRequiredContextParameters();
                  poPvcontainersproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pvcontainersproxy - After Call");
      }
   

      public Pvcontainers GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Pvcontainers pvcontainers = null;
         if (row != null)
         {
             pvcontainers = this.BuildFromRow(row);
         }
         return pvcontainers;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pvcontainers) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpvcontainers.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Pvcontainers> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pvcontainers)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Pvcontainers Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpvcontainers.AsEnumerable().SingleOrDefault();
         Pvcontainers pvcontainers = null;
         if (row != null)
         {
             pvcontainers = this.BuildFromRow(row);
         }
         return pvcontainers;
      }
	  
	  
      public IEnumerable<Pvcontainers> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pvcontainers.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Pvcontainers> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpvcontainers.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Pvcontainers Get(int cono, string oper2, string name, string type, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pvcontainers.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND pvcontainers.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape(" AND pvcontainers.name = '{0}'", name);
         }
         if (!string.IsNullOrEmpty(type)) 
         {
             sb.AppendFormatWithEscape(" AND pvcontainers.type = '{0}'", type);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Pvcontainers BuildFromRow(DataRow row)
      {
         var returnRecord = Pvcontainers.BuildPvcontainersFromRow(row);
         returnRecord = this.BuildExtraFromRow<Pvcontainers>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Pvcontainers record)
      {
         Pvcontainers.UpdateRowFromPvcontainers(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Pvcontainers Insert(Pvcontainers record)
      {
         DataRow row = this.dataSet.ttblpvcontainers.NewttblpvcontainersRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpvcontainers.AddttblpvcontainersRow((pdspvcontainersDataSet.ttblpvcontainersRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpvcontainers.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvcontainers.Rows[0]) : null;
      }
  

      public Pvcontainers Update(Pvcontainers record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpvcontainers.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvcontainers.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Pvcontainers record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpvcontainers.NewttblpvcontainersRow();
            Pvcontainers.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpvcontainers.AddttblpvcontainersRow((pdspvcontainersDataSet.ttblpvcontainersRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Pvcontainers() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  