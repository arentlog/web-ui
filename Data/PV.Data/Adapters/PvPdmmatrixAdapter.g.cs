//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvPdmmatrix;

   public partial class PvPdmmatrixAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_pdmmatrix";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvPdmmatrixTableControlKey;
		
      private pdspv_pdmmatrixDataSet dataSet;
        
      public PvPdmmatrixAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_pdmmatrixDataSet() { DataSetName = DataSetName };
            this.pvPdmmatrixTableControlKey = this.dataSet.ttblpv_pdmmatrix.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvPdmmatrixTableControlKey))
            {
               this.CreateTableControlParameters(this.pvPdmmatrixTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvPdmmatrixAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_pdmmatrixproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_pdmmatrixproxy = this.proxyAppObject.CreatePO_pv_pdmmatrixproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_pdmmatrixproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_pdmmatrixproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvPdmmatrixTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvPdmmatrixTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_pdmmatrixproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_pdmmatrixproxy = this.proxyAppObject.CreatePO_pv_pdmmatrixproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_pdmmatrixproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_pdmmatrixproxy - After Call");
      }
   

      public PvPdmmatrix GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvPdmmatrix pvPdmmatrix = null;
         if (row != null)
         {
             pvPdmmatrix = this.BuildFromRow(row);
         }
         return pvPdmmatrix;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_pdmmatrix) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_pdmmatrix.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvPdmmatrix> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_pdmmatrix)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvPdmmatrix Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_pdmmatrix.AsEnumerable().SingleOrDefault();
         PvPdmmatrix pvPdmmatrix = null;
         if (row != null)
         {
             pvPdmmatrix = this.BuildFromRow(row);
         }
         return pvPdmmatrix;
      }
	  
	  

      public IEnumerable<PvPdmmatrix> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_pdmmatrix.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvPdmmatrix Get(string destination, string source, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(destination)) 
         {
             sb.AppendFormatWithEscape("pv_pdmmatrix.Destination = '{0}'", destination);
         }
         if (!string.IsNullOrEmpty(source)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmmatrix.Source = '{0}'", source);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<PvPdmmatrix> GetListBySource(string source, string destination, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(source)) 
         {
             sb.AppendFormatWithEscape("pv_pdmmatrix.Source = '{0}'", source);
         }
         if (!string.IsNullOrEmpty(destination)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmmatrix.Destination = '{0}'", destination);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public PvPdmmatrix BuildFromRow(DataRow row)
      {
         var returnRecord = PvPdmmatrix.BuildPvPdmmatrixFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvPdmmatrix>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvPdmmatrix record)
      {
         PvPdmmatrix.UpdateRowFromPvPdmmatrix(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvPdmmatrix Insert(PvPdmmatrix record)
      {
         DataRow row = this.dataSet.ttblpv_pdmmatrix.Newttblpv_pdmmatrixRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_pdmmatrix.Addttblpv_pdmmatrixRow((pdspv_pdmmatrixDataSet.ttblpv_pdmmatrixRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_pdmmatrix.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_pdmmatrix.Rows[0]) : null;
      }
  

      public PvPdmmatrix Update(PvPdmmatrix record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_pdmmatrix.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_pdmmatrix.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvPdmmatrix record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_pdmmatrix.Newttblpv_pdmmatrixRow();
            PvPdmmatrix.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_pdmmatrix.Addttblpv_pdmmatrixRow((pdspv_pdmmatrixDataSet.ttblpv_pdmmatrixRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvPdmmatrix() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  