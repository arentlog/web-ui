//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvPdmhdr;

   public partial class PvPdmhdrAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_pdmhdr";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvPdmhdrTableControlKey;
		
      private pdspv_pdmhdrDataSet dataSet;
        
      public PvPdmhdrAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_pdmhdrDataSet() { DataSetName = DataSetName };
            this.pvPdmhdrTableControlKey = this.dataSet.ttblpv_pdmhdr.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvPdmhdrTableControlKey))
            {
               this.CreateTableControlParameters(this.pvPdmhdrTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvPdmhdrAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_pdmhdrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_pdmhdrproxy = this.proxyAppObject.CreatePO_pv_pdmhdrproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_pdmhdrproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_pdmhdrproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvPdmhdrTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvPdmhdrTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_pdmhdrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_pdmhdrproxy = this.proxyAppObject.CreatePO_pv_pdmhdrproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_pdmhdrproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_pdmhdrproxy - After Call");
      }
   

      public PvPdmhdr GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvPdmhdr pvPdmhdr = null;
         if (row != null)
         {
             pvPdmhdr = this.BuildFromRow(row);
         }
         return pvPdmhdr;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_pdmhdr) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_pdmhdr.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvPdmhdr> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_pdmhdr)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvPdmhdr Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_pdmhdr.AsEnumerable().SingleOrDefault();
         PvPdmhdr pvPdmhdr = null;
         if (row != null)
         {
             pvPdmhdr = this.BuildFromRow(row);
         }
         return pvPdmhdr;
      }
	  
	  

      public IEnumerable<PvPdmhdr> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_pdmhdr.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvPdmhdr Get(int cono, string setID, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (cono != int.MinValue) 
         {
             sb.AppendFormatWithEscape("pv_pdmhdr.Cono = {0}", cono);
         }
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmhdr.SetID = '{0}'", setID);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public PvPdmhdr BuildFromRow(DataRow row)
      {
         var returnRecord = PvPdmhdr.BuildPvPdmhdrFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvPdmhdr>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvPdmhdr record)
      {
         PvPdmhdr.UpdateRowFromPvPdmhdr(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvPdmhdr Insert(PvPdmhdr record)
      {
         DataRow row = this.dataSet.ttblpv_pdmhdr.Newttblpv_pdmhdrRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_pdmhdr.Addttblpv_pdmhdrRow((pdspv_pdmhdrDataSet.ttblpv_pdmhdrRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_pdmhdr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_pdmhdr.Rows[0]) : null;
      }
  

      public PvPdmhdr Update(PvPdmhdr record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_pdmhdr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_pdmhdr.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvPdmhdr record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_pdmhdr.Newttblpv_pdmhdrRow();
            PvPdmhdr.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_pdmhdr.Addttblpv_pdmhdrRow((pdspv_pdmhdrDataSet.ttblpv_pdmhdrRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvPdmhdr() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  