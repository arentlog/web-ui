//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvAdminlog;

   public partial class PvAdminlogAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_adminlog";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvAdminlogTableControlKey;
		
      private pdspv_adminlogDataSet dataSet;
        
      public PvAdminlogAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_adminlogDataSet() { DataSetName = DataSetName };
            this.pvAdminlogTableControlKey = this.dataSet.ttblpv_adminlog.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvAdminlogTableControlKey))
            {
               this.CreateTableControlParameters(this.pvAdminlogTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvAdminlogAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_adminlogproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_adminlogproxy = this.proxyAppObject.CreatePO_pv_adminlogproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_adminlogproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_adminlogproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvAdminlogTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvAdminlogTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_adminlogproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_adminlogproxy = this.proxyAppObject.CreatePO_pv_adminlogproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_adminlogproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_adminlogproxy - After Call");
      }
   

      public PvAdminlog GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvAdminlog pvAdminlog = null;
         if (row != null)
         {
             pvAdminlog = this.BuildFromRow(row);
         }
         return pvAdminlog;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_adminlog) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_adminlog.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvAdminlog> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_adminlog)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvAdminlog Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_adminlog.AsEnumerable().SingleOrDefault();
         PvAdminlog pvAdminlog = null;
         if (row != null)
         {
             pvAdminlog = this.BuildFromRow(row);
         }
         return pvAdminlog;
      }
	  
	  
      public IEnumerable<PvAdminlog> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pv_adminlog.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvAdminlog> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_adminlog.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvAdminlog Get(int cono, string subject, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_adminlog.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(subject)) 
         {
             sb.AppendFormatWithEscape(" AND pv_adminlog.subject = '{0}'", subject);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<PvAdminlog> GetListByFunction(int cono, string function, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_adminlog.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(function)) 
         {
             sb.AppendFormatWithEscape(" AND pv_adminlog.function = '{0}'", function);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public PvAdminlog BuildFromRow(DataRow row)
      {
         var returnRecord = PvAdminlog.BuildPvAdminlogFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvAdminlog>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvAdminlog record)
      {
         PvAdminlog.UpdateRowFromPvAdminlog(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvAdminlog Insert(PvAdminlog record)
      {
         DataRow row = this.dataSet.ttblpv_adminlog.Newttblpv_adminlogRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_adminlog.Addttblpv_adminlogRow((pdspv_adminlogDataSet.ttblpv_adminlogRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_adminlog.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_adminlog.Rows[0]) : null;
      }
  

      public PvAdminlog Update(PvAdminlog record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_adminlog.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_adminlog.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvAdminlog record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_adminlog.Newttblpv_adminlogRow();
            PvAdminlog.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_adminlog.Addttblpv_adminlogRow((pdspv_adminlogDataSet.ttblpv_adminlogRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvAdminlog() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public void pv_adminlogSave(IEnumerable<PvAdminlog> pv_adminlog)
      {   
         
         var pdspv_adminlog = new pdspv_adminlogDataSet();
            
         foreach (var obj in pv_adminlog)
         {
             DataRow row = pdspv_adminlog.ttblpv_adminlog.Newttblpv_adminlogRow();
             PvAdminlog.UpdateRowFromPvAdminlog(ref row, obj);
             pdspv_adminlog.ttblpv_adminlog.Addttblpv_adminlogRow((pdspv_adminlogDataSet.ttblpv_adminlogRow)row);
         }
        
         string cErrorMessage = string.Empty;
   
         
         NLogLoggerP.Trace("pv_adminlogSave - PvAdminlog - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPvAdminlogproxy = this.proxyAppObject.CreatePO_pv_adminlogproxy())
               {
                   this.SetRequiredContextParameters();
                   poPvAdminlogproxy.pv_adminlogSave(ref pdsContext,  pdspv_adminlog, out cErrorMessage);
               }
            });
         NLogLoggerP.Info("pv_adminlogSave - PvAdminlog - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdspv_adminlog);
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  