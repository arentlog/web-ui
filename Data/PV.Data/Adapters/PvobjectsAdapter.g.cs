//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspvobjects;

   public partial class PvobjectsAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspvobjects";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvobjectsTableControlKey;
		
      private pdspvobjectsDataSet dataSet;
        
      public PvobjectsAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspvobjectsDataSet() { DataSetName = DataSetName };
            this.pvobjectsTableControlKey = this.dataSet.ttblpvobjects.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvobjectsTableControlKey))
            {
               this.CreateTableControlParameters(this.pvobjectsTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvobjectsAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pvobjectsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPvobjectsproxy = this.proxyAppObject.CreatePO_pvobjectsproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPvobjectsproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pvobjectsproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvobjectsTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvobjectsTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pvobjectsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPvobjectsproxy = this.proxyAppObject.CreatePO_pvobjectsproxy())
               {
                  this.SetRequiredContextParameters();
                  poPvobjectsproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pvobjectsproxy - After Call");
      }
   

      public Pvobjects GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Pvobjects pvobjects = null;
         if (row != null)
         {
             pvobjects = this.BuildFromRow(row);
         }
         return pvobjects;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pvobjects) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpvobjects.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Pvobjects> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pvobjects)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Pvobjects Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpvobjects.AsEnumerable().SingleOrDefault();
         Pvobjects pvobjects = null;
         if (row != null)
         {
             pvobjects = this.BuildFromRow(row);
         }
         return pvobjects;
      }
	  
	  
      public IEnumerable<Pvobjects> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pvobjects.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Pvobjects> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpvobjects.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Pvobjects Get(int cono, string oper2, string name, string objectid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pvobjects.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND pvobjects.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape(" AND pvobjects.name = '{0}'", name);
         }
         if (!string.IsNullOrEmpty(objectid)) 
         {
             sb.AppendFormatWithEscape(" AND pvobjects.objectid = '{0}'", objectid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Pvobjects BuildFromRow(DataRow row)
      {
         var returnRecord = Pvobjects.BuildPvobjectsFromRow(row);
         returnRecord = this.BuildExtraFromRow<Pvobjects>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Pvobjects record)
      {
         Pvobjects.UpdateRowFromPvobjects(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Pvobjects Insert(Pvobjects record)
      {
         DataRow row = this.dataSet.ttblpvobjects.NewttblpvobjectsRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpvobjects.AddttblpvobjectsRow((pdspvobjectsDataSet.ttblpvobjectsRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpvobjects.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvobjects.Rows[0]) : null;
      }
  

      public Pvobjects Update(Pvobjects record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpvobjects.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvobjects.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Pvobjects record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpvobjects.NewttblpvobjectsRow();
            Pvobjects.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpvobjects.AddttblpvobjectsRow((pdspvobjectsDataSet.ttblpvobjectsRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Pvobjects() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  