//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspvsassmlink;

   public partial class PvsassmlinkAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspvsassmlink";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvsassmlinkTableControlKey;
		
      private pdspvsassmlinkDataSet dataSet;
        
      public PvsassmlinkAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspvsassmlinkDataSet() { DataSetName = DataSetName };
            this.pvsassmlinkTableControlKey = this.dataSet.ttblpvsassmlink.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvsassmlinkTableControlKey))
            {
               this.CreateTableControlParameters(this.pvsassmlinkTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvsassmlinkAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pvsassmlinkproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPvsassmlinkproxy = this.proxyAppObject.CreatePO_pvsassmlinkproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPvsassmlinkproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pvsassmlinkproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvsassmlinkTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvsassmlinkTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pvsassmlinkproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPvsassmlinkproxy = this.proxyAppObject.CreatePO_pvsassmlinkproxy())
               {
                  this.SetRequiredContextParameters();
                  poPvsassmlinkproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pvsassmlinkproxy - After Call");
      }
   

      public Pvsassmlink GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Pvsassmlink pvsassmlink = null;
         if (row != null)
         {
             pvsassmlink = this.BuildFromRow(row);
         }
         return pvsassmlink;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pvsassmlink) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpvsassmlink.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Pvsassmlink> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pvsassmlink)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Pvsassmlink Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpvsassmlink.AsEnumerable().SingleOrDefault();
         Pvsassmlink pvsassmlink = null;
         if (row != null)
         {
             pvsassmlink = this.BuildFromRow(row);
         }
         return pvsassmlink;
      }
	  
	  

      public IEnumerable<Pvsassmlink> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpvsassmlink.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Pvsassmlink Get(string menuset, string functionname, string trmgrlang, int objid, int linkid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuset)) 
         {
             sb.AppendFormatWithEscape("pvsassmlink.menuset = '{0}'", menuset);
         }
         if (!string.IsNullOrEmpty(functionname)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmlink.functionname = '{0}'", functionname);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmlink.trmgrlang = '{0}'", trmgrlang);
         }
         if (objid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmlink.objid = {0}", objid);
         }
         if (linkid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pvsassmlink.linkid = {0}", linkid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Pvsassmlink BuildFromRow(DataRow row)
      {
         var returnRecord = Pvsassmlink.BuildPvsassmlinkFromRow(row);
         returnRecord = this.BuildExtraFromRow<Pvsassmlink>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Pvsassmlink record)
      {
         Pvsassmlink.UpdateRowFromPvsassmlink(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Pvsassmlink Insert(Pvsassmlink record)
      {
         DataRow row = this.dataSet.ttblpvsassmlink.NewttblpvsassmlinkRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpvsassmlink.AddttblpvsassmlinkRow((pdspvsassmlinkDataSet.ttblpvsassmlinkRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpvsassmlink.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvsassmlink.Rows[0]) : null;
      }
  

      public Pvsassmlink Update(Pvsassmlink record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpvsassmlink.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpvsassmlink.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Pvsassmlink record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpvsassmlink.NewttblpvsassmlinkRow();
            Pvsassmlink.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpvsassmlink.AddttblpvsassmlinkRow((pdspvsassmlinkDataSet.ttblpvsassmlinkRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Pvsassmlink() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  