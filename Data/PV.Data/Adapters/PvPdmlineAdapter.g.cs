//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvPdmline;

   public partial class PvPdmlineAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_pdmline";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvPdmlineTableControlKey;
		
      private pdspv_pdmlineDataSet dataSet;
        
      public PvPdmlineAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_pdmlineDataSet() { DataSetName = DataSetName };
            this.pvPdmlineTableControlKey = this.dataSet.ttblpv_pdmline.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvPdmlineTableControlKey))
            {
               this.CreateTableControlParameters(this.pvPdmlineTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvPdmlineAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_pdmlineproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_pdmlineproxy = this.proxyAppObject.CreatePO_pv_pdmlineproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_pdmlineproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_pdmlineproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvPdmlineTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvPdmlineTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_pdmlineproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_pdmlineproxy = this.proxyAppObject.CreatePO_pv_pdmlineproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_pdmlineproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_pdmlineproxy - After Call");
      }
   

      public PvPdmline GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvPdmline pvPdmline = null;
         if (row != null)
         {
             pvPdmline = this.BuildFromRow(row);
         }
         return pvPdmline;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_pdmline) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_pdmline.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvPdmline> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_pdmline)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvPdmline Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_pdmline.AsEnumerable().SingleOrDefault();
         PvPdmline pvPdmline = null;
         if (row != null)
         {
             pvPdmline = this.BuildFromRow(row);
         }
         return pvPdmline;
      }
	  
	  
      public IEnumerable<PvPdmline> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pv_pdmline.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_pdmline.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvPdmline Get(int cono, string setID, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<PvPdmline> GetListByEnddt(int cono, string setID, DateTime? enddt, string sortkey, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (enddt != null) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.enddt = '{0}'", enddt);
         }
         if (!string.IsNullOrEmpty(sortkey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.sortkey = '{0}'", sortkey);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListByFilter1(int cono, string setID, string filter1, string sortkey, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (!string.IsNullOrEmpty(filter1)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.filter1 = '{0}'", filter1);
         }
         if (!string.IsNullOrEmpty(sortkey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.sortkey = '{0}'", sortkey);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListByFilter2(int cono, string setID, string filter2, string sortkey, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (!string.IsNullOrEmpty(filter2)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.filter2 = '{0}'", filter2);
         }
         if (!string.IsNullOrEmpty(sortkey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.sortkey = '{0}'", sortkey);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListByFilter3(int cono, string setID, string filter3, string sortkey, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (!string.IsNullOrEmpty(filter3)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.filter3 = '{0}'", filter3);
         }
         if (!string.IsNullOrEmpty(sortkey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.sortkey = '{0}'", sortkey);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListByFilter4(int cono, string setID, string filter4, string sortkey, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (!string.IsNullOrEmpty(filter4)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.filter4 = '{0}'", filter4);
         }
         if (!string.IsNullOrEmpty(sortkey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.sortkey = '{0}'", sortkey);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListByFilter5(int cono, string setID, string filter5, string sortkey, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (!string.IsNullOrEmpty(filter5)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.filter5 = '{0}'", filter5);
         }
         if (!string.IsNullOrEmpty(sortkey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.sortkey = '{0}'", sortkey);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListByFilter6(int cono, string setID, string filter6, string sortkey, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (!string.IsNullOrEmpty(filter6)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.filter6 = '{0}'", filter6);
         }
         if (!string.IsNullOrEmpty(sortkey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.sortkey = '{0}'", sortkey);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListByFilter7(int cono, string setID, string filter7, string sortkey, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (!string.IsNullOrEmpty(filter7)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.filter7 = '{0}'", filter7);
         }
         if (!string.IsNullOrEmpty(sortkey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.sortkey = '{0}'", sortkey);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListByPrimarykey(int cono, string setID, string primarykey, DateTime? startdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (!string.IsNullOrEmpty(primarykey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.primarykey = '{0}'", primarykey);
         }
         if (startdt != null) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.startdt = '{0}'", startdt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListBySortkey(int cono, string setID, string sortkey, decimal seqNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         if (!string.IsNullOrEmpty(sortkey)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.sortkey = '{0}'", sortkey);
         }
         if (seqNo != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SeqNo = {0}", seqNo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvPdmline> GetListByUpdt(int cono, string setID, bool updttype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_pdmline.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(setID)) 
         {
             sb.AppendFormatWithEscape(" AND pv_pdmline.SetID = '{0}'", setID);
         }
         sb.AppendFormatWithEscape(" AND pv_pdmline.updttype = {0}", updttype);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public PvPdmline BuildFromRow(DataRow row)
      {
         var returnRecord = PvPdmline.BuildPvPdmlineFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvPdmline>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvPdmline record)
      {
         PvPdmline.UpdateRowFromPvPdmline(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvPdmline Insert(PvPdmline record)
      {
         DataRow row = this.dataSet.ttblpv_pdmline.Newttblpv_pdmlineRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_pdmline.Addttblpv_pdmlineRow((pdspv_pdmlineDataSet.ttblpv_pdmlineRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_pdmline.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_pdmline.Rows[0]) : null;
      }
  

      public PvPdmline Update(PvPdmline record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_pdmline.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_pdmline.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvPdmline record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_pdmline.Newttblpv_pdmlineRow();
            PvPdmline.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_pdmline.Addttblpv_pdmlineRow((pdspv_pdmlineDataSet.ttblpv_pdmlineRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvPdmline() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  