//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvSassm;

   public partial class PvSassmAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_sassm";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvSassmTableControlKey;
		
      private pdspv_sassmDataSet dataSet;
        
      public PvSassmAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_sassmDataSet() { DataSetName = DataSetName };
            this.pvSassmTableControlKey = this.dataSet.ttblpv_sassm.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvSassmTableControlKey))
            {
               this.CreateTableControlParameters(this.pvSassmTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvSassmAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_sassmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_sassmproxy = this.proxyAppObject.CreatePO_pv_sassmproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_sassmproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_sassmproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvSassmTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvSassmTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_sassmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_sassmproxy = this.proxyAppObject.CreatePO_pv_sassmproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_sassmproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_sassmproxy - After Call");
      }
   

      public PvSassm GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvSassm pvSassm = null;
         if (row != null)
         {
             pvSassm = this.BuildFromRow(row);
         }
         return pvSassm;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_sassm) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_sassm.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvSassm> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_sassm)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvSassm Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_sassm.AsEnumerable().SingleOrDefault();
         PvSassm pvSassm = null;
         if (row != null)
         {
             pvSassm = this.BuildFromRow(row);
         }
         return pvSassm;
      }
	  
	  

      public IEnumerable<PvSassm> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_sassm.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvSassm Get(string menuSet, string functionName, string trmgrlang, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuSet)) 
         {
             sb.AppendFormatWithEscape("pv_sassm.MenuSet = '{0}'", menuSet);
         }
         if (!string.IsNullOrEmpty(functionName)) 
         {
             sb.AppendFormatWithEscape(" AND pv_sassm.FunctionName = '{0}'", functionName);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND pv_sassm.trmgrlang = '{0}'", trmgrlang);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<PvSassm> GetListByRecordType(string menuSet, string recordtype, string trmgrlang, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuSet)) 
         {
             sb.AppendFormatWithEscape("pv_sassm.MenuSet = '{0}'", menuSet);
         }
         if (!string.IsNullOrEmpty(recordtype)) 
         {
             sb.AppendFormatWithEscape(" AND pv_sassm.recordtype = '{0}'", recordtype);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND pv_sassm.trmgrlang = '{0}'", trmgrlang);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public PvSassm BuildFromRow(DataRow row)
      {
         var returnRecord = PvSassm.BuildPvSassmFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvSassm>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvSassm record)
      {
         PvSassm.UpdateRowFromPvSassm(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvSassm Insert(PvSassm record)
      {
         DataRow row = this.dataSet.ttblpv_sassm.Newttblpv_sassmRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_sassm.Addttblpv_sassmRow((pdspv_sassmDataSet.ttblpv_sassmRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_sassm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_sassm.Rows[0]) : null;
      }
  

      public PvSassm Update(PvSassm record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_sassm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_sassm.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvSassm record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_sassm.Newttblpv_sassmRow();
            PvSassm.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_sassm.Addttblpv_sassmRow((pdspv_sassmDataSet.ttblpv_sassmRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvSassm() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  