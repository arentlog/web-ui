//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvSapbq;

   public partial class PvSapbqAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_sapbq";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvSapbqTableControlKey;
		
      private pdspv_sapbqDataSet dataSet;
        
      public PvSapbqAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_sapbqDataSet() { DataSetName = DataSetName };
            this.pvSapbqTableControlKey = this.dataSet.ttblpv_sapbq.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvSapbqTableControlKey))
            {
               this.CreateTableControlParameters(this.pvSapbqTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvSapbqAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_sapbqproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_sapbqproxy = this.proxyAppObject.CreatePO_pv_sapbqproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_sapbqproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_sapbqproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvSapbqTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvSapbqTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_sapbqproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_sapbqproxy = this.proxyAppObject.CreatePO_pv_sapbqproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_sapbqproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_sapbqproxy - After Call");
      }
   

      public PvSapbq GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvSapbq pvSapbq = null;
         if (row != null)
         {
             pvSapbq = this.BuildFromRow(row);
         }
         return pvSapbq;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_sapbq) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_sapbq.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvSapbq> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_sapbq)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvSapbq Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_sapbq.AsEnumerable().SingleOrDefault();
         PvSapbq pvSapbq = null;
         if (row != null)
         {
             pvSapbq = this.BuildFromRow(row);
         }
         return pvSapbq;
      }
	  
	  

      public IEnumerable<PvSapbq> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_sapbq.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvSapbq Get(string queueName, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(queueName)) 
         {
             sb.AppendFormatWithEscape("pv_sapbq.queue_name = '{0}'", queueName);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public PvSapbq BuildFromRow(DataRow row)
      {
         var returnRecord = PvSapbq.BuildPvSapbqFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvSapbq>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvSapbq record)
      {
         PvSapbq.UpdateRowFromPvSapbq(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvSapbq Insert(PvSapbq record)
      {
         DataRow row = this.dataSet.ttblpv_sapbq.Newttblpv_sapbqRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_sapbq.Addttblpv_sapbqRow((pdspv_sapbqDataSet.ttblpv_sapbqRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_sapbq.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_sapbq.Rows[0]) : null;
      }
  

      public PvSapbq Update(PvSapbq record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_sapbq.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_sapbq.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvSapbq record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_sapbq.Newttblpv_sapbqRow();
            PvSapbq.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_sapbq.Addttblpv_sapbqRow((pdspv_sapbqDataSet.ttblpv_sapbqRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvSapbq() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  