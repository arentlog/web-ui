//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvApebc;

   public partial class PvApebcAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_apebc";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvApebcTableControlKey;
		
      private pdspv_apebcDataSet dataSet;
        
      public PvApebcAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_apebcDataSet() { DataSetName = DataSetName };
            this.pvApebcTableControlKey = this.dataSet.ttblpv_apebc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvApebcTableControlKey))
            {
               this.CreateTableControlParameters(this.pvApebcTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvApebcAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_apebcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_apebcproxy = this.proxyAppObject.CreatePO_pv_apebcproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_apebcproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_apebcproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvApebcTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvApebcTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_apebcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_apebcproxy = this.proxyAppObject.CreatePO_pv_apebcproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_apebcproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_apebcproxy - After Call");
      }
   

      public PvApebc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvApebc pvApebc = null;
         if (row != null)
         {
             pvApebc = this.BuildFromRow(row);
         }
         return pvApebc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_apebc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_apebc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvApebc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_apebc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvApebc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_apebc.AsEnumerable().SingleOrDefault();
         PvApebc pvApebc = null;
         if (row != null)
         {
             pvApebc = this.BuildFromRow(row);
         }
         return pvApebc;
      }
	  
	  
      public IEnumerable<PvApebc> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pv_apebc.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvApebc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_apebc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvApebc Get(int cono, string batchnm, int setno, int pono, decimal posuf, decimal lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_apebc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(batchnm)) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.batchnm = '{0}'", batchnm);
         }
         if (setno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.setno = {0}", setno);
         }
         if (pono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.pono = {0}", pono);
         }
         if (posuf != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.posuf = {0}", posuf);
         }
         if (lineno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<PvApebc> GetListByPono(int cono, int pono, decimal posuf, decimal lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_apebc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (pono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.pono = {0}", pono);
         }
         if (posuf != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.posuf = {0}", posuf);
         }
         if (lineno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<PvApebc> GetListByStatus(int cono, bool statustype, decimal vendno, string apinvno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pv_apebc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND pv_apebc.statustype = {0}", statustype);
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.vendno = {0}", vendno);
         }
         if (!string.IsNullOrEmpty(apinvno)) 
         {
             sb.AppendFormatWithEscape(" AND pv_apebc.apinvno = '{0}'", apinvno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public PvApebc BuildFromRow(DataRow row)
      {
         var returnRecord = PvApebc.BuildPvApebcFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvApebc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvApebc record)
      {
         PvApebc.UpdateRowFromPvApebc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvApebc Insert(PvApebc record)
      {
         DataRow row = this.dataSet.ttblpv_apebc.Newttblpv_apebcRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_apebc.Addttblpv_apebcRow((pdspv_apebcDataSet.ttblpv_apebcRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_apebc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_apebc.Rows[0]) : null;
      }
  

      public PvApebc Update(PvApebc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_apebc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_apebc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvApebc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_apebc.Newttblpv_apebcRow();
            PvApebc.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_apebc.Addttblpv_apebcRow((pdspv_apebcDataSet.ttblpv_apebcRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvApebc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  