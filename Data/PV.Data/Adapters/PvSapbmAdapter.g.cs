//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PV.Data.Adapters
{
   using com.infor.sxproxy.pvproxy;
   using com.infor.sxproxy.pvproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdspvSapbm;

   public partial class PvSapbmAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspv_sapbm";
      private PVProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pvSapbmTableControlKey;
		
      private pdspv_sapbmDataSet dataSet;
        
      public PvSapbmAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PVProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspv_sapbmDataSet() { DataSetName = DataSetName };
            this.pvSapbmTableControlKey = this.dataSet.ttblpv_sapbm.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pvSapbmTableControlKey))
            {
               this.CreateTableControlParameters(this.pvSapbmTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PvSapbmAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pv_sapbmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPv_sapbmproxy = this.proxyAppObject.CreatePO_pv_sapbmproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPv_sapbmproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pv_sapbmproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pvSapbmTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pvSapbmTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pv_sapbmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPv_sapbmproxy = this.proxyAppObject.CreatePO_pv_sapbmproxy())
               {
                  this.SetRequiredContextParameters();
                  poPv_sapbmproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pv_sapbmproxy - After Call");
      }
   

      public PvSapbm GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         PvSapbm pvSapbm = null;
         if (row != null)
         {
             pvSapbm = this.BuildFromRow(row);
         }
         return pvSapbm;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pv_sapbm) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpv_sapbm.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<PvSapbm> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pv_sapbm)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected PvSapbm Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpv_sapbm.AsEnumerable().SingleOrDefault();
         PvSapbm pvSapbm = null;
         if (row != null)
         {
             pvSapbm = this.BuildFromRow(row);
         }
         return pvSapbm;
      }
	  
	  

      public IEnumerable<PvSapbm> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpv_sapbm.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public PvSapbm Get(string queueName, int queueNo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(queueName)) 
         {
             sb.AppendFormatWithEscape("pv_sapbm.queue_name = '{0}'", queueName);
         }
         if (queueNo != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pv_sapbm.queue_no = {0}", queueNo);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public PvSapbm BuildFromRow(DataRow row)
      {
         var returnRecord = PvSapbm.BuildPvSapbmFromRow(row);
         returnRecord = this.BuildExtraFromRow<PvSapbm>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, PvSapbm record)
      {
         PvSapbm.UpdateRowFromPvSapbm(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public PvSapbm Insert(PvSapbm record)
      {
         DataRow row = this.dataSet.ttblpv_sapbm.Newttblpv_sapbmRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpv_sapbm.Addttblpv_sapbmRow((pdspv_sapbmDataSet.ttblpv_sapbmRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpv_sapbm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_sapbm.Rows[0]) : null;
      }
  

      public PvSapbm Update(PvSapbm record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpv_sapbm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpv_sapbm.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(PvSapbm record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpv_sapbm.Newttblpv_sapbmRow();
            PvSapbm.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpv_sapbm.Addttblpv_sapbmRow((pdspv_sapbmDataSet.ttblpv_sapbmRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new PvSapbm() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  