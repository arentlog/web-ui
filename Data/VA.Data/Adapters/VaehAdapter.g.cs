//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.VA.Data.Adapters
{
   using com.infor.sxproxy.vaproxy;
   using com.infor.sxproxy.vaproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvaeh;
   using Models.Pdsvalueaddlookup;
   using Models.Complex;

   public partial class VaehAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvaeh";
      private VAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> vaehTableControlKey;
		
      private pdsvaehDataSet dataSet;
        
      public VaehAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new VAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvaehDataSet() { DataSetName = DataSetName };
            this.vaehTableControlKey = this.dataSet.ttblvaeh.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.vaehTableControlKey))
            {
               this.CreateTableControlParameters(this.vaehTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VaehAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Vaehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVaehproxy = this.proxyAppObject.CreatePO_vaehproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVaehproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Vaehproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.vaehTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.vaehTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Vaehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaehproxy = this.proxyAppObject.CreatePO_vaehproxy())
               {
                  this.SetRequiredContextParameters();
                  poVaehproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Vaehproxy - After Call");
      }
   

      public Vaeh GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Vaeh vaeh = null;
         if (row != null)
         {
             vaeh = this.BuildFromRow(row);
         }
         return vaeh;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(vaeh) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvaeh.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Vaeh> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(vaeh)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Vaeh Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvaeh.AsEnumerable().SingleOrDefault();
         Vaeh vaeh = null;
         if (row != null)
         {
             vaeh = this.BuildFromRow(row);
         }
         return vaeh;
      }
	  
	  
	  public IEnumerable<Vaeh> GetListByRowpointers(List<string> rowpointers, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("vaeh.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("vaeh.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvaeh.AsEnumerable().SingleOrDefault();
      }
      public IEnumerable<Vaeh> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("vaeh.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaeh> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvaeh.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Vaeh Get(int cono, int vano, int vasuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaeh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.vasuf = {0}", vasuf);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Vaeh> GetListByActive(int cono, bool statustype, int vano, int vasuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaeh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND vaeh.statustype = {0}", statustype);
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.vasuf = {0}", vasuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaeh> GetListByJrnl(int cono, int jrnlno, int setno, int vano, int vasuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaeh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (jrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.jrnlno = {0}", jrnlno);
         }
         if (setno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.setno = {0}", setno);
         }
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.vasuf = {0}", vasuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaeh> GetListByProd(int cono, string shipprod, bool statustype, int vano, int vasuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaeh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.shipprod = '{0}'", shipprod);
         }
         sb.AppendFormatWithEscape(" AND vaeh.statustype = {0}", statustype);
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.vasuf = {0}", vasuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Vaeh GetByRowpointer(string rowpointer, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("vaeh.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, 1, fldList);
      }

      public IEnumerable<Vaeh> GetListByTransdttmz(DateTime? transdttmz, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (transdttmz != null) 
         {
             sb.AppendFormatWithEscape("vaeh.transdttmz = '{0}'", transdttmz);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaeh> GetListByVerno(int cono, string shipprod, int verno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaeh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.shipprod = '{0}'", shipprod);
         }
         if (verno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaeh.verno = {0}", verno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Vaeh BuildFromRow(DataRow row)
      {
         var returnRecord = Vaeh.BuildVaehFromRow(row);
         returnRecord = this.BuildExtraFromRow<Vaeh>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Vaeh record)
      {
         Vaeh.UpdateRowFromVaeh(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Vaeh Insert(Vaeh record)
      {
         DataRow row = this.dataSet.ttblvaeh.NewttblvaehRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvaeh.AddttblvaehRow((pdsvaehDataSet.ttblvaehRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvaeh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaeh.Rows[0]) : null;
      }
  

      public Vaeh Update(Vaeh record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvaeh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaeh.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Vaeh record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvaeh.NewttblvaehRow();
            Vaeh.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvaeh.AddttblvaehRow((pdsvaehDataSet.ttblvaehRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Vaeh record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttblvaeh.NewttblvaehRow();
            Vaeh.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvaeh.AddttblvaehRow((pdsvaehDataSet.ttblvaehRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Vaeh() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Vaeh() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      public VaehLookupResponseAPI Lookup(Valueaddlookupcriteria valueaddlookupcriteria)
      {   
         var result = new VaehLookupResponseAPI();
         
         var pdsvalueaddlookup = new pdsvalueaddlookupDataSet();
            
         DataRow ttblvalueaddlookupcriteriaCriteria = pdsvalueaddlookup.ttblvalueaddlookupcriteria.NewttblvalueaddlookupcriteriaRow();
         Valueaddlookupcriteria.UpdateRowFromValueaddlookupcriteria(ref ttblvalueaddlookupcriteriaCriteria, valueaddlookupcriteria);
         pdsvalueaddlookup.ttblvalueaddlookupcriteria.AddttblvalueaddlookupcriteriaRow((pdsvalueaddlookupDataSet.ttblvalueaddlookupcriteriaRow)ttblvalueaddlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Vaeh - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaehproxy = this.proxyAppObject.CreatePO_vaehproxy())
               {
                   this.SetRequiredContextParameters();
                   poVaehproxy.Lookup(ref pdsContext, ref pdsvalueaddlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Vaeh - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsvalueaddlookup); 
    
         foreach (DataRow row in pdsvalueaddlookup.ttblvalueaddlookupresults)
         {
            result.valueaddlookupresults.Add(Valueaddlookupresults.BuildValueaddlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      public IEnumerable<Vaeh> GetListByWordIndex(string searchTerm, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         where.AppendKeywords(string.Empty, searchTerm, true, true, false);
         this.SetAndFetch(where.ToString(), batchsize, fldList);
         this.SearchWordIndex(batchsize == 0 ? DefaultFetchWhereRowCount : batchsize , where.ToString());
         return (from DataRow row in this.dataSet.ttblvaeh.Rows select this.BuildFromRow(row)).ToList();
      }
  
      protected void SearchWordIndex(int batchSize, string searchTerm)
      {
         NLogLoggerP.Trace("SearchWordIndex - Vaeh - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaehProxy = this.proxyAppObject.CreatePO_vaehproxy())
               {
                   this.SetRequiredContextParameters();
                   poVaehProxy.SearchWordIndex(batchSize, searchTerm, ref this.pdsContext, out this.dataSet, out string cErrorMessage, out bool moreRecords);
                   ErrorReportingHelper.ReportErrors(cErrorMessage);
                   this.ReportErrors(this.pdsContext);
                   this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SearchWordIndex - Vaeh - After Call");
      }
  
      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  