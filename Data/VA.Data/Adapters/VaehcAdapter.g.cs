//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.VA.Data.Adapters
{
   using com.infor.sxproxy.vaproxy;
   using com.infor.sxproxy.vaproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvaehc;

   public partial class VaehcAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvaehc";
      private VAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> vaehcTableControlKey;
		
      private pdsvaehcDataSet dataSet;
        
      public VaehcAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new VAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvaehcDataSet() { DataSetName = DataSetName };
            this.vaehcTableControlKey = this.dataSet.ttblvaehc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.vaehcTableControlKey))
            {
               this.CreateTableControlParameters(this.vaehcTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VaehcAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Vaehcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVaehcproxy = this.proxyAppObject.CreatePO_vaehcproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVaehcproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Vaehcproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.vaehcTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.vaehcTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Vaehcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaehcproxy = this.proxyAppObject.CreatePO_vaehcproxy())
               {
                  this.SetRequiredContextParameters();
                  poVaehcproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Vaehcproxy - After Call");
      }
   

      public Vaehc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Vaehc vaehc = null;
         if (row != null)
         {
             vaehc = this.BuildFromRow(row);
         }
         return vaehc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(vaehc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvaehc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Vaehc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(vaehc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Vaehc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvaehc.AsEnumerable().SingleOrDefault();
         Vaehc vaehc = null;
         if (row != null)
         {
             vaehc = this.BuildFromRow(row);
         }
         return vaehc;
      }
	  
	  
      public IEnumerable<Vaehc> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("vaehc.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaehc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvaehc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Vaehc Get(int cono, int vano, int vasuf, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaehc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaehc.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaehc.vasuf = {0}", vasuf);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaehc.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Vaehc> GetListByEnterdt(int cono, int vano, int vasuf, DateTime? enterdt, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaehc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaehc.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaehc.vasuf = {0}", vasuf);
         }
         if (enterdt != null) 
         {
             sb.AppendFormatWithEscape(" AND vaehc.enterdt = '{0}'", enterdt);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaehc.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Vaehc BuildFromRow(DataRow row)
      {
         var returnRecord = Vaehc.BuildVaehcFromRow(row);
         returnRecord = this.BuildExtraFromRow<Vaehc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Vaehc record)
      {
         Vaehc.UpdateRowFromVaehc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Vaehc Insert(Vaehc record)
      {
         DataRow row = this.dataSet.ttblvaehc.NewttblvaehcRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvaehc.AddttblvaehcRow((pdsvaehcDataSet.ttblvaehcRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvaehc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaehc.Rows[0]) : null;
      }
  

      public Vaehc Update(Vaehc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvaehc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaehc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Vaehc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvaehc.NewttblvaehcRow();
            Vaehc.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvaehc.AddttblvaehcRow((pdsvaehcDataSet.ttblvaehcRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Vaehc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  