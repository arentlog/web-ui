//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.VA.Data.Adapters
{
   using com.infor.sxproxy.vaproxy;
   using com.infor.sxproxy.vaproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvaspsas;

   public partial class VaspsasAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvaspsas";
      private VAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> vaspsasTableControlKey;
		
      private pdsvaspsasDataSet dataSet;
        
      public VaspsasAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new VAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvaspsasDataSet() { DataSetName = DataSetName };
            this.vaspsasTableControlKey = this.dataSet.ttblvaspsas.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.vaspsasTableControlKey))
            {
               this.CreateTableControlParameters(this.vaspsasTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VaspsasAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Vaspsasproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVaspsasproxy = this.proxyAppObject.CreatePO_vaspsasproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVaspsasproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Vaspsasproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.vaspsasTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.vaspsasTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Vaspsasproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaspsasproxy = this.proxyAppObject.CreatePO_vaspsasproxy())
               {
                  this.SetRequiredContextParameters();
                  poVaspsasproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Vaspsasproxy - After Call");
      }
   

      public Vaspsas GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Vaspsas vaspsas = null;
         if (row != null)
         {
             vaspsas = this.BuildFromRow(row);
         }
         return vaspsas;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(vaspsas) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvaspsas.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Vaspsas> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(vaspsas)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Vaspsas Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvaspsas.AsEnumerable().SingleOrDefault();
         Vaspsas vaspsas = null;
         if (row != null)
         {
             vaspsas = this.BuildFromRow(row);
         }
         return vaspsas;
      }
	  
	  
      public IEnumerable<Vaspsas> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("vaspsas.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaspsas> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvaspsas.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Vaspsas Get(int cono, string shipprod, string whse, int segment, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaspsas.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND vaspsas.shipprod = '{0}'", shipprod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND vaspsas.whse = '{0}'", whse);
         }
         if (segment != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaspsas.segment = {0}", segment);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Vaspsas BuildFromRow(DataRow row)
      {
         var returnRecord = Vaspsas.BuildVaspsasFromRow(row);
         returnRecord = this.BuildExtraFromRow<Vaspsas>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Vaspsas record)
      {
         Vaspsas.UpdateRowFromVaspsas(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Vaspsas Insert(Vaspsas record)
      {
         DataRow row = this.dataSet.ttblvaspsas.NewttblvaspsasRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvaspsas.AddttblvaspsasRow((pdsvaspsasDataSet.ttblvaspsasRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvaspsas.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaspsas.Rows[0]) : null;
      }
  

      public Vaspsas Update(Vaspsas record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvaspsas.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaspsas.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Vaspsas record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvaspsas.NewttblvaspsasRow();
            Vaspsas.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvaspsas.AddttblvaspsasRow((pdsvaspsasDataSet.ttblvaspsasRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Vaspsas() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  