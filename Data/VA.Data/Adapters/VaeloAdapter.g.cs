//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.VA.Data.Adapters
{
   using com.infor.sxproxy.vaproxy;
   using com.infor.sxproxy.vaproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvaelo;

   public partial class VaeloAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvaelo";
      private VAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> vaeloTableControlKey;
		
      private pdsvaeloDataSet dataSet;
        
      public VaeloAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new VAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvaeloDataSet() { DataSetName = DataSetName };
            this.vaeloTableControlKey = this.dataSet.ttblvaelo.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.vaeloTableControlKey))
            {
               this.CreateTableControlParameters(this.vaeloTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VaeloAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Vaeloproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVaeloproxy = this.proxyAppObject.CreatePO_vaeloproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVaeloproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Vaeloproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.vaeloTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.vaeloTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Vaeloproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaeloproxy = this.proxyAppObject.CreatePO_vaeloproxy())
               {
                  this.SetRequiredContextParameters();
                  poVaeloproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Vaeloproxy - After Call");
      }
   

      public Vaelo GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Vaelo vaelo = null;
         if (row != null)
         {
             vaelo = this.BuildFromRow(row);
         }
         return vaelo;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(vaelo) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvaelo.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Vaelo> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(vaelo)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Vaelo Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvaelo.AsEnumerable().SingleOrDefault();
         Vaelo vaelo = null;
         if (row != null)
         {
             vaelo = this.BuildFromRow(row);
         }
         return vaelo;
      }
	  
	  
      public IEnumerable<Vaelo> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("vaelo.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaelo> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvaelo.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Vaelo Get(int cono, int vano, int vasuf, int lineno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaelo.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaelo.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaelo.vasuf = {0}", vasuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaelo.lineno = {0}", lineno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaelo.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Vaelo> GetListByOrder(int cono, string ordertype, int orderaltno, int orderaltsuf, int linealtno, int seqaltno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaelo.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(ordertype)) 
         {
             sb.AppendFormatWithEscape(" AND vaelo.ordertype = '{0}'", ordertype);
         }
         if (orderaltno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaelo.orderaltno = {0}", orderaltno);
         }
         if (orderaltsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaelo.orderaltsuf = {0}", orderaltsuf);
         }
         if (linealtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaelo.linealtno = {0}", linealtno);
         }
         if (seqaltno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaelo.seqaltno = {0}", seqaltno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Vaelo BuildFromRow(DataRow row)
      {
         var returnRecord = Vaelo.BuildVaeloFromRow(row);
         returnRecord = this.BuildExtraFromRow<Vaelo>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Vaelo record)
      {
         Vaelo.UpdateRowFromVaelo(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Vaelo Insert(Vaelo record)
      {
         DataRow row = this.dataSet.ttblvaelo.NewttblvaeloRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvaelo.AddttblvaeloRow((pdsvaeloDataSet.ttblvaeloRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvaelo.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaelo.Rows[0]) : null;
      }
  

      public Vaelo Update(Vaelo record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvaelo.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaelo.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Vaelo record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvaelo.NewttblvaeloRow();
            Vaelo.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvaelo.AddttblvaeloRow((pdsvaeloDataSet.ttblvaeloRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Vaelo() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  