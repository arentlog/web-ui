//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.VA.Data.Adapters
{
   using com.infor.sxproxy.vaproxy;
   using com.infor.sxproxy.vaproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvaspsasv;

   public partial class VaspsasvAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvaspsasv";
      private VAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> vaspsasvTableControlKey;
		
      private pdsvaspsasvDataSet dataSet;
        
      public VaspsasvAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new VAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvaspsasvDataSet() { DataSetName = DataSetName };
            this.vaspsasvTableControlKey = this.dataSet.ttblvaspsasv.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.vaspsasvTableControlKey))
            {
               this.CreateTableControlParameters(this.vaspsasvTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VaspsasvAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Vaspsasvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVaspsasvproxy = this.proxyAppObject.CreatePO_vaspsasvproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVaspsasvproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Vaspsasvproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.vaspsasvTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.vaspsasvTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Vaspsasvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaspsasvproxy = this.proxyAppObject.CreatePO_vaspsasvproxy())
               {
                  this.SetRequiredContextParameters();
                  poVaspsasvproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Vaspsasvproxy - After Call");
      }
   

      public Vaspsasv GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Vaspsasv vaspsasv = null;
         if (row != null)
         {
             vaspsasv = this.BuildFromRow(row);
         }
         return vaspsasv;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(vaspsasv) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvaspsasv.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Vaspsasv> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(vaspsasv)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Vaspsasv Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvaspsasv.AsEnumerable().SingleOrDefault();
         Vaspsasv vaspsasv = null;
         if (row != null)
         {
             vaspsasv = this.BuildFromRow(row);
         }
         return vaspsasv;
      }
	  
	  
      public IEnumerable<Vaspsasv> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("vaspsasv.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaspsasv> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvaspsasv.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Vaspsasv Get(int cono, string shipprod, string whse, int verno, int segment, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaspsasv.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasv.shipprod = '{0}'", shipprod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasv.whse = '{0}'", whse);
         }
         if (verno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasv.verno = {0}", verno);
         }
         if (segment != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasv.segment = {0}", segment);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Vaspsasv BuildFromRow(DataRow row)
      {
         var returnRecord = Vaspsasv.BuildVaspsasvFromRow(row);
         returnRecord = this.BuildExtraFromRow<Vaspsasv>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Vaspsasv record)
      {
         Vaspsasv.UpdateRowFromVaspsasv(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Vaspsasv Insert(Vaspsasv record)
      {
         DataRow row = this.dataSet.ttblvaspsasv.NewttblvaspsasvRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvaspsasv.AddttblvaspsasvRow((pdsvaspsasvDataSet.ttblvaspsasvRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvaspsasv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaspsasv.Rows[0]) : null;
      }
  

      public Vaspsasv Update(Vaspsasv record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvaspsasv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaspsasv.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Vaspsasv record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvaspsasv.NewttblvaspsasvRow();
            Vaspsasv.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvaspsasv.AddttblvaspsasvRow((pdsvaspsasvDataSet.ttblvaspsasvRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Vaspsasv() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  