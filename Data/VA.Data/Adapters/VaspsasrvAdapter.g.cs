//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.VA.Data.Adapters
{
   using com.infor.sxproxy.vaproxy;
   using com.infor.sxproxy.vaproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvaspsasrv;
   using Models.Pdsvasprulelookup;
   using Models.Complex;

   public partial class VaspsasrvAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvaspsasrv";
      private VAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> vaspsasrvTableControlKey;
		
      private pdsvaspsasrvDataSet dataSet;
        
      public VaspsasrvAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new VAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvaspsasrvDataSet() { DataSetName = DataSetName };
            this.vaspsasrvTableControlKey = this.dataSet.ttblvaspsasrv.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.vaspsasrvTableControlKey))
            {
               this.CreateTableControlParameters(this.vaspsasrvTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VaspsasrvAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Vaspsasrvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVaspsasrvproxy = this.proxyAppObject.CreatePO_vaspsasrvproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVaspsasrvproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Vaspsasrvproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.vaspsasrvTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.vaspsasrvTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Vaspsasrvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaspsasrvproxy = this.proxyAppObject.CreatePO_vaspsasrvproxy())
               {
                  this.SetRequiredContextParameters();
                  poVaspsasrvproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Vaspsasrvproxy - After Call");
      }
   

      public Vaspsasrv GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Vaspsasrv vaspsasrv = null;
         if (row != null)
         {
             vaspsasrv = this.BuildFromRow(row);
         }
         return vaspsasrv;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(vaspsasrv) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvaspsasrv.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Vaspsasrv> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(vaspsasrv)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Vaspsasrv Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvaspsasrv.AsEnumerable().SingleOrDefault();
         Vaspsasrv vaspsasrv = null;
         if (row != null)
         {
             vaspsasrv = this.BuildFromRow(row);
         }
         return vaspsasrv;
      }
	  
	  
      public IEnumerable<Vaspsasrv> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("vaspsasrv.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaspsasrv> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvaspsasrv.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Vaspsasrv Get(int cono, string shipprod, string whse, int verno, int segment, int sequence, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaspsasrv.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasrv.shipprod = '{0}'", shipprod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasrv.whse = '{0}'", whse);
         }
         if (verno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasrv.verno = {0}", verno);
         }
         if (segment != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasrv.segment = {0}", segment);
         }
         if (sequence != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasrv.sequence = {0}", sequence);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Vaspsasrv BuildFromRow(DataRow row)
      {
         var returnRecord = Vaspsasrv.BuildVaspsasrvFromRow(row);
         returnRecord = this.BuildExtraFromRow<Vaspsasrv>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Vaspsasrv record)
      {
         Vaspsasrv.UpdateRowFromVaspsasrv(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Vaspsasrv Insert(Vaspsasrv record)
      {
         DataRow row = this.dataSet.ttblvaspsasrv.NewttblvaspsasrvRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvaspsasrv.AddttblvaspsasrvRow((pdsvaspsasrvDataSet.ttblvaspsasrvRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvaspsasrv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaspsasrv.Rows[0]) : null;
      }
  

      public Vaspsasrv Update(Vaspsasrv record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvaspsasrv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaspsasrv.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Vaspsasrv record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvaspsasrv.NewttblvaspsasrvRow();
            Vaspsasrv.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvaspsasrv.AddttblvaspsasrvRow((pdsvaspsasrvDataSet.ttblvaspsasrvRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Vaspsasrv() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public VaspsasrvLookupResponseAPI Lookup(Vasprulelookupcriteria vasprulelookupcriteria)
      {   
         var result = new VaspsasrvLookupResponseAPI();
         
         var pdsvasprulelookup = new pdsvasprulelookupDataSet();
            
         DataRow ttblvasprulelookupcriteriaCriteria = pdsvasprulelookup.ttblvasprulelookupcriteria.NewttblvasprulelookupcriteriaRow();
         Vasprulelookupcriteria.UpdateRowFromVasprulelookupcriteria(ref ttblvasprulelookupcriteriaCriteria, vasprulelookupcriteria);
         pdsvasprulelookup.ttblvasprulelookupcriteria.AddttblvasprulelookupcriteriaRow((pdsvasprulelookupDataSet.ttblvasprulelookupcriteriaRow)ttblvasprulelookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Vaspsasrv - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaspsasrvproxy = this.proxyAppObject.CreatePO_vaspsasrvproxy())
               {
                   this.SetRequiredContextParameters();
                   poVaspsasrvproxy.Lookup(ref pdsContext, ref pdsvasprulelookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Vaspsasrv - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsvasprulelookup); 
    
         if (pdsvasprulelookup.ttblvasprulelookupcriteria.Count > 0)
         {
            result.vasprulelookupcriteria = Vasprulelookupcriteria.BuildVasprulelookupcriteriaFromRow(pdsvasprulelookup.ttblvasprulelookupcriteria[0]);
         }
                    
         foreach (DataRow row in pdsvasprulelookup.ttblvasprulelookupresults)
         {
            result.vasprulelookupresults.Add(Vasprulelookupresults.BuildVasprulelookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  