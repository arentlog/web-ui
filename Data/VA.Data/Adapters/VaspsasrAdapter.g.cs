//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.VA.Data.Adapters
{
   using com.infor.sxproxy.vaproxy;
   using com.infor.sxproxy.vaproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvaspsasr;
   using Models.Pdsvasprulelookup;
   using Models.Complex;

   public partial class VaspsasrAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvaspsasr";
      private VAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> vaspsasrTableControlKey;
		
      private pdsvaspsasrDataSet dataSet;
        
      public VaspsasrAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new VAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvaspsasrDataSet() { DataSetName = DataSetName };
            this.vaspsasrTableControlKey = this.dataSet.ttblvaspsasr.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.vaspsasrTableControlKey))
            {
               this.CreateTableControlParameters(this.vaspsasrTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VaspsasrAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Vaspsasrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVaspsasrproxy = this.proxyAppObject.CreatePO_vaspsasrproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVaspsasrproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Vaspsasrproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.vaspsasrTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.vaspsasrTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Vaspsasrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaspsasrproxy = this.proxyAppObject.CreatePO_vaspsasrproxy())
               {
                  this.SetRequiredContextParameters();
                  poVaspsasrproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Vaspsasrproxy - After Call");
      }
   

      public Vaspsasr GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Vaspsasr vaspsasr = null;
         if (row != null)
         {
             vaspsasr = this.BuildFromRow(row);
         }
         return vaspsasr;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(vaspsasr) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvaspsasr.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Vaspsasr> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(vaspsasr)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Vaspsasr Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvaspsasr.AsEnumerable().SingleOrDefault();
         Vaspsasr vaspsasr = null;
         if (row != null)
         {
             vaspsasr = this.BuildFromRow(row);
         }
         return vaspsasr;
      }
	  
	  
      public IEnumerable<Vaspsasr> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("vaspsasr.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaspsasr> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvaspsasr.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Vaspsasr Get(int cono, string shipprod, string whse, int segment, int sequence, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaspsasr.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasr.shipprod = '{0}'", shipprod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasr.whse = '{0}'", whse);
         }
         if (segment != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasr.segment = {0}", segment);
         }
         if (sequence != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaspsasr.sequence = {0}", sequence);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Vaspsasr BuildFromRow(DataRow row)
      {
         var returnRecord = Vaspsasr.BuildVaspsasrFromRow(row);
         returnRecord = this.BuildExtraFromRow<Vaspsasr>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Vaspsasr record)
      {
         Vaspsasr.UpdateRowFromVaspsasr(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Vaspsasr Insert(Vaspsasr record)
      {
         DataRow row = this.dataSet.ttblvaspsasr.NewttblvaspsasrRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvaspsasr.AddttblvaspsasrRow((pdsvaspsasrDataSet.ttblvaspsasrRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvaspsasr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaspsasr.Rows[0]) : null;
      }
  

      public Vaspsasr Update(Vaspsasr record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvaspsasr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaspsasr.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Vaspsasr record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvaspsasr.NewttblvaspsasrRow();
            Vaspsasr.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvaspsasr.AddttblvaspsasrRow((pdsvaspsasrDataSet.ttblvaspsasrRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Vaspsasr() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public VaspsasrLookupResponseAPI Lookup(Vasprulelookupcriteria vasprulelookupcriteria)
      {   
         var result = new VaspsasrLookupResponseAPI();
         
         var pdsvasprulelookup = new pdsvasprulelookupDataSet();
            
         DataRow ttblvasprulelookupcriteriaCriteria = pdsvasprulelookup.ttblvasprulelookupcriteria.NewttblvasprulelookupcriteriaRow();
         Vasprulelookupcriteria.UpdateRowFromVasprulelookupcriteria(ref ttblvasprulelookupcriteriaCriteria, vasprulelookupcriteria);
         pdsvasprulelookup.ttblvasprulelookupcriteria.AddttblvasprulelookupcriteriaRow((pdsvasprulelookupDataSet.ttblvasprulelookupcriteriaRow)ttblvasprulelookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Vaspsasr - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaspsasrproxy = this.proxyAppObject.CreatePO_vaspsasrproxy())
               {
                   this.SetRequiredContextParameters();
                   poVaspsasrproxy.Lookup(ref pdsContext, ref pdsvasprulelookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Vaspsasr - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsvasprulelookup); 
    
         foreach (DataRow row in pdsvasprulelookup.ttblvasprulelookupresults)
         {
            result.vasprulelookupresults.Add(Vasprulelookupresults.BuildVasprulelookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  