//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.VA.Data.Adapters
{
   using com.infor.sxproxy.vaproxy;
   using com.infor.sxproxy.vaproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvaesl;

   public partial class VaeslAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvaesl";
      private VAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> vaeslTableControlKey;
		
      private pdsvaeslDataSet dataSet;
        
      public VaeslAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new VAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvaeslDataSet() { DataSetName = DataSetName };
            this.vaeslTableControlKey = this.dataSet.ttblvaesl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.vaeslTableControlKey))
            {
               this.CreateTableControlParameters(this.vaeslTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VaeslAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Vaeslproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVaeslproxy = this.proxyAppObject.CreatePO_vaeslproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVaeslproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Vaeslproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.vaeslTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.vaeslTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Vaeslproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVaeslproxy = this.proxyAppObject.CreatePO_vaeslproxy())
               {
                  this.SetRequiredContextParameters();
                  poVaeslproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Vaeslproxy - After Call");
      }
   

      public Vaesl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Vaesl vaesl = null;
         if (row != null)
         {
             vaesl = this.BuildFromRow(row);
         }
         return vaesl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(vaesl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvaesl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Vaesl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(vaesl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Vaesl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvaesl.AsEnumerable().SingleOrDefault();
         Vaesl vaesl = null;
         if (row != null)
         {
             vaesl = this.BuildFromRow(row);
         }
         return vaesl;
      }
	  
	  
      public IEnumerable<Vaesl> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("vaesl.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaesl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvaesl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Vaesl Get(int cono, int vano, int vasuf, int seqno, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaesl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.vasuf = {0}", vasuf);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.seqno = {0}", seqno);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Vaesl> GetListByActive(int cono, string sctntype, bool completefl, int vano, int vasuf, int seqno, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaesl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(sctntype)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.sctntype = '{0}'", sctntype);
         }
         sb.AppendFormatWithEscape(" AND vaesl.completefl = {0}", completefl);
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.vasuf = {0}", vasuf);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.seqno = {0}", seqno);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaesl> GetListByProd(int cono, string shipprod, string whse, string sctntype, bool completefl, int vano, int vasuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaesl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.shipprod = '{0}'", shipprod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(sctntype)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.sctntype = '{0}'", sctntype);
         }
         sb.AppendFormatWithEscape(" AND vaesl.completefl = {0}", completefl);
         if (vano != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.vano = {0}", vano);
         }
         if (vasuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.vasuf = {0}", vasuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Vaesl> GetListBySpecns(int cono, bool completefl, string nonstockty, string whse, string orderalttype, string arpwhse, decimal arpvendno, string arpprodline, string shipprod, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("vaesl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND vaesl.completefl = {0}", completefl);
         if (!string.IsNullOrEmpty(nonstockty)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.nonstockty = '{0}'", nonstockty);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(orderalttype)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.orderalttype = '{0}'", orderalttype);
         }
         if (!string.IsNullOrEmpty(arpwhse)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.arpwhse = '{0}'", arpwhse);
         }
         if (arpvendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.arpvendno = {0}", arpvendno);
         }
         if (!string.IsNullOrEmpty(arpprodline)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.arpprodline = '{0}'", arpprodline);
         }
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND vaesl.shipprod = '{0}'", shipprod);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Vaesl BuildFromRow(DataRow row)
      {
         var returnRecord = Vaesl.BuildVaeslFromRow(row);
         returnRecord = this.BuildExtraFromRow<Vaesl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Vaesl record)
      {
         Vaesl.UpdateRowFromVaesl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Vaesl Insert(Vaesl record)
      {
         DataRow row = this.dataSet.ttblvaesl.NewttblvaeslRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvaesl.AddttblvaeslRow((pdsvaeslDataSet.ttblvaeslRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvaesl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaesl.Rows[0]) : null;
      }
  

      public Vaesl Update(Vaesl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvaesl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvaesl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Vaesl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvaesl.NewttblvaeslRow();
            Vaesl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvaesl.AddttblvaeslRow((pdsvaeslDataSet.ttblvaeslRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Vaesl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  