//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SL.Data.Adapters
{
   using com.infor.sxproxy.slproxy;
   using com.infor.sxproxy.slproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssledv;

   public partial class SledvAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssledv";
      private SLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sledvTableControlKey;
		
      private pdssledvDataSet dataSet;
        
      public SledvAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssledvDataSet() { DataSetName = DataSetName };
            this.sledvTableControlKey = this.dataSet.ttblsledv.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sledvTableControlKey))
            {
               this.CreateTableControlParameters(this.sledvTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SledvAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sledvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSledvproxy = this.proxyAppObject.CreatePO_sledvproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSledvproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sledvproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sledvTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sledvTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sledvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSledvproxy = this.proxyAppObject.CreatePO_sledvproxy())
               {
                  this.SetRequiredContextParameters();
                  poSledvproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sledvproxy - After Call");
      }
   

      public Sledv GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sledv sledv = null;
         if (row != null)
         {
             sledv = this.BuildFromRow(row);
         }
         return sledv;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sledv) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsledv.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sledv> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sledv)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sledv Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsledv.AsEnumerable().SingleOrDefault();
         Sledv sledv = null;
         if (row != null)
         {
             sledv = this.BuildFromRow(row);
         }
         return sledv;
      }
	  
	  
      public IEnumerable<Sledv> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sledv.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sledv> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsledv.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sledv Get(int cono, string imptype, string slupdtno, string prod, string whse, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sledv.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(imptype)) 
         {
             sb.AppendFormatWithEscape(" AND sledv.imptype = '{0}'", imptype);
         }
         if (!string.IsNullOrEmpty(slupdtno)) 
         {
             sb.AppendFormatWithEscape(" AND sledv.slupdtno = '{0}'", slupdtno);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND sledv.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND sledv.whse = '{0}'", whse);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sledv BuildFromRow(DataRow row)
      {
         var returnRecord = Sledv.BuildSledvFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sledv>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sledv record)
      {
         Sledv.UpdateRowFromSledv(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sledv Insert(Sledv record)
      {
         DataRow row = this.dataSet.ttblsledv.NewttblsledvRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsledv.AddttblsledvRow((pdssledvDataSet.ttblsledvRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsledv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsledv.Rows[0]) : null;
      }
  

      public Sledv Update(Sledv record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsledv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsledv.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sledv record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsledv.NewttblsledvRow();
            Sledv.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsledv.AddttblsledvRow((pdssledvDataSet.ttblsledvRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sledv() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  