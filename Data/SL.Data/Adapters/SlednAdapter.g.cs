//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SL.Data.Adapters
{
   using com.infor.sxproxy.slproxy;
   using com.infor.sxproxy.slproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssledn;

   public partial class SlednAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssledn";
      private SLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> slednTableControlKey;
		
      private pdsslednDataSet dataSet;
        
      public SlednAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsslednDataSet() { DataSetName = DataSetName };
            this.slednTableControlKey = this.dataSet.ttblsledn.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.slednTableControlKey))
            {
               this.CreateTableControlParameters(this.slednTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SlednAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Slednproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSlednproxy = this.proxyAppObject.CreatePO_slednproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSlednproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Slednproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.slednTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.slednTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Slednproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSlednproxy = this.proxyAppObject.CreatePO_slednproxy())
               {
                  this.SetRequiredContextParameters();
                  poSlednproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Slednproxy - After Call");
      }
   

      public Sledn GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sledn sledn = null;
         if (row != null)
         {
             sledn = this.BuildFromRow(row);
         }
         return sledn;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sledn) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsledn.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sledn> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sledn)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sledn Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsledn.AsEnumerable().SingleOrDefault();
         Sledn sledn = null;
         if (row != null)
         {
             sledn = this.BuildFromRow(row);
         }
         return sledn;
      }
	  
	  
      public IEnumerable<Sledn> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sledn.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sledn> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsledn.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sledn Get(int cono, string imptype, string slupdtno, string newpartno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sledn.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(imptype)) 
         {
             sb.AppendFormatWithEscape(" AND sledn.imptype = '{0}'", imptype);
         }
         if (!string.IsNullOrEmpty(slupdtno)) 
         {
             sb.AppendFormatWithEscape(" AND sledn.slupdtno = '{0}'", slupdtno);
         }
         if (!string.IsNullOrEmpty(newpartno)) 
         {
             sb.AppendFormatWithEscape(" AND sledn.newpartno = '{0}'", newpartno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sledn> GetListByProd(int cono, string imptype, string slupdtno, string prod, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sledn.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(imptype)) 
         {
             sb.AppendFormatWithEscape(" AND sledn.imptype = '{0}'", imptype);
         }
         if (!string.IsNullOrEmpty(slupdtno)) 
         {
             sb.AppendFormatWithEscape(" AND sledn.slupdtno = '{0}'", slupdtno);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND sledn.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sledn BuildFromRow(DataRow row)
      {
         var returnRecord = Sledn.BuildSlednFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sledn>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sledn record)
      {
         Sledn.UpdateRowFromSledn(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sledn Insert(Sledn record)
      {
         DataRow row = this.dataSet.ttblsledn.NewttblslednRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsledn.AddttblslednRow((pdsslednDataSet.ttblslednRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsledn.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsledn.Rows[0]) : null;
      }
  

      public Sledn Update(Sledn record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsledn.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsledn.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sledn record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsledn.NewttblslednRow();
            Sledn.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsledn.AddttblslednRow((pdsslednDataSet.ttblslednRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sledn() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  