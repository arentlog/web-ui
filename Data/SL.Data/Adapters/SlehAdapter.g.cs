//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SL.Data.Adapters
{
   using com.infor.sxproxy.slproxy;
   using com.infor.sxproxy.slproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssleh;
   using Models.Pdsslehlookup;

   public partial class SlehAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssleh";
      private SLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> slehTableControlKey;
		
      private pdsslehDataSet dataSet;
        
      public SlehAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsslehDataSet() { DataSetName = DataSetName };
            this.slehTableControlKey = this.dataSet.ttblsleh.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.slehTableControlKey))
            {
               this.CreateTableControlParameters(this.slehTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SlehAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Slehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSlehproxy = this.proxyAppObject.CreatePO_slehproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSlehproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Slehproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.slehTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.slehTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Slehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSlehproxy = this.proxyAppObject.CreatePO_slehproxy())
               {
                  this.SetRequiredContextParameters();
                  poSlehproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Slehproxy - After Call");
      }
   

      public Sleh GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sleh sleh = null;
         if (row != null)
         {
             sleh = this.BuildFromRow(row);
         }
         return sleh;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sleh) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsleh.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sleh> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sleh)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sleh Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsleh.AsEnumerable().SingleOrDefault();
         Sleh sleh = null;
         if (row != null)
         {
             sleh = this.BuildFromRow(row);
         }
         return sleh;
      }
	  
	  
      public IEnumerable<Sleh> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sleh.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sleh> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsleh.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sleh Get(int cono, bool statustype, string imptype, string slupdtno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sleh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND sleh.statustype = {0}", statustype);
         if (!string.IsNullOrEmpty(imptype)) 
         {
             sb.AppendFormatWithEscape(" AND sleh.imptype = '{0}'", imptype);
         }
         if (!string.IsNullOrEmpty(slupdtno)) 
         {
             sb.AppendFormatWithEscape(" AND sleh.slupdtno = '{0}'", slupdtno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sleh.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sleh> GetListByJrnlno(int cono, int jrnlno, int setno, string imptype, string slupdtno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sleh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (jrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sleh.jrnlno = {0}", jrnlno);
         }
         if (setno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sleh.setno = {0}", setno);
         }
         if (!string.IsNullOrEmpty(imptype)) 
         {
             sb.AppendFormatWithEscape(" AND sleh.imptype = '{0}'", imptype);
         }
         if (!string.IsNullOrEmpty(slupdtno)) 
         {
             sb.AppendFormatWithEscape(" AND sleh.slupdtno = '{0}'", slupdtno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sleh BuildFromRow(DataRow row)
      {
         var returnRecord = Sleh.BuildSlehFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sleh>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sleh record)
      {
         Sleh.UpdateRowFromSleh(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sleh Insert(Sleh record)
      {
         DataRow row = this.dataSet.ttblsleh.NewttblslehRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsleh.AddttblslehRow((pdsslehDataSet.ttblslehRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsleh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsleh.Rows[0]) : null;
      }
  

      public Sleh Update(Sleh record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsleh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsleh.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sleh record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsleh.NewttblslehRow();
            Sleh.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsleh.AddttblslehRow((pdsslehDataSet.ttblslehRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sleh() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public IEnumerable<Slehlookupresults> Lookup(Slehlookupcriteria slehlookupcriteria)
      {   
         var results = new List<Slehlookupresults>();
         
         var pdsslehlookup = new pdsslehlookupDataSet();
            
         DataRow ttblslehlookupcriteriaCriteria = pdsslehlookup.ttblslehlookupcriteria.NewttblslehlookupcriteriaRow();
         Slehlookupcriteria.UpdateRowFromSlehlookupcriteria(ref ttblslehlookupcriteriaCriteria, slehlookupcriteria);
         pdsslehlookup.ttblslehlookupcriteria.AddttblslehlookupcriteriaRow((pdsslehlookupDataSet.ttblslehlookupcriteriaRow)ttblslehlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
   
         
         NLogLoggerP.Trace("Lookup - Sleh - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSlehproxy = this.proxyAppObject.CreatePO_slehproxy())
               {
                   this.SetRequiredContextParameters();
                   poSlehproxy.Lookup(ref pdsContext, ref pdsslehlookup, out cErrorMessage);
               }
            });
         NLogLoggerP.Info("Lookup - Sleh - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsslehlookup); 
    
            foreach (DataRow row in pdsslehlookup.ttblslehlookupresults)
            {
                results.Add(Slehlookupresults.BuildSlehlookupresultsFromRow(row));
            }
            return results;
        
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  