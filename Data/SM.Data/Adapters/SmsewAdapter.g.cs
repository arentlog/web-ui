//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SM.Data.Adapters
{
   using com.infor.sxproxy.smproxy;
   using com.infor.sxproxy.smproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssmsew;

   public partial class SmsewAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssmsew";
      private SMProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> smsewTableControlKey;
		
      private pdssmsewDataSet dataSet;
        
      public SmsewAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SMProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssmsewDataSet() { DataSetName = DataSetName };
            this.smsewTableControlKey = this.dataSet.ttblsmsew.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.smsewTableControlKey))
            {
               this.CreateTableControlParameters(this.smsewTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SmsewAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Smsewproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSmsewproxy = this.proxyAppObject.CreatePO_smsewproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSmsewproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Smsewproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.smsewTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.smsewTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Smsewproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSmsewproxy = this.proxyAppObject.CreatePO_smsewproxy())
               {
                  this.SetRequiredContextParameters();
                  poSmsewproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Smsewproxy - After Call");
      }
   

      public Smsew GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Smsew smsew = null;
         if (row != null)
         {
             smsew = this.BuildFromRow(row);
         }
         return smsew;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(smsew) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsmsew.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Smsew> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(smsew)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Smsew Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsmsew.AsEnumerable().SingleOrDefault();
         Smsew smsew = null;
         if (row != null)
         {
             smsew = this.BuildFromRow(row);
         }
         return smsew;
      }
	  
	  
      public IEnumerable<Smsew> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("smsew.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Smsew> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsmsew.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Smsew Get(int cono, int yr, decimal custno, string shipto, string whse, string prod, bool stockfl, bool componentfl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("smsew.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (yr != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND smsew.yr = {0}", yr);
         }
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND smsew.custno = {0}", custno);
         }
         if (!string.IsNullOrEmpty(shipto)) 
         {
             sb.AppendFormatWithEscape(" AND smsew.shipto = '{0}'", shipto);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND smsew.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND smsew.prod = '{0}'", prod);
         }
         sb.AppendFormatWithEscape(" AND smsew.stockfl = {0}", stockfl);
         sb.AppendFormatWithEscape(" AND smsew.componentfl = {0}", componentfl);
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Smsew> GetListByProd(int cono, int yr, string prod, string whse, decimal custno, string shipto, bool stockfl, bool componentfl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("smsew.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (yr != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND smsew.yr = {0}", yr);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND smsew.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND smsew.whse = '{0}'", whse);
         }
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND smsew.custno = {0}", custno);
         }
         if (!string.IsNullOrEmpty(shipto)) 
         {
             sb.AppendFormatWithEscape(" AND smsew.shipto = '{0}'", shipto);
         }
         sb.AppendFormatWithEscape(" AND smsew.stockfl = {0}", stockfl);
         sb.AppendFormatWithEscape(" AND smsew.componentfl = {0}", componentfl);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Smsew> GetListByProdwh(int cono, string prod, string whse, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("smsew.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND smsew.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND smsew.whse = '{0}'", whse);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Smsew BuildFromRow(DataRow row)
      {
         var returnRecord = Smsew.BuildSmsewFromRow(row);
         returnRecord = this.BuildExtraFromRow<Smsew>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Smsew record)
      {
         Smsew.UpdateRowFromSmsew(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Smsew Insert(Smsew record)
      {
         DataRow row = this.dataSet.ttblsmsew.NewttblsmsewRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsmsew.AddttblsmsewRow((pdssmsewDataSet.ttblsmsewRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsmsew.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsmsew.Rows[0]) : null;
      }
  

      public Smsew Update(Smsew record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsmsew.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsmsew.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Smsew record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsmsew.NewttblsmsewRow();
            Smsew.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsmsew.AddttblsmsewRow((pdssmsewDataSet.ttblsmsewRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Smsew() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  