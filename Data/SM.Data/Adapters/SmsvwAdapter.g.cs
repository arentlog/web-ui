//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SM.Data.Adapters
{
   using com.infor.sxproxy.smproxy;
   using com.infor.sxproxy.smproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssmsvw;

   public partial class SmsvwAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssmsvw";
      private SMProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> smsvwTableControlKey;
		
      private pdssmsvwDataSet dataSet;
        
      public SmsvwAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SMProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssmsvwDataSet() { DataSetName = DataSetName };
            this.smsvwTableControlKey = this.dataSet.ttblsmsvw.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.smsvwTableControlKey))
            {
               this.CreateTableControlParameters(this.smsvwTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SmsvwAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Smsvwproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSmsvwproxy = this.proxyAppObject.CreatePO_smsvwproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSmsvwproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Smsvwproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.smsvwTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.smsvwTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Smsvwproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSmsvwproxy = this.proxyAppObject.CreatePO_smsvwproxy())
               {
                  this.SetRequiredContextParameters();
                  poSmsvwproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Smsvwproxy - After Call");
      }
   

      public Smsvw GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Smsvw smsvw = null;
         if (row != null)
         {
             smsvw = this.BuildFromRow(row);
         }
         return smsvw;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(smsvw) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsmsvw.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Smsvw> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(smsvw)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Smsvw Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsmsvw.AsEnumerable().SingleOrDefault();
         Smsvw smsvw = null;
         if (row != null)
         {
             smsvw = this.BuildFromRow(row);
         }
         return smsvw;
      }
	  
	  
      public IEnumerable<Smsvw> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("smsvw.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Smsvw> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsmsvw.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Smsvw Get(int cono, int yr, decimal vendno, string whse, string prod, bool stockfl, bool componentfl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("smsvw.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (yr != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.yr = {0}", yr);
         }
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.vendno = {0}", vendno);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.prod = '{0}'", prod);
         }
         sb.AppendFormatWithEscape(" AND smsvw.stockfl = {0}", stockfl);
         sb.AppendFormatWithEscape(" AND smsvw.componentfl = {0}", componentfl);
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Smsvw> GetListByProd(int cono, int yr, string prod, string whse, decimal vendno, bool stockfl, bool componentfl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("smsvw.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (yr != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.yr = {0}", yr);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.whse = '{0}'", whse);
         }
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.vendno = {0}", vendno);
         }
         sb.AppendFormatWithEscape(" AND smsvw.stockfl = {0}", stockfl);
         sb.AppendFormatWithEscape(" AND smsvw.componentfl = {0}", componentfl);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Smsvw> GetListByProdwh(int cono, string prod, string whse, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("smsvw.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND smsvw.whse = '{0}'", whse);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Smsvw BuildFromRow(DataRow row)
      {
         var returnRecord = Smsvw.BuildSmsvwFromRow(row);
         returnRecord = this.BuildExtraFromRow<Smsvw>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Smsvw record)
      {
         Smsvw.UpdateRowFromSmsvw(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Smsvw Insert(Smsvw record)
      {
         DataRow row = this.dataSet.ttblsmsvw.NewttblsmsvwRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsmsvw.AddttblsmsvwRow((pdssmsvwDataSet.ttblsmsvwRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsmsvw.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsmsvw.Rows[0]) : null;
      }
  

      public Smsvw Update(Smsvw record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsmsvw.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsmsvw.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Smsvw record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsmsvw.NewttblsmsvwRow();
            Smsvw.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsmsvw.AddttblsmsvwRow((pdssmsvwDataSet.ttblsmsvwRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Smsvw() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  