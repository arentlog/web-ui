//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscctrans;

   public partial class CctransAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscctrans";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cctransTableControlKey;
		
      private pdscctransDataSet dataSet;
        
      public CctransAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscctransDataSet() { DataSetName = DataSetName };
            this.cctransTableControlKey = this.dataSet.ttblcctrans.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cctransTableControlKey))
            {
               this.CreateTableControlParameters(this.cctransTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CctransAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Cctransproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCctransproxy = this.proxyAppObject.CreatePO_cctransproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCctransproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Cctransproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cctransTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cctransTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Cctransproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCctransproxy = this.proxyAppObject.CreatePO_cctransproxy())
               {
                  this.SetRequiredContextParameters();
                  poCctransproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Cctransproxy - After Call");
      }
   

      public Cctrans GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Cctrans cctrans = null;
         if (row != null)
         {
             cctrans = this.BuildFromRow(row);
         }
         return cctrans;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(cctrans) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcctrans.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Cctrans> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(cctrans)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Cctrans Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcctrans.AsEnumerable().SingleOrDefault();
         Cctrans cctrans = null;
         if (row != null)
         {
             cctrans = this.BuildFromRow(row);
         }
         return cctrans;
      }
	  
	  

      public IEnumerable<Cctrans> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcctrans.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Cctrans Get(string recordid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(recordid)) 
         {
             sb.AppendFormatWithEscape("cctrans.recordid = '{0}'", recordid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Cctrans> GetListByStatus(string statusty, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(statusty)) 
         {
             sb.AppendFormatWithEscape("cctrans.statusty = '{0}'", statusty);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Cctrans BuildFromRow(DataRow row)
      {
         var returnRecord = Cctrans.BuildCctransFromRow(row);
         returnRecord = this.BuildExtraFromRow<Cctrans>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Cctrans record)
      {
         Cctrans.UpdateRowFromCctrans(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Cctrans Insert(Cctrans record)
      {
         DataRow row = this.dataSet.ttblcctrans.NewttblcctransRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcctrans.AddttblcctransRow((pdscctransDataSet.ttblcctransRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcctrans.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcctrans.Rows[0]) : null;
      }
  

      public Cctrans Update(Cctrans record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcctrans.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcctrans.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Cctrans record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcctrans.NewttblcctransRow();
            Cctrans.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcctrans.AddttblcctransRow((pdscctransDataSet.ttblcctransRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Cctrans() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  