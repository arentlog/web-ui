//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdseventActivate;

   public partial class EventActivateAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsevent_activate";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> eventActivateTableControlKey;
		
      private pdsevent_activateDataSet dataSet;
        
      public EventActivateAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsevent_activateDataSet() { DataSetName = DataSetName };
            this.eventActivateTableControlKey = this.dataSet.ttblevent_activate.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.eventActivateTableControlKey))
            {
               this.CreateTableControlParameters(this.eventActivateTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EventActivateAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Event_activateproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEvent_activateproxy = this.proxyAppObject.CreatePO_event_activateproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEvent_activateproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Event_activateproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.eventActivateTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.eventActivateTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Event_activateproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEvent_activateproxy = this.proxyAppObject.CreatePO_event_activateproxy())
               {
                  this.SetRequiredContextParameters();
                  poEvent_activateproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Event_activateproxy - After Call");
      }
   

      public EventActivate GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         EventActivate eventActivate = null;
         if (row != null)
         {
             eventActivate = this.BuildFromRow(row);
         }
         return eventActivate;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(event_activate) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblevent_activate.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<EventActivate> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(event_activate)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected EventActivate Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblevent_activate.AsEnumerable().SingleOrDefault();
         EventActivate eventActivate = null;
         if (row != null)
         {
             eventActivate = this.BuildFromRow(row);
         }
         return eventActivate;
      }
	  
	  
      public IEnumerable<EventActivate> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("event_activate.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventActivate> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblevent_activate.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public EventActivate Get(int cono, string triggername, string eventname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("event_activate.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(triggername)) 
         {
             sb.AppendFormatWithEscape(" AND event_activate.triggername = '{0}'", triggername);
         }
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape(" AND event_activate.eventname = '{0}'", eventname);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<EventActivate> GetListByEvent(string eventname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape("event_activate.eventname = '{0}'", eventname);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventActivate> GetListByTrigger(string triggername, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(triggername)) 
         {
             sb.AppendFormatWithEscape("event_activate.triggername = '{0}'", triggername);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public EventActivate BuildFromRow(DataRow row)
      {
         var returnRecord = EventActivate.BuildEventActivateFromRow(row);
         returnRecord = this.BuildExtraFromRow<EventActivate>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, EventActivate record)
      {
         EventActivate.UpdateRowFromEventActivate(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public EventActivate Insert(EventActivate record)
      {
         DataRow row = this.dataSet.ttblevent_activate.Newttblevent_activateRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblevent_activate.Addttblevent_activateRow((pdsevent_activateDataSet.ttblevent_activateRow)row);
         this.SaveChanges();
         return this.dataSet.ttblevent_activate.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_activate.Rows[0]) : null;
      }
  

      public EventActivate Update(EventActivate record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblevent_activate.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_activate.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(EventActivate record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblevent_activate.Newttblevent_activateRow();
            EventActivate.BuildMinimalRow(ref row, record);
            this.dataSet.ttblevent_activate.Addttblevent_activateRow((pdsevent_activateDataSet.ttblevent_activateRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new EventActivate() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  