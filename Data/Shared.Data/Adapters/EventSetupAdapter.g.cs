//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdseventSetup;
   using Models.Pdssaeventlookup;
   using Models.Complex;

   public partial class EventSetupAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsevent_setup";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> eventSetupTableControlKey;
		
      private pdsevent_setupDataSet dataSet;
        
      public EventSetupAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsevent_setupDataSet() { DataSetName = DataSetName };
            this.eventSetupTableControlKey = this.dataSet.ttblevent_setup.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.eventSetupTableControlKey))
            {
               this.CreateTableControlParameters(this.eventSetupTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EventSetupAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Event_setupproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEvent_setupproxy = this.proxyAppObject.CreatePO_event_setupproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEvent_setupproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Event_setupproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.eventSetupTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.eventSetupTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Event_setupproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEvent_setupproxy = this.proxyAppObject.CreatePO_event_setupproxy())
               {
                  this.SetRequiredContextParameters();
                  poEvent_setupproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Event_setupproxy - After Call");
      }
   

      public EventSetup GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         EventSetup eventSetup = null;
         if (row != null)
         {
             eventSetup = this.BuildFromRow(row);
         }
         return eventSetup;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(event_setup) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblevent_setup.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<EventSetup> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(event_setup)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected EventSetup Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblevent_setup.AsEnumerable().SingleOrDefault();
         EventSetup eventSetup = null;
         if (row != null)
         {
             eventSetup = this.BuildFromRow(row);
         }
         return eventSetup;
      }
	  
	  

      public IEnumerable<EventSetup> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblevent_setup.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public EventSetup Get(string eventname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape("event_setup.eventname = '{0}'", eventname);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<EventSetup> GetListBySource(string sourcename, string eventname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(sourcename)) 
         {
             sb.AppendFormatWithEscape("event_setup.sourcename = '{0}'", sourcename);
         }
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape(" AND event_setup.eventname = '{0}'", eventname);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventSetup> GetListByTrigger(string triggername, string eventname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(triggername)) 
         {
             sb.AppendFormatWithEscape("event_setup.triggername = '{0}'", triggername);
         }
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape(" AND event_setup.eventname = '{0}'", eventname);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public EventSetup BuildFromRow(DataRow row)
      {
         var returnRecord = EventSetup.BuildEventSetupFromRow(row);
         returnRecord = this.BuildExtraFromRow<EventSetup>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, EventSetup record)
      {
         EventSetup.UpdateRowFromEventSetup(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public EventSetup Insert(EventSetup record)
      {
         DataRow row = this.dataSet.ttblevent_setup.Newttblevent_setupRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblevent_setup.Addttblevent_setupRow((pdsevent_setupDataSet.ttblevent_setupRow)row);
         this.SaveChanges();
         return this.dataSet.ttblevent_setup.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_setup.Rows[0]) : null;
      }
  

      public EventSetup Update(EventSetup record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblevent_setup.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_setup.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(EventSetup record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblevent_setup.Newttblevent_setupRow();
            EventSetup.BuildMinimalRow(ref row, record);
            this.dataSet.ttblevent_setup.Addttblevent_setupRow((pdsevent_setupDataSet.ttblevent_setupRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new EventSetup() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public EventSetupLookupResponseAPI Lookup(Saeventlookupcriteria saeventlookupcriteria)
      {   
         var result = new EventSetupLookupResponseAPI();
         
         var pdssaeventlookup = new pdssaeventlookupDataSet();
            
         DataRow ttblsaeventlookupcriteriaCriteria = pdssaeventlookup.ttblsaeventlookupcriteria.NewttblsaeventlookupcriteriaRow();
         Saeventlookupcriteria.UpdateRowFromSaeventlookupcriteria(ref ttblsaeventlookupcriteriaCriteria, saeventlookupcriteria);
         pdssaeventlookup.ttblsaeventlookupcriteria.AddttblsaeventlookupcriteriaRow((pdssaeventlookupDataSet.ttblsaeventlookupcriteriaRow)ttblsaeventlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - EventSetup - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEventSetupproxy = this.proxyAppObject.CreatePO_event_setupproxy())
               {
                   this.SetRequiredContextParameters();
                   poEventSetupproxy.Lookup(ref pdsContext, ref pdssaeventlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - EventSetup - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdssaeventlookup); 
    
         foreach (DataRow row in pdssaeventlookup.ttblsaeventlookupresults)
         {
            result.saeventlookupresults.Add(Saeventlookupresults.BuildSaeventlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  