//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsccpreauth;

   public partial class CcpreauthAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsccpreauth";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> ccpreauthTableControlKey;
		
      private pdsccpreauthDataSet dataSet;
        
      public CcpreauthAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsccpreauthDataSet() { DataSetName = DataSetName };
            this.ccpreauthTableControlKey = this.dataSet.ttblccpreauth.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.ccpreauthTableControlKey))
            {
               this.CreateTableControlParameters(this.ccpreauthTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CcpreauthAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Ccpreauthproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCcpreauthproxy = this.proxyAppObject.CreatePO_ccpreauthproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCcpreauthproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Ccpreauthproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.ccpreauthTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.ccpreauthTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Ccpreauthproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCcpreauthproxy = this.proxyAppObject.CreatePO_ccpreauthproxy())
               {
                  this.SetRequiredContextParameters();
                  poCcpreauthproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Ccpreauthproxy - After Call");
      }
   

      public Ccpreauth GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Ccpreauth ccpreauth = null;
         if (row != null)
         {
             ccpreauth = this.BuildFromRow(row);
         }
         return ccpreauth;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(ccpreauth) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblccpreauth.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Ccpreauth> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(ccpreauth)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Ccpreauth Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblccpreauth.AsEnumerable().SingleOrDefault();
         Ccpreauth ccpreauth = null;
         if (row != null)
         {
             ccpreauth = this.BuildFromRow(row);
         }
         return ccpreauth;
      }
	  
	  
      public IEnumerable<Ccpreauth> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("ccpreauth.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Ccpreauth> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblccpreauth.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Ccpreauth Get(int cono, decimal custno, string shipto, string whse, decimal recordno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("ccpreauth.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND ccpreauth.custno = {0}", custno);
         }
         if (!string.IsNullOrEmpty(shipto)) 
         {
             sb.AppendFormatWithEscape(" AND ccpreauth.shipto = '{0}'", shipto);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND ccpreauth.whse = '{0}'", whse);
         }
         if (recordno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND ccpreauth.recordno = {0}", recordno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Ccpreauth BuildFromRow(DataRow row)
      {
         var returnRecord = Ccpreauth.BuildCcpreauthFromRow(row);
         returnRecord = this.BuildExtraFromRow<Ccpreauth>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Ccpreauth record)
      {
         Ccpreauth.UpdateRowFromCcpreauth(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Ccpreauth Insert(Ccpreauth record)
      {
         DataRow row = this.dataSet.ttblccpreauth.NewttblccpreauthRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblccpreauth.AddttblccpreauthRow((pdsccpreauthDataSet.ttblccpreauthRow)row);
         this.SaveChanges();
         return this.dataSet.ttblccpreauth.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblccpreauth.Rows[0]) : null;
      }
  

      public Ccpreauth Update(Ccpreauth record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblccpreauth.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblccpreauth.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Ccpreauth record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblccpreauth.NewttblccpreauthRow();
            Ccpreauth.BuildMinimalRow(ref row, record);
            this.dataSet.ttblccpreauth.AddttblccpreauthRow((pdsccpreauthDataSet.ttblccpreauthRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Ccpreauth() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  