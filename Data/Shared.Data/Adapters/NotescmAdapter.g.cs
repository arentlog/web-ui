//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsnotescm;

   public partial class NotescmAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsnotescm";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> notescmTableControlKey;
		
      private pdsnotescmDataSet dataSet;
        
      public NotescmAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsnotescmDataSet() { DataSetName = DataSetName };
            this.notescmTableControlKey = this.dataSet.ttblnotescm.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.notescmTableControlKey))
            {
               this.CreateTableControlParameters(this.notescmTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in NotescmAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Notescmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poNotescmproxy = this.proxyAppObject.CreatePO_notescmproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poNotescmproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Notescmproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.notescmTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.notescmTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Notescmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poNotescmproxy = this.proxyAppObject.CreatePO_notescmproxy())
               {
                  this.SetRequiredContextParameters();
                  poNotescmproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Notescmproxy - After Call");
      }
   

      public Notescm GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Notescm notescm = null;
         if (row != null)
         {
             notescm = this.BuildFromRow(row);
         }
         return notescm;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(notescm) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblnotescm.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Notescm> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(notescm)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Notescm Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblnotescm.AsEnumerable().SingleOrDefault();
         Notescm notescm = null;
         if (row != null)
         {
             notescm = this.BuildFromRow(row);
         }
         return notescm;
      }
	  
	  

      public IEnumerable<Notescm> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblnotescm.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Notescm Get(string notestype, string primarykey, string secondarykey, int pageno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(notestype)) 
         {
             sb.AppendFormatWithEscape("notescm.notestype = '{0}'", notestype);
         }
         if (!string.IsNullOrEmpty(primarykey)) 
         {
             sb.AppendFormatWithEscape(" AND notescm.primarykey = '{0}'", primarykey);
         }
         if (!string.IsNullOrEmpty(secondarykey)) 
         {
             sb.AppendFormatWithEscape(" AND notescm.secondarykey = '{0}'", secondarykey);
         }
         if (pageno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND notescm.pageno = {0}", pageno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Notescm> GetListByTransdt(string notestype, string primarykey, DateTime? transdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(notestype)) 
         {
             sb.AppendFormatWithEscape("notescm.notestype = '{0}'", notestype);
         }
         if (!string.IsNullOrEmpty(primarykey)) 
         {
             sb.AppendFormatWithEscape(" AND notescm.primarykey = '{0}'", primarykey);
         }
         if (transdt != null) 
         {
             sb.AppendFormatWithEscape(" AND notescm.transdt = '{0}'", transdt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Notescm BuildFromRow(DataRow row)
      {
         var returnRecord = Notescm.BuildNotescmFromRow(row);
         returnRecord = this.BuildExtraFromRow<Notescm>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Notescm record)
      {
         Notescm.UpdateRowFromNotescm(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Notescm Insert(Notescm record)
      {
         DataRow row = this.dataSet.ttblnotescm.NewttblnotescmRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblnotescm.AddttblnotescmRow((pdsnotescmDataSet.ttblnotescmRow)row);
         this.SaveChanges();
         return this.dataSet.ttblnotescm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblnotescm.Rows[0]) : null;
      }
  

      public Notescm Update(Notescm record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblnotescm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblnotescm.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Notescm record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblnotescm.NewttblnotescmRow();
            Notescm.BuildMinimalRow(ref row, record);
            this.dataSet.ttblnotescm.AddttblnotescmRow((pdsnotescmDataSet.ttblnotescmRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Notescm() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  