//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsesbwlstatl;

   public partial class EsbwlstatlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsesbwlstatl";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> esbwlstatlTableControlKey;
		
      private pdsesbwlstatlDataSet dataSet;
        
      public EsbwlstatlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsesbwlstatlDataSet() { DataSetName = DataSetName };
            this.esbwlstatlTableControlKey = this.dataSet.ttblesbwlstatl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.esbwlstatlTableControlKey))
            {
               this.CreateTableControlParameters(this.esbwlstatlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EsbwlstatlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Esbwlstatlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEsbwlstatlproxy = this.proxyAppObject.CreatePO_esbwlstatlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEsbwlstatlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Esbwlstatlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.esbwlstatlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.esbwlstatlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Esbwlstatlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEsbwlstatlproxy = this.proxyAppObject.CreatePO_esbwlstatlproxy())
               {
                  this.SetRequiredContextParameters();
                  poEsbwlstatlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Esbwlstatlproxy - After Call");
      }
   

      public Esbwlstatl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Esbwlstatl esbwlstatl = null;
         if (row != null)
         {
             esbwlstatl = this.BuildFromRow(row);
         }
         return esbwlstatl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(esbwlstatl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblesbwlstatl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Esbwlstatl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(esbwlstatl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Esbwlstatl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblesbwlstatl.AsEnumerable().SingleOrDefault();
         Esbwlstatl esbwlstatl = null;
         if (row != null)
         {
             esbwlstatl = this.BuildFromRow(row);
         }
         return esbwlstatl;
      }
	  
	  
      public IEnumerable<Esbwlstatl> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("esbwlstatl.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Esbwlstatl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblesbwlstatl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Esbwlstatl Get(int cono, string whse, string ordertype, int orderno, int ordersuf, int seqno, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("esbwlstatl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstatl.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(ordertype)) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstatl.ordertype = '{0}'", ordertype);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstatl.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstatl.ordersuf = {0}", ordersuf);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstatl.seqno = {0}", seqno);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstatl.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Esbwlstatl BuildFromRow(DataRow row)
      {
         var returnRecord = Esbwlstatl.BuildEsbwlstatlFromRow(row);
         returnRecord = this.BuildExtraFromRow<Esbwlstatl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Esbwlstatl record)
      {
         Esbwlstatl.UpdateRowFromEsbwlstatl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Esbwlstatl Insert(Esbwlstatl record)
      {
         DataRow row = this.dataSet.ttblesbwlstatl.NewttblesbwlstatlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblesbwlstatl.AddttblesbwlstatlRow((pdsesbwlstatlDataSet.ttblesbwlstatlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblesbwlstatl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblesbwlstatl.Rows[0]) : null;
      }
  

      public Esbwlstatl Update(Esbwlstatl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblesbwlstatl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblesbwlstatl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Esbwlstatl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblesbwlstatl.NewttblesbwlstatlRow();
            Esbwlstatl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblesbwlstatl.AddttblesbwlstatlRow((pdsesbwlstatlDataSet.ttblesbwlstatlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Esbwlstatl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  