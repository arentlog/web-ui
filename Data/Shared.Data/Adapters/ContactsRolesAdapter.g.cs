//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdscontactsRoles;

   public partial class ContactsRolesAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscontactsRoles";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> contactsRolesTableControlKey;
		
      private pdscontactsRolesDataSet dataSet;
        
      public ContactsRolesAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscontactsRolesDataSet() { DataSetName = DataSetName };
            this.contactsRolesTableControlKey = this.dataSet.ttblcontactsRoles.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.contactsRolesTableControlKey))
            {
               this.CreateTableControlParameters(this.contactsRolesTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ContactsRolesAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - ContactsRolesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poContactsRolesproxy = this.proxyAppObject.CreatePO_contactsRolesproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poContactsRolesproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - ContactsRolesproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.contactsRolesTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.contactsRolesTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - ContactsRolesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poContactsRolesproxy = this.proxyAppObject.CreatePO_contactsRolesproxy())
               {
                  this.SetRequiredContextParameters();
                  poContactsRolesproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - ContactsRolesproxy - After Call");
      }
   

      public ContactsRoles GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         ContactsRoles contactsRoles = null;
         if (row != null)
         {
             contactsRoles = this.BuildFromRow(row);
         }
         return contactsRoles;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(contacts-roles) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcontactsRoles.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<ContactsRoles> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(contacts-roles)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected ContactsRoles Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcontactsRoles.AsEnumerable().SingleOrDefault();
         ContactsRoles contactsRoles = null;
         if (row != null)
         {
             contactsRoles = this.BuildFromRow(row);
         }
         return contactsRoles;
      }
	  
	  

      public IEnumerable<ContactsRoles> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcontactsRoles.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public ContactsRoles Get(int cono, string roletype, string primarykey, string secondarykey, decimal contactid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("contacts-roles.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(roletype)) 
         {
             sb.AppendFormatWithEscape(" AND contacts-roles.roletype = '{0}'", roletype);
         }
         if (!string.IsNullOrEmpty(primarykey)) 
         {
             sb.AppendFormatWithEscape(" AND contacts-roles.primarykey = '{0}'", primarykey);
         }
         if (!string.IsNullOrEmpty(secondarykey)) 
         {
             sb.AppendFormatWithEscape(" AND contacts-roles.secondarykey = '{0}'", secondarykey);
         }
         if (contactid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND contacts-roles.contactid = {0}", contactid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<ContactsRoles> GetListByContactid(int cono, decimal contactid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("contacts-roles.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (contactid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND contacts-roles.contactid = {0}", contactid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<ContactsRoles> GetListByTransdttmz(DateTime? transdttmz, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (transdttmz != null) 
         {
             sb.AppendFormatWithEscape("contacts-roles.transdttmz = '{0}'", transdttmz);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public ContactsRoles BuildFromRow(DataRow row)
      {
         var returnRecord = ContactsRoles.BuildContactsRolesFromRow(row);
         returnRecord = this.BuildExtraFromRow<ContactsRoles>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, ContactsRoles record)
      {
         ContactsRoles.UpdateRowFromContactsRoles(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public ContactsRoles Insert(ContactsRoles record)
      {
         DataRow row = this.dataSet.ttblcontactsRoles.NewttblcontactsRolesRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcontactsRoles.AddttblcontactsRolesRow((pdscontactsRolesDataSet.ttblcontactsRolesRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcontactsRoles.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcontactsRoles.Rows[0]) : null;
      }
  

      public ContactsRoles Update(ContactsRoles record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcontactsRoles.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcontactsRoles.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(ContactsRoles record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcontactsRoles.NewttblcontactsRolesRow();
            ContactsRoles.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcontactsRoles.AddttblcontactsRolesRow((pdscontactsRolesDataSet.ttblcontactsRolesRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new ContactsRoles() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  