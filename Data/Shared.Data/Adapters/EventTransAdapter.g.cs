//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdseventTrans;

   public partial class EventTransAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsevent_trans";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> eventTransTableControlKey;
		
      private pdsevent_transDataSet dataSet;
        
      public EventTransAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsevent_transDataSet() { DataSetName = DataSetName };
            this.eventTransTableControlKey = this.dataSet.ttblevent_trans.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.eventTransTableControlKey))
            {
               this.CreateTableControlParameters(this.eventTransTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EventTransAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Event_transproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEvent_transproxy = this.proxyAppObject.CreatePO_event_transproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEvent_transproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Event_transproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.eventTransTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.eventTransTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Event_transproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEvent_transproxy = this.proxyAppObject.CreatePO_event_transproxy())
               {
                  this.SetRequiredContextParameters();
                  poEvent_transproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Event_transproxy - After Call");
      }
   

      public EventTrans GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         EventTrans eventTrans = null;
         if (row != null)
         {
             eventTrans = this.BuildFromRow(row);
         }
         return eventTrans;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(event_trans) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblevent_trans.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<EventTrans> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(event_trans)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected EventTrans Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblevent_trans.AsEnumerable().SingleOrDefault();
         EventTrans eventTrans = null;
         if (row != null)
         {
             eventTrans = this.BuildFromRow(row);
         }
         return eventTrans;
      }
	  
	  
      public IEnumerable<EventTrans> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("event_trans.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventTrans> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblevent_trans.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public EventTrans Get(int cono, string eventname, DateTime? eventdt, string eventtm, decimal recordno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("event_trans.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventname = '{0}'", eventname);
         }
         if (eventdt != null) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventdt = '{0}'", eventdt);
         }
         if (!string.IsNullOrEmpty(eventtm)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventtm = '{0}'", eventtm);
         }
         if (recordno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.recordno = {0}", recordno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<EventTrans> GetListByCustno(int cono, decimal custno, DateTime? eventdt, string eventtm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("event_trans.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.custno = {0}", custno);
         }
         if (eventdt != null) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventdt = '{0}'", eventdt);
         }
         if (!string.IsNullOrEmpty(eventtm)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventtm = '{0}'", eventtm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventTrans> GetListByDocument(int cono, string doctype, int docorderno, int docordersuf, DateTime? eventdt, string eventtm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("event_trans.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(doctype)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.doctype = '{0}'", doctype);
         }
         if (docorderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.docorderno = {0}", docorderno);
         }
         if (docordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.docordersuf = {0}", docordersuf);
         }
         if (eventdt != null) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventdt = '{0}'", eventdt);
         }
         if (!string.IsNullOrEmpty(eventtm)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventtm = '{0}'", eventtm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventTrans> GetListByProd(int cono, string prod, string whse, DateTime? eventdt, string eventtm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("event_trans.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.whse = '{0}'", whse);
         }
         if (eventdt != null) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventdt = '{0}'", eventdt);
         }
         if (!string.IsNullOrEmpty(eventtm)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventtm = '{0}'", eventtm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventTrans> GetListByVendno(int cono, decimal vendno, DateTime? eventdt, string eventtm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("event_trans.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.vendno = {0}", vendno);
         }
         if (eventdt != null) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventdt = '{0}'", eventdt);
         }
         if (!string.IsNullOrEmpty(eventtm)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans.eventtm = '{0}'", eventtm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public EventTrans BuildFromRow(DataRow row)
      {
         var returnRecord = EventTrans.BuildEventTransFromRow(row);
         returnRecord = this.BuildExtraFromRow<EventTrans>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, EventTrans record)
      {
         EventTrans.UpdateRowFromEventTrans(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public EventTrans Insert(EventTrans record)
      {
         DataRow row = this.dataSet.ttblevent_trans.Newttblevent_transRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblevent_trans.Addttblevent_transRow((pdsevent_transDataSet.ttblevent_transRow)row);
         this.SaveChanges();
         return this.dataSet.ttblevent_trans.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_trans.Rows[0]) : null;
      }
  

      public EventTrans Update(EventTrans record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblevent_trans.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_trans.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(EventTrans record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblevent_trans.Newttblevent_transRow();
            EventTrans.BuildMinimalRow(ref row, record);
            this.dataSet.ttblevent_trans.Addttblevent_transRow((pdsevent_transDataSet.ttblevent_transRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new EventTrans() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  