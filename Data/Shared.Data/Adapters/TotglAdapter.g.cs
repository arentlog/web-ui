//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdstotgl;

   public partial class TotglAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdstotgl";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> totglTableControlKey;
		
      private pdstotglDataSet dataSet;
        
      public TotglAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdstotglDataSet() { DataSetName = DataSetName };
            this.totglTableControlKey = this.dataSet.ttbltotgl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.totglTableControlKey))
            {
               this.CreateTableControlParameters(this.totglTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in TotglAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Totglproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poTotglproxy = this.proxyAppObject.CreatePO_totglproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poTotglproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Totglproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.totglTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.totglTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Totglproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poTotglproxy = this.proxyAppObject.CreatePO_totglproxy())
               {
                  this.SetRequiredContextParameters();
                  poTotglproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Totglproxy - After Call");
      }
   

      public Totgl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Totgl totgl = null;
         if (row != null)
         {
             totgl = this.BuildFromRow(row);
         }
         return totgl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(totgl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltotgl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Totgl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(totgl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Totgl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbltotgl.AsEnumerable().SingleOrDefault();
         Totgl totgl = null;
         if (row != null)
         {
             totgl = this.BuildFromRow(row);
         }
         return totgl;
      }
	  
	  
      public IEnumerable<Totgl> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("totgl.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Totgl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbltotgl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Totgl Get(int cono, int jrnlno, int gldivno, int gldeptno, int glacctno, int glsubno, bool baltype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("totgl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (jrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.jrnlno = {0}", jrnlno);
         }
         if (gldivno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.gldivno = {0}", gldivno);
         }
         if (gldeptno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.gldeptno = {0}", gldeptno);
         }
         if (glacctno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.glacctno = {0}", glacctno);
         }
         if (glsubno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.glsubno = {0}", glsubno);
         }
         sb.AppendFormatWithEscape(" AND totgl.baltype = {0}", baltype);
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Totgl> GetListByOrder(int cono, int jrnlno, int orderno, int ordersuf, int gldivno, int gldeptno, int glacctno, int glsubno, bool baltype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("totgl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (jrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.jrnlno = {0}", jrnlno);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.ordersuf = {0}", ordersuf);
         }
         if (gldivno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.gldivno = {0}", gldivno);
         }
         if (gldeptno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.gldeptno = {0}", gldeptno);
         }
         if (glacctno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.glacctno = {0}", glacctno);
         }
         if (glsubno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND totgl.glsubno = {0}", glsubno);
         }
         sb.AppendFormatWithEscape(" AND totgl.baltype = {0}", baltype);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Totgl BuildFromRow(DataRow row)
      {
         var returnRecord = Totgl.BuildTotglFromRow(row);
         returnRecord = this.BuildExtraFromRow<Totgl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Totgl record)
      {
         Totgl.UpdateRowFromTotgl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Totgl Insert(Totgl record)
      {
         DataRow row = this.dataSet.ttbltotgl.NewttbltotglRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbltotgl.AddttbltotglRow((pdstotglDataSet.ttbltotglRow)row);
         this.SaveChanges();
         return this.dataSet.ttbltotgl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltotgl.Rows[0]) : null;
      }
  

      public Totgl Update(Totgl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbltotgl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltotgl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Totgl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbltotgl.NewttbltotglRow();
            Totgl.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltotgl.AddttbltotglRow((pdstotglDataSet.ttbltotglRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Totgl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  