//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsjurtaxdet;

   public partial class JurtaxdetAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsjurtaxdet";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> jurtaxdetTableControlKey;
		
      private pdsjurtaxdetDataSet dataSet;
        
      public JurtaxdetAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsjurtaxdetDataSet() { DataSetName = DataSetName };
            this.jurtaxdetTableControlKey = this.dataSet.ttbljurtaxdet.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.jurtaxdetTableControlKey))
            {
               this.CreateTableControlParameters(this.jurtaxdetTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in JurtaxdetAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Jurtaxdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poJurtaxdetproxy = this.proxyAppObject.CreatePO_jurtaxdetproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poJurtaxdetproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Jurtaxdetproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.jurtaxdetTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.jurtaxdetTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Jurtaxdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poJurtaxdetproxy = this.proxyAppObject.CreatePO_jurtaxdetproxy())
               {
                  this.SetRequiredContextParameters();
                  poJurtaxdetproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Jurtaxdetproxy - After Call");
      }
   

      public Jurtaxdet GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Jurtaxdet jurtaxdet = null;
         if (row != null)
         {
             jurtaxdet = this.BuildFromRow(row);
         }
         return jurtaxdet;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(jurtaxdet) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbljurtaxdet.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Jurtaxdet> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(jurtaxdet)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Jurtaxdet Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbljurtaxdet.AsEnumerable().SingleOrDefault();
         Jurtaxdet jurtaxdet = null;
         if (row != null)
         {
             jurtaxdet = this.BuildFromRow(row);
         }
         return jurtaxdet;
      }
	  
	  
      public IEnumerable<Jurtaxdet> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("jurtaxdet.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Jurtaxdet> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbljurtaxdet.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Jurtaxdet Get(int cono, string srcrowpointer, int jurid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("jurtaxdet.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(srcrowpointer)) 
         {
             sb.AppendFormatWithEscape(" AND jurtaxdet.srcrowpointer = '{0}'", srcrowpointer);
         }
         if (jurid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND jurtaxdet.jurid = {0}", jurid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Jurtaxdet> GetListByDisplay(int cono, string dispjurtype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("jurtaxdet.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(dispjurtype)) 
         {
             sb.AppendFormatWithEscape(" AND jurtaxdet.dispjurtype = '{0}'", dispjurtype);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Jurtaxdet> GetListBySrc(int cono, string srcrowpointer, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("jurtaxdet.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(srcrowpointer)) 
         {
             sb.AppendFormatWithEscape(" AND jurtaxdet.srcrowpointer = '{0}'", srcrowpointer);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Jurtaxdet BuildFromRow(DataRow row)
      {
         var returnRecord = Jurtaxdet.BuildJurtaxdetFromRow(row);
         returnRecord = this.BuildExtraFromRow<Jurtaxdet>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Jurtaxdet record)
      {
         Jurtaxdet.UpdateRowFromJurtaxdet(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Jurtaxdet Insert(Jurtaxdet record)
      {
         DataRow row = this.dataSet.ttbljurtaxdet.NewttbljurtaxdetRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbljurtaxdet.AddttbljurtaxdetRow((pdsjurtaxdetDataSet.ttbljurtaxdetRow)row);
         this.SaveChanges();
         return this.dataSet.ttbljurtaxdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbljurtaxdet.Rows[0]) : null;
      }
  

      public Jurtaxdet Update(Jurtaxdet record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbljurtaxdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbljurtaxdet.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Jurtaxdet record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbljurtaxdet.NewttbljurtaxdetRow();
            Jurtaxdet.BuildMinimalRow(ref row, record);
            this.dataSet.ttbljurtaxdet.AddttbljurtaxdetRow((pdsjurtaxdetDataSet.ttbljurtaxdetRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Jurtaxdet() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  