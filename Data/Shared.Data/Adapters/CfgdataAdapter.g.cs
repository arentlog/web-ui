//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscfgdata;

   public partial class CfgdataAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscfgdata";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cfgdataTableControlKey;
		
      private pdscfgdataDataSet dataSet;
        
      public CfgdataAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscfgdataDataSet() { DataSetName = DataSetName };
            this.cfgdataTableControlKey = this.dataSet.ttblcfgdata.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cfgdataTableControlKey))
            {
               this.CreateTableControlParameters(this.cfgdataTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CfgdataAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Cfgdataproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCfgdataproxy = this.proxyAppObject.CreatePO_cfgdataproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCfgdataproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Cfgdataproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cfgdataTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cfgdataTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Cfgdataproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCfgdataproxy = this.proxyAppObject.CreatePO_cfgdataproxy())
               {
                  this.SetRequiredContextParameters();
                  poCfgdataproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Cfgdataproxy - After Call");
      }
   

      public Cfgdata GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Cfgdata cfgdata = null;
         if (row != null)
         {
             cfgdata = this.BuildFromRow(row);
         }
         return cfgdata;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(cfgdata) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcfgdata.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Cfgdata> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(cfgdata)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Cfgdata Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcfgdata.AsEnumerable().SingleOrDefault();
         Cfgdata cfgdata = null;
         if (row != null)
         {
             cfgdata = this.BuildFromRow(row);
         }
         return cfgdata;
      }
	  
	  
      public IEnumerable<Cfgdata> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("cfgdata.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Cfgdata> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcfgdata.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Cfgdata Get(int cono, string ordertype, int orderno, int ordersuf, int lineno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("cfgdata.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(ordertype)) 
         {
             sb.AppendFormatWithEscape(" AND cfgdata.ordertype = '{0}'", ordertype);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND cfgdata.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND cfgdata.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND cfgdata.lineno = {0}", lineno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND cfgdata.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Cfgdata BuildFromRow(DataRow row)
      {
         var returnRecord = Cfgdata.BuildCfgdataFromRow(row);
         returnRecord = this.BuildExtraFromRow<Cfgdata>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Cfgdata record)
      {
         Cfgdata.UpdateRowFromCfgdata(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Cfgdata Insert(Cfgdata record)
      {
         DataRow row = this.dataSet.ttblcfgdata.NewttblcfgdataRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcfgdata.AddttblcfgdataRow((pdscfgdataDataSet.ttblcfgdataRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcfgdata.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcfgdata.Rows[0]) : null;
      }
  

      public Cfgdata Update(Cfgdata record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcfgdata.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcfgdata.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Cfgdata record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcfgdata.NewttblcfgdataRow();
            Cfgdata.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcfgdata.AddttblcfgdataRow((pdscfgdataDataSet.ttblcfgdataRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Cfgdata() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  