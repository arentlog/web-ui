//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsesbwlstath;

   public partial class EsbwlstathAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsesbwlstath";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> esbwlstathTableControlKey;
		
      private pdsesbwlstathDataSet dataSet;
        
      public EsbwlstathAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsesbwlstathDataSet() { DataSetName = DataSetName };
            this.esbwlstathTableControlKey = this.dataSet.ttblesbwlstath.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.esbwlstathTableControlKey))
            {
               this.CreateTableControlParameters(this.esbwlstathTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EsbwlstathAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Esbwlstathproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEsbwlstathproxy = this.proxyAppObject.CreatePO_esbwlstathproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEsbwlstathproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Esbwlstathproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.esbwlstathTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.esbwlstathTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Esbwlstathproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEsbwlstathproxy = this.proxyAppObject.CreatePO_esbwlstathproxy())
               {
                  this.SetRequiredContextParameters();
                  poEsbwlstathproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Esbwlstathproxy - After Call");
      }
   

      public Esbwlstath GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Esbwlstath esbwlstath = null;
         if (row != null)
         {
             esbwlstath = this.BuildFromRow(row);
         }
         return esbwlstath;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(esbwlstath) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblesbwlstath.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Esbwlstath> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(esbwlstath)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Esbwlstath Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblesbwlstath.AsEnumerable().SingleOrDefault();
         Esbwlstath esbwlstath = null;
         if (row != null)
         {
             esbwlstath = this.BuildFromRow(row);
         }
         return esbwlstath;
      }
	  
	  
      public IEnumerable<Esbwlstath> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("esbwlstath.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Esbwlstath> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblesbwlstath.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Esbwlstath Get(int cono, string whse, string ordertype, int orderno, int ordersuf, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("esbwlstath.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstath.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(ordertype)) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstath.ordertype = '{0}'", ordertype);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstath.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstath.ordersuf = {0}", ordersuf);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND esbwlstath.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Esbwlstath BuildFromRow(DataRow row)
      {
         var returnRecord = Esbwlstath.BuildEsbwlstathFromRow(row);
         returnRecord = this.BuildExtraFromRow<Esbwlstath>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Esbwlstath record)
      {
         Esbwlstath.UpdateRowFromEsbwlstath(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Esbwlstath Insert(Esbwlstath record)
      {
         DataRow row = this.dataSet.ttblesbwlstath.NewttblesbwlstathRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblesbwlstath.AddttblesbwlstathRow((pdsesbwlstathDataSet.ttblesbwlstathRow)row);
         this.SaveChanges();
         return this.dataSet.ttblesbwlstath.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblesbwlstath.Rows[0]) : null;
      }
  

      public Esbwlstath Update(Esbwlstath record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblesbwlstath.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblesbwlstath.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Esbwlstath record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblesbwlstath.NewttblesbwlstathRow();
            Esbwlstath.BuildMinimalRow(ref row, record);
            this.dataSet.ttblesbwlstath.AddttblesbwlstathRow((pdsesbwlstathDataSet.ttblesbwlstathRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Esbwlstath() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  