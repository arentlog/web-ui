//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdseventTransSub;

   public partial class EventTransSubAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsevent_trans_sub";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> eventTransSubTableControlKey;
		
      private pdsevent_trans_subDataSet dataSet;
        
      public EventTransSubAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsevent_trans_subDataSet() { DataSetName = DataSetName };
            this.eventTransSubTableControlKey = this.dataSet.ttblevent_trans_sub.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.eventTransSubTableControlKey))
            {
               this.CreateTableControlParameters(this.eventTransSubTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EventTransSubAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Event_trans_subproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEvent_trans_subproxy = this.proxyAppObject.CreatePO_event_trans_subproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEvent_trans_subproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Event_trans_subproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.eventTransSubTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.eventTransSubTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Event_trans_subproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEvent_trans_subproxy = this.proxyAppObject.CreatePO_event_trans_subproxy())
               {
                  this.SetRequiredContextParameters();
                  poEvent_trans_subproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Event_trans_subproxy - After Call");
      }
   

      public EventTransSub GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         EventTransSub eventTransSub = null;
         if (row != null)
         {
             eventTransSub = this.BuildFromRow(row);
         }
         return eventTransSub;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(event_trans_sub) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblevent_trans_sub.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<EventTransSub> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(event_trans_sub)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected EventTransSub Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblevent_trans_sub.AsEnumerable().SingleOrDefault();
         EventTransSub eventTransSub = null;
         if (row != null)
         {
             eventTransSub = this.BuildFromRow(row);
         }
         return eventTransSub;
      }
	  
	  
      public IEnumerable<EventTransSub> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("event_trans_sub.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventTransSub> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblevent_trans_sub.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public EventTransSub Get(int cono, string eventname, DateTime? eventdt, string eventtm, decimal recordno, int subseqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("event_trans_sub.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_sub.eventname = '{0}'", eventname);
         }
         if (eventdt != null) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_sub.eventdt = '{0}'", eventdt);
         }
         if (!string.IsNullOrEmpty(eventtm)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_sub.eventtm = '{0}'", eventtm);
         }
         if (recordno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_sub.recordno = {0}", recordno);
         }
         if (subseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_sub.subseqno = {0}", subseqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public EventTransSub BuildFromRow(DataRow row)
      {
         var returnRecord = EventTransSub.BuildEventTransSubFromRow(row);
         returnRecord = this.BuildExtraFromRow<EventTransSub>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, EventTransSub record)
      {
         EventTransSub.UpdateRowFromEventTransSub(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public EventTransSub Insert(EventTransSub record)
      {
         DataRow row = this.dataSet.ttblevent_trans_sub.Newttblevent_trans_subRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblevent_trans_sub.Addttblevent_trans_subRow((pdsevent_trans_subDataSet.ttblevent_trans_subRow)row);
         this.SaveChanges();
         return this.dataSet.ttblevent_trans_sub.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_trans_sub.Rows[0]) : null;
      }
  

      public EventTransSub Update(EventTransSub record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblevent_trans_sub.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_trans_sub.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(EventTransSub record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblevent_trans_sub.Newttblevent_trans_subRow();
            EventTransSub.BuildMinimalRow(ref row, record);
            this.dataSet.ttblevent_trans_sub.Addttblevent_trans_subRow((pdsevent_trans_subDataSet.ttblevent_trans_subRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new EventTransSub() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  