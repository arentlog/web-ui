//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssfcorpgrp;

   public partial class SfcorpgrpAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssfcorpgrp";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sfcorpgrpTableControlKey;
		
      private pdssfcorpgrpDataSet dataSet;
        
      public SfcorpgrpAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssfcorpgrpDataSet() { DataSetName = DataSetName };
            this.sfcorpgrpTableControlKey = this.dataSet.ttblsfcorpgrp.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sfcorpgrpTableControlKey))
            {
               this.CreateTableControlParameters(this.sfcorpgrpTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SfcorpgrpAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sfcorpgrpproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSfcorpgrpproxy = this.proxyAppObject.CreatePO_sfcorpgrpproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSfcorpgrpproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sfcorpgrpproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sfcorpgrpTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sfcorpgrpTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sfcorpgrpproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSfcorpgrpproxy = this.proxyAppObject.CreatePO_sfcorpgrpproxy())
               {
                  this.SetRequiredContextParameters();
                  poSfcorpgrpproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sfcorpgrpproxy - After Call");
      }
   

      public Sfcorpgrp GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sfcorpgrp sfcorpgrp = null;
         if (row != null)
         {
             sfcorpgrp = this.BuildFromRow(row);
         }
         return sfcorpgrp;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sfcorpgrp) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsfcorpgrp.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sfcorpgrp> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sfcorpgrp)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sfcorpgrp Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsfcorpgrp.AsEnumerable().SingleOrDefault();
         Sfcorpgrp sfcorpgrp = null;
         if (row != null)
         {
             sfcorpgrp = this.BuildFromRow(row);
         }
         return sfcorpgrp;
      }
	  
	  
      public IEnumerable<Sfcorpgrp> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sfcorpgrp.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sfcorpgrp> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsfcorpgrp.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sfcorpgrp Get(int cono, string groupid, decimal custno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sfcorpgrp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(groupid)) 
         {
             sb.AppendFormatWithEscape(" AND sfcorpgrp.groupid = '{0}'", groupid);
         }
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sfcorpgrp.custno = {0}", custno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sfcorpgrp> GetListByCustno(int cono, decimal custno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sfcorpgrp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sfcorpgrp.custno = {0}", custno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sfcorpgrp BuildFromRow(DataRow row)
      {
         var returnRecord = Sfcorpgrp.BuildSfcorpgrpFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sfcorpgrp>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sfcorpgrp record)
      {
         Sfcorpgrp.UpdateRowFromSfcorpgrp(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sfcorpgrp Insert(Sfcorpgrp record)
      {
         DataRow row = this.dataSet.ttblsfcorpgrp.NewttblsfcorpgrpRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsfcorpgrp.AddttblsfcorpgrpRow((pdssfcorpgrpDataSet.ttblsfcorpgrpRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsfcorpgrp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsfcorpgrp.Rows[0]) : null;
      }
  

      public Sfcorpgrp Update(Sfcorpgrp record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsfcorpgrp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsfcorpgrp.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sfcorpgrp record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsfcorpgrp.NewttblsfcorpgrpRow();
            Sfcorpgrp.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsfcorpgrp.AddttblsfcorpgrpRow((pdssfcorpgrpDataSet.ttblsfcorpgrpRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sfcorpgrp() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  