//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsauthtrans;

   public partial class AuthtransAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsauthtrans";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> authtransTableControlKey;
		
      private pdsauthtransDataSet dataSet;
        
      public AuthtransAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsauthtransDataSet() { DataSetName = DataSetName };
            this.authtransTableControlKey = this.dataSet.ttblauthtrans.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.authtransTableControlKey))
            {
               this.CreateTableControlParameters(this.authtransTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in AuthtransAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Authtransproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poAuthtransproxy = this.proxyAppObject.CreatePO_authtransproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poAuthtransproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Authtransproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.authtransTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.authtransTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Authtransproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poAuthtransproxy = this.proxyAppObject.CreatePO_authtransproxy())
               {
                  this.SetRequiredContextParameters();
                  poAuthtransproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Authtransproxy - After Call");
      }
   

      public Authtrans GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Authtrans authtrans = null;
         if (row != null)
         {
             authtrans = this.BuildFromRow(row);
         }
         return authtrans;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(authtrans) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblauthtrans.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Authtrans> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(authtrans)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Authtrans Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblauthtrans.AsEnumerable().SingleOrDefault();
         Authtrans authtrans = null;
         if (row != null)
         {
             authtrans = this.BuildFromRow(row);
         }
         return authtrans;
      }
	  
	  
      public IEnumerable<Authtrans> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("authtrans.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Authtrans> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblauthtrans.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Authtrans Get(int cono, string oper2, DateTime? attemptdt, string attempttm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("authtrans.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.oper2 = '{0}'", oper2);
         }
         if (attemptdt != null) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.attemptdt = '{0}'", attemptdt);
         }
         if (!string.IsNullOrEmpty(attempttm)) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.attempttm = '{0}'", attempttm);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Authtrans> GetListByAuthpoints(int cono, string ourproc, string key1, string key2, string mode, string transtype, DateTime? attemptdt, string attempttm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("authtrans.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(ourproc)) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.ourproc = '{0}'", ourproc);
         }
         if (!string.IsNullOrEmpty(key1)) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.key1 = '{0}'", key1);
         }
         if (!string.IsNullOrEmpty(key2)) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.key2 = '{0}'", key2);
         }
         if (!string.IsNullOrEmpty(mode)) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.mode = '{0}'", mode);
         }
         if (!string.IsNullOrEmpty(transtype)) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.transtype = '{0}'", transtype);
         }
         if (attemptdt != null) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.attemptdt = '{0}'", attemptdt);
         }
         if (!string.IsNullOrEmpty(attempttm)) 
         {
             sb.AppendFormatWithEscape(" AND authtrans.attempttm = '{0}'", attempttm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Authtrans BuildFromRow(DataRow row)
      {
         var returnRecord = Authtrans.BuildAuthtransFromRow(row);
         returnRecord = this.BuildExtraFromRow<Authtrans>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Authtrans record)
      {
         Authtrans.UpdateRowFromAuthtrans(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Authtrans Insert(Authtrans record)
      {
         DataRow row = this.dataSet.ttblauthtrans.NewttblauthtransRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblauthtrans.AddttblauthtransRow((pdsauthtransDataSet.ttblauthtransRow)row);
         this.SaveChanges();
         return this.dataSet.ttblauthtrans.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblauthtrans.Rows[0]) : null;
      }
  

      public Authtrans Update(Authtrans record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblauthtrans.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblauthtrans.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Authtrans record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblauthtrans.NewttblauthtransRow();
            Authtrans.BuildMinimalRow(ref row, record);
            this.dataSet.ttblauthtrans.AddttblauthtransRow((pdsauthtransDataSet.ttblauthtransRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Authtrans() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  