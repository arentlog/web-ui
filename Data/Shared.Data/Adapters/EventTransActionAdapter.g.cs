//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdseventTransAction;

   public partial class EventTransActionAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsevent_trans_action";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> eventTransActionTableControlKey;
		
      private pdsevent_trans_actionDataSet dataSet;
        
      public EventTransActionAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsevent_trans_actionDataSet() { DataSetName = DataSetName };
            this.eventTransActionTableControlKey = this.dataSet.ttblevent_trans_action.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.eventTransActionTableControlKey))
            {
               this.CreateTableControlParameters(this.eventTransActionTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EventTransActionAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Event_trans_actionproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEvent_trans_actionproxy = this.proxyAppObject.CreatePO_event_trans_actionproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEvent_trans_actionproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Event_trans_actionproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.eventTransActionTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.eventTransActionTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Event_trans_actionproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEvent_trans_actionproxy = this.proxyAppObject.CreatePO_event_trans_actionproxy())
               {
                  this.SetRequiredContextParameters();
                  poEvent_trans_actionproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Event_trans_actionproxy - After Call");
      }
   

      public EventTransAction GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         EventTransAction eventTransAction = null;
         if (row != null)
         {
             eventTransAction = this.BuildFromRow(row);
         }
         return eventTransAction;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(event_trans_action) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblevent_trans_action.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<EventTransAction> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(event_trans_action)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected EventTransAction Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblevent_trans_action.AsEnumerable().SingleOrDefault();
         EventTransAction eventTransAction = null;
         if (row != null)
         {
             eventTransAction = this.BuildFromRow(row);
         }
         return eventTransAction;
      }
	  
	  
      public IEnumerable<EventTransAction> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("event_trans_action.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventTransAction> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblevent_trans_action.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public EventTransAction Get(int cono, string eventname, DateTime? eventdt, string eventtm, decimal recordno, int actionseqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("event_trans_action.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_action.eventname = '{0}'", eventname);
         }
         if (eventdt != null) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_action.eventdt = '{0}'", eventdt);
         }
         if (!string.IsNullOrEmpty(eventtm)) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_action.eventtm = '{0}'", eventtm);
         }
         if (recordno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_action.recordno = {0}", recordno);
         }
         if (actionseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_trans_action.actionseqno = {0}", actionseqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public EventTransAction BuildFromRow(DataRow row)
      {
         var returnRecord = EventTransAction.BuildEventTransActionFromRow(row);
         returnRecord = this.BuildExtraFromRow<EventTransAction>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, EventTransAction record)
      {
         EventTransAction.UpdateRowFromEventTransAction(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public EventTransAction Insert(EventTransAction record)
      {
         DataRow row = this.dataSet.ttblevent_trans_action.Newttblevent_trans_actionRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblevent_trans_action.Addttblevent_trans_actionRow((pdsevent_trans_actionDataSet.ttblevent_trans_actionRow)row);
         this.SaveChanges();
         return this.dataSet.ttblevent_trans_action.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_trans_action.Rows[0]) : null;
      }
  

      public EventTransAction Update(EventTransAction record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblevent_trans_action.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_trans_action.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(EventTransAction record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblevent_trans_action.Newttblevent_trans_actionRow();
            EventTransAction.BuildMinimalRow(ref row, record);
            this.dataSet.ttblevent_trans_action.Addttblevent_trans_actionRow((pdsevent_trans_actionDataSet.ttblevent_trans_actionRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new EventTransAction() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  