//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsactivities;

   public partial class ActivitiesAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsactivities";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> activitiesTableControlKey;
		
      private pdsactivitiesDataSet dataSet;
        
      public ActivitiesAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsactivitiesDataSet() { DataSetName = DataSetName };
            this.activitiesTableControlKey = this.dataSet.ttblactivities.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.activitiesTableControlKey))
            {
               this.CreateTableControlParameters(this.activitiesTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ActivitiesAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Activitiesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poActivitiesproxy = this.proxyAppObject.CreatePO_activitiesproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poActivitiesproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Activitiesproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.activitiesTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.activitiesTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Activitiesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poActivitiesproxy = this.proxyAppObject.CreatePO_activitiesproxy())
               {
                  this.SetRequiredContextParameters();
                  poActivitiesproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Activitiesproxy - After Call");
      }
   

      public Activities GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Activities activities = null;
         if (row != null)
         {
             activities = this.BuildFromRow(row);
         }
         return activities;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(activities) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblactivities.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Activities> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(activities)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Activities Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblactivities.AsEnumerable().SingleOrDefault();
         Activities activities = null;
         if (row != null)
         {
             activities = this.BuildFromRow(row);
         }
         return activities;
      }
	  
	  
      public IEnumerable<Activities> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("activities.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Activities> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblactivities.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Activities Get(int cono, decimal activityid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("activities.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (activityid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.activityid = {0}", activityid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Activities> GetListByContact(int cono, decimal contactid, string statuscd, DateTime? schstartdt, string schstarttm, decimal activityid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("activities.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (contactid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.contactid = {0}", contactid);
         }
         if (!string.IsNullOrEmpty(statuscd)) 
         {
             sb.AppendFormatWithEscape(" AND activities.statuscd = '{0}'", statuscd);
         }
         if (schstartdt != null) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstartdt = '{0}'", schstartdt);
         }
         if (!string.IsNullOrEmpty(schstarttm)) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstarttm = '{0}'", schstarttm);
         }
         if (activityid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.activityid = {0}", activityid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Activities> GetListByDocjobid(int cono, string doctype, string docjobid, int docjobrevno, string statuscd, DateTime? schstartdt, string schstarttm, decimal activityid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("activities.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(doctype)) 
         {
             sb.AppendFormatWithEscape(" AND activities.doctype = '{0}'", doctype);
         }
         if (!string.IsNullOrEmpty(docjobid)) 
         {
             sb.AppendFormatWithEscape(" AND activities.docjobid = '{0}'", docjobid);
         }
         if (docjobrevno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.docjobrevno = {0}", docjobrevno);
         }
         if (!string.IsNullOrEmpty(statuscd)) 
         {
             sb.AppendFormatWithEscape(" AND activities.statuscd = '{0}'", statuscd);
         }
         if (schstartdt != null) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstartdt = '{0}'", schstartdt);
         }
         if (!string.IsNullOrEmpty(schstarttm)) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstarttm = '{0}'", schstarttm);
         }
         if (activityid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.activityid = {0}", activityid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Activities> GetListByDocument(int cono, string doctype, int docorderno, int docordersuf, string statuscd, DateTime? schstartdt, string schstarttm, decimal activityid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("activities.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(doctype)) 
         {
             sb.AppendFormatWithEscape(" AND activities.doctype = '{0}'", doctype);
         }
         if (docorderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.docorderno = {0}", docorderno);
         }
         if (docordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.docordersuf = {0}", docordersuf);
         }
         if (!string.IsNullOrEmpty(statuscd)) 
         {
             sb.AppendFormatWithEscape(" AND activities.statuscd = '{0}'", statuscd);
         }
         if (schstartdt != null) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstartdt = '{0}'", schstartdt);
         }
         if (!string.IsNullOrEmpty(schstarttm)) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstarttm = '{0}'", schstarttm);
         }
         if (activityid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.activityid = {0}", activityid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Activities> GetListByGroup(int cono, string groupnm, int groupseqno, string oper2, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("activities.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(groupnm)) 
         {
             sb.AppendFormatWithEscape(" AND activities.groupnm = '{0}'", groupnm);
         }
         if (groupseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.groupseqno = {0}", groupseqno);
         }
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND activities.oper2 = '{0}'", oper2);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Activities> GetListByOperator(int cono, string oper2, string statuscd, DateTime? schstartdt, string schstarttm, decimal activityid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("activities.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND activities.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(statuscd)) 
         {
             sb.AppendFormatWithEscape(" AND activities.statuscd = '{0}'", statuscd);
         }
         if (schstartdt != null) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstartdt = '{0}'", schstartdt);
         }
         if (!string.IsNullOrEmpty(schstarttm)) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstarttm = '{0}'", schstarttm);
         }
         if (activityid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.activityid = {0}", activityid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Activities> GetListByParent(int cono, decimal parentactvid, string statuscd, DateTime? schstartdt, string schstarttm, decimal activityid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("activities.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (parentactvid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.parentactvid = {0}", parentactvid);
         }
         if (!string.IsNullOrEmpty(statuscd)) 
         {
             sb.AppendFormatWithEscape(" AND activities.statuscd = '{0}'", statuscd);
         }
         if (schstartdt != null) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstartdt = '{0}'", schstartdt);
         }
         if (!string.IsNullOrEmpty(schstarttm)) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstarttm = '{0}'", schstarttm);
         }
         if (activityid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.activityid = {0}", activityid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Activities> GetListByPrimary(int cono, string subjectprimarykey, string subjecttype, string statuscd, DateTime? schstartdt, string schstarttm, decimal activityid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("activities.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(subjectprimarykey)) 
         {
             sb.AppendFormatWithEscape(" AND activities.subjectprimarykey = '{0}'", subjectprimarykey);
         }
         if (!string.IsNullOrEmpty(subjecttype)) 
         {
             sb.AppendFormatWithEscape(" AND activities.subjecttype = '{0}'", subjecttype);
         }
         if (!string.IsNullOrEmpty(statuscd)) 
         {
             sb.AppendFormatWithEscape(" AND activities.statuscd = '{0}'", statuscd);
         }
         if (schstartdt != null) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstartdt = '{0}'", schstartdt);
         }
         if (!string.IsNullOrEmpty(schstarttm)) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstarttm = '{0}'", schstarttm);
         }
         if (activityid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.activityid = {0}", activityid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Activities> GetListBySubject(int cono, string subjecttype, string subjectprimarykey, string subjectsecondkey, string statuscd, DateTime? schstartdt, string schstarttm, decimal activityid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("activities.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(subjecttype)) 
         {
             sb.AppendFormatWithEscape(" AND activities.subjecttype = '{0}'", subjecttype);
         }
         if (!string.IsNullOrEmpty(subjectprimarykey)) 
         {
             sb.AppendFormatWithEscape(" AND activities.subjectprimarykey = '{0}'", subjectprimarykey);
         }
         if (!string.IsNullOrEmpty(subjectsecondkey)) 
         {
             sb.AppendFormatWithEscape(" AND activities.subjectsecondkey = '{0}'", subjectsecondkey);
         }
         if (!string.IsNullOrEmpty(statuscd)) 
         {
             sb.AppendFormatWithEscape(" AND activities.statuscd = '{0}'", statuscd);
         }
         if (schstartdt != null) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstartdt = '{0}'", schstartdt);
         }
         if (!string.IsNullOrEmpty(schstarttm)) 
         {
             sb.AppendFormatWithEscape(" AND activities.schstarttm = '{0}'", schstarttm);
         }
         if (activityid != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND activities.activityid = {0}", activityid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Activities BuildFromRow(DataRow row)
      {
         var returnRecord = Activities.BuildActivitiesFromRow(row);
         returnRecord = this.BuildExtraFromRow<Activities>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Activities record)
      {
         Activities.UpdateRowFromActivities(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Activities Insert(Activities record)
      {
         DataRow row = this.dataSet.ttblactivities.NewttblactivitiesRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblactivities.AddttblactivitiesRow((pdsactivitiesDataSet.ttblactivitiesRow)row);
         this.SaveChanges();
         return this.dataSet.ttblactivities.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblactivities.Rows[0]) : null;
      }
  

      public Activities Update(Activities record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblactivities.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblactivities.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Activities record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblactivities.NewttblactivitiesRow();
            Activities.BuildMinimalRow(ref row, record);
            this.dataSet.ttblactivities.AddttblactivitiesRow((pdsactivitiesDataSet.ttblactivitiesRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Activities() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  