//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsesbnoun;

   public partial class EsbnounAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsesbnoun";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> esbnounTableControlKey;
		
      private pdsesbnounDataSet dataSet;
        
      public EsbnounAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsesbnounDataSet() { DataSetName = DataSetName };
            this.esbnounTableControlKey = this.dataSet.ttblesbnoun.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.esbnounTableControlKey))
            {
               this.CreateTableControlParameters(this.esbnounTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EsbnounAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Esbnounproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEsbnounproxy = this.proxyAppObject.CreatePO_esbnounproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEsbnounproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Esbnounproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.esbnounTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.esbnounTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Esbnounproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEsbnounproxy = this.proxyAppObject.CreatePO_esbnounproxy())
               {
                  this.SetRequiredContextParameters();
                  poEsbnounproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Esbnounproxy - After Call");
      }
   

      public Esbnoun GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Esbnoun esbnoun = null;
         if (row != null)
         {
             esbnoun = this.BuildFromRow(row);
         }
         return esbnoun;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(esbnoun) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblesbnoun.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Esbnoun> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(esbnoun)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Esbnoun Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblesbnoun.AsEnumerable().SingleOrDefault();
         Esbnoun esbnoun = null;
         if (row != null)
         {
             esbnoun = this.BuildFromRow(row);
         }
         return esbnoun;
      }
	  
	  
      public IEnumerable<Esbnoun> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("esbnoun.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Esbnoun> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblesbnoun.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Esbnoun Get(int cono, string noun, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("esbnoun.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(noun)) 
         {
             sb.AppendFormatWithEscape(" AND esbnoun.noun = '{0}'", noun);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Esbnoun BuildFromRow(DataRow row)
      {
         var returnRecord = Esbnoun.BuildEsbnounFromRow(row);
         returnRecord = this.BuildExtraFromRow<Esbnoun>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Esbnoun record)
      {
         Esbnoun.UpdateRowFromEsbnoun(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Esbnoun Insert(Esbnoun record)
      {
         DataRow row = this.dataSet.ttblesbnoun.NewttblesbnounRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblesbnoun.AddttblesbnounRow((pdsesbnounDataSet.ttblesbnounRow)row);
         this.SaveChanges();
         return this.dataSet.ttblesbnoun.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblesbnoun.Rows[0]) : null;
      }
  

      public Esbnoun Update(Esbnoun record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblesbnoun.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblesbnoun.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Esbnoun record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblesbnoun.NewttblesbnounRow();
            Esbnoun.BuildMinimalRow(ref row, record);
            this.dataSet.ttblesbnoun.AddttblesbnounRow((pdsesbnounDataSet.ttblesbnounRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Esbnoun() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  