//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdseventSetupAction;

   public partial class EventSetupActionAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsevent_setup_action";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> eventSetupActionTableControlKey;
		
      private pdsevent_setup_actionDataSet dataSet;
        
      public EventSetupActionAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsevent_setup_actionDataSet() { DataSetName = DataSetName };
            this.eventSetupActionTableControlKey = this.dataSet.ttblevent_setup_action.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.eventSetupActionTableControlKey))
            {
               this.CreateTableControlParameters(this.eventSetupActionTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EventSetupActionAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Event_setup_actionproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEvent_setup_actionproxy = this.proxyAppObject.CreatePO_event_setup_actionproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEvent_setup_actionproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Event_setup_actionproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.eventSetupActionTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.eventSetupActionTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Event_setup_actionproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEvent_setup_actionproxy = this.proxyAppObject.CreatePO_event_setup_actionproxy())
               {
                  this.SetRequiredContextParameters();
                  poEvent_setup_actionproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Event_setup_actionproxy - After Call");
      }
   

      public EventSetupAction GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         EventSetupAction eventSetupAction = null;
         if (row != null)
         {
             eventSetupAction = this.BuildFromRow(row);
         }
         return eventSetupAction;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(event_setup_action) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblevent_setup_action.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<EventSetupAction> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(event_setup_action)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected EventSetupAction Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblevent_setup_action.AsEnumerable().SingleOrDefault();
         EventSetupAction eventSetupAction = null;
         if (row != null)
         {
             eventSetupAction = this.BuildFromRow(row);
         }
         return eventSetupAction;
      }
	  
	  
      public IEnumerable<EventSetupAction> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("event_setup_action.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<EventSetupAction> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblevent_setup_action.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public EventSetupAction Get(int cono, string eventname, int actionseqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("event_setup_action.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape(" AND event_setup_action.eventname = '{0}'", eventname);
         }
         if (actionseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND event_setup_action.actionseqno = {0}", actionseqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<EventSetupAction> GetListByEvent(string eventname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(eventname)) 
         {
             sb.AppendFormatWithEscape("event_setup_action.eventname = '{0}'", eventname);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public EventSetupAction BuildFromRow(DataRow row)
      {
         var returnRecord = EventSetupAction.BuildEventSetupActionFromRow(row);
         returnRecord = this.BuildExtraFromRow<EventSetupAction>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, EventSetupAction record)
      {
         EventSetupAction.UpdateRowFromEventSetupAction(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public EventSetupAction Insert(EventSetupAction record)
      {
         DataRow row = this.dataSet.ttblevent_setup_action.Newttblevent_setup_actionRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblevent_setup_action.Addttblevent_setup_actionRow((pdsevent_setup_actionDataSet.ttblevent_setup_actionRow)row);
         this.SaveChanges();
         return this.dataSet.ttblevent_setup_action.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_setup_action.Rows[0]) : null;
      }
  

      public EventSetupAction Update(EventSetupAction record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblevent_setup_action.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblevent_setup_action.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(EventSetupAction record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblevent_setup_action.Newttblevent_setup_actionRow();
            EventSetupAction.BuildMinimalRow(ref row, record);
            this.dataSet.ttblevent_setup_action.Addttblevent_setup_actionRow((pdsevent_setup_actionDataSet.ttblevent_setup_actionRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new EventSetupAction() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  