//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsrecordsync;

   public partial class RecordsyncAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsrecordsync";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> recordsyncTableControlKey;
		
      private pdsrecordsyncDataSet dataSet;
        
      public RecordsyncAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsrecordsyncDataSet() { DataSetName = DataSetName };
            this.recordsyncTableControlKey = this.dataSet.ttblrecordsync.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.recordsyncTableControlKey))
            {
               this.CreateTableControlParameters(this.recordsyncTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in RecordsyncAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Recordsyncproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poRecordsyncproxy = this.proxyAppObject.CreatePO_recordsyncproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poRecordsyncproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Recordsyncproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.recordsyncTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.recordsyncTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Recordsyncproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRecordsyncproxy = this.proxyAppObject.CreatePO_recordsyncproxy())
               {
                  this.SetRequiredContextParameters();
                  poRecordsyncproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Recordsyncproxy - After Call");
      }
   

      public Recordsync GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Recordsync recordsync = null;
         if (row != null)
         {
             recordsync = this.BuildFromRow(row);
         }
         return recordsync;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(recordsync) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblrecordsync.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Recordsync> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(recordsync)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Recordsync Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblrecordsync.AsEnumerable().SingleOrDefault();
         Recordsync recordsync = null;
         if (row != null)
         {
             recordsync = this.BuildFromRow(row);
         }
         return recordsync;
      }
	  
	  
      public IEnumerable<Recordsync> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("recordsync.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Recordsync> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblrecordsync.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Recordsync Get(int cono, string synctype, string tablenm, string key1, string key2, string key3, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("recordsync.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(synctype)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.synctype = '{0}'", synctype);
         }
         if (!string.IsNullOrEmpty(tablenm)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.tablenm = '{0}'", tablenm);
         }
         if (!string.IsNullOrEmpty(key1)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.key1 = '{0}'", key1);
         }
         if (!string.IsNullOrEmpty(key2)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.key2 = '{0}'", key2);
         }
         if (!string.IsNullOrEmpty(key3)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.key3 = '{0}'", key3);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Recordsync> GetListByStatustype(int cono, string statustype, string synctype, string tablenm, string updatetype, DateTime? updatedt, string updatetm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("recordsync.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(statustype)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.statustype = '{0}'", statustype);
         }
         if (!string.IsNullOrEmpty(synctype)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.synctype = '{0}'", synctype);
         }
         if (!string.IsNullOrEmpty(tablenm)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.tablenm = '{0}'", tablenm);
         }
         if (!string.IsNullOrEmpty(updatetype)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.updatetype = '{0}'", updatetype);
         }
         if (updatedt != null) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.updatedt = '{0}'", updatedt);
         }
         if (!string.IsNullOrEmpty(updatetm)) 
         {
             sb.AppendFormatWithEscape(" AND recordsync.updatetm = '{0}'", updatetm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Recordsync BuildFromRow(DataRow row)
      {
         var returnRecord = Recordsync.BuildRecordsyncFromRow(row);
         returnRecord = this.BuildExtraFromRow<Recordsync>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Recordsync record)
      {
         Recordsync.UpdateRowFromRecordsync(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Recordsync Insert(Recordsync record)
      {
         DataRow row = this.dataSet.ttblrecordsync.NewttblrecordsyncRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblrecordsync.AddttblrecordsyncRow((pdsrecordsyncDataSet.ttblrecordsyncRow)row);
         this.SaveChanges();
         return this.dataSet.ttblrecordsync.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrecordsync.Rows[0]) : null;
      }
  

      public Recordsync Update(Recordsync record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblrecordsync.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrecordsync.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Recordsync record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblrecordsync.NewttblrecordsyncRow();
            Recordsync.BuildMinimalRow(ref row, record);
            this.dataSet.ttblrecordsync.AddttblrecordsyncRow((pdsrecordsyncDataSet.ttblrecordsyncRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Recordsync() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  