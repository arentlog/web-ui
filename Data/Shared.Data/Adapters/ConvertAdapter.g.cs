//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsconvert;

   public partial class ConvertAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsconvert";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> convertTableControlKey;
		
      private pdsconvertDataSet dataSet;
        
      public ConvertAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsconvertDataSet() { DataSetName = DataSetName };
            this.convertTableControlKey = this.dataSet.ttblconvert.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.convertTableControlKey))
            {
               this.CreateTableControlParameters(this.convertTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ConvertAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Convertproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poConvertproxy = this.proxyAppObject.CreatePO_convertproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poConvertproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Convertproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.convertTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.convertTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Convertproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poConvertproxy = this.proxyAppObject.CreatePO_convertproxy())
               {
                  this.SetRequiredContextParameters();
                  poConvertproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Convertproxy - After Call");
      }
   

      public Convert GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Convert convert = null;
         if (row != null)
         {
             convert = this.BuildFromRow(row);
         }
         return convert;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(convert) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblconvert.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Convert> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(convert)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Convert Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblconvert.AsEnumerable().SingleOrDefault();
         Convert convert = null;
         if (row != null)
         {
             convert = this.BuildFromRow(row);
         }
         return convert;
      }
	  
	  
      public IEnumerable<Convert> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("convert.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Convert> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblconvert.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Convert Get(int cono, string oper2, string reportnm, string c24, string c2, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("convert.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND convert.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND convert.reportnm = '{0}'", reportnm);
         }
         if (!string.IsNullOrEmpty(c24)) 
         {
             sb.AppendFormatWithEscape(" AND convert.c24 = '{0}'", c24);
         }
         if (!string.IsNullOrEmpty(c2)) 
         {
             sb.AppendFormatWithEscape(" AND convert.c2 = '{0}'", c2);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Convert> GetListByC20(int cono, string oper2, string reportnm, string c20, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("convert.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND convert.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND convert.reportnm = '{0}'", reportnm);
         }
         if (!string.IsNullOrEmpty(c20)) 
         {
             sb.AppendFormatWithEscape(" AND convert.c20 = '{0}'", c20);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Convert> GetListByCust(int cono, string oper2, string reportnm, string outputty, decimal de12d0, string c12, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("convert.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND convert.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND convert.reportnm = '{0}'", reportnm);
         }
         if (!string.IsNullOrEmpty(outputty)) 
         {
             sb.AppendFormatWithEscape(" AND convert.outputty = '{0}'", outputty);
         }
         if (de12d0 != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND convert.de12d0 = {0}", de12d0);
         }
         if (!string.IsNullOrEmpty(c12)) 
         {
             sb.AppendFormatWithEscape(" AND convert.c12 = '{0}'", c12);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Convert BuildFromRow(DataRow row)
      {
         var returnRecord = Convert.BuildConvertFromRow(row);
         returnRecord = this.BuildExtraFromRow<Convert>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Convert record)
      {
         Convert.UpdateRowFromConvert(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Convert Insert(Convert record)
      {
         DataRow row = this.dataSet.ttblconvert.NewttblconvertRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblconvert.AddttblconvertRow((pdsconvertDataSet.ttblconvertRow)row);
         this.SaveChanges();
         return this.dataSet.ttblconvert.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblconvert.Rows[0]) : null;
      }
  

      public Convert Update(Convert record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblconvert.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblconvert.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Convert record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblconvert.NewttblconvertRow();
            Convert.BuildMinimalRow(ref row, record);
            this.dataSet.ttblconvert.AddttblconvertRow((pdsconvertDataSet.ttblconvertRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Convert() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  