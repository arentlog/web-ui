//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsdealph;

   public partial class DealphAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsdealph";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> dealphTableControlKey;
		
      private pdsdealphDataSet dataSet;
        
      public DealphAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsdealphDataSet() { DataSetName = DataSetName };
            this.dealphTableControlKey = this.dataSet.ttbldealph.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.dealphTableControlKey))
            {
               this.CreateTableControlParameters(this.dealphTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in DealphAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Dealphproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poDealphproxy = this.proxyAppObject.CreatePO_dealphproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poDealphproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Dealphproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.dealphTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.dealphTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Dealphproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poDealphproxy = this.proxyAppObject.CreatePO_dealphproxy())
               {
                  this.SetRequiredContextParameters();
                  poDealphproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Dealphproxy - After Call");
      }
   

      public Dealph GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Dealph dealph = null;
         if (row != null)
         {
             dealph = this.BuildFromRow(row);
         }
         return dealph;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(dealph) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbldealph.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Dealph> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(dealph)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Dealph Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbldealph.AsEnumerable().SingleOrDefault();
         Dealph dealph = null;
         if (row != null)
         {
             dealph = this.BuildFromRow(row);
         }
         return dealph;
      }
	  
	  
      public IEnumerable<Dealph> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("dealph.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Dealph> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbldealph.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Dealph Get(int cono, string qualtype, string qualentity, string whse, string pricetype, DateTime? startdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("dealph.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(qualtype)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.qualtype = '{0}'", qualtype);
         }
         if (!string.IsNullOrEmpty(qualentity)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.qualentity = '{0}'", qualentity);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(pricetype)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.pricetype = '{0}'", pricetype);
         }
         if (startdt != null) 
         {
             sb.AppendFormatWithEscape(" AND dealph.startdt = '{0}'", startdt);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Dealph> GetListByDealrecno(int cono, int dealrecno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("dealph.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (dealrecno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND dealph.dealrecno = {0}", dealrecno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Dealph> GetListByStartdt(int cono, DateTime? startdt, string qualtype, string qualentity, string whse, string pricetype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("dealph.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (startdt != null) 
         {
             sb.AppendFormatWithEscape(" AND dealph.startdt = '{0}'", startdt);
         }
         if (!string.IsNullOrEmpty(qualtype)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.qualtype = '{0}'", qualtype);
         }
         if (!string.IsNullOrEmpty(qualentity)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.qualentity = '{0}'", qualentity);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(pricetype)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.pricetype = '{0}'", pricetype);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Dealph> GetListByWhse(int cono, string whse, string qualtype, string qualentity, string pricetype, DateTime? startdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("dealph.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(qualtype)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.qualtype = '{0}'", qualtype);
         }
         if (!string.IsNullOrEmpty(qualentity)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.qualentity = '{0}'", qualentity);
         }
         if (!string.IsNullOrEmpty(pricetype)) 
         {
             sb.AppendFormatWithEscape(" AND dealph.pricetype = '{0}'", pricetype);
         }
         if (startdt != null) 
         {
             sb.AppendFormatWithEscape(" AND dealph.startdt = '{0}'", startdt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Dealph BuildFromRow(DataRow row)
      {
         var returnRecord = Dealph.BuildDealphFromRow(row);
         returnRecord = this.BuildExtraFromRow<Dealph>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Dealph record)
      {
         Dealph.UpdateRowFromDealph(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Dealph Insert(Dealph record)
      {
         DataRow row = this.dataSet.ttbldealph.NewttbldealphRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbldealph.AddttbldealphRow((pdsdealphDataSet.ttbldealphRow)row);
         this.SaveChanges();
         return this.dataSet.ttbldealph.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldealph.Rows[0]) : null;
      }
  

      public Dealph Update(Dealph record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbldealph.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldealph.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Dealph record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbldealph.NewttbldealphRow();
            Dealph.BuildMinimalRow(ref row, record);
            this.dataSet.ttbldealph.AddttbldealphRow((pdsdealphDataSet.ttbldealphRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Dealph() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  