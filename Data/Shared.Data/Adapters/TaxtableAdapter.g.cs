//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdstaxtable;

   public partial class TaxtableAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdstaxtable";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> taxtableTableControlKey;
		
      private pdstaxtableDataSet dataSet;
        
      public TaxtableAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdstaxtableDataSet() { DataSetName = DataSetName };
            this.taxtableTableControlKey = this.dataSet.ttbltaxtable.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.taxtableTableControlKey))
            {
               this.CreateTableControlParameters(this.taxtableTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in TaxtableAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Taxtableproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poTaxtableproxy = this.proxyAppObject.CreatePO_taxtableproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poTaxtableproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Taxtableproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.taxtableTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.taxtableTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Taxtableproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poTaxtableproxy = this.proxyAppObject.CreatePO_taxtableproxy())
               {
                  this.SetRequiredContextParameters();
                  poTaxtableproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Taxtableproxy - After Call");
      }
   

      public Taxtable GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Taxtable taxtable = null;
         if (row != null)
         {
             taxtable = this.BuildFromRow(row);
         }
         return taxtable;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(taxtable) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltaxtable.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Taxtable> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(taxtable)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Taxtable Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbltaxtable.AsEnumerable().SingleOrDefault();
         Taxtable taxtable = null;
         if (row != null)
         {
             taxtable = this.BuildFromRow(row);
         }
         return taxtable;
      }
	  
	  
	  public IEnumerable<Taxtable> GetListByRowpointers(List<string> rowpointers, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("taxtable.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("taxtable.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltaxtable.AsEnumerable().SingleOrDefault();
      }
      public IEnumerable<Taxtable> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("taxtable.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Taxtable> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbltaxtable.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Taxtable Get(int cono, string sourcecd, string srcrowpointer, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("taxtable.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(sourcecd)) 
         {
             sb.AppendFormatWithEscape(" AND taxtable.sourcecd = '{0}'", sourcecd);
         }
         if (!string.IsNullOrEmpty(srcrowpointer)) 
         {
             sb.AppendFormatWithEscape(" AND taxtable.srcrowpointer = '{0}'", srcrowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Taxtable> GetListByInvoicedt(int cono, DateTime? invoicedt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("taxtable.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (invoicedt != null) 
         {
             sb.AppendFormatWithEscape(" AND taxtable.invoicedt = '{0}'", invoicedt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Taxtable> GetListByPaiddt(int cono, DateTime? paiddt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("taxtable.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (paiddt != null) 
         {
             sb.AppendFormatWithEscape(" AND taxtable.paiddt = '{0}'", paiddt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Taxtable> GetListByPeriod(int cono, string periodid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("taxtable.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(periodid)) 
         {
             sb.AppendFormatWithEscape(" AND taxtable.periodid = '{0}'", periodid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Taxtable> GetListByReturndt(int cono, DateTime? returndt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("taxtable.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (returndt != null) 
         {
             sb.AppendFormatWithEscape(" AND taxtable.returndt = '{0}'", returndt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Taxtable GetByRowpointer(string rowpointer, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("taxtable.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, 1, fldList);
      }

      public Taxtable BuildFromRow(DataRow row)
      {
         var returnRecord = Taxtable.BuildTaxtableFromRow(row);
         returnRecord = this.BuildExtraFromRow<Taxtable>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Taxtable record)
      {
         Taxtable.UpdateRowFromTaxtable(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Taxtable Insert(Taxtable record)
      {
         DataRow row = this.dataSet.ttbltaxtable.NewttbltaxtableRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbltaxtable.AddttbltaxtableRow((pdstaxtableDataSet.ttbltaxtableRow)row);
         this.SaveChanges();
         return this.dataSet.ttbltaxtable.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltaxtable.Rows[0]) : null;
      }
  

      public Taxtable Update(Taxtable record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbltaxtable.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltaxtable.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Taxtable record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbltaxtable.NewttbltaxtableRow();
            Taxtable.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltaxtable.AddttbltaxtableRow((pdstaxtableDataSet.ttbltaxtableRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Taxtable record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttbltaxtable.NewttbltaxtableRow();
            Taxtable.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltaxtable.AddttbltaxtableRow((pdstaxtableDataSet.ttbltaxtableRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Taxtable() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Taxtable() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  