//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdstriggerSetup;

   public partial class TriggerSetupAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdstrigger_setup";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> triggerSetupTableControlKey;
		
      private pdstrigger_setupDataSet dataSet;
        
      public TriggerSetupAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdstrigger_setupDataSet() { DataSetName = DataSetName };
            this.triggerSetupTableControlKey = this.dataSet.ttbltrigger_setup.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.triggerSetupTableControlKey))
            {
               this.CreateTableControlParameters(this.triggerSetupTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in TriggerSetupAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Trigger_setupproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poTrigger_setupproxy = this.proxyAppObject.CreatePO_trigger_setupproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poTrigger_setupproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Trigger_setupproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.triggerSetupTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.triggerSetupTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Trigger_setupproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poTrigger_setupproxy = this.proxyAppObject.CreatePO_trigger_setupproxy())
               {
                  this.SetRequiredContextParameters();
                  poTrigger_setupproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Trigger_setupproxy - After Call");
      }
   

      public TriggerSetup GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         TriggerSetup triggerSetup = null;
         if (row != null)
         {
             triggerSetup = this.BuildFromRow(row);
         }
         return triggerSetup;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(trigger_setup) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltrigger_setup.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<TriggerSetup> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(trigger_setup)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected TriggerSetup Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbltrigger_setup.AsEnumerable().SingleOrDefault();
         TriggerSetup triggerSetup = null;
         if (row != null)
         {
             triggerSetup = this.BuildFromRow(row);
         }
         return triggerSetup;
      }
	  
	  

      public IEnumerable<TriggerSetup> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbltrigger_setup.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public TriggerSetup Get(string triggername, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(triggername)) 
         {
             sb.AppendFormatWithEscape("trigger_setup.triggername = '{0}'", triggername);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public TriggerSetup BuildFromRow(DataRow row)
      {
         var returnRecord = TriggerSetup.BuildTriggerSetupFromRow(row);
         returnRecord = this.BuildExtraFromRow<TriggerSetup>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, TriggerSetup record)
      {
         TriggerSetup.UpdateRowFromTriggerSetup(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public TriggerSetup Insert(TriggerSetup record)
      {
         DataRow row = this.dataSet.ttbltrigger_setup.Newttbltrigger_setupRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbltrigger_setup.Addttbltrigger_setupRow((pdstrigger_setupDataSet.ttbltrigger_setupRow)row);
         this.SaveChanges();
         return this.dataSet.ttbltrigger_setup.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltrigger_setup.Rows[0]) : null;
      }
  

      public TriggerSetup Update(TriggerSetup record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbltrigger_setup.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltrigger_setup.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(TriggerSetup record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbltrigger_setup.Newttbltrigger_setupRow();
            TriggerSetup.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltrigger_setup.Addttbltrigger_setupRow((pdstrigger_setupDataSet.ttbltrigger_setupRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new TriggerSetup() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  