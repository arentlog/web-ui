//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdstaxtabledtl;

   public partial class TaxtabledtlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdstaxtabledtl";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> taxtabledtlTableControlKey;
		
      private pdstaxtabledtlDataSet dataSet;
        
      public TaxtabledtlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdstaxtabledtlDataSet() { DataSetName = DataSetName };
            this.taxtabledtlTableControlKey = this.dataSet.ttbltaxtabledtl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.taxtabledtlTableControlKey))
            {
               this.CreateTableControlParameters(this.taxtabledtlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in TaxtabledtlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Taxtabledtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poTaxtabledtlproxy = this.proxyAppObject.CreatePO_taxtabledtlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poTaxtabledtlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Taxtabledtlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.taxtabledtlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.taxtabledtlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Taxtabledtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poTaxtabledtlproxy = this.proxyAppObject.CreatePO_taxtabledtlproxy())
               {
                  this.SetRequiredContextParameters();
                  poTaxtabledtlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Taxtabledtlproxy - After Call");
      }
   

      public Taxtabledtl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Taxtabledtl taxtabledtl = null;
         if (row != null)
         {
             taxtabledtl = this.BuildFromRow(row);
         }
         return taxtabledtl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(taxtabledtl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltaxtabledtl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Taxtabledtl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(taxtabledtl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Taxtabledtl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbltaxtabledtl.AsEnumerable().SingleOrDefault();
         Taxtabledtl taxtabledtl = null;
         if (row != null)
         {
             taxtabledtl = this.BuildFromRow(row);
         }
         return taxtabledtl;
      }
	  
	  
      public IEnumerable<Taxtabledtl> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("taxtabledtl.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Taxtabledtl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbltaxtabledtl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Taxtabledtl Get(string taxrowpointer, int taxgroupno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(taxrowpointer)) 
         {
             sb.AppendFormatWithEscape("taxtabledtl.taxrowpointer = '{0}'", taxrowpointer);
         }
         if (taxgroupno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND taxtabledtl.taxgroupno = {0}", taxgroupno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Taxtabledtl> GetListByWriteoffdt(int cono, DateTime? writeoffdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("taxtabledtl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (writeoffdt != null) 
         {
             sb.AppendFormatWithEscape(" AND taxtabledtl.writeoffdt = '{0}'", writeoffdt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Taxtabledtl BuildFromRow(DataRow row)
      {
         var returnRecord = Taxtabledtl.BuildTaxtabledtlFromRow(row);
         returnRecord = this.BuildExtraFromRow<Taxtabledtl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Taxtabledtl record)
      {
         Taxtabledtl.UpdateRowFromTaxtabledtl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Taxtabledtl Insert(Taxtabledtl record)
      {
         DataRow row = this.dataSet.ttbltaxtabledtl.NewttbltaxtabledtlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbltaxtabledtl.AddttbltaxtabledtlRow((pdstaxtabledtlDataSet.ttbltaxtabledtlRow)row);
         this.SaveChanges();
         return this.dataSet.ttbltaxtabledtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltaxtabledtl.Rows[0]) : null;
      }
  

      public Taxtabledtl Update(Taxtabledtl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbltaxtabledtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltaxtabledtl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Taxtabledtl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbltaxtabledtl.NewttbltaxtabledtlRow();
            Taxtabledtl.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltaxtabledtl.AddttbltaxtabledtlRow((pdstaxtabledtlDataSet.ttbltaxtabledtlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Taxtabledtl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  