//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdstigeocd;

   public partial class TigeocdAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdstigeocd";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> tigeocdTableControlKey;
		
      private pdstigeocdDataSet dataSet;
        
      public TigeocdAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdstigeocdDataSet() { DataSetName = DataSetName };
            this.tigeocdTableControlKey = this.dataSet.ttbltigeocd.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.tigeocdTableControlKey))
            {
               this.CreateTableControlParameters(this.tigeocdTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in TigeocdAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Tigeocdproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poTigeocdproxy = this.proxyAppObject.CreatePO_tigeocdproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poTigeocdproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Tigeocdproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.tigeocdTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.tigeocdTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Tigeocdproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poTigeocdproxy = this.proxyAppObject.CreatePO_tigeocdproxy())
               {
                  this.SetRequiredContextParameters();
                  poTigeocdproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Tigeocdproxy - After Call");
      }
   

      public Tigeocd GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Tigeocd tigeocd = null;
         if (row != null)
         {
             tigeocd = this.BuildFromRow(row);
         }
         return tigeocd;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(tigeocd) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltigeocd.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Tigeocd> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(tigeocd)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Tigeocd Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbltigeocd.AsEnumerable().SingleOrDefault();
         Tigeocd tigeocd = null;
         if (row != null)
         {
             tigeocd = this.BuildFromRow(row);
         }
         return tigeocd;
      }
	  
	  
      public IEnumerable<Tigeocd> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("tigeocd.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Tigeocd> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbltigeocd.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Tigeocd Get(string srcrowpointer, int geocd, string city, string zipcd, string addr, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(srcrowpointer)) 
         {
             sb.AppendFormatWithEscape("tigeocd.srcrowpointer = '{0}'", srcrowpointer);
         }
         if (geocd != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND tigeocd.geocd = {0}", geocd);
         }
         if (!string.IsNullOrEmpty(city)) 
         {
             sb.AppendFormatWithEscape(" AND tigeocd.city = '{0}'", city);
         }
         if (!string.IsNullOrEmpty(zipcd)) 
         {
             sb.AppendFormatWithEscape(" AND tigeocd.zipcd = '{0}'", zipcd);
         }
         if (!string.IsNullOrEmpty(addr)) 
         {
             sb.AppendFormatWithEscape(" AND tigeocd.addr = '{0}'", addr);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Tigeocd> GetListByGeocd(int cono, int geocd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("tigeocd.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (geocd != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND tigeocd.geocd = {0}", geocd);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Tigeocd BuildFromRow(DataRow row)
      {
         var returnRecord = Tigeocd.BuildTigeocdFromRow(row);
         returnRecord = this.BuildExtraFromRow<Tigeocd>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Tigeocd record)
      {
         Tigeocd.UpdateRowFromTigeocd(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Tigeocd Insert(Tigeocd record)
      {
         DataRow row = this.dataSet.ttbltigeocd.NewttbltigeocdRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbltigeocd.AddttbltigeocdRow((pdstigeocdDataSet.ttbltigeocdRow)row);
         this.SaveChanges();
         return this.dataSet.ttbltigeocd.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltigeocd.Rows[0]) : null;
      }
  

      public Tigeocd Update(Tigeocd record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbltigeocd.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltigeocd.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Tigeocd record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbltigeocd.NewttbltigeocdRow();
            Tigeocd.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltigeocd.AddttbltigeocdRow((pdstigeocdDataSet.ttbltigeocdRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Tigeocd() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  