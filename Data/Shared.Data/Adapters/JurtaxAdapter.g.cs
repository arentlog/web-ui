//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsjurtax;

   public partial class JurtaxAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsjurtax";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> jurtaxTableControlKey;
		
      private pdsjurtaxDataSet dataSet;
        
      public JurtaxAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsjurtaxDataSet() { DataSetName = DataSetName };
            this.jurtaxTableControlKey = this.dataSet.ttbljurtax.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.jurtaxTableControlKey))
            {
               this.CreateTableControlParameters(this.jurtaxTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in JurtaxAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Jurtaxproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poJurtaxproxy = this.proxyAppObject.CreatePO_jurtaxproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poJurtaxproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Jurtaxproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.jurtaxTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.jurtaxTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Jurtaxproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poJurtaxproxy = this.proxyAppObject.CreatePO_jurtaxproxy())
               {
                  this.SetRequiredContextParameters();
                  poJurtaxproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Jurtaxproxy - After Call");
      }
   

      public Jurtax GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Jurtax jurtax = null;
         if (row != null)
         {
             jurtax = this.BuildFromRow(row);
         }
         return jurtax;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(jurtax) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbljurtax.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Jurtax> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(jurtax)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Jurtax Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbljurtax.AsEnumerable().SingleOrDefault();
         Jurtax jurtax = null;
         if (row != null)
         {
             jurtax = this.BuildFromRow(row);
         }
         return jurtax;
      }
	  
	  
	  public IEnumerable<Jurtax> GetListByRowpointers(List<string> rowpointers, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("jurtax.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("jurtax.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbljurtax.AsEnumerable().SingleOrDefault();
      }
      public IEnumerable<Jurtax> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("jurtax.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Jurtax> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbljurtax.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Jurtax Get(int cono, string srcrowpointer, string jurtype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("jurtax.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(srcrowpointer)) 
         {
             sb.AppendFormatWithEscape(" AND jurtax.srcrowpointer = '{0}'", srcrowpointer);
         }
         if (!string.IsNullOrEmpty(jurtype)) 
         {
             sb.AppendFormatWithEscape(" AND jurtax.jurtype = '{0}'", jurtype);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Jurtax> GetListByDocid(int cono, string docid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("jurtax.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(docid)) 
         {
             sb.AppendFormatWithEscape(" AND jurtax.docid = '{0}'", docid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Jurtax GetByRowpointer(string rowpointer, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("jurtax.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, 1, fldList);
      }

      public Jurtax BuildFromRow(DataRow row)
      {
         var returnRecord = Jurtax.BuildJurtaxFromRow(row);
         returnRecord = this.BuildExtraFromRow<Jurtax>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Jurtax record)
      {
         Jurtax.UpdateRowFromJurtax(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Jurtax Insert(Jurtax record)
      {
         DataRow row = this.dataSet.ttbljurtax.NewttbljurtaxRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbljurtax.AddttbljurtaxRow((pdsjurtaxDataSet.ttbljurtaxRow)row);
         this.SaveChanges();
         return this.dataSet.ttbljurtax.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbljurtax.Rows[0]) : null;
      }
  

      public Jurtax Update(Jurtax record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbljurtax.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbljurtax.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Jurtax record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbljurtax.NewttbljurtaxRow();
            Jurtax.BuildMinimalRow(ref row, record);
            this.dataSet.ttbljurtax.AddttbljurtaxRow((pdsjurtaxDataSet.ttbljurtaxRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Jurtax record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttbljurtax.NewttbljurtaxRow();
            Jurtax.BuildMinimalRow(ref row, record);
            this.dataSet.ttbljurtax.AddttbljurtaxRow((pdsjurtaxDataSet.ttbljurtaxRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Jurtax() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Jurtax() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  