//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.Shared.Data.Adapters
{
   using com.infor.sxproxy.sharedproxy;
   using com.infor.sxproxy.sharedproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdstopicMap;

   public partial class TopicMapAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdstopicMap";
      private SharedProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> topicMapTableControlKey;
		
      private pdstopicMapDataSet dataSet;
        
      public TopicMapAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SharedProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdstopicMapDataSet() { DataSetName = DataSetName };
            this.topicMapTableControlKey = this.dataSet.ttbltopicMap.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.topicMapTableControlKey))
            {
               this.CreateTableControlParameters(this.topicMapTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in TopicMapAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - TopicMapproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poTopicMapproxy = this.proxyAppObject.CreatePO_topicMapproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poTopicMapproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - TopicMapproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.topicMapTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.topicMapTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - TopicMapproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poTopicMapproxy = this.proxyAppObject.CreatePO_topicMapproxy())
               {
                  this.SetRequiredContextParameters();
                  poTopicMapproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - TopicMapproxy - After Call");
      }
   

      public TopicMap GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         TopicMap topicMap = null;
         if (row != null)
         {
             topicMap = this.BuildFromRow(row);
         }
         return topicMap;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(topic-map) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltopicMap.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<TopicMap> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(topic-map)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected TopicMap Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbltopicMap.AsEnumerable().SingleOrDefault();
         TopicMap topicMap = null;
         if (row != null)
         {
             topicMap = this.BuildFromRow(row);
         }
         return topicMap;
      }
	  
	  

      public IEnumerable<TopicMap> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbltopicMap.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public TopicMap Get(string objectFilename, string topicName, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(objectFilename)) 
         {
             sb.AppendFormatWithEscape("topic-map.Object-filename = '{0}'", objectFilename);
         }
         if (!string.IsNullOrEmpty(topicName)) 
         {
             sb.AppendFormatWithEscape(" AND topic-map.Topic-name = '{0}'", topicName);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<TopicMap> GetListByObject(string objectFilename, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(objectFilename)) 
         {
             sb.AppendFormatWithEscape("topic-map.Object-filename = '{0}'", objectFilename);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<TopicMap> GetListByTopic(string topicName, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(topicName)) 
         {
             sb.AppendFormatWithEscape("topic-map.Topic-name = '{0}'", topicName);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public TopicMap BuildFromRow(DataRow row)
      {
         var returnRecord = TopicMap.BuildTopicMapFromRow(row);
         returnRecord = this.BuildExtraFromRow<TopicMap>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, TopicMap record)
      {
         TopicMap.UpdateRowFromTopicMap(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public TopicMap Insert(TopicMap record)
      {
         DataRow row = this.dataSet.ttbltopicMap.NewttbltopicMapRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbltopicMap.AddttbltopicMapRow((pdstopicMapDataSet.ttbltopicMapRow)row);
         this.SaveChanges();
         return this.dataSet.ttbltopicMap.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltopicMap.Rows[0]) : null;
      }
  

      public TopicMap Update(TopicMap record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbltopicMap.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltopicMap.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(TopicMap record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbltopicMap.NewttbltopicMapRow();
            TopicMap.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltopicMap.AddttbltopicMapRow((pdstopicMapDataSet.ttbltopicMapRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new TopicMap() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  