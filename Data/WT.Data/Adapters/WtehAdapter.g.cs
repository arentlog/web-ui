//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.WT.Data.Adapters
{
   using com.infor.sxproxy.wtproxy;
   using com.infor.sxproxy.wtproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdswteh;
   using Models.Pdswtorderlookup;
   using Models.Complex;

   public partial class WtehAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdswteh";
      private WTProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> wtehTableControlKey;
		
      private pdswtehDataSet dataSet;
        
      public WtehAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new WTProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdswtehDataSet() { DataSetName = DataSetName };
            this.wtehTableControlKey = this.dataSet.ttblwteh.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.wtehTableControlKey))
            {
               this.CreateTableControlParameters(this.wtehTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in WtehAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Wtehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poWtehproxy = this.proxyAppObject.CreatePO_wtehproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poWtehproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Wtehproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.wtehTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.wtehTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Wtehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWtehproxy = this.proxyAppObject.CreatePO_wtehproxy())
               {
                  this.SetRequiredContextParameters();
                  poWtehproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Wtehproxy - After Call");
      }
   

      public Wteh GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Wteh wteh = null;
         if (row != null)
         {
             wteh = this.BuildFromRow(row);
         }
         return wteh;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(wteh) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwteh.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Wteh> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(wteh)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Wteh Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblwteh.AsEnumerable().SingleOrDefault();
         Wteh wteh = null;
         if (row != null)
         {
             wteh = this.BuildFromRow(row);
         }
         return wteh;
      }
	  
	  
	  public IEnumerable<Wteh> GetListByRowpointers(List<string> rowpointers, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("wteh.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("wteh.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwteh.AsEnumerable().SingleOrDefault();
      }
      public IEnumerable<Wteh> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("wteh.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wteh> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblwteh.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Wteh Get(int wtno, int wtsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("wteh.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtsuf = {0}", wtsuf);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Wteh> GetListByFmwhse(int cono, string shipfmwhse, int wtno, int wtsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wteh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipfmwhse)) 
         {
             sb.AppendFormatWithEscape(" AND wteh.shipfmwhse = '{0}'", shipfmwhse);
         }
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtsuf = {0}", wtsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wteh> GetListByJrnlno(int cono, int jrnlno, int wtno, int wtsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wteh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (jrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.jrnlno = {0}", jrnlno);
         }
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtsuf = {0}", wtsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wteh> GetListByJrnlno2(int cono2, int jrnlno2, int wtno, int wtsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (cono2 != int.MinValue) 
         {
             sb.AppendFormatWithEscape("wteh.cono2 = {0}", cono2);
         }
         if (jrnlno2 != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.jrnlno2 = {0}", jrnlno2);
         }
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtsuf = {0}", wtsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wteh> GetListByJrnlno3(int cono, int jrnlno3, int wtno, int wtsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wteh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (jrnlno3 != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.jrnlno3 = {0}", jrnlno3);
         }
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtsuf = {0}", wtsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Wteh GetByRowpointer(string rowpointer, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("wteh.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, 1, fldList);
      }

      public IEnumerable<Wteh> GetListByTowhse(int cono2, string shiptowhse, int wtno, int wtsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (cono2 != int.MinValue) 
         {
             sb.AppendFormatWithEscape("wteh.cono2 = {0}", cono2);
         }
         if (!string.IsNullOrEmpty(shiptowhse)) 
         {
             sb.AppendFormatWithEscape(" AND wteh.shiptowhse = '{0}'", shiptowhse);
         }
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wteh.wtsuf = {0}", wtsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wteh> GetListByTransdttmz(DateTime? transdttmz, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (transdttmz != null) 
         {
             sb.AppendFormatWithEscape("wteh.transdttmz = '{0}'", transdttmz);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Wteh BuildFromRow(DataRow row)
      {
         var returnRecord = Wteh.BuildWtehFromRow(row);
         returnRecord = this.BuildExtraFromRow<Wteh>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Wteh record)
      {
         Wteh.UpdateRowFromWteh(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Wteh Insert(Wteh record)
      {
         DataRow row = this.dataSet.ttblwteh.NewttblwtehRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblwteh.AddttblwtehRow((pdswtehDataSet.ttblwtehRow)row);
         this.SaveChanges();
         return this.dataSet.ttblwteh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwteh.Rows[0]) : null;
      }
  

      public Wteh Update(Wteh record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblwteh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwteh.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Wteh record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblwteh.NewttblwtehRow();
            Wteh.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwteh.AddttblwtehRow((pdswtehDataSet.ttblwtehRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Wteh record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttblwteh.NewttblwtehRow();
            Wteh.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwteh.AddttblwtehRow((pdswtehDataSet.ttblwtehRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Wteh() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Wteh() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      public WtehLookupResponseAPI Lookup(Wtorderlookupcriteria wtorderlookupcriteria)
      {   
         var result = new WtehLookupResponseAPI();
         
         var pdswtorderlookup = new pdswtorderlookupDataSet();
            
         DataRow ttblwtorderlookupcriteriaCriteria = pdswtorderlookup.ttblwtorderlookupcriteria.NewttblwtorderlookupcriteriaRow();
         Wtorderlookupcriteria.UpdateRowFromWtorderlookupcriteria(ref ttblwtorderlookupcriteriaCriteria, wtorderlookupcriteria);
         pdswtorderlookup.ttblwtorderlookupcriteria.AddttblwtorderlookupcriteriaRow((pdswtorderlookupDataSet.ttblwtorderlookupcriteriaRow)ttblwtorderlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Wteh - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWtehproxy = this.proxyAppObject.CreatePO_wtehproxy())
               {
                   this.SetRequiredContextParameters();
                   poWtehproxy.Lookup(ref pdsContext, ref pdswtorderlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Wteh - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdswtorderlookup); 
    
         foreach (DataRow row in pdswtorderlookup.ttblwtorderlookupresults)
         {
            result.wtorderlookupresults.Add(Wtorderlookupresults.BuildWtorderlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      public IEnumerable<Wteh> GetListByWordIndex(string searchTerm, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         where.AppendKeywords(string.Empty, searchTerm, true, true, false);
         this.SetAndFetch(where.ToString(), batchsize, fldList);
         this.SearchWordIndex(batchsize == 0 ? DefaultFetchWhereRowCount : batchsize , where.ToString());
         return (from DataRow row in this.dataSet.ttblwteh.Rows select this.BuildFromRow(row)).ToList();
      }
  
      protected void SearchWordIndex(int batchSize, string searchTerm)
      {
         NLogLoggerP.Trace("SearchWordIndex - Wteh - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWtehProxy = this.proxyAppObject.CreatePO_wtehproxy())
               {
                   this.SetRequiredContextParameters();
                   poWtehProxy.SearchWordIndex(batchSize, searchTerm, ref this.pdsContext, out this.dataSet, out string cErrorMessage, out bool moreRecords);
                   ErrorReportingHelper.ReportErrors(cErrorMessage);
                   this.ReportErrors(this.pdsContext);
                   this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SearchWordIndex - Wteh - After Call");
      }
  
      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  