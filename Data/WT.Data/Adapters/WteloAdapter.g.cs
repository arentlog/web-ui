//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.WT.Data.Adapters
{
   using com.infor.sxproxy.wtproxy;
   using com.infor.sxproxy.wtproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdswtelo;

   public partial class WteloAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdswtelo";
      private WTProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> wteloTableControlKey;
		
      private pdswteloDataSet dataSet;
        
      public WteloAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new WTProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdswteloDataSet() { DataSetName = DataSetName };
            this.wteloTableControlKey = this.dataSet.ttblwtelo.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.wteloTableControlKey))
            {
               this.CreateTableControlParameters(this.wteloTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in WteloAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Wteloproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poWteloproxy = this.proxyAppObject.CreatePO_wteloproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poWteloproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Wteloproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.wteloTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.wteloTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Wteloproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWteloproxy = this.proxyAppObject.CreatePO_wteloproxy())
               {
                  this.SetRequiredContextParameters();
                  poWteloproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Wteloproxy - After Call");
      }
   

      public Wtelo GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Wtelo wtelo = null;
         if (row != null)
         {
             wtelo = this.BuildFromRow(row);
         }
         return wtelo;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(wtelo) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwtelo.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Wtelo> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(wtelo)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Wtelo Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblwtelo.AsEnumerable().SingleOrDefault();
         Wtelo wtelo = null;
         if (row != null)
         {
             wtelo = this.BuildFromRow(row);
         }
         return wtelo;
      }
	  
	  
      public IEnumerable<Wtelo> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("wtelo.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wtelo> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblwtelo.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Wtelo Get(int wtno, int wtsuf, int lineno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("wtelo.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtelo.wtsuf = {0}", wtsuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtelo.lineno = {0}", lineno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtelo.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Wtelo> GetListByOrder(int wtcono, string ordertype, int orderaltno, int orderaltsuf, int linealtno, int seqaltno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (wtcono != int.MinValue) 
         {
             sb.AppendFormatWithEscape("wtelo.wtcono = {0}", wtcono);
         }
         if (!string.IsNullOrEmpty(ordertype)) 
         {
             sb.AppendFormatWithEscape(" AND wtelo.ordertype = '{0}'", ordertype);
         }
         if (orderaltno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtelo.orderaltno = {0}", orderaltno);
         }
         if (orderaltsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtelo.orderaltsuf = {0}", orderaltsuf);
         }
         if (linealtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtelo.linealtno = {0}", linealtno);
         }
         if (seqaltno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtelo.seqaltno = {0}", seqaltno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Wtelo BuildFromRow(DataRow row)
      {
         var returnRecord = Wtelo.BuildWteloFromRow(row);
         returnRecord = this.BuildExtraFromRow<Wtelo>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Wtelo record)
      {
         Wtelo.UpdateRowFromWtelo(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Wtelo Insert(Wtelo record)
      {
         DataRow row = this.dataSet.ttblwtelo.NewttblwteloRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblwtelo.AddttblwteloRow((pdswteloDataSet.ttblwteloRow)row);
         this.SaveChanges();
         return this.dataSet.ttblwtelo.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwtelo.Rows[0]) : null;
      }
  

      public Wtelo Update(Wtelo record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblwtelo.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwtelo.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Wtelo record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblwtelo.NewttblwteloRow();
            Wtelo.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwtelo.AddttblwteloRow((pdswteloDataSet.ttblwteloRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Wtelo() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  