//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.WT.Data.Adapters
{
   using com.infor.sxproxy.wtproxy;
   using com.infor.sxproxy.wtproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdswtel;

   public partial class WtelAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdswtel";
      private WTProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> wtelTableControlKey;
		
      private pdswtelDataSet dataSet;
        
      public WtelAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new WTProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdswtelDataSet() { DataSetName = DataSetName };
            this.wtelTableControlKey = this.dataSet.ttblwtel.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.wtelTableControlKey))
            {
               this.CreateTableControlParameters(this.wtelTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in WtelAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Wtelproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poWtelproxy = this.proxyAppObject.CreatePO_wtelproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poWtelproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Wtelproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.wtelTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.wtelTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Wtelproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWtelproxy = this.proxyAppObject.CreatePO_wtelproxy())
               {
                  this.SetRequiredContextParameters();
                  poWtelproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Wtelproxy - After Call");
      }
   

      public Wtel GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Wtel wtel = null;
         if (row != null)
         {
             wtel = this.BuildFromRow(row);
         }
         return wtel;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(wtel) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwtel.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Wtel> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(wtel)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Wtel Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblwtel.AsEnumerable().SingleOrDefault();
         Wtel wtel = null;
         if (row != null)
         {
             wtel = this.BuildFromRow(row);
         }
         return wtel;
      }
	  
	  
	  public IEnumerable<Wtel> GetListByRowpointers(List<string> rowpointers, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("wtel.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("wtel.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwtel.AsEnumerable().SingleOrDefault();
      }
      public IEnumerable<Wtel> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("wtel.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wtel> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblwtel.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Wtel Get(int wtno, int wtsuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("wtel.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtel.wtsuf = {0}", wtsuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtel.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Wtel> GetListByFill(int cono, string statustype, string shipfmwhse, string shipprod, string nonstockty, int bono, string approvety, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wtel.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(statustype)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.statustype = '{0}'", statustype);
         }
         if (!string.IsNullOrEmpty(shipfmwhse)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.shipfmwhse = '{0}'", shipfmwhse);
         }
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.shipprod = '{0}'", shipprod);
         }
         if (!string.IsNullOrEmpty(nonstockty)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.nonstockty = '{0}'", nonstockty);
         }
         if (bono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtel.bono = {0}", bono);
         }
         if (!string.IsNullOrEmpty(approvety)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.approvety = '{0}'", approvety);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wtel> GetListByProdi(int cono2, string shipprod, string shiptowhse, string statustype, string transtype, DateTime? duedt, int wtno, int wtsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (cono2 != int.MinValue) 
         {
             sb.AppendFormatWithEscape("wtel.cono2 = {0}", cono2);
         }
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.shipprod = '{0}'", shipprod);
         }
         if (!string.IsNullOrEmpty(shiptowhse)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.shiptowhse = '{0}'", shiptowhse);
         }
         if (!string.IsNullOrEmpty(statustype)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.statustype = '{0}'", statustype);
         }
         if (!string.IsNullOrEmpty(transtype)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.transtype = '{0}'", transtype);
         }
         if (duedt != null) 
         {
             sb.AppendFormatWithEscape(" AND wtel.duedt = '{0}'", duedt);
         }
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtel.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtel.wtsuf = {0}", wtsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wtel> GetListByProdo(int cono, string shipprod, string shipfmwhse, int wtno, int wtsuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wtel.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.shipprod = '{0}'", shipprod);
         }
         if (!string.IsNullOrEmpty(shipfmwhse)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.shipfmwhse = '{0}'", shipfmwhse);
         }
         if (wtno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtel.wtno = {0}", wtno);
         }
         if (wtsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wtel.wtsuf = {0}", wtsuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Wtel GetByRowpointer(string rowpointer, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("wtel.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, 1, fldList);
      }

      public IEnumerable<Wtel> GetListBySpecns(int cono, string statustype, string nonstockty, string shipfmwhse, string ordertype, string shipprod, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wtel.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(statustype)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.statustype = '{0}'", statustype);
         }
         if (!string.IsNullOrEmpty(nonstockty)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.nonstockty = '{0}'", nonstockty);
         }
         if (!string.IsNullOrEmpty(shipfmwhse)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.shipfmwhse = '{0}'", shipfmwhse);
         }
         if (!string.IsNullOrEmpty(ordertype)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.ordertype = '{0}'", ordertype);
         }
         if (!string.IsNullOrEmpty(shipprod)) 
         {
             sb.AppendFormatWithEscape(" AND wtel.shipprod = '{0}'", shipprod);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wtel> GetListByTransdttmz(DateTime? transdttmz, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (transdttmz != null) 
         {
             sb.AppendFormatWithEscape("wtel.transdttmz = '{0}'", transdttmz);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Wtel BuildFromRow(DataRow row)
      {
         var returnRecord = Wtel.BuildWtelFromRow(row);
         returnRecord = this.BuildExtraFromRow<Wtel>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Wtel record)
      {
         Wtel.UpdateRowFromWtel(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Wtel Insert(Wtel record)
      {
         DataRow row = this.dataSet.ttblwtel.NewttblwtelRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblwtel.AddttblwtelRow((pdswtelDataSet.ttblwtelRow)row);
         this.SaveChanges();
         return this.dataSet.ttblwtel.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwtel.Rows[0]) : null;
      }
  

      public Wtel Update(Wtel record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblwtel.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwtel.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Wtel record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblwtel.NewttblwtelRow();
            Wtel.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwtel.AddttblwtelRow((pdswtelDataSet.ttblwtelRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Wtel record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttblwtel.NewttblwtelRow();
            Wtel.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwtel.AddttblwtelRow((pdswtelDataSet.ttblwtelRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Wtel() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Wtel() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  