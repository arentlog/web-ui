//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.WT.Data.Adapters
{
   using com.infor.sxproxy.wtproxy;
   using com.infor.sxproxy.wtproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdswterah;

   public partial class WterahAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdswterah";
      private WTProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> wterahTableControlKey;
		
      private pdswterahDataSet dataSet;
        
      public WterahAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new WTProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdswterahDataSet() { DataSetName = DataSetName };
            this.wterahTableControlKey = this.dataSet.ttblwterah.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.wterahTableControlKey))
            {
               this.CreateTableControlParameters(this.wterahTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in WterahAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Wterahproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poWterahproxy = this.proxyAppObject.CreatePO_wterahproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poWterahproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Wterahproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.wterahTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.wterahTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Wterahproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWterahproxy = this.proxyAppObject.CreatePO_wterahproxy())
               {
                  this.SetRequiredContextParameters();
                  poWterahproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Wterahproxy - After Call");
      }
   

      public Wterah GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Wterah wterah = null;
         if (row != null)
         {
             wterah = this.BuildFromRow(row);
         }
         return wterah;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(wterah) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwterah.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Wterah> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(wterah)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Wterah Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblwterah.AsEnumerable().SingleOrDefault();
         Wterah wterah = null;
         if (row != null)
         {
             wterah = this.BuildFromRow(row);
         }
         return wterah;
      }
	  
	  
      public IEnumerable<Wterah> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("wterah.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wterah> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblwterah.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Wterah Get(int cono, int reportno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wterah.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (reportno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wterah.reportno = {0}", reportno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Wterah> GetListByRrar(int cono, string oper2, string reportnm, string shipfmwhse, int cono2, string shiptowhse, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wterah.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND wterah.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND wterah.reportnm = '{0}'", reportnm);
         }
         if (!string.IsNullOrEmpty(shipfmwhse)) 
         {
             sb.AppendFormatWithEscape(" AND wterah.shipfmwhse = '{0}'", shipfmwhse);
         }
         if (cono2 != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wterah.cono2 = {0}", cono2);
         }
         if (!string.IsNullOrEmpty(shiptowhse)) 
         {
             sb.AppendFormatWithEscape(" AND wterah.shiptowhse = '{0}'", shiptowhse);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wterah> GetListByRush(int cono, string shipfmwhse, int cono2, string shiptowhse, bool rushfl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wterah.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(shipfmwhse)) 
         {
             sb.AppendFormatWithEscape(" AND wterah.shipfmwhse = '{0}'", shipfmwhse);
         }
         if (cono2 != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wterah.cono2 = {0}", cono2);
         }
         if (!string.IsNullOrEmpty(shiptowhse)) 
         {
             sb.AppendFormatWithEscape(" AND wterah.shiptowhse = '{0}'", shiptowhse);
         }
         sb.AppendFormatWithEscape(" AND wterah.rushfl = {0}", rushfl);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Wterah BuildFromRow(DataRow row)
      {
         var returnRecord = Wterah.BuildWterahFromRow(row);
         returnRecord = this.BuildExtraFromRow<Wterah>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Wterah record)
      {
         Wterah.UpdateRowFromWterah(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Wterah Insert(Wterah record)
      {
         DataRow row = this.dataSet.ttblwterah.NewttblwterahRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblwterah.AddttblwterahRow((pdswterahDataSet.ttblwterahRow)row);
         this.SaveChanges();
         return this.dataSet.ttblwterah.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwterah.Rows[0]) : null;
      }
  

      public Wterah Update(Wterah record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblwterah.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwterah.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Wterah record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblwterah.NewttblwterahRow();
            Wterah.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwterah.AddttblwterahRow((pdswterahDataSet.ttblwterahRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Wterah() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  