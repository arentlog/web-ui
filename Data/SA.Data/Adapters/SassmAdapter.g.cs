//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssassm;

   public partial class SassmAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssassm";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sassmTableControlKey;
		
      private pdssassmDataSet dataSet;
        
      public SassmAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssassmDataSet() { DataSetName = DataSetName };
            this.sassmTableControlKey = this.dataSet.ttblsassm.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sassmTableControlKey))
            {
               this.CreateTableControlParameters(this.sassmTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SassmAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sassmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSassmproxy = this.proxyAppObject.CreatePO_sassmproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSassmproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sassmproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sassmTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sassmTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sassmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSassmproxy = this.proxyAppObject.CreatePO_sassmproxy())
               {
                  this.SetRequiredContextParameters();
                  poSassmproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sassmproxy - After Call");
      }
   

      public Sassm GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sassm sassm = null;
         if (row != null)
         {
             sassm = this.BuildFromRow(row);
         }
         return sassm;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sassm) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsassm.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sassm> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sassm)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sassm Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsassm.AsEnumerable().SingleOrDefault();
         Sassm sassm = null;
         if (row != null)
         {
             sassm = this.BuildFromRow(row);
         }
         return sassm;
      }
	  
	  

      public IEnumerable<Sassm> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsassm.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sassm Get(string currproc, string trmgrlang, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(currproc)) 
         {
             sb.AppendFormatWithEscape("sassm.currproc = '{0}'", currproc);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND sassm.trmgrlang = '{0}'", trmgrlang);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sassm> GetListByMenupos(string menuproc, int menupos, string trmgrlang, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuproc)) 
         {
             sb.AppendFormatWithEscape("sassm.menuproc = '{0}'", menuproc);
         }
         if (menupos != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sassm.menupos = {0}", menupos);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND sassm.trmgrlang = '{0}'", trmgrlang);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sassm> GetListBySecurpos(string menuproc, int securpos, string trmgrlang, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(menuproc)) 
         {
             sb.AppendFormatWithEscape("sassm.menuproc = '{0}'", menuproc);
         }
         if (securpos != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sassm.securpos = {0}", securpos);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND sassm.trmgrlang = '{0}'", trmgrlang);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sassm BuildFromRow(DataRow row)
      {
         var returnRecord = Sassm.BuildSassmFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sassm>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sassm record)
      {
         Sassm.UpdateRowFromSassm(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sassm Insert(Sassm record)
      {
         DataRow row = this.dataSet.ttblsassm.NewttblsassmRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsassm.AddttblsassmRow((pdssassmDataSet.ttblsassmRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsassm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsassm.Rows[0]) : null;
      }
  

      public Sassm Update(Sassm record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsassm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsassm.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sassm record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsassm.NewttblsassmRow();
            Sassm.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsassm.AddttblsassmRow((pdssassmDataSet.ttblsassmRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sassm() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  