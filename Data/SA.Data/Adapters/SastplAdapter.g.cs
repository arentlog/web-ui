//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssastpl;

   public partial class SastplAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssastpl";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sastplTableControlKey;
		
      private pdssastplDataSet dataSet;
        
      public SastplAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssastplDataSet() { DataSetName = DataSetName };
            this.sastplTableControlKey = this.dataSet.ttblsastpl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sastplTableControlKey))
            {
               this.CreateTableControlParameters(this.sastplTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SastplAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sastplproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSastplproxy = this.proxyAppObject.CreatePO_sastplproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSastplproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sastplproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sastplTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sastplTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sastplproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSastplproxy = this.proxyAppObject.CreatePO_sastplproxy())
               {
                  this.SetRequiredContextParameters();
                  poSastplproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sastplproxy - After Call");
      }
   

      public Sastpl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sastpl sastpl = null;
         if (row != null)
         {
             sastpl = this.BuildFromRow(row);
         }
         return sastpl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sastpl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsastpl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sastpl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sastpl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sastpl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsastpl.AsEnumerable().SingleOrDefault();
         Sastpl sastpl = null;
         if (row != null)
         {
             sastpl = this.BuildFromRow(row);
         }
         return sastpl;
      }
	  
	  

      public IEnumerable<Sastpl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsastpl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sastpl Get(string appname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(appname)) 
         {
             sb.AppendFormatWithEscape("sastpl.appname = '{0}'", appname);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sastpl BuildFromRow(DataRow row)
      {
         var returnRecord = Sastpl.BuildSastplFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sastpl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sastpl record)
      {
         Sastpl.UpdateRowFromSastpl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sastpl Insert(Sastpl record)
      {
         DataRow row = this.dataSet.ttblsastpl.NewttblsastplRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsastpl.AddttblsastplRow((pdssastplDataSet.ttblsastplRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsastpl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastpl.Rows[0]) : null;
      }
  

      public Sastpl Update(Sastpl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsastpl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastpl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sastpl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsastpl.NewttblsastplRow();
            Sastpl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsastpl.AddttblsastplRow((pdssastplDataSet.ttblsastplRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sastpl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  