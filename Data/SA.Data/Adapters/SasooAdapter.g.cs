//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssasoo;

   public partial class SasooAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssasoo";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sasooTableControlKey;
		
      private pdssasooDataSet dataSet;
        
      public SasooAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssasooDataSet() { DataSetName = DataSetName };
            this.sasooTableControlKey = this.dataSet.ttblsasoo.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sasooTableControlKey))
            {
               this.CreateTableControlParameters(this.sasooTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SasooAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sasooproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSasooproxy = this.proxyAppObject.CreatePO_sasooproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSasooproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sasooproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sasooTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sasooTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sasooproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSasooproxy = this.proxyAppObject.CreatePO_sasooproxy())
               {
                  this.SetRequiredContextParameters();
                  poSasooproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sasooproxy - After Call");
      }
   

      public Sasoo GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sasoo sasoo = null;
         if (row != null)
         {
             sasoo = this.BuildFromRow(row);
         }
         return sasoo;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sasoo) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsasoo.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sasoo> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sasoo)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sasoo Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsasoo.AsEnumerable().SingleOrDefault();
         Sasoo sasoo = null;
         if (row != null)
         {
             sasoo = this.BuildFromRow(row);
         }
         return sasoo;
      }
	  
	  
      public IEnumerable<Sasoo> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sasoo.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sasoo> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsasoo.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sasoo Get(int cono, string oper2, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sasoo.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND sasoo.oper2 = '{0}'", oper2);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sasoo> GetListByIfsuser(int cono, string ifsuser, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sasoo.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(ifsuser)) 
         {
             sb.AppendFormatWithEscape(" AND sasoo.ifsuser = '{0}'", ifsuser);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sasoo> GetListByOper2(bool loginfl, string oper2, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sasoo.loginfl = {0}", loginfl);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND sasoo.oper2 = '{0}'", oper2);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sasoo> GetListByUsers(bool loginfl, string ourproc, string oper2, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sasoo.loginfl = {0}", loginfl);
         if (!string.IsNullOrEmpty(ourproc)) 
         {
             sb.AppendFormatWithEscape(" AND sasoo.ourproc = '{0}'", ourproc);
         }
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND sasoo.oper2 = '{0}'", oper2);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sasoo BuildFromRow(DataRow row)
      {
         var returnRecord = Sasoo.BuildSasooFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sasoo>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sasoo record)
      {
         Sasoo.UpdateRowFromSasoo(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sasoo Insert(Sasoo record)
      {
         DataRow row = this.dataSet.ttblsasoo.NewttblsasooRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsasoo.AddttblsasooRow((pdssasooDataSet.ttblsasooRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsasoo.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasoo.Rows[0]) : null;
      }
  

      public Sasoo Update(Sasoo record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsasoo.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasoo.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sasoo record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsasoo.NewttblsasooRow();
            Sasoo.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsasoo.AddttblsasooRow((pdssasooDataSet.ttblsasooRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sasoo() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  