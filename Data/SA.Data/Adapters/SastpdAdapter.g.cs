//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssastpd;

   public partial class SastpdAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssastpd";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sastpdTableControlKey;
		
      private pdssastpdDataSet dataSet;
        
      public SastpdAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssastpdDataSet() { DataSetName = DataSetName };
            this.sastpdTableControlKey = this.dataSet.ttblsastpd.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sastpdTableControlKey))
            {
               this.CreateTableControlParameters(this.sastpdTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SastpdAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sastpdproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSastpdproxy = this.proxyAppObject.CreatePO_sastpdproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSastpdproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sastpdproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sastpdTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sastpdTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sastpdproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSastpdproxy = this.proxyAppObject.CreatePO_sastpdproxy())
               {
                  this.SetRequiredContextParameters();
                  poSastpdproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sastpdproxy - After Call");
      }
   

      public Sastpd GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sastpd sastpd = null;
         if (row != null)
         {
             sastpd = this.BuildFromRow(row);
         }
         return sastpd;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sastpd) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsastpd.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sastpd> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sastpd)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sastpd Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsastpd.AsEnumerable().SingleOrDefault();
         Sastpd sastpd = null;
         if (row != null)
         {
             sastpd = this.BuildFromRow(row);
         }
         return sastpd;
      }
	  
	  

      public IEnumerable<Sastpd> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsastpd.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sastpd Get(bool statusfl, string appname, string deviceid, string appuserid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastpd.statusfl = {0}", statusfl);
         if (!string.IsNullOrEmpty(appname)) 
         {
             sb.AppendFormatWithEscape(" AND sastpd.appname = '{0}'", appname);
         }
         if (!string.IsNullOrEmpty(deviceid)) 
         {
             sb.AppendFormatWithEscape(" AND sastpd.deviceid = '{0}'", deviceid);
         }
         if (!string.IsNullOrEmpty(appuserid)) 
         {
             sb.AppendFormatWithEscape(" AND sastpd.appuserid = '{0}'", appuserid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sastpd BuildFromRow(DataRow row)
      {
         var returnRecord = Sastpd.BuildSastpdFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sastpd>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sastpd record)
      {
         Sastpd.UpdateRowFromSastpd(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sastpd Insert(Sastpd record)
      {
         DataRow row = this.dataSet.ttblsastpd.NewttblsastpdRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsastpd.AddttblsastpdRow((pdssastpdDataSet.ttblsastpdRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsastpd.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastpd.Rows[0]) : null;
      }
  

      public Sastpd Update(Sastpd record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsastpd.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastpd.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sastpd record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsastpd.NewttblsastpdRow();
            Sastpd.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsastpd.AddttblsastpdRow((pdssastpdDataSet.ttblsastpdRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sastpd() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  