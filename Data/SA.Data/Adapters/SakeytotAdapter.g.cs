//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssakeytot;

   public partial class SakeytotAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssakeytot";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sakeytotTableControlKey;
		
      private pdssakeytotDataSet dataSet;
        
      public SakeytotAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssakeytotDataSet() { DataSetName = DataSetName };
            this.sakeytotTableControlKey = this.dataSet.ttblsakeytot.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sakeytotTableControlKey))
            {
               this.CreateTableControlParameters(this.sakeytotTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SakeytotAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sakeytotproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSakeytotproxy = this.proxyAppObject.CreatePO_sakeytotproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSakeytotproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sakeytotproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sakeytotTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sakeytotTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sakeytotproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSakeytotproxy = this.proxyAppObject.CreatePO_sakeytotproxy())
               {
                  this.SetRequiredContextParameters();
                  poSakeytotproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sakeytotproxy - After Call");
      }
   

      public Sakeytot GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sakeytot sakeytot = null;
         if (row != null)
         {
             sakeytot = this.BuildFromRow(row);
         }
         return sakeytot;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sakeytot) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsakeytot.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sakeytot> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sakeytot)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sakeytot Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsakeytot.AsEnumerable().SingleOrDefault();
         Sakeytot sakeytot = null;
         if (row != null)
         {
             sakeytot = this.BuildFromRow(row);
         }
         return sakeytot;
      }
	  
	  
      public IEnumerable<Sakeytot> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sakeytot.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sakeytot> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsakeytot.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sakeytot Get(int cono, string xtype, string crossref, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sakeytot.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(xtype)) 
         {
             sb.AppendFormatWithEscape(" AND sakeytot.xtype = '{0}'", xtype);
         }
         if (!string.IsNullOrEmpty(crossref)) 
         {
             sb.AppendFormatWithEscape(" AND sakeytot.crossref = '{0}'", crossref);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sakeytot BuildFromRow(DataRow row)
      {
         var returnRecord = Sakeytot.BuildSakeytotFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sakeytot>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sakeytot record)
      {
         Sakeytot.UpdateRowFromSakeytot(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sakeytot Insert(Sakeytot record)
      {
         DataRow row = this.dataSet.ttblsakeytot.NewttblsakeytotRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsakeytot.AddttblsakeytotRow((pdssakeytotDataSet.ttblsakeytotRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsakeytot.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsakeytot.Rows[0]) : null;
      }
  

      public Sakeytot Update(Sakeytot record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsakeytot.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsakeytot.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sakeytot record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsakeytot.NewttblsakeytotRow();
            Sakeytot.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsakeytot.AddttblsakeytotRow((pdssakeytotDataSet.ttblsakeytotRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sakeytot() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  