//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssasgl;
   using Models.Pdssalocaltaxlookup;
   using Models.Complex;

   public partial class SasglAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssasgl";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sasglTableControlKey;
		
      private pdssasglDataSet dataSet;
        
      public SasglAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssasglDataSet() { DataSetName = DataSetName };
            this.sasglTableControlKey = this.dataSet.ttblsasgl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sasglTableControlKey))
            {
               this.CreateTableControlParameters(this.sasglTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SasglAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sasglproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSasglproxy = this.proxyAppObject.CreatePO_sasglproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSasglproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sasglproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sasglTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sasglTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sasglproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSasglproxy = this.proxyAppObject.CreatePO_sasglproxy())
               {
                  this.SetRequiredContextParameters();
                  poSasglproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sasglproxy - After Call");
      }
   

      public Sasgl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sasgl sasgl = null;
         if (row != null)
         {
             sasgl = this.BuildFromRow(row);
         }
         return sasgl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sasgl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsasgl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sasgl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sasgl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sasgl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsasgl.AsEnumerable().SingleOrDefault();
         Sasgl sasgl = null;
         if (row != null)
         {
             sasgl = this.BuildFromRow(row);
         }
         return sasgl;
      }
	  
	  
      public IEnumerable<Sasgl> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sasgl.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sasgl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsasgl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sasgl Get(int cono, string state, string taxauth, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sasgl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(state)) 
         {
             sb.AppendFormatWithEscape(" AND sasgl.state = '{0}'", state);
         }
         if (!string.IsNullOrEmpty(taxauth)) 
         {
             sb.AppendFormatWithEscape(" AND sasgl.taxauth = '{0}'", taxauth);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sasgl BuildFromRow(DataRow row)
      {
         var returnRecord = Sasgl.BuildSasglFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sasgl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sasgl record)
      {
         Sasgl.UpdateRowFromSasgl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sasgl Insert(Sasgl record)
      {
         DataRow row = this.dataSet.ttblsasgl.NewttblsasglRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsasgl.AddttblsasglRow((pdssasglDataSet.ttblsasglRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsasgl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasgl.Rows[0]) : null;
      }
  

      public Sasgl Update(Sasgl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsasgl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasgl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sasgl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsasgl.NewttblsasglRow();
            Sasgl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsasgl.AddttblsasglRow((pdssasglDataSet.ttblsasglRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sasgl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public SasglLookupResponseAPI Lookup(Salocaltaxlookupcriteria salocaltaxlookupcriteria)
      {   
         var result = new SasglLookupResponseAPI();
         
         var pdssalocaltaxlookup = new pdssalocaltaxlookupDataSet();
            
         DataRow ttblsalocaltaxlookupcriteriaCriteria = pdssalocaltaxlookup.ttblsalocaltaxlookupcriteria.NewttblsalocaltaxlookupcriteriaRow();
         Salocaltaxlookupcriteria.UpdateRowFromSalocaltaxlookupcriteria(ref ttblsalocaltaxlookupcriteriaCriteria, salocaltaxlookupcriteria);
         pdssalocaltaxlookup.ttblsalocaltaxlookupcriteria.AddttblsalocaltaxlookupcriteriaRow((pdssalocaltaxlookupDataSet.ttblsalocaltaxlookupcriteriaRow)ttblsalocaltaxlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Sasgl - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSasglproxy = this.proxyAppObject.CreatePO_sasglproxy())
               {
                   this.SetRequiredContextParameters();
                   poSasglproxy.Lookup(ref pdsContext, ref pdssalocaltaxlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Sasgl - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdssalocaltaxlookup); 
    
         foreach (DataRow row in pdssalocaltaxlookup.ttblsalocaltaxlookupresults)
         {
            result.salocaltaxlookupresults.Add(Salocaltaxlookupresults.BuildSalocaltaxlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  