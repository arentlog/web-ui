//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssastf;

   public partial class SastfAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssastf";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sastfTableControlKey;
		
      private pdssastfDataSet dataSet;
        
      public SastfAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssastfDataSet() { DataSetName = DataSetName };
            this.sastfTableControlKey = this.dataSet.ttblsastf.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sastfTableControlKey))
            {
               this.CreateTableControlParameters(this.sastfTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SastfAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sastfproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSastfproxy = this.proxyAppObject.CreatePO_sastfproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSastfproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sastfproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sastfTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sastfTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sastfproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSastfproxy = this.proxyAppObject.CreatePO_sastfproxy())
               {
                  this.SetRequiredContextParameters();
                  poSastfproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sastfproxy - After Call");
      }
   

      public Sastf GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sastf sastf = null;
         if (row != null)
         {
             sastf = this.BuildFromRow(row);
         }
         return sastf;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sastf) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsastf.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sastf> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sastf)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sastf Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsastf.AsEnumerable().SingleOrDefault();
         Sastf sastf = null;
         if (row != null)
         {
             sastf = this.BuildFromRow(row);
         }
         return sastf;
      }
	  
	  
      public IEnumerable<Sastf> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sastf.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sastf> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsastf.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sastf Get(int cono, string billlevelcd, string srcrowpointer, string carrierid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastf.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(billlevelcd)) 
         {
             sb.AppendFormatWithEscape(" AND sastf.billlevelcd = '{0}'", billlevelcd);
         }
         if (!string.IsNullOrEmpty(srcrowpointer)) 
         {
             sb.AppendFormatWithEscape(" AND sastf.srcrowpointer = '{0}'", srcrowpointer);
         }
         if (!string.IsNullOrEmpty(carrierid)) 
         {
             sb.AppendFormatWithEscape(" AND sastf.carrierid = '{0}'", carrierid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sastf> GetListByCarrierid(int cono, string carrierid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastf.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(carrierid)) 
         {
             sb.AppendFormatWithEscape(" AND sastf.carrierid = '{0}'", carrierid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sastf> GetListBySrcrowpointer(string srcrowpointer, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(srcrowpointer)) 
         {
             sb.AppendFormatWithEscape("sastf.srcrowpointer = '{0}'", srcrowpointer);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sastf BuildFromRow(DataRow row)
      {
         var returnRecord = Sastf.BuildSastfFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sastf>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sastf record)
      {
         Sastf.UpdateRowFromSastf(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sastf Insert(Sastf record)
      {
         DataRow row = this.dataSet.ttblsastf.NewttblsastfRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsastf.AddttblsastfRow((pdssastfDataSet.ttblsastfRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsastf.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastf.Rows[0]) : null;
      }
  

      public Sastf Update(Sastf record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsastf.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastf.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sastf record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsastf.NewttblsastfRow();
            Sastf.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsastf.AddttblsastfRow((pdssastfDataSet.ttblsastfRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sastf() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  