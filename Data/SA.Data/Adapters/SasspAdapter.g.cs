//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssassp;

   public partial class SasspAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssassp";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sasspTableControlKey;
		
      private pdssasspDataSet dataSet;
        
      public SasspAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssasspDataSet() { DataSetName = DataSetName };
            this.sasspTableControlKey = this.dataSet.ttblsassp.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sasspTableControlKey))
            {
               this.CreateTableControlParameters(this.sasspTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SasspAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sasspproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSasspproxy = this.proxyAppObject.CreatePO_sasspproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSasspproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sasspproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sasspTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sasspTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sasspproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSasspproxy = this.proxyAppObject.CreatePO_sasspproxy())
               {
                  this.SetRequiredContextParameters();
                  poSasspproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sasspproxy - After Call");
      }
   

      public Sassp GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sassp sassp = null;
         if (row != null)
         {
             sassp = this.BuildFromRow(row);
         }
         return sassp;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sassp) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsassp.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sassp> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sassp)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sassp Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsassp.AsEnumerable().SingleOrDefault();
         Sassp sassp = null;
         if (row != null)
         {
             sassp = this.BuildFromRow(row);
         }
         return sassp;
      }
	  
	  

      public IEnumerable<Sassp> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsassp.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sassp Get(string areacd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(areacd)) 
         {
             sb.AppendFormatWithEscape("sassp.areacd = '{0}'", areacd);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sassp BuildFromRow(DataRow row)
      {
         var returnRecord = Sassp.BuildSasspFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sassp>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sassp record)
      {
         Sassp.UpdateRowFromSassp(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sassp Insert(Sassp record)
      {
         DataRow row = this.dataSet.ttblsassp.NewttblsasspRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsassp.AddttblsasspRow((pdssasspDataSet.ttblsasspRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsassp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsassp.Rows[0]) : null;
      }
  

      public Sassp Update(Sassp record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsassp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsassp.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sassp record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsassp.NewttblsasspRow();
            Sassp.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsassp.AddttblsasspRow((pdssasspDataSet.ttblsasspRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sassp() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  