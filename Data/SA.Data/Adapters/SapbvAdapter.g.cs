//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssapbv;

   public partial class SapbvAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssapbv";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sapbvTableControlKey;
		
      private pdssapbvDataSet dataSet;
        
      public SapbvAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssapbvDataSet() { DataSetName = DataSetName };
            this.sapbvTableControlKey = this.dataSet.ttblsapbv.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sapbvTableControlKey))
            {
               this.CreateTableControlParameters(this.sapbvTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SapbvAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sapbvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSapbvproxy = this.proxyAppObject.CreatePO_sapbvproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSapbvproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sapbvproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sapbvTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sapbvTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sapbvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSapbvproxy = this.proxyAppObject.CreatePO_sapbvproxy())
               {
                  this.SetRequiredContextParameters();
                  poSapbvproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sapbvproxy - After Call");
      }
   

      public Sapbv GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sapbv sapbv = null;
         if (row != null)
         {
             sapbv = this.BuildFromRow(row);
         }
         return sapbv;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sapbv) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsapbv.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sapbv> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sapbv)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sapbv Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsapbv.AsEnumerable().SingleOrDefault();
         Sapbv sapbv = null;
         if (row != null)
         {
             sapbv = this.BuildFromRow(row);
         }
         return sapbv;
      }
	  
	  
      public IEnumerable<Sapbv> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sapbv.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sapbv> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsapbv.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sapbv Get(int cono, string reportnm, decimal vendno, bool type, string apinvno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sapbv.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND sapbv.reportnm = '{0}'", reportnm);
         }
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sapbv.vendno = {0}", vendno);
         }
         sb.AppendFormatWithEscape(" AND sapbv.type = {0}", type);
         if (!string.IsNullOrEmpty(apinvno)) 
         {
             sb.AppendFormatWithEscape(" AND sapbv.apinvno = '{0}'", apinvno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sapbv.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sapbv> GetListBySortno(int cono, string operinit, string reportnm, int sortno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sapbv.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(operinit)) 
         {
             sb.AppendFormatWithEscape(" AND sapbv.operinit = '{0}'", operinit);
         }
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND sapbv.reportnm = '{0}'", reportnm);
         }
         if (sortno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND sapbv.sortno = {0}", sortno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sapbv BuildFromRow(DataRow row)
      {
         var returnRecord = Sapbv.BuildSapbvFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sapbv>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sapbv record)
      {
         Sapbv.UpdateRowFromSapbv(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sapbv Insert(Sapbv record)
      {
         DataRow row = this.dataSet.ttblsapbv.NewttblsapbvRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsapbv.AddttblsapbvRow((pdssapbvDataSet.ttblsapbvRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsapbv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsapbv.Rows[0]) : null;
      }
  

      public Sapbv Update(Sapbv record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsapbv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsapbv.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sapbv record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsapbv.NewttblsapbvRow();
            Sapbv.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsapbv.AddttblsapbvRow((pdssapbvDataSet.ttblsapbvRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sapbv() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  