//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssasse;

   public partial class SasseAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssasse";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sasseTableControlKey;
		
      private pdssasseDataSet dataSet;
        
      public SasseAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssasseDataSet() { DataSetName = DataSetName };
            this.sasseTableControlKey = this.dataSet.ttblsasse.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sasseTableControlKey))
            {
               this.CreateTableControlParameters(this.sasseTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SasseAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sasseproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSasseproxy = this.proxyAppObject.CreatePO_sasseproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSasseproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sasseproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sasseTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sasseTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sasseproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSasseproxy = this.proxyAppObject.CreatePO_sasseproxy())
               {
                  this.SetRequiredContextParameters();
                  poSasseproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sasseproxy - After Call");
      }
   

      public Sasse GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sasse sasse = null;
         if (row != null)
         {
             sasse = this.BuildFromRow(row);
         }
         return sasse;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sasse) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsasse.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sasse> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sasse)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sasse Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsasse.AsEnumerable().SingleOrDefault();
         Sasse sasse = null;
         if (row != null)
         {
             sasse = this.BuildFromRow(row);
         }
         return sasse;
      }
	  
	  

      public IEnumerable<Sasse> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsasse.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sasse Get(int errorno, string trmgrlang, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (errorno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("sasse.errorno = {0}", errorno);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND sasse.trmgrlang = '{0}'", trmgrlang);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sasse BuildFromRow(DataRow row)
      {
         var returnRecord = Sasse.BuildSasseFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sasse>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sasse record)
      {
         Sasse.UpdateRowFromSasse(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sasse Insert(Sasse record)
      {
         DataRow row = this.dataSet.ttblsasse.NewttblsasseRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsasse.AddttblsasseRow((pdssasseDataSet.ttblsasseRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsasse.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasse.Rows[0]) : null;
      }
  

      public Sasse Update(Sasse record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsasse.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasse.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sasse record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsasse.NewttblsasseRow();
            Sasse.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsasse.AddttblsasseRow((pdssasseDataSet.ttblsasseRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sasse() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  