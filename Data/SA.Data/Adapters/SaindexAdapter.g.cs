//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssaindex;

   public partial class SaindexAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssaindex";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> saindexTableControlKey;
		
      private pdssaindexDataSet dataSet;
        
      public SaindexAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssaindexDataSet() { DataSetName = DataSetName };
            this.saindexTableControlKey = this.dataSet.ttblsaindex.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.saindexTableControlKey))
            {
               this.CreateTableControlParameters(this.saindexTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SaindexAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Saindexproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSaindexproxy = this.proxyAppObject.CreatePO_saindexproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSaindexproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Saindexproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.saindexTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.saindexTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Saindexproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSaindexproxy = this.proxyAppObject.CreatePO_saindexproxy())
               {
                  this.SetRequiredContextParameters();
                  poSaindexproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Saindexproxy - After Call");
      }
   

      public Saindex GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Saindex saindex = null;
         if (row != null)
         {
             saindex = this.BuildFromRow(row);
         }
         return saindex;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(saindex) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsaindex.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Saindex> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(saindex)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Saindex Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsaindex.AsEnumerable().SingleOrDefault();
         Saindex saindex = null;
         if (row != null)
         {
             saindex = this.BuildFromRow(row);
         }
         return saindex;
      }
	  
	  
      public IEnumerable<Saindex> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("saindex.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Saindex> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsaindex.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Saindex Get(int cono, string xtype, string crossref, string prod, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("saindex.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(xtype)) 
         {
             sb.AppendFormatWithEscape(" AND saindex.xtype = '{0}'", xtype);
         }
         if (!string.IsNullOrEmpty(crossref)) 
         {
             sb.AppendFormatWithEscape(" AND saindex.crossref = '{0}'", crossref);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND saindex.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Saindex> GetListByProd(int cono, string xtype, string prod, string crossref, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("saindex.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(xtype)) 
         {
             sb.AppendFormatWithEscape(" AND saindex.xtype = '{0}'", xtype);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND saindex.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(crossref)) 
         {
             sb.AppendFormatWithEscape(" AND saindex.crossref = '{0}'", crossref);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Saindex BuildFromRow(DataRow row)
      {
         var returnRecord = Saindex.BuildSaindexFromRow(row);
         returnRecord = this.BuildExtraFromRow<Saindex>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Saindex record)
      {
         Saindex.UpdateRowFromSaindex(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Saindex Insert(Saindex record)
      {
         DataRow row = this.dataSet.ttblsaindex.NewttblsaindexRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsaindex.AddttblsaindexRow((pdssaindexDataSet.ttblsaindexRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsaindex.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsaindex.Rows[0]) : null;
      }
  

      public Saindex Update(Saindex record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsaindex.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsaindex.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Saindex record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsaindex.NewttblsaindexRow();
            Saindex.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsaindex.AddttblsaindexRow((pdssaindexDataSet.ttblsaindexRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Saindex() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  