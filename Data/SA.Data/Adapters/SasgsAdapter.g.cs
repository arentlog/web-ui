//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssasgs;
   using Models.Pdssastatetaxlookup;
   using Models.Complex;

   public partial class SasgsAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssasgs";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sasgsTableControlKey;
		
      private pdssasgsDataSet dataSet;
        
      public SasgsAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssasgsDataSet() { DataSetName = DataSetName };
            this.sasgsTableControlKey = this.dataSet.ttblsasgs.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sasgsTableControlKey))
            {
               this.CreateTableControlParameters(this.sasgsTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SasgsAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sasgsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSasgsproxy = this.proxyAppObject.CreatePO_sasgsproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSasgsproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sasgsproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sasgsTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sasgsTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sasgsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSasgsproxy = this.proxyAppObject.CreatePO_sasgsproxy())
               {
                  this.SetRequiredContextParameters();
                  poSasgsproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sasgsproxy - After Call");
      }
   

      public Sasgs GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sasgs sasgs = null;
         if (row != null)
         {
             sasgs = this.BuildFromRow(row);
         }
         return sasgs;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sasgs) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsasgs.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sasgs> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sasgs)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sasgs Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsasgs.AsEnumerable().SingleOrDefault();
         Sasgs sasgs = null;
         if (row != null)
         {
             sasgs = this.BuildFromRow(row);
         }
         return sasgs;
      }
	  
	  
      public IEnumerable<Sasgs> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sasgs.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sasgs> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsasgs.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sasgs Get(int cono, string state, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sasgs.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(state)) 
         {
             sb.AppendFormatWithEscape(" AND sasgs.state = '{0}'", state);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sasgs BuildFromRow(DataRow row)
      {
         var returnRecord = Sasgs.BuildSasgsFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sasgs>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sasgs record)
      {
         Sasgs.UpdateRowFromSasgs(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sasgs Insert(Sasgs record)
      {
         DataRow row = this.dataSet.ttblsasgs.NewttblsasgsRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsasgs.AddttblsasgsRow((pdssasgsDataSet.ttblsasgsRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsasgs.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasgs.Rows[0]) : null;
      }
  

      public Sasgs Update(Sasgs record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsasgs.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasgs.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sasgs record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsasgs.NewttblsasgsRow();
            Sasgs.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsasgs.AddttblsasgsRow((pdssasgsDataSet.ttblsasgsRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sasgs() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public SasgsLookupResponseAPI Lookup(Sastatetaxlookupcriteria sastatetaxlookupcriteria)
      {   
         var result = new SasgsLookupResponseAPI();
         
         var pdssastatetaxlookup = new pdssastatetaxlookupDataSet();
            
         DataRow ttblsastatetaxlookupcriteriaCriteria = pdssastatetaxlookup.ttblsastatetaxlookupcriteria.NewttblsastatetaxlookupcriteriaRow();
         Sastatetaxlookupcriteria.UpdateRowFromSastatetaxlookupcriteria(ref ttblsastatetaxlookupcriteriaCriteria, sastatetaxlookupcriteria);
         pdssastatetaxlookup.ttblsastatetaxlookupcriteria.AddttblsastatetaxlookupcriteriaRow((pdssastatetaxlookupDataSet.ttblsastatetaxlookupcriteriaRow)ttblsastatetaxlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Sasgs - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSasgsproxy = this.proxyAppObject.CreatePO_sasgsproxy())
               {
                   this.SetRequiredContextParameters();
                   poSasgsproxy.Lookup(ref pdsContext, ref pdssastatetaxlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Sasgs - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdssastatetaxlookup); 
    
         foreach (DataRow row in pdssastatetaxlookup.ttblsastatetaxlookupresults)
         {
            result.sastatetaxlookupresults.Add(Sastatetaxlookupresults.BuildSastatetaxlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  