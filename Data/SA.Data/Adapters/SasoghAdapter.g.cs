//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssasogh;

   public partial class SasoghAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssasogh";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sasoghTableControlKey;
		
      private pdssasoghDataSet dataSet;
        
      public SasoghAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssasoghDataSet() { DataSetName = DataSetName };
            this.sasoghTableControlKey = this.dataSet.ttblsasogh.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sasoghTableControlKey))
            {
               this.CreateTableControlParameters(this.sasoghTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SasoghAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sasoghproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSasoghproxy = this.proxyAppObject.CreatePO_sasoghproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSasoghproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sasoghproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sasoghTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sasoghTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sasoghproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSasoghproxy = this.proxyAppObject.CreatePO_sasoghproxy())
               {
                  this.SetRequiredContextParameters();
                  poSasoghproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sasoghproxy - After Call");
      }
   

      public Sasogh GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sasogh sasogh = null;
         if (row != null)
         {
             sasogh = this.BuildFromRow(row);
         }
         return sasogh;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sasogh) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsasogh.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sasogh> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sasogh)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sasogh Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsasogh.AsEnumerable().SingleOrDefault();
         Sasogh sasogh = null;
         if (row != null)
         {
             sasogh = this.BuildFromRow(row);
         }
         return sasogh;
      }
	  
	  
      public IEnumerable<Sasogh> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sasogh.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sasogh> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsasogh.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sasogh Get(int cono, string groupnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sasogh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(groupnm)) 
         {
             sb.AppendFormatWithEscape(" AND sasogh.groupnm = '{0}'", groupnm);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sasogh BuildFromRow(DataRow row)
      {
         var returnRecord = Sasogh.BuildSasoghFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sasogh>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sasogh record)
      {
         Sasogh.UpdateRowFromSasogh(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sasogh Insert(Sasogh record)
      {
         DataRow row = this.dataSet.ttblsasogh.NewttblsasoghRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsasogh.AddttblsasoghRow((pdssasoghDataSet.ttblsasoghRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsasogh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasogh.Rows[0]) : null;
      }
  

      public Sasogh Update(Sasogh record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsasogh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsasogh.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sasogh record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsasogh.NewttblsasoghRow();
            Sasogh.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsasogh.AddttblsasoghRow((pdssasoghDataSet.ttblsasoghRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sasogh() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  