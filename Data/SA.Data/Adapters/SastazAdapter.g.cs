//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssastaz;

   public partial class SastazAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssastaz";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sastazTableControlKey;
		
      private pdssastazDataSet dataSet;
        
      public SastazAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssastazDataSet() { DataSetName = DataSetName };
            this.sastazTableControlKey = this.dataSet.ttblsastaz.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sastazTableControlKey))
            {
               this.CreateTableControlParameters(this.sastazTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SastazAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sastazproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSastazproxy = this.proxyAppObject.CreatePO_sastazproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSastazproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sastazproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sastazTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sastazTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sastazproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSastazproxy = this.proxyAppObject.CreatePO_sastazproxy())
               {
                  this.SetRequiredContextParameters();
                  poSastazproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sastazproxy - After Call");
      }
   

      public Sastaz GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sastaz sastaz = null;
         if (row != null)
         {
             sastaz = this.BuildFromRow(row);
         }
         return sastaz;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sastaz) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsastaz.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sastaz> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sastaz)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sastaz Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsastaz.AsEnumerable().SingleOrDefault();
         Sastaz sastaz = null;
         if (row != null)
         {
             sastaz = this.BuildFromRow(row);
         }
         return sastaz;
      }
	  
	  
      public IEnumerable<Sastaz> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sastaz.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sastaz> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsastaz.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sastaz Get(int cono, string codeiden, string primarykey, string secondarykey, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastaz.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(codeiden)) 
         {
             sb.AppendFormatWithEscape(" AND sastaz.codeiden = '{0}'", codeiden);
         }
         if (!string.IsNullOrEmpty(primarykey)) 
         {
             sb.AppendFormatWithEscape(" AND sastaz.primarykey = '{0}'", primarykey);
         }
         if (!string.IsNullOrEmpty(secondarykey)) 
         {
             sb.AppendFormatWithEscape(" AND sastaz.secondarykey = '{0}'", secondarykey);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sastaz> GetListByLabel(int cono, bool labelfl, string codeiden, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastaz.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND sastaz.labelfl = {0}", labelfl);
         if (!string.IsNullOrEmpty(codeiden)) 
         {
             sb.AppendFormatWithEscape(" AND sastaz.codeiden = '{0}'", codeiden);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sastaz> GetListByPrimary(int cono, bool labelfl, string codeiden, string primarykey, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastaz.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND sastaz.labelfl = {0}", labelfl);
         if (!string.IsNullOrEmpty(codeiden)) 
         {
             sb.AppendFormatWithEscape(" AND sastaz.codeiden = '{0}'", codeiden);
         }
         if (!string.IsNullOrEmpty(primarykey)) 
         {
             sb.AppendFormatWithEscape(" AND sastaz.primarykey = '{0}'", primarykey);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sastaz> GetListBySecond(int cono, string codeiden, string secondarykey, string primarykey, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastaz.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(codeiden)) 
         {
             sb.AppendFormatWithEscape(" AND sastaz.codeiden = '{0}'", codeiden);
         }
         if (!string.IsNullOrEmpty(secondarykey)) 
         {
             sb.AppendFormatWithEscape(" AND sastaz.secondarykey = '{0}'", secondarykey);
         }
         if (!string.IsNullOrEmpty(primarykey)) 
         {
             sb.AppendFormatWithEscape(" AND sastaz.primarykey = '{0}'", primarykey);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sastaz BuildFromRow(DataRow row)
      {
         var returnRecord = Sastaz.BuildSastazFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sastaz>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sastaz record)
      {
         Sastaz.UpdateRowFromSastaz(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sastaz Insert(Sastaz record)
      {
         DataRow row = this.dataSet.ttblsastaz.NewttblsastazRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsastaz.AddttblsastazRow((pdssastazDataSet.ttblsastazRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsastaz.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastaz.Rows[0]) : null;
      }
  

      public Sastaz Update(Sastaz record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsastaz.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastaz.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sastaz record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsastaz.NewttblsastazRow();
            Sastaz.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsastaz.AddttblsastazRow((pdssastazDataSet.ttblsastazRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sastaz() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  