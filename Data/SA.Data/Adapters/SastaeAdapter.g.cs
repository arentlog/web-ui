//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssastae;

   public partial class SastaeAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssastae";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sastaeTableControlKey;
		
      private pdssastaeDataSet dataSet;
        
      public SastaeAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssastaeDataSet() { DataSetName = DataSetName };
            this.sastaeTableControlKey = this.dataSet.ttblsastae.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sastaeTableControlKey))
            {
               this.CreateTableControlParameters(this.sastaeTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SastaeAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sastaeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSastaeproxy = this.proxyAppObject.CreatePO_sastaeproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSastaeproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sastaeproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sastaeTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sastaeTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sastaeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSastaeproxy = this.proxyAppObject.CreatePO_sastaeproxy())
               {
                  this.SetRequiredContextParameters();
                  poSastaeproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sastaeproxy - After Call");
      }
   

      public Sastae GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sastae sastae = null;
         if (row != null)
         {
             sastae = this.BuildFromRow(row);
         }
         return sastae;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sastae) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsastae.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sastae> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sastae)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sastae Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsastae.AsEnumerable().SingleOrDefault();
         Sastae sastae = null;
         if (row != null)
         {
             sastae = this.BuildFromRow(row);
         }
         return sastae;
      }
	  
	  
      public IEnumerable<Sastae> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sastae.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sastae> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsastae.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sastae Get(int cono, string codeiden, string codeval, string codedata, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastae.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(codeiden)) 
         {
             sb.AppendFormatWithEscape(" AND sastae.codeiden = '{0}'", codeiden);
         }
         if (!string.IsNullOrEmpty(codeval)) 
         {
             sb.AppendFormatWithEscape(" AND sastae.codeval = '{0}'", codeval);
         }
         if (!string.IsNullOrEmpty(codedata)) 
         {
             sb.AppendFormatWithEscape(" AND sastae.codedata = '{0}'", codedata);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sastae> GetListByData(int cono, string codeiden, string codedata, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastae.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(codeiden)) 
         {
             sb.AppendFormatWithEscape(" AND sastae.codeiden = '{0}'", codeiden);
         }
         if (!string.IsNullOrEmpty(codedata)) 
         {
             sb.AppendFormatWithEscape(" AND sastae.codedata = '{0}'", codedata);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sastae BuildFromRow(DataRow row)
      {
         var returnRecord = Sastae.BuildSastaeFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sastae>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sastae record)
      {
         Sastae.UpdateRowFromSastae(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sastae Insert(Sastae record)
      {
         DataRow row = this.dataSet.ttblsastae.NewttblsastaeRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsastae.AddttblsastaeRow((pdssastaeDataSet.ttblsastaeRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsastae.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastae.Rows[0]) : null;
      }
  

      public Sastae Update(Sastae record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsastae.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastae.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sastae record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsastae.NewttblsastaeRow();
            Sastae.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsastae.AddttblsastaeRow((pdssastaeDataSet.ttblsastaeRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sastae() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  