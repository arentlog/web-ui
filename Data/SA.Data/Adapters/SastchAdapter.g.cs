//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssastch;

   public partial class SastchAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssastch";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sastchTableControlKey;
		
      private pdssastchDataSet dataSet;
        
      public SastchAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssastchDataSet() { DataSetName = DataSetName };
            this.sastchTableControlKey = this.dataSet.ttblsastch.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sastchTableControlKey))
            {
               this.CreateTableControlParameters(this.sastchTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SastchAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sastchproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSastchproxy = this.proxyAppObject.CreatePO_sastchproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSastchproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sastchproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sastchTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sastchTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sastchproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSastchproxy = this.proxyAppObject.CreatePO_sastchproxy())
               {
                  this.SetRequiredContextParameters();
                  poSastchproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sastchproxy - After Call");
      }
   

      public Sastch GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sastch sastch = null;
         if (row != null)
         {
             sastch = this.BuildFromRow(row);
         }
         return sastch;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sastch) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsastch.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sastch> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sastch)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sastch Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsastch.AsEnumerable().SingleOrDefault();
         Sastch sastch = null;
         if (row != null)
         {
             sastch = this.BuildFromRow(row);
         }
         return sastch;
      }
	  
	  
      public IEnumerable<Sastch> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("sastch.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sastch> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsastch.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sastch Get(int cono, string currencyty, string changety, DateTime? changedt, string changetm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastch.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(currencyty)) 
         {
             sb.AppendFormatWithEscape(" AND sastch.currencyty = '{0}'", currencyty);
         }
         if (!string.IsNullOrEmpty(changety)) 
         {
             sb.AppendFormatWithEscape(" AND sastch.changety = '{0}'", changety);
         }
         if (changedt != null) 
         {
             sb.AppendFormatWithEscape(" AND sastch.changedt = '{0}'", changedt);
         }
         if (!string.IsNullOrEmpty(changetm)) 
         {
             sb.AppendFormatWithEscape(" AND sastch.changetm = '{0}'", changetm);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Sastch> GetListByChangedt(int cono, string currencyty, DateTime? changedt, string changetm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastch.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(currencyty)) 
         {
             sb.AppendFormatWithEscape(" AND sastch.currencyty = '{0}'", currencyty);
         }
         if (changedt != null) 
         {
             sb.AppendFormatWithEscape(" AND sastch.changedt = '{0}'", changedt);
         }
         if (!string.IsNullOrEmpty(changetm)) 
         {
             sb.AppendFormatWithEscape(" AND sastch.changetm = '{0}'", changetm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Sastch> GetListByChangety(int cono, string changety, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("sastch.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(changety)) 
         {
             sb.AppendFormatWithEscape(" AND sastch.changety = '{0}'", changety);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Sastch BuildFromRow(DataRow row)
      {
         var returnRecord = Sastch.BuildSastchFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sastch>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sastch record)
      {
         Sastch.UpdateRowFromSastch(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sastch Insert(Sastch record)
      {
         DataRow row = this.dataSet.ttblsastch.NewttblsastchRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsastch.AddttblsastchRow((pdssastchDataSet.ttblsastchRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsastch.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastch.Rows[0]) : null;
      }
  

      public Sastch Update(Sastch record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsastch.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsastch.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sastch record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsastch.NewttblsastchRow();
            Sastch.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsastch.AddttblsastchRow((pdssastchDataSet.ttblsastchRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sastch() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  