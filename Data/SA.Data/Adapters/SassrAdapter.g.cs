//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.SA.Data.Adapters
{
   using com.infor.sxproxy.saproxy;
   using com.infor.sxproxy.saproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdssassr;

   public partial class SassrAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssassr";
      private SAProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sassrTableControlKey;
		
      private pdssassrDataSet dataSet;
        
      public SassrAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new SAProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssassrDataSet() { DataSetName = DataSetName };
            this.sassrTableControlKey = this.dataSet.ttblsassr.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sassrTableControlKey))
            {
               this.CreateTableControlParameters(this.sassrTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SassrAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Sassrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSassrproxy = this.proxyAppObject.CreatePO_sassrproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSassrproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Sassrproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sassrTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sassrTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Sassrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSassrproxy = this.proxyAppObject.CreatePO_sassrproxy())
               {
                  this.SetRequiredContextParameters();
                  poSassrproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Sassrproxy - After Call");
      }
   

      public Sassr GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Sassr sassr = null;
         if (row != null)
         {
             sassr = this.BuildFromRow(row);
         }
         return sassr;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(sassr) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsassr.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Sassr> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(sassr)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Sassr Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsassr.AsEnumerable().SingleOrDefault();
         Sassr sassr = null;
         if (row != null)
         {
             sassr = this.BuildFromRow(row);
         }
         return sassr;
      }
	  
	  

      public IEnumerable<Sassr> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsassr.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Sassr Get(string currproc, string trmgrlang, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(currproc)) 
         {
             sb.AppendFormatWithEscape("sassr.currproc = '{0}'", currproc);
         }
         if (!string.IsNullOrEmpty(trmgrlang)) 
         {
             sb.AppendFormatWithEscape(" AND sassr.trmgrlang = '{0}'", trmgrlang);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Sassr BuildFromRow(DataRow row)
      {
         var returnRecord = Sassr.BuildSassrFromRow(row);
         returnRecord = this.BuildExtraFromRow<Sassr>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Sassr record)
      {
         Sassr.UpdateRowFromSassr(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Sassr Insert(Sassr record)
      {
         DataRow row = this.dataSet.ttblsassr.NewttblsassrRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsassr.AddttblsassrRow((pdssassrDataSet.ttblsassrRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsassr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsassr.Rows[0]) : null;
      }
  

      public Sassr Update(Sassr record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsassr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsassr.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Sassr record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsassr.NewttblsassrRow();
            Sassr.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsassr.AddttblsassrRow((pdssassrDataSet.ttblsassrRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Sassr() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  