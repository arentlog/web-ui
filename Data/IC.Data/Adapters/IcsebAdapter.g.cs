//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicseb;

   public partial class IcsebAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicseb";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsebTableControlKey;
		
      private pdsicsebDataSet dataSet;
        
      public IcsebAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsebDataSet() { DataSetName = DataSetName };
            this.icsebTableControlKey = this.dataSet.ttblicseb.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsebTableControlKey))
            {
               this.CreateTableControlParameters(this.icsebTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsebAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsebproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsebproxy = this.proxyAppObject.CreatePO_icsebproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsebproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsebproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsebTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsebTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsebproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsebproxy = this.proxyAppObject.CreatePO_icsebproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsebproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsebproxy - After Call");
      }
   

      public Icseb GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icseb icseb = null;
         if (row != null)
         {
             icseb = this.BuildFromRow(row);
         }
         return icseb;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icseb) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicseb.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icseb> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icseb)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icseb Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicseb.AsEnumerable().SingleOrDefault();
         Icseb icseb = null;
         if (row != null)
         {
             icseb = this.BuildFromRow(row);
         }
         return icseb;
      }
	  
	  
      public IEnumerable<Icseb> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icseb.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icseb> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicseb.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icseb Get(int cono, string whse, string prod, string bundleid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icseb.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icseb.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icseb.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(bundleid)) 
         {
             sb.AppendFormatWithEscape(" AND icseb.bundleid = '{0}'", bundleid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icseb> GetListByProd(int cono, string whse, string prod, string bundlestatus, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icseb.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icseb.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icseb.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(bundlestatus)) 
         {
             sb.AppendFormatWithEscape(" AND icseb.bundlestatus = '{0}'", bundlestatus);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icseb BuildFromRow(DataRow row)
      {
         var returnRecord = Icseb.BuildIcsebFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icseb>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icseb record)
      {
         Icseb.UpdateRowFromIcseb(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icseb Insert(Icseb record)
      {
         DataRow row = this.dataSet.ttblicseb.NewttblicsebRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicseb.AddttblicsebRow((pdsicsebDataSet.ttblicsebRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicseb.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseb.Rows[0]) : null;
      }
  

      public Icseb Update(Icseb record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicseb.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseb.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icseb record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicseb.NewttblicsebRow();
            Icseb.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicseb.AddttblicsebRow((pdsicsebDataSet.ttblicsebRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icseb() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  