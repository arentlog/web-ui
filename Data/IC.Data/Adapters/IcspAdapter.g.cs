//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsp;
   using Models.Pdsicproductlookup;
   using Models.Complex;

   public partial class IcspAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsp";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icspTableControlKey;
		private readonly Tuple<string, string> icssTableControlKey;
      private pdsicspDataSet dataSet;
        
      public IcspAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicspDataSet() { DataSetName = DataSetName };
            this.icspTableControlKey = this.dataSet.ttblicsp.GetTableControlParametersKey();
            this.icssTableControlKey = this.dataSet.ttblicss.GetTableControlParametersKey();
            if (!this.tempTableControlParameters.ContainsKey(this.icspTableControlKey))
            {
               this.CreateTableControlParameters(this.icspTableControlKey);
            }
            if (!this.tempTableControlParameters.ContainsKey(this.icssTableControlKey))
            {
               this.CreateTableControlParameters(this.icssTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcspAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, bool fillMode, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icspproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, fillMode, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcspproxy = this.proxyAppObject.CreatePO_icspproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcspproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icspproxy - After Call");
      }
      
      private void SetAndFetch(string where, bool fillMode, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icspTableControlKey, where, batchsize, fldList);
         this.SetFetchWhereParameters(this.icssTableControlKey, fillMode);
         this.SetTableParametersOnContext(this.icspTableControlKey, true);
         this.SetTableParametersOnContext(this.icssTableControlKey);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icspproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcspproxy = this.proxyAppObject.CreatePO_icspproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcspproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icspproxy - After Call");
      }
   

      public Icsp GetByRowId(string rowId, bool fillMode, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fillMode, fldList);
         Icsp icsp = null;
         if (row != null)
         {
             icsp = this.BuildFromRow(row);
         }
         return icsp;
      }
   
      private DataRow GetRowByRowId(string rowId, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsp) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), fillMode, 1, fldList);
         return this.dataSet.ttblicsp.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsp> GetListByRowIdList(List<string> rowIds, bool fillMode, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsp)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), fillMode, batchsize, fldList);
      }

      protected Icsp Fetch(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         var row = this.dataSet.ttblicsp.AsEnumerable().SingleOrDefault();
         Icsp icsp = null;
         if (row != null)
         {
             icsp = this.BuildFromRow(row);
         }
         return icsp;
      }
	  
	  
	  public IEnumerable<Icsp> GetListByRowpointers(List<string> rowpointers, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("icsp.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), fillMode, rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("icsp.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), fillMode, 1, fldList);
         return this.dataSet.ttblicsp.AsEnumerable().SingleOrDefault();
      }
      public IEnumerable<Icsp> GetListAllByCono(int cono, bool fillMode, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icsp.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<Icsp> GetList(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsp.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsp Get(int cono, string prod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icsp.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.Fetch(where, fillMode, batchsize, fldList);
      }
  
      public IEnumerable<Icsp> GetListByDirtycore(int cono, string dirtycoreprod, string prod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(dirtycoreprod)) 
         {
             sb.AppendFormatWithEscape(" AND icsp.dirtycoreprod = '{0}'", dirtycoreprod);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icsp.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<Icsp> GetListByImpliedcore(int cono, string impliedcoreprod, string prod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(impliedcoreprod)) 
         {
             sb.AppendFormatWithEscape(" AND icsp.impliedcoreprod = '{0}'", impliedcoreprod);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icsp.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<Icsp> GetListByKit(int cono, string kittype, string statustype, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(kittype)) 
         {
             sb.AppendFormatWithEscape(" AND icsp.kittype = '{0}'", kittype);
         }
         if (!string.IsNullOrEmpty(statustype)) 
         {
             sb.AppendFormatWithEscape(" AND icsp.statustype = '{0}'", statustype);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<Icsp> GetListByLkup(int cono, string lookupnm, int seqno, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(lookupnm)) 
         {
             sb.AppendFormatWithEscape(" AND icsp.lookupnm = '{0}'", lookupnm);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsp.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public Icsp GetByRowpointer(string rowpointer, bool fillMode, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("icsp.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, fillMode, 1, fldList);
      }

      public IEnumerable<Icsp> GetListByTransdttmz(DateTime? transdttmz, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (transdttmz != null) 
         {
             sb.AppendFormatWithEscape("icsp.transdttmz = '{0}'", transdttmz);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public Icsp BuildFromRow(DataRow row)
      {
         var returnRecord = Icsp.BuildIcspFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsp>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsp record)
      {
         Icsp.UpdateRowFromIcsp(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsp Insert(Icsp record)
      {
         DataRow row = this.dataSet.ttblicsp.NewttblicspRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsp.AddttblicspRow((pdsicspDataSet.ttblicspRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsp.Rows[0]) : null;
      }
  

      public Icsp Update(Icsp record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, false, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsp.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsp record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, false, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsp.NewttblicspRow();
            Icsp.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsp.AddttblicspRow((pdsicspDataSet.ttblicspRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Icsp record)
      {
         var row = this.GetRowByRowId(record.rowID, false, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttblicsp.NewttblicspRow();
            Icsp.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsp.AddttblicspRow((pdsicspDataSet.ttblicspRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsp() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Icsp() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      public IcspLookupResponseAPI Lookup(Icproductlookupcriteria icproductlookupcriteria)
      {   
         var result = new IcspLookupResponseAPI();
         
         var pdsicproductlookup = new pdsicproductlookupDataSet();
            
         DataRow ttblicproductlookupcriteriaCriteria = pdsicproductlookup.ttblicproductlookupcriteria.NewttblicproductlookupcriteriaRow();
         Icproductlookupcriteria.UpdateRowFromIcproductlookupcriteria(ref ttblicproductlookupcriteriaCriteria, icproductlookupcriteria);
         pdsicproductlookup.ttblicproductlookupcriteria.AddttblicproductlookupcriteriaRow((pdsicproductlookupDataSet.ttblicproductlookupcriteriaRow)ttblicproductlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Icsp - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcspproxy = this.proxyAppObject.CreatePO_icspproxy())
               {
                   this.SetRequiredContextParameters();
                   poIcspproxy.Lookup(ref pdsContext, ref pdsicproductlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Icsp - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsicproductlookup); 
    
         foreach (DataRow row in pdsicproductlookup.ttblicproductlookupresults)
         {
            result.icproductlookupresults.Add(Icproductlookupresults.BuildIcproductlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      public IEnumerable<Icsp> GetListByWordIndex(string searchTerm, bool fillMode, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         where.AppendKeywords(string.Empty, searchTerm, true, true, false);
         this.SetAndFetch(where.ToString(), fillMode, batchsize, fldList);
         this.SearchWordIndex(batchsize == 0 ? DefaultFetchWhereRowCount : batchsize , where.ToString());
         return (from DataRow row in this.dataSet.ttblicsp.Rows select this.BuildFromRow(row)).ToList();
      }
  
      protected void SearchWordIndex(int batchSize, string searchTerm)
      {
         NLogLoggerP.Trace("SearchWordIndex - Icsp - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcspProxy = this.proxyAppObject.CreatePO_icspproxy())
               {
                   this.SetRequiredContextParameters();
                   poIcspProxy.SearchWordIndex(batchSize, searchTerm, ref this.pdsContext, out this.dataSet, out string cErrorMessage, out bool moreRecords);
                   ErrorReportingHelper.ReportErrors(cErrorMessage);
                   this.ReportErrors(this.pdsContext);
                   this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SearchWordIndex - Icsp - After Call");
      }
  
      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  