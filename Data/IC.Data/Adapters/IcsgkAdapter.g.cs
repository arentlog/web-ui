//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsgk;

   public partial class IcsgkAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsgk";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsgkTableControlKey;
		
      private pdsicsgkDataSet dataSet;
        
      public IcsgkAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsgkDataSet() { DataSetName = DataSetName };
            this.icsgkTableControlKey = this.dataSet.ttblicsgk.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsgkTableControlKey))
            {
               this.CreateTableControlParameters(this.icsgkTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsgkAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsgkproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsgkproxy = this.proxyAppObject.CreatePO_icsgkproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsgkproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsgkproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsgkTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsgkTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsgkproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsgkproxy = this.proxyAppObject.CreatePO_icsgkproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsgkproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsgkproxy - After Call");
      }
   

      public Icsgk GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsgk icsgk = null;
         if (row != null)
         {
             icsgk = this.BuildFromRow(row);
         }
         return icsgk;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsgk) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsgk.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsgk> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsgk)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsgk Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsgk.AsEnumerable().SingleOrDefault();
         Icsgk icsgk = null;
         if (row != null)
         {
             icsgk = this.BuildFromRow(row);
         }
         return icsgk;
      }
	  
	  

      public IEnumerable<Icsgk> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsgk.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsgk Get(string tag, int corpid, int storeid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(tag)) 
         {
             sb.AppendFormatWithEscape("icsgk.tag = '{0}'", tag);
         }
         if (corpid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.corpid = {0}", corpid);
         }
         if (storeid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.storeid = {0}", storeid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icsgk> GetListByBatch(string ecbatchnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(ecbatchnm)) 
         {
             sb.AppendFormatWithEscape("icsgk.ecbatchnm = '{0}'", ecbatchnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgk> GetListByGav(string gav, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(gav)) 
         {
             sb.AppendFormatWithEscape("icsgk.gav = '{0}'", gav);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgk> GetListByLevel(int corpid, string level1, string level2, string level3, string level4, string level5, string level6, string level7, string level8, string level9, string level10, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (corpid != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgk.corpid = {0}", corpid);
         }
         if (!string.IsNullOrEmpty(level1)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level1 = '{0}'", level1);
         }
         if (!string.IsNullOrEmpty(level2)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level2 = '{0}'", level2);
         }
         if (!string.IsNullOrEmpty(level3)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level3 = '{0}'", level3);
         }
         if (!string.IsNullOrEmpty(level4)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level4 = '{0}'", level4);
         }
         if (!string.IsNullOrEmpty(level5)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level5 = '{0}'", level5);
         }
         if (!string.IsNullOrEmpty(level6)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level6 = '{0}'", level6);
         }
         if (!string.IsNullOrEmpty(level7)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level7 = '{0}'", level7);
         }
         if (!string.IsNullOrEmpty(level8)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level8 = '{0}'", level8);
         }
         if (!string.IsNullOrEmpty(level9)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level9 = '{0}'", level9);
         }
         if (!string.IsNullOrEmpty(level10)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.level10 = '{0}'", level10);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgk> GetListByNodeid(int nodeid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (nodeid != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgk.nodeid = {0}", nodeid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgk> GetListByReferencePcat(string referencePcat, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(referencePcat)) 
         {
             sb.AppendFormatWithEscape("icsgk.reference-pcat = '{0}'", referencePcat);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgk> GetListByReferenceProd(string referenceProd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(referenceProd)) 
         {
             sb.AppendFormatWithEscape("icsgk.reference-prod = '{0}'", referenceProd);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgk> GetListByStoreid(int corpid, int storeid, string gav, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (corpid != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgk.corpid = {0}", corpid);
         }
         if (storeid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.storeid = {0}", storeid);
         }
         if (!string.IsNullOrEmpty(gav)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.gav = '{0}'", gav);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgk> GetListByTagno(string tagType, int corpid, int storeid, int tagno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(tagType)) 
         {
             sb.AppendFormatWithEscape("icsgk.tag-type = '{0}'", tagType);
         }
         if (corpid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.corpid = {0}", corpid);
         }
         if (storeid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.storeid = {0}", storeid);
         }
         if (tagno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.tagno = {0}", tagno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgk> GetListByType(string tagType, int corpid, int storeid, string tag, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(tagType)) 
         {
             sb.AppendFormatWithEscape("icsgk.tag-type = '{0}'", tagType);
         }
         if (corpid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.corpid = {0}", corpid);
         }
         if (storeid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.storeid = {0}", storeid);
         }
         if (!string.IsNullOrEmpty(tag)) 
         {
             sb.AppendFormatWithEscape(" AND icsgk.tag = '{0}'", tag);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icsgk BuildFromRow(DataRow row)
      {
         var returnRecord = Icsgk.BuildIcsgkFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsgk>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsgk record)
      {
         Icsgk.UpdateRowFromIcsgk(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsgk Insert(Icsgk record)
      {
         DataRow row = this.dataSet.ttblicsgk.NewttblicsgkRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsgk.AddttblicsgkRow((pdsicsgkDataSet.ttblicsgkRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsgk.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgk.Rows[0]) : null;
      }
  

      public Icsgk Update(Icsgk record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsgk.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgk.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsgk record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsgk.NewttblicsgkRow();
            Icsgk.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsgk.AddttblicsgkRow((pdsicsgkDataSet.ttblicsgkRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsgk() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  