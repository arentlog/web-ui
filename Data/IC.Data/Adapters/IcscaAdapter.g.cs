//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsca;

   public partial class IcscaAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsca";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icscaTableControlKey;
		
      private pdsicscaDataSet dataSet;
        
      public IcscaAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicscaDataSet() { DataSetName = DataSetName };
            this.icscaTableControlKey = this.dataSet.ttblicsca.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icscaTableControlKey))
            {
               this.CreateTableControlParameters(this.icscaTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcscaAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icscaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcscaproxy = this.proxyAppObject.CreatePO_icscaproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcscaproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icscaproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icscaTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icscaTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icscaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcscaproxy = this.proxyAppObject.CreatePO_icscaproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcscaproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icscaproxy - After Call");
      }
   

      public Icsca GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsca icsca = null;
         if (row != null)
         {
             icsca = this.BuildFromRow(row);
         }
         return icsca;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsca) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsca.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsca> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsca)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsca Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsca.AsEnumerable().SingleOrDefault();
         Icsca icsca = null;
         if (row != null)
         {
             icsca = this.BuildFromRow(row);
         }
         return icsca;
      }
	  
	  

      public IEnumerable<Icsca> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsca.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsca Get(int cono, string catalog, string authgrp, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (cono != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsca.Cono = {0}", cono);
         }
         if (!string.IsNullOrEmpty(catalog)) 
         {
             sb.AppendFormatWithEscape(" AND icsca.catalog = '{0}'", catalog);
         }
         if (!string.IsNullOrEmpty(authgrp)) 
         {
             sb.AppendFormatWithEscape(" AND icsca.authgrp = '{0}'", authgrp);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icsca> GetListByAuthgrp(int cono, string authgrp, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (cono != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsca.Cono = {0}", cono);
         }
         if (!string.IsNullOrEmpty(authgrp)) 
         {
             sb.AppendFormatWithEscape(" AND icsca.authgrp = '{0}'", authgrp);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsca> GetListByBatch(string ecbatchnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(ecbatchnm)) 
         {
             sb.AppendFormatWithEscape("icsca.ecbatchnm = '{0}'", ecbatchnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icsca BuildFromRow(DataRow row)
      {
         var returnRecord = Icsca.BuildIcscaFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsca>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsca record)
      {
         Icsca.UpdateRowFromIcsca(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsca Insert(Icsca record)
      {
         DataRow row = this.dataSet.ttblicsca.NewttblicscaRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsca.AddttblicscaRow((pdsicscaDataSet.ttblicscaRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsca.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsca.Rows[0]) : null;
      }
  

      public Icsca Update(Icsca record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsca.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsca.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsca record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsca.NewttblicscaRow();
            Icsca.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsca.AddttblicscaRow((pdsicscaDataSet.ttblicscaRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsca() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  