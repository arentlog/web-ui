//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicamap;

   public partial class IcamapAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicamap";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icamapTableControlKey;
		private readonly Tuple<string, string> icamap_addfldsTableControlKey;
      private pdsicamapDataSet dataSet;
        
      public IcamapAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicamapDataSet() { DataSetName = DataSetName };
            this.icamapTableControlKey = this.dataSet.ttblicamap.GetTableControlParametersKey();
            this.icamap_addfldsTableControlKey = this.dataSet.ttblicamap_addflds.GetTableControlParametersKey();
            if (!this.tempTableControlParameters.ContainsKey(this.icamapTableControlKey))
            {
               this.CreateTableControlParameters(this.icamapTableControlKey);
            }
            if (!this.tempTableControlParameters.ContainsKey(this.icamap_addfldsTableControlKey))
            {
               this.CreateTableControlParameters(this.icamap_addfldsTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcamapAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, bool fillMode, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icamapproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, fillMode, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcamapproxy = this.proxyAppObject.CreatePO_icamapproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcamapproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icamapproxy - After Call");
      }
      
      private void SetAndFetch(string where, bool fillMode, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icamapTableControlKey, where, batchsize, fldList);
         this.SetFetchWhereParameters(this.icamap_addfldsTableControlKey, fillMode);
         this.SetTableParametersOnContext(this.icamapTableControlKey, true);
         this.SetTableParametersOnContext(this.icamap_addfldsTableControlKey);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icamapproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcamapproxy = this.proxyAppObject.CreatePO_icamapproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcamapproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icamapproxy - After Call");
      }
   

      public Icamap GetByRowId(string rowId, bool fillMode, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fillMode, fldList);
         Icamap icamap = null;
         if (row != null)
         {
             icamap = this.BuildFromRow(row);
         }
         return icamap;
      }
   
      private DataRow GetRowByRowId(string rowId, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icamap) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), fillMode, 1, fldList);
         return this.dataSet.ttblicamap.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icamap> GetListByRowIdList(List<string> rowIds, bool fillMode, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icamap)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), fillMode, batchsize, fldList);
      }

      protected Icamap Fetch(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         var row = this.dataSet.ttblicamap.AsEnumerable().SingleOrDefault();
         Icamap icamap = null;
         if (row != null)
         {
             icamap = this.BuildFromRow(row);
         }
         return icamap;
      }
	  
	  
      public IEnumerable<Icamap> GetListAllByCono(int cono, bool fillMode, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icamap.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<Icamap> GetList(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicamap.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icamap Get(int cono, int reportno, string whse, string prod, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icamap.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (reportno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icamap.reportno = {0}", reportno);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icamap.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icamap.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.Fetch(where, fillMode, batchsize, fldList);
      }
  
      public IEnumerable<Icamap> GetListByProd(int cono, int reportno, string prod, string whse, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icamap.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (reportno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icamap.reportno = {0}", reportno);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icamap.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icamap.whse = '{0}'", whse);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public Icamap BuildFromRow(DataRow row)
      {
         var returnRecord = Icamap.BuildIcamapFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icamap>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icamap record)
      {
         Icamap.UpdateRowFromIcamap(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icamap Insert(Icamap record)
      {
         DataRow row = this.dataSet.ttblicamap.NewttblicamapRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicamap.AddttblicamapRow((pdsicamapDataSet.ttblicamapRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicamap.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicamap.Rows[0]) : null;
      }
  

      public Icamap Update(Icamap record)
      {
        var row = this.GetRowByRowId(record.rowID, false, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicamap.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicamap.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icamap record)
      {
         var row = this.GetRowByRowId(record.rowID, false, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicamap.NewttblicamapRow();
            Icamap.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicamap.AddttblicamapRow((pdsicamapDataSet.ttblicamapRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icamap() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  