//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsgn;

   public partial class IcsgnAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsgn";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsgnTableControlKey;
		
      private pdsicsgnDataSet dataSet;
        
      public IcsgnAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsgnDataSet() { DataSetName = DataSetName };
            this.icsgnTableControlKey = this.dataSet.ttblicsgn.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsgnTableControlKey))
            {
               this.CreateTableControlParameters(this.icsgnTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsgnAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsgnproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsgnproxy = this.proxyAppObject.CreatePO_icsgnproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsgnproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsgnproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsgnTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsgnTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsgnproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsgnproxy = this.proxyAppObject.CreatePO_icsgnproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsgnproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsgnproxy - After Call");
      }
   

      public Icsgn GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsgn icsgn = null;
         if (row != null)
         {
             icsgn = this.BuildFromRow(row);
         }
         return icsgn;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsgn) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsgn.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsgn> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsgn)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsgn Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsgn.AsEnumerable().SingleOrDefault();
         Icsgn icsgn = null;
         if (row != null)
         {
             icsgn = this.BuildFromRow(row);
         }
         return icsgn;
      }
	  
	  

      public IEnumerable<Icsgn> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsgn.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsgn Get(int nodeid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (nodeid != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgn.nodeid = {0}", nodeid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icsgn> GetListByBatch(string ecbatchnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(ecbatchnm)) 
         {
             sb.AppendFormatWithEscape("icsgn.ecbatchnm = '{0}'", ecbatchnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgn> GetListByGav(string gav, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(gav)) 
         {
             sb.AppendFormatWithEscape("icsgn.gav = '{0}'", gav);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgn> GetListByGroup(int groupno, string gav, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (groupno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgn.groupno = {0}", groupno);
         }
         if (!string.IsNullOrEmpty(gav)) 
         {
             sb.AppendFormatWithEscape(" AND icsgn.gav = '{0}'", gav);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgn> GetListByNodekey(string nodekey, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(nodekey)) 
         {
             sb.AppendFormatWithEscape("icsgn.nodekey = '{0}'", nodekey);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgn> GetListByStoreid(int corpid, int storeid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (corpid != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgn.corpid = {0}", corpid);
         }
         if (storeid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgn.storeid = {0}", storeid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icsgn BuildFromRow(DataRow row)
      {
         var returnRecord = Icsgn.BuildIcsgnFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsgn>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsgn record)
      {
         Icsgn.UpdateRowFromIcsgn(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsgn Insert(Icsgn record)
      {
         DataRow row = this.dataSet.ttblicsgn.NewttblicsgnRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsgn.AddttblicsgnRow((pdsicsgnDataSet.ttblicsgnRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsgn.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgn.Rows[0]) : null;
      }
  

      public Icsgn Update(Icsgn record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsgn.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgn.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsgn record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsgn.NewttblicsgnRow();
            Icsgn.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsgn.AddttblicsgnRow((pdsicsgnDataSet.ttblicsgnRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsgn() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  