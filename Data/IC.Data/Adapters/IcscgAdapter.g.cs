//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicscg;

   public partial class IcscgAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicscg";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icscgTableControlKey;
		
      private pdsicscgDataSet dataSet;
        
      public IcscgAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicscgDataSet() { DataSetName = DataSetName };
            this.icscgTableControlKey = this.dataSet.ttblicscg.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icscgTableControlKey))
            {
               this.CreateTableControlParameters(this.icscgTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcscgAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icscgproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcscgproxy = this.proxyAppObject.CreatePO_icscgproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcscgproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icscgproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icscgTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icscgTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icscgproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcscgproxy = this.proxyAppObject.CreatePO_icscgproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcscgproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icscgproxy - After Call");
      }
   

      public Icscg GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icscg icscg = null;
         if (row != null)
         {
             icscg = this.BuildFromRow(row);
         }
         return icscg;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icscg) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicscg.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icscg> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icscg)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icscg Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicscg.AsEnumerable().SingleOrDefault();
         Icscg icscg = null;
         if (row != null)
         {
             icscg = this.BuildFromRow(row);
         }
         return icscg;
      }
	  
	  

      public IEnumerable<Icscg> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicscg.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icscg Get(string catalog, int groupno, int attrno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(catalog)) 
         {
             sb.AppendFormatWithEscape("icscg.catalog = '{0}'", catalog);
         }
         if (groupno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icscg.groupno = {0}", groupno);
         }
         if (attrno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icscg.attrno = {0}", attrno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icscg> GetListByBatch(string ecbatchnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(ecbatchnm)) 
         {
             sb.AppendFormatWithEscape("icscg.ecbatchnm = '{0}'", ecbatchnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icscg> GetListByGroupno(int groupno, int attrno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (groupno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icscg.groupno = {0}", groupno);
         }
         if (attrno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icscg.attrno = {0}", attrno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icscg BuildFromRow(DataRow row)
      {
         var returnRecord = Icscg.BuildIcscgFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icscg>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icscg record)
      {
         Icscg.UpdateRowFromIcscg(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icscg Insert(Icscg record)
      {
         DataRow row = this.dataSet.ttblicscg.NewttblicscgRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicscg.AddttblicscgRow((pdsicscgDataSet.ttblicscgRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicscg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicscg.Rows[0]) : null;
      }
  

      public Icscg Update(Icscg record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicscg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicscg.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icscg record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicscg.NewttblicscgRow();
            Icscg.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicscg.AddttblicscgRow((pdsicscgDataSet.ttblicscgRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icscg() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  