//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicetc;

   public partial class IcetcAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicetc";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icetcTableControlKey;
		
      private pdsicetcDataSet dataSet;
        
      public IcetcAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicetcDataSet() { DataSetName = DataSetName };
            this.icetcTableControlKey = this.dataSet.ttblicetc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icetcTableControlKey))
            {
               this.CreateTableControlParameters(this.icetcTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcetcAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icetcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcetcproxy = this.proxyAppObject.CreatePO_icetcproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcetcproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icetcproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icetcTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icetcTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icetcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcetcproxy = this.proxyAppObject.CreatePO_icetcproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcetcproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icetcproxy - After Call");
      }
   

      public Icetc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icetc icetc = null;
         if (row != null)
         {
             icetc = this.BuildFromRow(row);
         }
         return icetc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icetc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicetc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icetc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icetc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icetc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicetc.AsEnumerable().SingleOrDefault();
         Icetc icetc = null;
         if (row != null)
         {
             icetc = this.BuildFromRow(row);
         }
         return icetc;
      }
	  
	  
      public IEnumerable<Icetc> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icetc.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icetc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicetc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icetc Get(int cono, string whse, string prod, DateTime? postdt, string transtype, int icseqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icetc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.prod = '{0}'", prod);
         }
         if (postdt != null) 
         {
             sb.AppendFormatWithEscape(" AND icetc.postdt = '{0}'", postdt);
         }
         if (!string.IsNullOrEmpty(transtype)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.transtype = '{0}'", transtype);
         }
         if (icseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icetc.icseqno = {0}", icseqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icetc> GetListByCustprod(int cono, string whse, string custprod, DateTime? postdt, string transtype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icetc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(custprod)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.custprod = '{0}'", custprod);
         }
         if (postdt != null) 
         {
             sb.AppendFormatWithEscape(" AND icetc.postdt = '{0}'", postdt);
         }
         if (!string.IsNullOrEmpty(transtype)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.transtype = '{0}'", transtype);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icetc> GetListByJrnl(int cono, int jrnlno, int setno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icetc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (jrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icetc.jrnlno = {0}", jrnlno);
         }
         if (setno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icetc.setno = {0}", setno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icetc> GetListByOrdno(int cono, string module, int orderno, int ordersuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icetc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(module)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.module = '{0}'", module);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icetc.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icetc.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icetc.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icetc> GetListByProd(int cono, string prod, string whse, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icetc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.whse = '{0}'", whse);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icetc> GetListByUsgwhse(int cono, bool usagefl, bool mergedfl, string usagewhse, string usageprod, DateTime? postdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icetc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND icetc.usagefl = {0}", usagefl);
         sb.AppendFormatWithEscape(" AND icetc.mergedfl = {0}", mergedfl);
         if (!string.IsNullOrEmpty(usagewhse)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.usagewhse = '{0}'", usagewhse);
         }
         if (!string.IsNullOrEmpty(usageprod)) 
         {
             sb.AppendFormatWithEscape(" AND icetc.usageprod = '{0}'", usageprod);
         }
         if (postdt != null) 
         {
             sb.AppendFormatWithEscape(" AND icetc.postdt = '{0}'", postdt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icetc BuildFromRow(DataRow row)
      {
         var returnRecord = Icetc.BuildIcetcFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icetc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icetc record)
      {
         Icetc.UpdateRowFromIcetc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icetc Insert(Icetc record)
      {
         DataRow row = this.dataSet.ttblicetc.NewttblicetcRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicetc.AddttblicetcRow((pdsicetcDataSet.ttblicetcRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicetc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicetc.Rows[0]) : null;
      }
  

      public Icetc Update(Icetc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicetc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicetc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icetc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicetc.NewttblicetcRow();
            Icetc.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicetc.AddttblicetcRow((pdsicetcDataSet.ttblicetcRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icetc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  