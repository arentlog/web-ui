//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsiceaa;

   public partial class IceaaAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsiceaa";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> iceaaTableControlKey;
		
      private pdsiceaaDataSet dataSet;
        
      public IceaaAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsiceaaDataSet() { DataSetName = DataSetName };
            this.iceaaTableControlKey = this.dataSet.ttbliceaa.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.iceaaTableControlKey))
            {
               this.CreateTableControlParameters(this.iceaaTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IceaaAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Iceaaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIceaaproxy = this.proxyAppObject.CreatePO_iceaaproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIceaaproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Iceaaproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.iceaaTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.iceaaTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Iceaaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIceaaproxy = this.proxyAppObject.CreatePO_iceaaproxy())
               {
                  this.SetRequiredContextParameters();
                  poIceaaproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Iceaaproxy - After Call");
      }
   

      public Iceaa GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Iceaa iceaa = null;
         if (row != null)
         {
             iceaa = this.BuildFromRow(row);
         }
         return iceaa;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(iceaa) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbliceaa.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Iceaa> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(iceaa)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Iceaa Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbliceaa.AsEnumerable().SingleOrDefault();
         Iceaa iceaa = null;
         if (row != null)
         {
             iceaa = this.BuildFromRow(row);
         }
         return iceaa;
      }
	  
	  
      public IEnumerable<Iceaa> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("iceaa.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Iceaa> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbliceaa.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Iceaa Get(int cono, string transty, int orderno, int ordersuf, int lineno, int retorderno, int retordersuf, int retlineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceaa.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.transty = '{0}'", transty);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.lineno = {0}", lineno);
         }
         if (retorderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.retorderno = {0}", retorderno);
         }
         if (retordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.retordersuf = {0}", retordersuf);
         }
         if (retlineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.retlineno = {0}", retlineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Iceaa> GetListByProd(int cono, string prod, string transty, int orderno, int ordersuf, int lineno, int retorderno, int retordersuf, int retlineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceaa.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.transty = '{0}'", transty);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.lineno = {0}", lineno);
         }
         if (retorderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.retorderno = {0}", retorderno);
         }
         if (retordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.retordersuf = {0}", retordersuf);
         }
         if (retlineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.retlineno = {0}", retlineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Iceaa> GetListByReturn(int cono, string transty, int retorderno, int retordersuf, int retlineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceaa.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.transty = '{0}'", transty);
         }
         if (retorderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.retorderno = {0}", retorderno);
         }
         if (retordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.retordersuf = {0}", retordersuf);
         }
         if (retlineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceaa.retlineno = {0}", retlineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Iceaa BuildFromRow(DataRow row)
      {
         var returnRecord = Iceaa.BuildIceaaFromRow(row);
         returnRecord = this.BuildExtraFromRow<Iceaa>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Iceaa record)
      {
         Iceaa.UpdateRowFromIceaa(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Iceaa Insert(Iceaa record)
      {
         DataRow row = this.dataSet.ttbliceaa.NewttbliceaaRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbliceaa.AddttbliceaaRow((pdsiceaaDataSet.ttbliceaaRow)row);
         this.SaveChanges();
         return this.dataSet.ttbliceaa.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbliceaa.Rows[0]) : null;
      }
  

      public Iceaa Update(Iceaa record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbliceaa.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbliceaa.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Iceaa record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbliceaa.NewttbliceaaRow();
            Iceaa.BuildMinimalRow(ref row, record);
            this.dataSet.ttbliceaa.AddttbliceaaRow((pdsiceaaDataSet.ttbliceaaRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Iceaa() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  