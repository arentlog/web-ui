//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicenl;

   public partial class IcenlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicenl";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icenlTableControlKey;
		
      private pdsicenlDataSet dataSet;
        
      public IcenlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicenlDataSet() { DataSetName = DataSetName };
            this.icenlTableControlKey = this.dataSet.ttblicenl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icenlTableControlKey))
            {
               this.CreateTableControlParameters(this.icenlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcenlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icenlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcenlproxy = this.proxyAppObject.CreatePO_icenlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcenlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icenlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icenlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icenlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icenlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcenlproxy = this.proxyAppObject.CreatePO_icenlproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcenlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icenlproxy - After Call");
      }
   

      public Icenl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icenl icenl = null;
         if (row != null)
         {
             icenl = this.BuildFromRow(row);
         }
         return icenl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icenl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicenl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icenl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icenl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icenl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicenl.AsEnumerable().SingleOrDefault();
         Icenl icenl = null;
         if (row != null)
         {
             icenl = this.BuildFromRow(row);
         }
         return icenl;
      }
	  
	  
      public IEnumerable<Icenl> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icenl.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icenl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicenl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icenl Get(int cono, string typecd, string whse, string prod, string prodcat, int seqnoh, int seqnod, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icenl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(typecd)) 
         {
             sb.AppendFormatWithEscape(" AND icenl.typecd = '{0}'", typecd);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icenl.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icenl.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(prodcat)) 
         {
             sb.AppendFormatWithEscape(" AND icenl.prodcat = '{0}'", prodcat);
         }
         if (seqnoh != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icenl.seqnoh = {0}", seqnoh);
         }
         if (seqnod != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icenl.seqnod = {0}", seqnod);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icenl> GetListByJrnlno(int cono, int jrnlno, int setno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icenl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (jrnlno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icenl.jrnlno = {0}", jrnlno);
         }
         if (setno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icenl.setno = {0}", setno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icenl> GetListByOrder(int cono, int orderno, int ordersuf, string ordtype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icenl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icenl.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icenl.ordersuf = {0}", ordersuf);
         }
         if (!string.IsNullOrEmpty(ordtype)) 
         {
             sb.AppendFormatWithEscape(" AND icenl.ordtype = '{0}'", ordtype);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icenl BuildFromRow(DataRow row)
      {
         var returnRecord = Icenl.BuildIcenlFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icenl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icenl record)
      {
         Icenl.UpdateRowFromIcenl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icenl Insert(Icenl record)
      {
         DataRow row = this.dataSet.ttblicenl.NewttblicenlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicenl.AddttblicenlRow((pdsicenlDataSet.ttblicenlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicenl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicenl.Rows[0]) : null;
      }
  

      public Icenl Update(Icenl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicenl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicenl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icenl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicenl.NewttblicenlRow();
            Icenl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicenl.AddttblicenlRow((pdsicenlDataSet.ttblicenlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icenl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  