//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsiceat;

   public partial class IceatAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsiceat";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> iceatTableControlKey;
		
      private pdsiceatDataSet dataSet;
        
      public IceatAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsiceatDataSet() { DataSetName = DataSetName };
            this.iceatTableControlKey = this.dataSet.ttbliceat.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.iceatTableControlKey))
            {
               this.CreateTableControlParameters(this.iceatTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IceatAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Iceatproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIceatproxy = this.proxyAppObject.CreatePO_iceatproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIceatproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Iceatproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.iceatTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.iceatTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Iceatproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIceatproxy = this.proxyAppObject.CreatePO_iceatproxy())
               {
                  this.SetRequiredContextParameters();
                  poIceatproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Iceatproxy - After Call");
      }
   

      public Iceat GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Iceat iceat = null;
         if (row != null)
         {
             iceat = this.BuildFromRow(row);
         }
         return iceat;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(iceat) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbliceat.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Iceat> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(iceat)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Iceat Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbliceat.AsEnumerable().SingleOrDefault();
         Iceat iceat = null;
         if (row != null)
         {
             iceat = this.BuildFromRow(row);
         }
         return iceat;
      }
	  
	  
      public IEnumerable<Iceat> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("iceat.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Iceat> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbliceat.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Iceat Get(int cono, string transty, int orderno, int ordersuf, int lineno, int compseqno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceat.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.transty = '{0}'", transty);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.lineno = {0}", lineno);
         }
         if (compseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.compseqno = {0}", compseqno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Iceat> GetListByClaim(int cono, bool claimpdfl, DateTime? claimpddt, string transty, string whse, string prod, int orderno, int ordersuf, int lineno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceat.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND iceat.claimpdfl = {0}", claimpdfl);
         if (claimpddt != null) 
         {
             sb.AppendFormatWithEscape(" AND iceat.claimpddt = '{0}'", claimpddt);
         }
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.transty = '{0}'", transty);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.prod = '{0}'", prod);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.lineno = {0}", lineno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Iceat> GetListByDelete(int cono, string transty, int retorderno, int retordersuf, int retlineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceat.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.transty = '{0}'", transty);
         }
         if (retorderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.retorderno = {0}", retorderno);
         }
         if (retordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.retordersuf = {0}", retordersuf);
         }
         if (retlineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.retlineno = {0}", retlineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Iceat> GetListByImplyprod(int cono, string implyprod, string transty, int orderno, int ordersuf, int implylineno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceat.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(implyprod)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.implyprod = '{0}'", implyprod);
         }
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.transty = '{0}'", transty);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.ordersuf = {0}", ordersuf);
         }
         if (implylineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.implylineno = {0}", implylineno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Iceat> GetListByProd(int cono, string prod, string transty, int orderno, int ordersuf, int lineno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceat.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.transty = '{0}'", transty);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.lineno = {0}", lineno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Iceat> GetListBySub(int cono, bool subfl, string prod, string transty, int orderno, int ordersuf, int lineno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceat.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND iceat.subfl = {0}", subfl);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.transty = '{0}'", transty);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.lineno = {0}", lineno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Iceat> GetListByWarr(int cono, bool warrchfl, string transty, string whse, string prod, int orderno, int ordersuf, int lineno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("iceat.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND iceat.warrchfl = {0}", warrchfl);
         if (!string.IsNullOrEmpty(transty)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.transty = '{0}'", transty);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND iceat.prod = '{0}'", prod);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.lineno = {0}", lineno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND iceat.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Iceat BuildFromRow(DataRow row)
      {
         var returnRecord = Iceat.BuildIceatFromRow(row);
         returnRecord = this.BuildExtraFromRow<Iceat>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Iceat record)
      {
         Iceat.UpdateRowFromIceat(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Iceat Insert(Iceat record)
      {
         DataRow row = this.dataSet.ttbliceat.NewttbliceatRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbliceat.AddttbliceatRow((pdsiceatDataSet.ttbliceatRow)row);
         this.SaveChanges();
         return this.dataSet.ttbliceat.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbliceat.Rows[0]) : null;
      }
  

      public Iceat Update(Iceat record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbliceat.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbliceat.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Iceat record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbliceat.NewttbliceatRow();
            Iceat.BuildMinimalRow(ref row, record);
            this.dataSet.ttbliceat.AddttbliceatRow((pdsiceatDataSet.ttbliceatRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Iceat() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  