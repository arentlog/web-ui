//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsev;

   public partial class IcsevAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsev";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsevTableControlKey;
		
      private pdsicsevDataSet dataSet;
        
      public IcsevAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsevDataSet() { DataSetName = DataSetName };
            this.icsevTableControlKey = this.dataSet.ttblicsev.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsevTableControlKey))
            {
               this.CreateTableControlParameters(this.icsevTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsevAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsevproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsevproxy = this.proxyAppObject.CreatePO_icsevproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsevproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsevproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsevTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsevTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsevproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsevproxy = this.proxyAppObject.CreatePO_icsevproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsevproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsevproxy - After Call");
      }
   

      public Icsev GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsev icsev = null;
         if (row != null)
         {
             icsev = this.BuildFromRow(row);
         }
         return icsev;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsev) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsev.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsev> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsev)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsev Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsev.AsEnumerable().SingleOrDefault();
         Icsev icsev = null;
         if (row != null)
         {
             icsev = this.BuildFromRow(row);
         }
         return icsev;
      }
	  
	  
      public IEnumerable<Icsev> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icsev.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsev> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsev.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsev Get(int cono, string lifocat, int yr, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsev.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(lifocat)) 
         {
             sb.AppendFormatWithEscape(" AND icsev.lifocat = '{0}'", lifocat);
         }
         if (yr != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsev.yr = {0}", yr);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Icsev BuildFromRow(DataRow row)
      {
         var returnRecord = Icsev.BuildIcsevFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsev>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsev record)
      {
         Icsev.UpdateRowFromIcsev(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsev Insert(Icsev record)
      {
         DataRow row = this.dataSet.ttblicsev.NewttblicsevRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsev.AddttblicsevRow((pdsicsevDataSet.ttblicsevRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsev.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsev.Rows[0]) : null;
      }
  

      public Icsev Update(Icsev record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsev.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsev.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsev record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsev.NewttblicsevRow();
            Icsev.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsev.AddttblicsevRow((pdsicsevDataSet.ttblicsevRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsev() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  