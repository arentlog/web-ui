//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsgv;

   public partial class IcsgvAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsgv";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsgvTableControlKey;
		
      private pdsicsgvDataSet dataSet;
        
      public IcsgvAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsgvDataSet() { DataSetName = DataSetName };
            this.icsgvTableControlKey = this.dataSet.ttblicsgv.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsgvTableControlKey))
            {
               this.CreateTableControlParameters(this.icsgvTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsgvAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsgvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsgvproxy = this.proxyAppObject.CreatePO_icsgvproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsgvproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsgvproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsgvTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsgvTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsgvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsgvproxy = this.proxyAppObject.CreatePO_icsgvproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsgvproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsgvproxy - After Call");
      }
   

      public Icsgv GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsgv icsgv = null;
         if (row != null)
         {
             icsgv = this.BuildFromRow(row);
         }
         return icsgv;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsgv) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsgv.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsgv> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsgv)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsgv Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsgv.AsEnumerable().SingleOrDefault();
         Icsgv icsgv = null;
         if (row != null)
         {
             icsgv = this.BuildFromRow(row);
         }
         return icsgv;
      }
	  
	  

      public IEnumerable<Icsgv> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsgv.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsgv Get(int groupno, int attrno, int valueno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (groupno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgv.groupno = {0}", groupno);
         }
         if (attrno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgv.attrno = {0}", attrno);
         }
         if (valueno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgv.valueno = {0}", valueno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icsgv> GetListByBatch(string ecbatchnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(ecbatchnm)) 
         {
             sb.AppendFormatWithEscape("icsgv.ecbatchnm = '{0}'", ecbatchnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgv> GetListByDescrip(int groupno, int attrno, string descrip, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (groupno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgv.groupno = {0}", groupno);
         }
         if (attrno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgv.attrno = {0}", attrno);
         }
         if (!string.IsNullOrEmpty(descrip)) 
         {
             sb.AppendFormatWithEscape(" AND icsgv.descrip = '{0}'", descrip);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgv> GetListByKeyLiteral(string keyLiteral, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(keyLiteral)) 
         {
             sb.AppendFormatWithEscape("icsgv.key-literal = '{0}'", keyLiteral);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgv> GetListByNodeid(int nodeid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (nodeid != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgv.nodeid = {0}", nodeid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgv> GetListByUnspcno(int unspcno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (unspcno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgv.unspcno = {0}", unspcno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icsgv BuildFromRow(DataRow row)
      {
         var returnRecord = Icsgv.BuildIcsgvFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsgv>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsgv record)
      {
         Icsgv.UpdateRowFromIcsgv(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsgv Insert(Icsgv record)
      {
         DataRow row = this.dataSet.ttblicsgv.NewttblicsgvRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsgv.AddttblicsgvRow((pdsicsgvDataSet.ttblicsgvRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsgv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgv.Rows[0]) : null;
      }
  

      public Icsgv Update(Icsgv record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsgv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgv.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsgv record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsgv.NewttblicsgvRow();
            Icsgv.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsgv.AddttblicsgvRow((pdsicsgvDataSet.ttblicsgvRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsgv() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  