//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicscm;

   public partial class IcscmAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicscm";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icscmTableControlKey;
		
      private pdsicscmDataSet dataSet;
        
      public IcscmAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicscmDataSet() { DataSetName = DataSetName };
            this.icscmTableControlKey = this.dataSet.ttblicscm.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icscmTableControlKey))
            {
               this.CreateTableControlParameters(this.icscmTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcscmAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icscmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcscmproxy = this.proxyAppObject.CreatePO_icscmproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcscmproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icscmproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icscmTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icscmTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icscmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcscmproxy = this.proxyAppObject.CreatePO_icscmproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcscmproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icscmproxy - After Call");
      }
   

      public Icscm GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icscm icscm = null;
         if (row != null)
         {
             icscm = this.BuildFromRow(row);
         }
         return icscm;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icscm) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicscm.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icscm> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icscm)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icscm Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicscm.AsEnumerable().SingleOrDefault();
         Icscm icscm = null;
         if (row != null)
         {
             icscm = this.BuildFromRow(row);
         }
         return icscm;
      }
	  
	  

      public IEnumerable<Icscm> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicscm.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icscm Get(decimal mfgno, string name, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (mfgno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape("icscm.mfgno = {0}", mfgno);
         }
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape(" AND icscm.name = '{0}'", name);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icscm> GetListByBatch(string ecbatchnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(ecbatchnm)) 
         {
             sb.AppendFormatWithEscape("icscm.ecbatchnm = '{0}'", ecbatchnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icscm> GetListByMfgnm(string mfgnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(mfgnm)) 
         {
             sb.AppendFormatWithEscape("icscm.mfgnm = '{0}'", mfgnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icscm> GetListByMfgno(decimal mfgno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (mfgno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape("icscm.mfgno = {0}", mfgno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icscm> GetListByName(string name, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape("icscm.name = '{0}'", name);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icscm> GetListByStoreid(int corpid, int storeid, decimal mfgno, string name, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (corpid != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icscm.corpid = {0}", corpid);
         }
         if (storeid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icscm.storeid = {0}", storeid);
         }
         if (mfgno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icscm.mfgno = {0}", mfgno);
         }
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape(" AND icscm.name = '{0}'", name);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icscm BuildFromRow(DataRow row)
      {
         var returnRecord = Icscm.BuildIcscmFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icscm>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icscm record)
      {
         Icscm.UpdateRowFromIcscm(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icscm Insert(Icscm record)
      {
         DataRow row = this.dataSet.ttblicscm.NewttblicscmRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicscm.AddttblicscmRow((pdsicscmDataSet.ttblicscmRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicscm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicscm.Rows[0]) : null;
      }
  

      public Icscm Update(Icscm record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicscm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicscm.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icscm record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicscm.NewttblicscmRow();
            Icscm.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicscm.AddttblicscmRow((pdsicscmDataSet.ttblicscmRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icscm() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  