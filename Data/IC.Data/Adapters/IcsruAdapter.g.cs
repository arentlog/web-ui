//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsru;

   public partial class IcsruAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsru";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsruTableControlKey;
		
      private pdsicsruDataSet dataSet;
        
      public IcsruAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsruDataSet() { DataSetName = DataSetName };
            this.icsruTableControlKey = this.dataSet.ttblicsru.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsruTableControlKey))
            {
               this.CreateTableControlParameters(this.icsruTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsruAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsruproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsruproxy = this.proxyAppObject.CreatePO_icsruproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsruproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsruproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsruTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsruTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsruproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsruproxy = this.proxyAppObject.CreatePO_icsruproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsruproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsruproxy - After Call");
      }
   

      public Icsru GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsru icsru = null;
         if (row != null)
         {
             icsru = this.BuildFromRow(row);
         }
         return icsru;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsru) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsru.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsru> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsru)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsru Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsru.AsEnumerable().SingleOrDefault();
         Icsru icsru = null;
         if (row != null)
         {
             icsru = this.BuildFromRow(row);
         }
         return icsru;
      }
	  
	  
      public IEnumerable<Icsru> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icsru.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsru> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsru.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsru Get(int cono, decimal vendno, string whse, string prodline, int usagelevel, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsru.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsru.vendno = {0}", vendno);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icsru.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prodline)) 
         {
             sb.AppendFormatWithEscape(" AND icsru.prodline = '{0}'", prodline);
         }
         if (usagelevel != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsru.usagelevel = {0}", usagelevel);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Icsru BuildFromRow(DataRow row)
      {
         var returnRecord = Icsru.BuildIcsruFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsru>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsru record)
      {
         Icsru.UpdateRowFromIcsru(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsru Insert(Icsru record)
      {
         DataRow row = this.dataSet.ttblicsru.NewttblicsruRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsru.AddttblicsruRow((pdsicsruDataSet.ttblicsruRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsru.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsru.Rows[0]) : null;
      }
  

      public Icsru Update(Icsru record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsru.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsru.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsru record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsru.NewttblicsruRow();
            Icsru.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsru.AddttblicsruRow((pdsicsruDataSet.ttblicsruRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsru() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  