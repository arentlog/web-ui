//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsgr;

   public partial class IcsgrAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsgr";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsgrTableControlKey;
		
      private pdsicsgrDataSet dataSet;
        
      public IcsgrAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsgrDataSet() { DataSetName = DataSetName };
            this.icsgrTableControlKey = this.dataSet.ttblicsgr.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsgrTableControlKey))
            {
               this.CreateTableControlParameters(this.icsgrTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsgrAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsgrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsgrproxy = this.proxyAppObject.CreatePO_icsgrproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsgrproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsgrproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsgrTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsgrTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsgrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsgrproxy = this.proxyAppObject.CreatePO_icsgrproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsgrproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsgrproxy - After Call");
      }
   

      public Icsgr GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsgr icsgr = null;
         if (row != null)
         {
             icsgr = this.BuildFromRow(row);
         }
         return icsgr;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsgr) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsgr.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsgr> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsgr)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsgr Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsgr.AsEnumerable().SingleOrDefault();
         Icsgr icsgr = null;
         if (row != null)
         {
             icsgr = this.BuildFromRow(row);
         }
         return icsgr;
      }
	  
	  

      public IEnumerable<Icsgr> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsgr.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsgr Get(int groupno, int parentattrno, int parentvalueno, int childattrno, int childvalueno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (groupno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsgr.groupno = {0}", groupno);
         }
         if (parentattrno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgr.parentattrno = {0}", parentattrno);
         }
         if (parentvalueno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgr.parentvalueno = {0}", parentvalueno);
         }
         if (childattrno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgr.childattrno = {0}", childattrno);
         }
         if (childvalueno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsgr.childvalueno = {0}", childvalueno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icsgr> GetListByBatch(string ecbatchnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(ecbatchnm)) 
         {
             sb.AppendFormatWithEscape("icsgr.ecbatchnm = '{0}'", ecbatchnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icsgr BuildFromRow(DataRow row)
      {
         var returnRecord = Icsgr.BuildIcsgrFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsgr>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsgr record)
      {
         Icsgr.UpdateRowFromIcsgr(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsgr Insert(Icsgr record)
      {
         DataRow row = this.dataSet.ttblicsgr.NewttblicsgrRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsgr.AddttblicsgrRow((pdsicsgrDataSet.ttblicsgrRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsgr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgr.Rows[0]) : null;
      }
  

      public Icsgr Update(Icsgr record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsgr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgr.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsgr record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsgr.NewttblicsgrRow();
            Icsgr.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsgr.AddttblicsgrRow((pdsicsgrDataSet.ttblicsgrRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsgr() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  