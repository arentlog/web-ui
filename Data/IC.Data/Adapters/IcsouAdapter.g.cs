//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsou;

   public partial class IcsouAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsou";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsouTableControlKey;
		
      private pdsicsouDataSet dataSet;
        
      public IcsouAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsouDataSet() { DataSetName = DataSetName };
            this.icsouTableControlKey = this.dataSet.ttblicsou.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsouTableControlKey))
            {
               this.CreateTableControlParameters(this.icsouTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsouAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsouproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsouproxy = this.proxyAppObject.CreatePO_icsouproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsouproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsouproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsouTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsouTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsouproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsouproxy = this.proxyAppObject.CreatePO_icsouproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsouproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsouproxy - After Call");
      }
   

      public Icsou GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsou icsou = null;
         if (row != null)
         {
             icsou = this.BuildFromRow(row);
         }
         return icsou;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsou) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsou.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsou> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsou)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsou Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsou.AsEnumerable().SingleOrDefault();
         Icsou icsou = null;
         if (row != null)
         {
             icsou = this.BuildFromRow(row);
         }
         return icsou;
      }
	  
	  
      public IEnumerable<Icsou> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icsou.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsou> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsou.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsou Get(int cono, string whse, string prod, string reasunavty, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsou.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icsou.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icsou.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(reasunavty)) 
         {
             sb.AppendFormatWithEscape(" AND icsou.reasunavty = '{0}'", reasunavty);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icsou> GetListByReason(int cono, string whse, string reasunavty, string prod, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsou.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icsou.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(reasunavty)) 
         {
             sb.AppendFormatWithEscape(" AND icsou.reasunavty = '{0}'", reasunavty);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icsou.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icsou BuildFromRow(DataRow row)
      {
         var returnRecord = Icsou.BuildIcsouFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsou>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsou record)
      {
         Icsou.UpdateRowFromIcsou(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsou Insert(Icsou record)
      {
         DataRow row = this.dataSet.ttblicsou.NewttblicsouRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsou.AddttblicsouRow((pdsicsouDataSet.ttblicsouRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsou.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsou.Rows[0]) : null;
      }
  

      public Icsou Update(Icsou record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsou.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsou.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsou record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsou.NewttblicsouRow();
            Icsou.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsou.AddttblicsouRow((pdsicsouDataSet.ttblicsouRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsou() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  