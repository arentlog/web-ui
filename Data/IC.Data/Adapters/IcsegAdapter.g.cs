//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicseg;
   using Models.Pdsicseglookup;
   using Models.Complex;

   public partial class IcsegAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicseg";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsegTableControlKey;
		
      private pdsicsegDataSet dataSet;
        
      public IcsegAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsegDataSet() { DataSetName = DataSetName };
            this.icsegTableControlKey = this.dataSet.ttblicseg.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsegTableControlKey))
            {
               this.CreateTableControlParameters(this.icsegTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsegAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsegproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsegproxy = this.proxyAppObject.CreatePO_icsegproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsegproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsegproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsegTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsegTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsegproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsegproxy = this.proxyAppObject.CreatePO_icsegproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsegproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsegproxy - After Call");
      }
   

      public Icseg GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icseg icseg = null;
         if (row != null)
         {
             icseg = this.BuildFromRow(row);
         }
         return icseg;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icseg) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicseg.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icseg> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icseg)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icseg Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicseg.AsEnumerable().SingleOrDefault();
         Icseg icseg = null;
         if (row != null)
         {
             icseg = this.BuildFromRow(row);
         }
         return icseg;
      }
	  
	  
      public IEnumerable<Icseg> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icseg.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icseg> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicseg.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icseg Get(int cono, bool accttype, int disttype, string key1, string key2, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icseg.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND icseg.accttype = {0}", accttype);
         if (disttype != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icseg.disttype = {0}", disttype);
         }
         if (!string.IsNullOrEmpty(key1)) 
         {
             sb.AppendFormatWithEscape(" AND icseg.key1 = '{0}'", key1);
         }
         if (!string.IsNullOrEmpty(key2)) 
         {
             sb.AppendFormatWithEscape(" AND icseg.key2 = '{0}'", key2);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Icseg BuildFromRow(DataRow row)
      {
         var returnRecord = Icseg.BuildIcsegFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icseg>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icseg record)
      {
         Icseg.UpdateRowFromIcseg(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icseg Insert(Icseg record)
      {
         DataRow row = this.dataSet.ttblicseg.NewttblicsegRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicseg.AddttblicsegRow((pdsicsegDataSet.ttblicsegRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicseg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseg.Rows[0]) : null;
      }
  

      public Icseg Update(Icseg record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicseg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseg.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icseg record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicseg.NewttblicsegRow();
            Icseg.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicseg.AddttblicsegRow((pdsicsegDataSet.ttblicsegRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icseg() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public IcsegLookupResponseAPI Lookup(Icseglookupcriteria icseglookupcriteria)
      {   
         var result = new IcsegLookupResponseAPI();
         
         var pdsicseglookup = new pdsicseglookupDataSet();
            
         DataRow ttblicseglookupcriteriaCriteria = pdsicseglookup.ttblicseglookupcriteria.NewttblicseglookupcriteriaRow();
         Icseglookupcriteria.UpdateRowFromIcseglookupcriteria(ref ttblicseglookupcriteriaCriteria, icseglookupcriteria);
         pdsicseglookup.ttblicseglookupcriteria.AddttblicseglookupcriteriaRow((pdsicseglookupDataSet.ttblicseglookupcriteriaRow)ttblicseglookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Icseg - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsegproxy = this.proxyAppObject.CreatePO_icsegproxy())
               {
                   this.SetRequiredContextParameters();
                   poIcsegproxy.Lookup(ref pdsContext, ref pdsicseglookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Icseg - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsicseglookup); 
    
         foreach (DataRow row in pdsicseglookup.ttblicseglookupresults)
         {
            result.icseglookupresults.Add(Icseglookupresults.BuildIcseglookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  