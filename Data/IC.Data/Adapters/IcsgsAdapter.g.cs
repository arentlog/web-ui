//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsgs;

   public partial class IcsgsAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsgs";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsgsTableControlKey;
		
      private pdsicsgsDataSet dataSet;
        
      public IcsgsAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsgsDataSet() { DataSetName = DataSetName };
            this.icsgsTableControlKey = this.dataSet.ttblicsgs.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsgsTableControlKey))
            {
               this.CreateTableControlParameters(this.icsgsTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsgsAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsgsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsgsproxy = this.proxyAppObject.CreatePO_icsgsproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsgsproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsgsproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsgsTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsgsTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsgsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsgsproxy = this.proxyAppObject.CreatePO_icsgsproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsgsproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsgsproxy - After Call");
      }
   

      public Icsgs GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsgs icsgs = null;
         if (row != null)
         {
             icsgs = this.BuildFromRow(row);
         }
         return icsgs;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsgs) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsgs.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsgs> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsgs)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsgs Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsgs.AsEnumerable().SingleOrDefault();
         Icsgs icsgs = null;
         if (row != null)
         {
             icsgs = this.BuildFromRow(row);
         }
         return icsgs;
      }
	  
	  

      public IEnumerable<Icsgs> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsgs.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsgs Get(string wordfrom, string wordto, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(wordfrom)) 
         {
             sb.AppendFormatWithEscape("icsgs.wordfrom = '{0}'", wordfrom);
         }
         if (!string.IsNullOrEmpty(wordto)) 
         {
             sb.AppendFormatWithEscape(" AND icsgs.wordto = '{0}'", wordto);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icsgs> GetListByBatch(string ecbatchnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(ecbatchnm)) 
         {
             sb.AppendFormatWithEscape("icsgs.ecbatchnm = '{0}'", ecbatchnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsgs> GetListByWordto(string wordto, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(wordto)) 
         {
             sb.AppendFormatWithEscape("icsgs.wordto = '{0}'", wordto);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icsgs BuildFromRow(DataRow row)
      {
         var returnRecord = Icsgs.BuildIcsgsFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsgs>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsgs record)
      {
         Icsgs.UpdateRowFromIcsgs(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsgs Insert(Icsgs record)
      {
         DataRow row = this.dataSet.ttblicsgs.NewttblicsgsRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsgs.AddttblicsgsRow((pdsicsgsDataSet.ttblicsgsRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsgs.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgs.Rows[0]) : null;
      }
  

      public Icsgs Update(Icsgs record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsgs.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsgs.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsgs record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsgs.NewttblicsgsRow();
            Icsgs.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsgs.AddttblicsgsRow((pdsicsgsDataSet.ttblicsgsRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsgs() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  