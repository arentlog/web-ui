//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicamld;

   public partial class IcamldAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicamld";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icamldTableControlKey;
		
      private pdsicamldDataSet dataSet;
        
      public IcamldAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicamldDataSet() { DataSetName = DataSetName };
            this.icamldTableControlKey = this.dataSet.ttblicamld.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icamldTableControlKey))
            {
               this.CreateTableControlParameters(this.icamldTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcamldAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icamldproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcamldproxy = this.proxyAppObject.CreatePO_icamldproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcamldproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icamldproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icamldTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icamldTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icamldproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcamldproxy = this.proxyAppObject.CreatePO_icamldproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcamldproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icamldproxy - After Call");
      }
   

      public Icamld GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icamld icamld = null;
         if (row != null)
         {
             icamld = this.BuildFromRow(row);
         }
         return icamld;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icamld) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicamld.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icamld> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icamld)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icamld Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicamld.AsEnumerable().SingleOrDefault();
         Icamld icamld = null;
         if (row != null)
         {
             icamld = this.BuildFromRow(row);
         }
         return icamld;
      }
	  
	  
      public IEnumerable<Icamld> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icamld.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icamld> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicamld.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icamld Get(int cono, string whse, decimal arpvendno, string prodline, string recordtype, decimal urcvalue, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icamld.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icamld.whse = '{0}'", whse);
         }
         if (arpvendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icamld.arpvendno = {0}", arpvendno);
         }
         if (!string.IsNullOrEmpty(prodline)) 
         {
             sb.AppendFormatWithEscape(" AND icamld.prodline = '{0}'", prodline);
         }
         if (!string.IsNullOrEmpty(recordtype)) 
         {
             sb.AppendFormatWithEscape(" AND icamld.recordtype = '{0}'", recordtype);
         }
         if (urcvalue != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icamld.urcvalue = {0}", urcvalue);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Icamld BuildFromRow(DataRow row)
      {
         var returnRecord = Icamld.BuildIcamldFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icamld>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icamld record)
      {
         Icamld.UpdateRowFromIcamld(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icamld Insert(Icamld record)
      {
         DataRow row = this.dataSet.ttblicamld.NewttblicamldRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicamld.AddttblicamldRow((pdsicamldDataSet.ttblicamldRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicamld.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicamld.Rows[0]) : null;
      }
  

      public Icamld Update(Icamld record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicamld.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicamld.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icamld record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicamld.NewttblicamldRow();
            Icamld.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicamld.AddttblicamldRow((pdsicamldDataSet.ttblicamldRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icamld() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  