//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsiceh;

   public partial class IcehAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsiceh";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icehTableControlKey;
		
      private pdsicehDataSet dataSet;
        
      public IcehAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicehDataSet() { DataSetName = DataSetName };
            this.icehTableControlKey = this.dataSet.ttbliceh.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icehTableControlKey))
            {
               this.CreateTableControlParameters(this.icehTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcehAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcehproxy = this.proxyAppObject.CreatePO_icehproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcehproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icehproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icehTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icehTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcehproxy = this.proxyAppObject.CreatePO_icehproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcehproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icehproxy - After Call");
      }
   

      public Iceh GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Iceh iceh = null;
         if (row != null)
         {
             iceh = this.BuildFromRow(row);
         }
         return iceh;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(iceh) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbliceh.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Iceh> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(iceh)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Iceh Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbliceh.AsEnumerable().SingleOrDefault();
         Iceh iceh = null;
         if (row != null)
         {
             iceh = this.BuildFromRow(row);
         }
         return iceh;
      }
	  
	  

      public IEnumerable<Iceh> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbliceh.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Iceh Get(decimal keyno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (keyno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape("iceh.keyno = {0}", keyno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Iceh BuildFromRow(DataRow row)
      {
         var returnRecord = Iceh.BuildIcehFromRow(row);
         returnRecord = this.BuildExtraFromRow<Iceh>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Iceh record)
      {
         Iceh.UpdateRowFromIceh(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Iceh Insert(Iceh record)
      {
         DataRow row = this.dataSet.ttbliceh.NewttblicehRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbliceh.AddttblicehRow((pdsicehDataSet.ttblicehRow)row);
         this.SaveChanges();
         return this.dataSet.ttbliceh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbliceh.Rows[0]) : null;
      }
  

      public Iceh Update(Iceh record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbliceh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbliceh.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Iceh record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbliceh.NewttblicehRow();
            Iceh.BuildMinimalRow(ref row, record);
            this.dataSet.ttbliceh.AddttblicehRow((pdsicehDataSet.ttblicehRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Iceh() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  