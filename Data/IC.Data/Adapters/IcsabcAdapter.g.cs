//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsabc;

   public partial class IcsabcAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsabc";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsabcTableControlKey;
		
      private pdsicsabcDataSet dataSet;
        
      public IcsabcAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsabcDataSet() { DataSetName = DataSetName };
            this.icsabcTableControlKey = this.dataSet.ttblicsabc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsabcTableControlKey))
            {
               this.CreateTableControlParameters(this.icsabcTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsabcAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsabcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsabcproxy = this.proxyAppObject.CreatePO_icsabcproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsabcproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsabcproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsabcTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsabcTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsabcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsabcproxy = this.proxyAppObject.CreatePO_icsabcproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsabcproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsabcproxy - After Call");
      }
   

      public Icsabc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsabc icsabc = null;
         if (row != null)
         {
             icsabc = this.BuildFromRow(row);
         }
         return icsabc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsabc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsabc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsabc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsabc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsabc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsabc.AsEnumerable().SingleOrDefault();
         Icsabc icsabc = null;
         if (row != null)
         {
             icsabc = this.BuildFromRow(row);
         }
         return icsabc;
      }
	  
	  
      public IEnumerable<Icsabc> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icsabc.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsabc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsabc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsabc Get(int cono, string whse, string stkoan, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsabc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icsabc.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(stkoan)) 
         {
             sb.AppendFormatWithEscape(" AND icsabc.stkoan = '{0}'", stkoan);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Icsabc BuildFromRow(DataRow row)
      {
         var returnRecord = Icsabc.BuildIcsabcFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsabc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsabc record)
      {
         Icsabc.UpdateRowFromIcsabc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsabc Insert(Icsabc record)
      {
         DataRow row = this.dataSet.ttblicsabc.NewttblicsabcRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsabc.AddttblicsabcRow((pdsicsabcDataSet.ttblicsabcRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsabc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsabc.Rows[0]) : null;
      }
  

      public Icsabc Update(Icsabc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsabc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsabc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsabc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsabc.NewttblicsabcRow();
            Icsabc.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsabc.AddttblicsabcRow((pdsicsabcDataSet.ttblicsabcRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsabc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  