//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsel;
   using Models.Pdsiclotlookup;
   using Models.Complex;

   public partial class IcselAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsel";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icselTableControlKey;
		
      private pdsicselDataSet dataSet;
        
      public IcselAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicselDataSet() { DataSetName = DataSetName };
            this.icselTableControlKey = this.dataSet.ttblicsel.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icselTableControlKey))
            {
               this.CreateTableControlParameters(this.icselTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcselAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icselproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcselproxy = this.proxyAppObject.CreatePO_icselproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcselproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icselproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icselTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icselTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icselproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcselproxy = this.proxyAppObject.CreatePO_icselproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcselproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icselproxy - After Call");
      }
   

      public Icsel GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsel icsel = null;
         if (row != null)
         {
             icsel = this.BuildFromRow(row);
         }
         return icsel;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsel) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsel.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsel> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsel)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsel Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsel.AsEnumerable().SingleOrDefault();
         Icsel icsel = null;
         if (row != null)
         {
             icsel = this.BuildFromRow(row);
         }
         return icsel;
      }
	  
	  
	  public IEnumerable<Icsel> GetListByRowpointers(List<string> rowpointers, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("icsel.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("icsel.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsel.AsEnumerable().SingleOrDefault();
      }
      public IEnumerable<Icsel> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icsel.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsel> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsel.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsel Get(int cono, string whse, string prod, string lotno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsel.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icsel.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icsel.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(lotno)) 
         {
             sb.AppendFormatWithEscape(" AND icsel.lotno = '{0}'", lotno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icsel> GetListByAvail(int cono, string whse, string prod, string statustype, DateTime? opendt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icsel.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icsel.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icsel.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(statustype)) 
         {
             sb.AppendFormatWithEscape(" AND icsel.statustype = '{0}'", statustype);
         }
         if (opendt != null) 
         {
             sb.AppendFormatWithEscape(" AND icsel.opendt = '{0}'", opendt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icsel GetByRowpointer(string rowpointer, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("icsel.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, 1, fldList);
      }

      public Icsel BuildFromRow(DataRow row)
      {
         var returnRecord = Icsel.BuildIcselFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsel>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsel record)
      {
         Icsel.UpdateRowFromIcsel(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsel Insert(Icsel record)
      {
         DataRow row = this.dataSet.ttblicsel.NewttblicselRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsel.AddttblicselRow((pdsicselDataSet.ttblicselRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsel.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsel.Rows[0]) : null;
      }
  

      public Icsel Update(Icsel record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsel.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsel.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsel record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsel.NewttblicselRow();
            Icsel.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsel.AddttblicselRow((pdsicselDataSet.ttblicselRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Icsel record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttblicsel.NewttblicselRow();
            Icsel.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsel.AddttblicselRow((pdsicselDataSet.ttblicselRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsel() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Icsel() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      public IcselLookupResponseAPI Lookup(Iclotlookupcriteria iclotlookupcriteria)
      {   
         var result = new IcselLookupResponseAPI();
         
         var pdsiclotlookup = new pdsiclotlookupDataSet();
            
         DataRow ttbliclotlookupcriteriaCriteria = pdsiclotlookup.ttbliclotlookupcriteria.NewttbliclotlookupcriteriaRow();
         Iclotlookupcriteria.UpdateRowFromIclotlookupcriteria(ref ttbliclotlookupcriteriaCriteria, iclotlookupcriteria);
         pdsiclotlookup.ttbliclotlookupcriteria.AddttbliclotlookupcriteriaRow((pdsiclotlookupDataSet.ttbliclotlookupcriteriaRow)ttbliclotlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Icsel - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcselproxy = this.proxyAppObject.CreatePO_icselproxy())
               {
                   this.SetRequiredContextParameters();
                   poIcselproxy.Lookup(ref pdsContext, ref pdsiclotlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Icsel - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsiclotlookup); 
    
         foreach (DataRow row in pdsiclotlookup.ttbliclotlookupresults)
         {
            result.iclotlookupresults.Add(Iclotlookupresults.BuildIclotlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  