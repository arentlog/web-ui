//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicseps;

   public partial class IcsepsAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicseps";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsepsTableControlKey;
		
      private pdsicsepsDataSet dataSet;
        
      public IcsepsAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsepsDataSet() { DataSetName = DataSetName };
            this.icsepsTableControlKey = this.dataSet.ttblicseps.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsepsTableControlKey))
            {
               this.CreateTableControlParameters(this.icsepsTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsepsAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsepsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsepsproxy = this.proxyAppObject.CreatePO_icsepsproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsepsproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsepsproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsepsTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsepsTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsepsproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsepsproxy = this.proxyAppObject.CreatePO_icsepsproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsepsproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsepsproxy - After Call");
      }
   

      public Icseps GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icseps icseps = null;
         if (row != null)
         {
             icseps = this.BuildFromRow(row);
         }
         return icseps;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icseps) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicseps.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icseps> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icseps)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icseps Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicseps.AsEnumerable().SingleOrDefault();
         Icseps icseps = null;
         if (row != null)
         {
             icseps = this.BuildFromRow(row);
         }
         return icseps;
      }
	  
	  
      public IEnumerable<Icseps> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icseps.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icseps> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicseps.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icseps Get(int cono, string whse, int runno, string prod, string serialno, bool unavailfl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icseps.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND icseps.whse = '{0}'", whse);
         }
         if (runno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icseps.runno = {0}", runno);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icseps.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(serialno)) 
         {
             sb.AppendFormatWithEscape(" AND icseps.serialno = '{0}'", serialno);
         }
         sb.AppendFormatWithEscape(" AND icseps.unavailfl = {0}", unavailfl);
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icseps> GetListByProd(int cono, string prod, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icseps.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icseps.prod = '{0}'", prod);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icseps BuildFromRow(DataRow row)
      {
         var returnRecord = Icseps.BuildIcsepsFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icseps>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icseps record)
      {
         Icseps.UpdateRowFromIcseps(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icseps Insert(Icseps record)
      {
         DataRow row = this.dataSet.ttblicseps.NewttblicsepsRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicseps.AddttblicsepsRow((pdsicsepsDataSet.ttblicsepsRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicseps.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseps.Rows[0]) : null;
      }
  

      public Icseps Update(Icseps record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicseps.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseps.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icseps record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicseps.NewttblicsepsRow();
            Icseps.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicseps.AddttblicsepsRow((pdsicsepsDataSet.ttblicsepsRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icseps() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  