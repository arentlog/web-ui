//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicseu;
   using Models.Pdsicproductunitlookup;
   using Models.Complex;

   public partial class IcseuAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicseu";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icseuTableControlKey;
		private readonly Tuple<string, string> icspTableControlKey;
      private pdsicseuDataSet dataSet;
        
      public IcseuAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicseuDataSet() { DataSetName = DataSetName };
            this.icseuTableControlKey = this.dataSet.ttblicseu.GetTableControlParametersKey();
            this.icspTableControlKey = this.dataSet.ttblicsp.GetTableControlParametersKey();
            if (!this.tempTableControlParameters.ContainsKey(this.icseuTableControlKey))
            {
               this.CreateTableControlParameters(this.icseuTableControlKey);
            }
            if (!this.tempTableControlParameters.ContainsKey(this.icspTableControlKey))
            {
               this.CreateTableControlParameters(this.icspTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcseuAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, bool fillMode, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icseuproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, fillMode, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcseuproxy = this.proxyAppObject.CreatePO_icseuproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcseuproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icseuproxy - After Call");
      }
      
      private void SetAndFetch(string where, bool fillMode, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icseuTableControlKey, where, batchsize, fldList);
         this.SetFetchWhereParameters(this.icspTableControlKey, fillMode);
         this.SetTableParametersOnContext(this.icseuTableControlKey, true);
         this.SetTableParametersOnContext(this.icspTableControlKey);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icseuproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcseuproxy = this.proxyAppObject.CreatePO_icseuproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcseuproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icseuproxy - After Call");
      }
   

      public Icseu GetByRowId(string rowId, bool fillMode, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fillMode, fldList);
         Icseu icseu = null;
         if (row != null)
         {
             icseu = this.BuildFromRow(row);
         }
         return icseu;
      }
   
      private DataRow GetRowByRowId(string rowId, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icseu) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), fillMode, 1, fldList);
         return this.dataSet.ttblicseu.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icseu> GetListByRowIdList(List<string> rowIds, bool fillMode, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icseu)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), fillMode, batchsize, fldList);
      }

      protected Icseu Fetch(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         var row = this.dataSet.ttblicseu.AsEnumerable().SingleOrDefault();
         Icseu icseu = null;
         if (row != null)
         {
             icseu = this.BuildFromRow(row);
         }
         return icseu;
      }
	  
	  
	  public IEnumerable<Icseu> GetListByRowpointers(List<string> rowpointers, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("icseu.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), fillMode, rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, bool fillMode, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("icseu.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), fillMode, 1, fldList);
         return this.dataSet.ttblicseu.AsEnumerable().SingleOrDefault();
      }
      public IEnumerable<Icseu> GetListAllByCono(int cono, bool fillMode, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("icseu.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public IEnumerable<Icseu> GetList(string where, bool fillMode, int batchsize, string fldList)
      {
         this.FetchWhere(where, fillMode, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicseu.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icseu Get(int cono, string prod, string units, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("icseu.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND icseu.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(units)) 
         {
             sb.AppendFormatWithEscape(" AND icseu.units = '{0}'", units);
         }
         var where = sb.ToString();
         return this.Fetch(where, fillMode, batchsize, fldList);
      }
  
      public Icseu GetByRowpointer(string rowpointer, bool fillMode, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("icseu.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, fillMode, 1, fldList);
      }

      public IEnumerable<Icseu> GetListByTransdttmz(DateTime? transdttmz, bool fillMode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (transdttmz != null) 
         {
             sb.AppendFormatWithEscape("icseu.transdttmz = '{0}'", transdttmz);
         }
         var where = sb.ToString();
         return this.GetList(where, fillMode, batchsize, fldList);
      }

      public Icseu BuildFromRow(DataRow row)
      {
         var returnRecord = Icseu.BuildIcseuFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icseu>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icseu record)
      {
         Icseu.UpdateRowFromIcseu(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icseu Insert(Icseu record)
      {
         DataRow row = this.dataSet.ttblicseu.NewttblicseuRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicseu.AddttblicseuRow((pdsicseuDataSet.ttblicseuRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicseu.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseu.Rows[0]) : null;
      }
  

      public Icseu Update(Icseu record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, false, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicseu.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseu.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icseu record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, false, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicseu.NewttblicseuRow();
            Icseu.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicseu.AddttblicseuRow((pdsicseuDataSet.ttblicseuRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Icseu record)
      {
         var row = this.GetRowByRowId(record.rowID, false, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttblicseu.NewttblicseuRow();
            Icseu.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicseu.AddttblicseuRow((pdsicseuDataSet.ttblicseuRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icseu() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Icseu() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      public IcseuLookupResponseAPI Lookup(Icproductunitlookupcriteria icproductunitlookupcriteria)
      {   
         var result = new IcseuLookupResponseAPI();
         
         var pdsicproductunitlookup = new pdsicproductunitlookupDataSet();
            
         DataRow ttblicproductunitlookupcriteriaCriteria = pdsicproductunitlookup.ttblicproductunitlookupcriteria.NewttblicproductunitlookupcriteriaRow();
         Icproductunitlookupcriteria.UpdateRowFromIcproductunitlookupcriteria(ref ttblicproductunitlookupcriteriaCriteria, icproductunitlookupcriteria);
         pdsicproductunitlookup.ttblicproductunitlookupcriteria.AddttblicproductunitlookupcriteriaRow((pdsicproductunitlookupDataSet.ttblicproductunitlookupcriteriaRow)ttblicproductunitlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Icseu - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcseuproxy = this.proxyAppObject.CreatePO_icseuproxy())
               {
                   this.SetRequiredContextParameters();
                   poIcseuproxy.Lookup(ref pdsContext, ref pdsicproductunitlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Icseu - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsicproductunitlookup); 
    
         foreach (DataRow row in pdsicproductunitlookup.ttblicproductunitlookupresults)
         {
            result.icproductunitlookupresults.Add(Icproductunitlookupresults.BuildIcproductunitlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  