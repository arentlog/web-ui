//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicsga;

   public partial class IcsgaAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicsga";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsgaTableControlKey;
		
      private pdsicsgaDataSet dataSet;
        
      public IcsgaAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsgaDataSet() { DataSetName = DataSetName };
            this.icsgaTableControlKey = this.dataSet.ttblicsga.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsgaTableControlKey))
            {
               this.CreateTableControlParameters(this.icsgaTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsgaAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsgaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsgaproxy = this.proxyAppObject.CreatePO_icsgaproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsgaproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsgaproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsgaTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsgaTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsgaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsgaproxy = this.proxyAppObject.CreatePO_icsgaproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsgaproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsgaproxy - After Call");
      }
   

      public Icsga GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icsga icsga = null;
         if (row != null)
         {
             icsga = this.BuildFromRow(row);
         }
         return icsga;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icsga) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicsga.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icsga> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icsga)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icsga Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicsga.AsEnumerable().SingleOrDefault();
         Icsga icsga = null;
         if (row != null)
         {
             icsga = this.BuildFromRow(row);
         }
         return icsga;
      }
	  
	  

      public IEnumerable<Icsga> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicsga.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icsga Get(int groupno, int attrno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (groupno != int.MinValue) 
         {
             sb.AppendFormatWithEscape("icsga.groupno = {0}", groupno);
         }
         if (attrno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND icsga.attrno = {0}", attrno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Icsga> GetListByBatch(string ecbatchnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(ecbatchnm)) 
         {
             sb.AppendFormatWithEscape("icsga.ecbatchnm = '{0}'", ecbatchnm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Icsga> GetListByDescrip(string descrip, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(descrip)) 
         {
             sb.AppendFormatWithEscape("icsga.descrip = '{0}'", descrip);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Icsga BuildFromRow(DataRow row)
      {
         var returnRecord = Icsga.BuildIcsgaFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icsga>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icsga record)
      {
         Icsga.UpdateRowFromIcsga(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icsga Insert(Icsga record)
      {
         DataRow row = this.dataSet.ttblicsga.NewttblicsgaRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicsga.AddttblicsgaRow((pdsicsgaDataSet.ttblicsgaRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicsga.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsga.Rows[0]) : null;
      }
  

      public Icsga Update(Icsga record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicsga.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicsga.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icsga record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicsga.NewttblicsgaRow();
            Icsga.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicsga.AddttblicsgaRow((pdsicsgaDataSet.ttblicsgaRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icsga() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  