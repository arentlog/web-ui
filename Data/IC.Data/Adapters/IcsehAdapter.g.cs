//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.IC.Data.Adapters
{
   using com.infor.sxproxy.icproxy;
   using com.infor.sxproxy.icproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsicseh;

   public partial class IcsehAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsicseh";
      private ICProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> icsehTableControlKey;
		
      private pdsicsehDataSet dataSet;
        
      public IcsehAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ICProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsicsehDataSet() { DataSetName = DataSetName };
            this.icsehTableControlKey = this.dataSet.ttblicseh.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.icsehTableControlKey))
            {
               this.CreateTableControlParameters(this.icsehTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in IcsehAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Icsehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poIcsehproxy = this.proxyAppObject.CreatePO_icsehproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poIcsehproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Icsehproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.icsehTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.icsehTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Icsehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poIcsehproxy = this.proxyAppObject.CreatePO_icsehproxy())
               {
                  this.SetRequiredContextParameters();
                  poIcsehproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Icsehproxy - After Call");
      }
   

      public Icseh GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Icseh icseh = null;
         if (row != null)
         {
             icseh = this.BuildFromRow(row);
         }
         return icseh;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(icseh) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblicseh.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Icseh> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(icseh)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Icseh Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblicseh.AsEnumerable().SingleOrDefault();
         Icseh icseh = null;
         if (row != null)
         {
             icseh = this.BuildFromRow(row);
         }
         return icseh;
      }
	  
	  

      public IEnumerable<Icseh> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblicseh.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Icseh Get(string msdssheetno, string langcd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(msdssheetno)) 
         {
             sb.AppendFormatWithEscape("icseh.msdssheetno = '{0}'", msdssheetno);
         }
         if (!string.IsNullOrEmpty(langcd)) 
         {
             sb.AppendFormatWithEscape(" AND icseh.langcd = '{0}'", langcd);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Icseh BuildFromRow(DataRow row)
      {
         var returnRecord = Icseh.BuildIcsehFromRow(row);
         returnRecord = this.BuildExtraFromRow<Icseh>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Icseh record)
      {
         Icseh.UpdateRowFromIcseh(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Icseh Insert(Icseh record)
      {
         DataRow row = this.dataSet.ttblicseh.NewttblicsehRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblicseh.AddttblicsehRow((pdsicsehDataSet.ttblicsehRow)row);
         this.SaveChanges();
         return this.dataSet.ttblicseh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseh.Rows[0]) : null;
      }
  

      public Icseh Update(Icseh record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblicseh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblicseh.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Icseh record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblicseh.NewttblicsehRow();
            Icseh.BuildMinimalRow(ref row, record);
            this.dataSet.ttblicseh.AddttblicsehRow((pdsicsehDataSet.ttblicsehRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Icseh() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  