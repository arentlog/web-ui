//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.WL.Data.Adapters
{
   using com.infor.sxproxy.wlproxy;
   using com.infor.sxproxy.wlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdswlicsw;

   public partial class WlicswAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdswlicsw";
      private WLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> wlicswTableControlKey;
		
      private pdswlicswDataSet dataSet;
        
      public WlicswAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new WLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdswlicswDataSet() { DataSetName = DataSetName };
            this.wlicswTableControlKey = this.dataSet.ttblwlicsw.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.wlicswTableControlKey))
            {
               this.CreateTableControlParameters(this.wlicswTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in WlicswAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Wlicswproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poWlicswproxy = this.proxyAppObject.CreatePO_wlicswproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poWlicswproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Wlicswproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.wlicswTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.wlicswTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Wlicswproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWlicswproxy = this.proxyAppObject.CreatePO_wlicswproxy())
               {
                  this.SetRequiredContextParameters();
                  poWlicswproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Wlicswproxy - After Call");
      }
   

      public Wlicsw GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Wlicsw wlicsw = null;
         if (row != null)
         {
             wlicsw = this.BuildFromRow(row);
         }
         return wlicsw;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(wlicsw) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwlicsw.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Wlicsw> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(wlicsw)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Wlicsw Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblwlicsw.AsEnumerable().SingleOrDefault();
         Wlicsw wlicsw = null;
         if (row != null)
         {
             wlicsw = this.BuildFromRow(row);
         }
         return wlicsw;
      }
	  
	  
      public IEnumerable<Wlicsw> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("wlicsw.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wlicsw> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblwlicsw.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Wlicsw Get(int cono, string prod, string whse, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wlicsw.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND wlicsw.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND wlicsw.whse = '{0}'", whse);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Wlicsw> GetListByUpdatefl(int cono, string whse, bool updatefl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("wlicsw.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND wlicsw.whse = '{0}'", whse);
         }
         sb.AppendFormatWithEscape(" AND wlicsw.updatefl = {0}", updatefl);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Wlicsw BuildFromRow(DataRow row)
      {
         var returnRecord = Wlicsw.BuildWlicswFromRow(row);
         returnRecord = this.BuildExtraFromRow<Wlicsw>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Wlicsw record)
      {
         Wlicsw.UpdateRowFromWlicsw(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Wlicsw Insert(Wlicsw record)
      {
         DataRow row = this.dataSet.ttblwlicsw.NewttblwlicswRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblwlicsw.AddttblwlicswRow((pdswlicswDataSet.ttblwlicswRow)row);
         this.SaveChanges();
         return this.dataSet.ttblwlicsw.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwlicsw.Rows[0]) : null;
      }
  

      public Wlicsw Update(Wlicsw record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblwlicsw.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwlicsw.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Wlicsw record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblwlicsw.NewttblwlicswRow();
            Wlicsw.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwlicsw.AddttblwlicswRow((pdswlicswDataSet.ttblwlicswRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Wlicsw() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  