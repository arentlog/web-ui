//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.WL.Data.Adapters
{
   using com.infor.sxproxy.wlproxy;
   using com.infor.sxproxy.wlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdswlal;

   public partial class WlalAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdswlal";
      private WLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> wlalTableControlKey;
		
      private pdswlalDataSet dataSet;
        
      public WlalAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new WLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdswlalDataSet() { DataSetName = DataSetName };
            this.wlalTableControlKey = this.dataSet.ttblwlal.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.wlalTableControlKey))
            {
               this.CreateTableControlParameters(this.wlalTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in WlalAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Wlalproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poWlalproxy = this.proxyAppObject.CreatePO_wlalproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poWlalproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Wlalproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.wlalTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.wlalTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Wlalproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWlalproxy = this.proxyAppObject.CreatePO_wlalproxy())
               {
                  this.SetRequiredContextParameters();
                  poWlalproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Wlalproxy - After Call");
      }
   

      public Wlal GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Wlal wlal = null;
         if (row != null)
         {
             wlal = this.BuildFromRow(row);
         }
         return wlal;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(wlal) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwlal.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Wlal> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(wlal)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Wlal Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblwlal.AsEnumerable().SingleOrDefault();
         Wlal wlal = null;
         if (row != null)
         {
             wlal = this.BuildFromRow(row);
         }
         return wlal;
      }
	  
	  
      public IEnumerable<Wlal> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("wlal.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Wlal> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblwlal.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Wlal Get(string wlloc, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(wlloc)) 
         {
             sb.AppendFormatWithEscape("wlal.wlloc = '{0}'", wlloc);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Wlal BuildFromRow(DataRow row)
      {
         var returnRecord = Wlal.BuildWlalFromRow(row);
         returnRecord = this.BuildExtraFromRow<Wlal>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Wlal record)
      {
         Wlal.UpdateRowFromWlal(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Wlal Insert(Wlal record)
      {
         DataRow row = this.dataSet.ttblwlal.NewttblwlalRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblwlal.AddttblwlalRow((pdswlalDataSet.ttblwlalRow)row);
         this.SaveChanges();
         return this.dataSet.ttblwlal.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwlal.Rows[0]) : null;
      }
  

      public Wlal Update(Wlal record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblwlal.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwlal.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Wlal record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblwlal.NewttblwlalRow();
            Wlal.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwlal.AddttblwlalRow((pdswlalDataSet.ttblwlalRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Wlal() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  