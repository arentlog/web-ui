//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.BP.Data.Adapters
{
   using com.infor.sxproxy.bpproxy;
   using com.infor.sxproxy.bpproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsbpehc;

   public partial class BpehcAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbpehc";
      private BPProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> bpehcTableControlKey;
		
      private pdsbpehcDataSet dataSet;
        
      public BpehcAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new BPProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbpehcDataSet() { DataSetName = DataSetName };
            this.bpehcTableControlKey = this.dataSet.ttblbpehc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.bpehcTableControlKey))
            {
               this.CreateTableControlParameters(this.bpehcTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BpehcAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Bpehcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBpehcproxy = this.proxyAppObject.CreatePO_bpehcproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBpehcproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Bpehcproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.bpehcTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.bpehcTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Bpehcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBpehcproxy = this.proxyAppObject.CreatePO_bpehcproxy())
               {
                  this.SetRequiredContextParameters();
                  poBpehcproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Bpehcproxy - After Call");
      }
   

      public Bpehc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Bpehc bpehc = null;
         if (row != null)
         {
             bpehc = this.BuildFromRow(row);
         }
         return bpehc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(bpehc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbpehc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Bpehc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(bpehc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Bpehc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbpehc.AsEnumerable().SingleOrDefault();
         Bpehc bpehc = null;
         if (row != null)
         {
             bpehc = this.BuildFromRow(row);
         }
         return bpehc;
      }
	  
	  
      public IEnumerable<Bpehc> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("bpehc.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Bpehc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbpehc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Bpehc Get(int cono, string bpid, int revno, string custpros, string cptype, DateTime? sentdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("bpehc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(bpid)) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.bpid = '{0}'", bpid);
         }
         if (revno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.revno = {0}", revno);
         }
         if (!string.IsNullOrEmpty(custpros)) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.custpros = '{0}'", custpros);
         }
         if (!string.IsNullOrEmpty(cptype)) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.cptype = '{0}'", cptype);
         }
         if (sentdt != null) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.sentdt = '{0}'", sentdt);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Bpehc> GetListByCustpros(int cono, string custpros, string cptype, string bpid, int revno, DateTime? sentdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("bpehc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(custpros)) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.custpros = '{0}'", custpros);
         }
         if (!string.IsNullOrEmpty(cptype)) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.cptype = '{0}'", cptype);
         }
         if (!string.IsNullOrEmpty(bpid)) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.bpid = '{0}'", bpid);
         }
         if (revno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.revno = {0}", revno);
         }
         if (sentdt != null) 
         {
             sb.AppendFormatWithEscape(" AND bpehc.sentdt = '{0}'", sentdt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Bpehc BuildFromRow(DataRow row)
      {
         var returnRecord = Bpehc.BuildBpehcFromRow(row);
         returnRecord = this.BuildExtraFromRow<Bpehc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Bpehc record)
      {
         Bpehc.UpdateRowFromBpehc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Bpehc Insert(Bpehc record)
      {
         DataRow row = this.dataSet.ttblbpehc.NewttblbpehcRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbpehc.AddttblbpehcRow((pdsbpehcDataSet.ttblbpehcRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbpehc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbpehc.Rows[0]) : null;
      }
  

      public Bpehc Update(Bpehc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbpehc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbpehc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Bpehc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbpehc.NewttblbpehcRow();
            Bpehc.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbpehc.AddttblbpehcRow((pdsbpehcDataSet.ttblbpehcRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Bpehc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  