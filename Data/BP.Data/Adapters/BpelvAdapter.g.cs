//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.BP.Data.Adapters
{
   using com.infor.sxproxy.bpproxy;
   using com.infor.sxproxy.bpproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsbpelv;

   public partial class BpelvAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbpelv";
      private BPProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> bpelvTableControlKey;
		
      private pdsbpelvDataSet dataSet;
        
      public BpelvAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new BPProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbpelvDataSet() { DataSetName = DataSetName };
            this.bpelvTableControlKey = this.dataSet.ttblbpelv.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.bpelvTableControlKey))
            {
               this.CreateTableControlParameters(this.bpelvTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BpelvAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Bpelvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBpelvproxy = this.proxyAppObject.CreatePO_bpelvproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBpelvproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Bpelvproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.bpelvTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.bpelvTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Bpelvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBpelvproxy = this.proxyAppObject.CreatePO_bpelvproxy())
               {
                  this.SetRequiredContextParameters();
                  poBpelvproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Bpelvproxy - After Call");
      }
   

      public Bpelv GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Bpelv bpelv = null;
         if (row != null)
         {
             bpelv = this.BuildFromRow(row);
         }
         return bpelv;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(bpelv) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbpelv.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Bpelv> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(bpelv)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Bpelv Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbpelv.AsEnumerable().SingleOrDefault();
         Bpelv bpelv = null;
         if (row != null)
         {
             bpelv = this.BuildFromRow(row);
         }
         return bpelv;
      }
	  
	  
      public IEnumerable<Bpelv> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("bpelv.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Bpelv> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbpelv.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Bpelv Get(int cono, string bpid, int revno, decimal vendno, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("bpelv.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(bpid)) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.bpid = '{0}'", bpid);
         }
         if (revno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.revno = {0}", revno);
         }
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.vendno = {0}", vendno);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Bpelv> GetListByLineprod(int cono, string bpid, int revno, decimal vendno, string prod, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("bpelv.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(bpid)) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.bpid = '{0}'", bpid);
         }
         if (revno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.revno = {0}", revno);
         }
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.vendno = {0}", vendno);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.prod = '{0}'", prod);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Bpelv> GetListByLinevend(int cono, string bpid, int revno, int lineno, decimal vendno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("bpelv.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(bpid)) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.bpid = '{0}'", bpid);
         }
         if (revno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.revno = {0}", revno);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.lineno = {0}", lineno);
         }
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpelv.vendno = {0}", vendno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Bpelv BuildFromRow(DataRow row)
      {
         var returnRecord = Bpelv.BuildBpelvFromRow(row);
         returnRecord = this.BuildExtraFromRow<Bpelv>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Bpelv record)
      {
         Bpelv.UpdateRowFromBpelv(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Bpelv Insert(Bpelv record)
      {
         DataRow row = this.dataSet.ttblbpelv.NewttblbpelvRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbpelv.AddttblbpelvRow((pdsbpelvDataSet.ttblbpelvRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbpelv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbpelv.Rows[0]) : null;
      }
  

      public Bpelv Update(Bpelv record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbpelv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbpelv.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Bpelv record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbpelv.NewttblbpelvRow();
            Bpelv.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbpelv.AddttblbpelvRow((pdsbpelvDataSet.ttblbpelvRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Bpelv() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  