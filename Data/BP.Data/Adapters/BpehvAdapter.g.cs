//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.BP.Data.Adapters
{
   using com.infor.sxproxy.bpproxy;
   using com.infor.sxproxy.bpproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsbpehv;

   public partial class BpehvAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbpehv";
      private BPProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> bpehvTableControlKey;
		
      private pdsbpehvDataSet dataSet;
        
      public BpehvAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new BPProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbpehvDataSet() { DataSetName = DataSetName };
            this.bpehvTableControlKey = this.dataSet.ttblbpehv.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.bpehvTableControlKey))
            {
               this.CreateTableControlParameters(this.bpehvTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BpehvAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Bpehvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBpehvproxy = this.proxyAppObject.CreatePO_bpehvproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBpehvproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Bpehvproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.bpehvTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.bpehvTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Bpehvproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBpehvproxy = this.proxyAppObject.CreatePO_bpehvproxy())
               {
                  this.SetRequiredContextParameters();
                  poBpehvproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Bpehvproxy - After Call");
      }
   

      public Bpehv GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Bpehv bpehv = null;
         if (row != null)
         {
             bpehv = this.BuildFromRow(row);
         }
         return bpehv;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(bpehv) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbpehv.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Bpehv> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(bpehv)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Bpehv Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbpehv.AsEnumerable().SingleOrDefault();
         Bpehv bpehv = null;
         if (row != null)
         {
             bpehv = this.BuildFromRow(row);
         }
         return bpehv;
      }
	  
	  
      public IEnumerable<Bpehv> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("bpehv.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Bpehv> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbpehv.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Bpehv Get(int cono, string bpid, decimal vendno, int shipfmno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("bpehv.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(bpid)) 
         {
             sb.AppendFormatWithEscape(" AND bpehv.bpid = '{0}'", bpid);
         }
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpehv.vendno = {0}", vendno);
         }
         if (shipfmno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND bpehv.shipfmno = {0}", shipfmno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Bpehv BuildFromRow(DataRow row)
      {
         var returnRecord = Bpehv.BuildBpehvFromRow(row);
         returnRecord = this.BuildExtraFromRow<Bpehv>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Bpehv record)
      {
         Bpehv.UpdateRowFromBpehv(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Bpehv Insert(Bpehv record)
      {
         DataRow row = this.dataSet.ttblbpehv.NewttblbpehvRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbpehv.AddttblbpehvRow((pdsbpehvDataSet.ttblbpehvRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbpehv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbpehv.Rows[0]) : null;
      }
  

      public Bpehv Update(Bpehv record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbpehv.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbpehv.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Bpehv record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbpehv.NewttblbpehvRow();
            Bpehv.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbpehv.AddttblbpehvRow((pdsbpehvDataSet.ttblbpehvRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Bpehv() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  