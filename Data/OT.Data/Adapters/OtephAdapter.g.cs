//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OT.Data.Adapters
{
   using com.infor.sxproxy.otproxy;
   using com.infor.sxproxy.otproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoteph;

   public partial class OtephAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoteph";
      private OTProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> otephTableControlKey;
		
      private pdsotephDataSet dataSet;
        
      public OtephAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OTProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsotephDataSet() { DataSetName = DataSetName };
            this.otephTableControlKey = this.dataSet.ttbloteph.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.otephTableControlKey))
            {
               this.CreateTableControlParameters(this.otephTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OtephAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Otephproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOtephproxy = this.proxyAppObject.CreatePO_otephproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOtephproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Otephproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.otephTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.otephTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Otephproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOtephproxy = this.proxyAppObject.CreatePO_otephproxy())
               {
                  this.SetRequiredContextParameters();
                  poOtephproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Otephproxy - After Call");
      }
   

      public Oteph GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oteph oteph = null;
         if (row != null)
         {
             oteph = this.BuildFromRow(row);
         }
         return oteph;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oteph) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloteph.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oteph> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oteph)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oteph Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloteph.AsEnumerable().SingleOrDefault();
         Oteph oteph = null;
         if (row != null)
         {
             oteph = this.BuildFromRow(row);
         }
         return oteph;
      }
	  
	  
      public IEnumerable<Oteph> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oteph.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oteph> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloteph.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oteph Get(int cono, int trackno, int pono, int posuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oteph.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (trackno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteph.trackno = {0}", trackno);
         }
         if (pono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteph.pono = {0}", pono);
         }
         if (posuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteph.posuf = {0}", posuf);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Oteph> GetListByOtpo(int cono, int pono, int posuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oteph.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (pono != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteph.pono = {0}", pono);
         }
         if (posuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteph.posuf = {0}", posuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oteph> GetListByStatus(int cono, string statustype, int trackno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oteph.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(statustype)) 
         {
             sb.AppendFormatWithEscape(" AND oteph.statustype = '{0}'", statustype);
         }
         if (trackno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteph.trackno = {0}", trackno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Oteph BuildFromRow(DataRow row)
      {
         var returnRecord = Oteph.BuildOtephFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oteph>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oteph record)
      {
         Oteph.UpdateRowFromOteph(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oteph Insert(Oteph record)
      {
         DataRow row = this.dataSet.ttbloteph.NewttblotephRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloteph.AddttblotephRow((pdsotephDataSet.ttblotephRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloteph.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloteph.Rows[0]) : null;
      }
  

      public Oteph Update(Oteph record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloteph.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloteph.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oteph record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloteph.NewttblotephRow();
            Oteph.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloteph.AddttblotephRow((pdsotephDataSet.ttblotephRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oteph() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  