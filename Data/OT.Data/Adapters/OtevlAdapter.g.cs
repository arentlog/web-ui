//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OT.Data.Adapters
{
   using com.infor.sxproxy.otproxy;
   using com.infor.sxproxy.otproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsotevl;

   public partial class OtevlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsotevl";
      private OTProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> otevlTableControlKey;
		
      private pdsotevlDataSet dataSet;
        
      public OtevlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OTProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsotevlDataSet() { DataSetName = DataSetName };
            this.otevlTableControlKey = this.dataSet.ttblotevl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.otevlTableControlKey))
            {
               this.CreateTableControlParameters(this.otevlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OtevlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Otevlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOtevlproxy = this.proxyAppObject.CreatePO_otevlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOtevlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Otevlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.otevlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.otevlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Otevlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOtevlproxy = this.proxyAppObject.CreatePO_otevlproxy())
               {
                  this.SetRequiredContextParameters();
                  poOtevlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Otevlproxy - After Call");
      }
   

      public Otevl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Otevl otevl = null;
         if (row != null)
         {
             otevl = this.BuildFromRow(row);
         }
         return otevl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(otevl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblotevl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Otevl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(otevl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Otevl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblotevl.AsEnumerable().SingleOrDefault();
         Otevl otevl = null;
         if (row != null)
         {
             otevl = this.BuildFromRow(row);
         }
         return otevl;
      }
	  
	  
      public IEnumerable<Otevl> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("otevl.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Otevl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblotevl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Otevl Get(int cono, int vesselno, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("otevl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vesselno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND otevl.vesselno = {0}", vesselno);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND otevl.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Otevl> GetListByStatus(int cono, bool statustype, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("otevl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         sb.AppendFormatWithEscape(" AND otevl.statustype = {0}", statustype);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Otevl> GetListByTrack(int cono, int trackno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("otevl.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (trackno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND otevl.trackno = {0}", trackno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Otevl BuildFromRow(DataRow row)
      {
         var returnRecord = Otevl.BuildOtevlFromRow(row);
         returnRecord = this.BuildExtraFromRow<Otevl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Otevl record)
      {
         Otevl.UpdateRowFromOtevl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Otevl Insert(Otevl record)
      {
         DataRow row = this.dataSet.ttblotevl.NewttblotevlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblotevl.AddttblotevlRow((pdsotevlDataSet.ttblotevlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblotevl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblotevl.Rows[0]) : null;
      }
  

      public Otevl Update(Otevl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblotevl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblotevl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Otevl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblotevl.NewttblotevlRow();
            Otevl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblotevl.AddttblotevlRow((pdsotevlDataSet.ttblotevlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Otevl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  