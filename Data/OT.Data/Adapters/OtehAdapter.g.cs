//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OT.Data.Adapters
{
   using com.infor.sxproxy.otproxy;
   using com.infor.sxproxy.otproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoteh;
   using Models.Pdsotorderlookup;
   using Models.Complex;

   public partial class OtehAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoteh";
      private OTProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> otehTableControlKey;
		
      private pdsotehDataSet dataSet;
        
      public OtehAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OTProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsotehDataSet() { DataSetName = DataSetName };
            this.otehTableControlKey = this.dataSet.ttbloteh.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.otehTableControlKey))
            {
               this.CreateTableControlParameters(this.otehTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OtehAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Otehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOtehproxy = this.proxyAppObject.CreatePO_otehproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOtehproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Otehproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.otehTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.otehTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Otehproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOtehproxy = this.proxyAppObject.CreatePO_otehproxy())
               {
                  this.SetRequiredContextParameters();
                  poOtehproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Otehproxy - After Call");
      }
   

      public Oteh GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oteh oteh = null;
         if (row != null)
         {
             oteh = this.BuildFromRow(row);
         }
         return oteh;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oteh) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloteh.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oteh> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oteh)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oteh Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloteh.AsEnumerable().SingleOrDefault();
         Oteh oteh = null;
         if (row != null)
         {
             oteh = this.BuildFromRow(row);
         }
         return oteh;
      }
	  
	  
      public IEnumerable<Oteh> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oteh.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oteh> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloteh.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oteh Get(int cono, int trackno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oteh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (trackno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteh.trackno = {0}", trackno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Oteh> GetListByStage(int cono, int stagecd, int trackno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oteh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (stagecd != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteh.stagecd = {0}", stagecd);
         }
         if (trackno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteh.trackno = {0}", trackno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oteh> GetListByVendno(int cono, decimal vendno, int trackno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oteh.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (vendno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteh.vendno = {0}", vendno);
         }
         if (trackno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oteh.trackno = {0}", trackno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Oteh BuildFromRow(DataRow row)
      {
         var returnRecord = Oteh.BuildOtehFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oteh>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oteh record)
      {
         Oteh.UpdateRowFromOteh(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oteh Insert(Oteh record)
      {
         DataRow row = this.dataSet.ttbloteh.NewttblotehRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloteh.AddttblotehRow((pdsotehDataSet.ttblotehRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloteh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloteh.Rows[0]) : null;
      }
  

      public Oteh Update(Oteh record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloteh.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloteh.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oteh record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloteh.NewttblotehRow();
            Oteh.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloteh.AddttblotehRow((pdsotehDataSet.ttblotehRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oteh() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public OtehLookupResponseAPI Lookup(Otorderlookupcriteria otorderlookupcriteria)
      {   
         var result = new OtehLookupResponseAPI();
         
         var pdsotorderlookup = new pdsotorderlookupDataSet();
            
         DataRow ttblotorderlookupcriteriaCriteria = pdsotorderlookup.ttblotorderlookupcriteria.NewttblotorderlookupcriteriaRow();
         Otorderlookupcriteria.UpdateRowFromOtorderlookupcriteria(ref ttblotorderlookupcriteriaCriteria, otorderlookupcriteria);
         pdsotorderlookup.ttblotorderlookupcriteria.AddttblotorderlookupcriteriaRow((pdsotorderlookupDataSet.ttblotorderlookupcriteriaRow)ttblotorderlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Oteh - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOtehproxy = this.proxyAppObject.CreatePO_otehproxy())
               {
                   this.SetRequiredContextParameters();
                   poOtehproxy.Lookup(ref pdsContext, ref pdsotorderlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Oteh - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsotorderlookup); 
    
         foreach (DataRow row in pdsotorderlookup.ttblotorderlookupresults)
         {
            result.otorderlookupresults.Add(Otorderlookupresults.BuildOtorderlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  