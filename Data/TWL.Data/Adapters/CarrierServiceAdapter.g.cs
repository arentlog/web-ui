//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdscarrierService;

   public partial class CarrierServiceAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscarrier_service";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> carrierServiceTableControlKey;
		
      private pdscarrier_serviceDataSet dataSet;
        
      public CarrierServiceAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscarrier_serviceDataSet() { DataSetName = DataSetName };
            this.carrierServiceTableControlKey = this.dataSet.ttblcarrier_service.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.carrierServiceTableControlKey))
            {
               this.CreateTableControlParameters(this.carrierServiceTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CarrierServiceAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Carrier_serviceproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCarrier_serviceproxy = this.proxyAppObject.CreatePO_carrier_serviceproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCarrier_serviceproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Carrier_serviceproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.carrierServiceTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.carrierServiceTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Carrier_serviceproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCarrier_serviceproxy = this.proxyAppObject.CreatePO_carrier_serviceproxy())
               {
                  this.SetRequiredContextParameters();
                  poCarrier_serviceproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Carrier_serviceproxy - After Call");
      }
   

      public CarrierService GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         CarrierService carrierService = null;
         if (row != null)
         {
             carrierService = this.BuildFromRow(row);
         }
         return carrierService;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(carrier_service) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcarrier_service.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<CarrierService> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(carrier_service)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected CarrierService Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcarrier_service.AsEnumerable().SingleOrDefault();
         CarrierService carrierService = null;
         if (row != null)
         {
             carrierService = this.BuildFromRow(row);
         }
         return carrierService;
      }
	  
	  

      public IEnumerable<CarrierService> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcarrier_service.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public CarrierService Get(string coNum, string whNum, string carrierId, string service, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("carrier_service.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND carrier_service.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(carrierId)) 
         {
             sb.AppendFormatWithEscape(" AND carrier_service.carrier_id = '{0}'", carrierId);
         }
         if (!string.IsNullOrEmpty(service)) 
         {
             sb.AppendFormatWithEscape(" AND carrier_service.service = '{0}'", service);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public CarrierService BuildFromRow(DataRow row)
      {
         var returnRecord = CarrierService.BuildCarrierServiceFromRow(row);
         returnRecord = this.BuildExtraFromRow<CarrierService>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, CarrierService record)
      {
         CarrierService.UpdateRowFromCarrierService(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public CarrierService Insert(CarrierService record)
      {
         DataRow row = this.dataSet.ttblcarrier_service.Newttblcarrier_serviceRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcarrier_service.Addttblcarrier_serviceRow((pdscarrier_serviceDataSet.ttblcarrier_serviceRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcarrier_service.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcarrier_service.Rows[0]) : null;
      }
  

      public CarrierService Update(CarrierService record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcarrier_service.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcarrier_service.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(CarrierService record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcarrier_service.Newttblcarrier_serviceRow();
            CarrierService.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcarrier_service.Addttblcarrier_serviceRow((pdscarrier_serviceDataSet.ttblcarrier_serviceRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new CarrierService() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  