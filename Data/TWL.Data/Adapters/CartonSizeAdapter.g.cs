//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdscartonSize;

   public partial class CartonSizeAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscarton_size";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cartonSizeTableControlKey;
		
      private pdscarton_sizeDataSet dataSet;
        
      public CartonSizeAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscarton_sizeDataSet() { DataSetName = DataSetName };
            this.cartonSizeTableControlKey = this.dataSet.ttblcarton_size.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cartonSizeTableControlKey))
            {
               this.CreateTableControlParameters(this.cartonSizeTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CartonSizeAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Carton_sizeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCarton_sizeproxy = this.proxyAppObject.CreatePO_carton_sizeproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCarton_sizeproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Carton_sizeproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cartonSizeTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cartonSizeTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Carton_sizeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCarton_sizeproxy = this.proxyAppObject.CreatePO_carton_sizeproxy())
               {
                  this.SetRequiredContextParameters();
                  poCarton_sizeproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Carton_sizeproxy - After Call");
      }
   

      public CartonSize GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         CartonSize cartonSize = null;
         if (row != null)
         {
             cartonSize = this.BuildFromRow(row);
         }
         return cartonSize;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(carton_size) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcarton_size.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<CartonSize> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(carton_size)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected CartonSize Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcarton_size.AsEnumerable().SingleOrDefault();
         CartonSize cartonSize = null;
         if (row != null)
         {
             cartonSize = this.BuildFromRow(row);
         }
         return cartonSize;
      }
	  
	  

      public IEnumerable<CartonSize> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcarton_size.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public CartonSize Get(string coNum, string whNum, string boxId, decimal cube, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("carton_size.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND carton_size.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(boxId)) 
         {
             sb.AppendFormatWithEscape(" AND carton_size.box_id = '{0}'", boxId);
         }
         if (cube != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND carton_size.cube = {0}", cube);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<CartonSize> GetListByCoWhCarrierBoxCube(string coNum, string whNum, string carrierId, string boxId, decimal cube, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("carton_size.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND carton_size.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(carrierId)) 
         {
             sb.AppendFormatWithEscape(" AND carton_size.carrier_id = '{0}'", carrierId);
         }
         if (!string.IsNullOrEmpty(boxId)) 
         {
             sb.AppendFormatWithEscape(" AND carton_size.box_id = '{0}'", boxId);
         }
         if (cube != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND carton_size.cube = {0}", cube);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public CartonSize BuildFromRow(DataRow row)
      {
         var returnRecord = CartonSize.BuildCartonSizeFromRow(row);
         returnRecord = this.BuildExtraFromRow<CartonSize>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, CartonSize record)
      {
         CartonSize.UpdateRowFromCartonSize(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public CartonSize Insert(CartonSize record)
      {
         DataRow row = this.dataSet.ttblcarton_size.Newttblcarton_sizeRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcarton_size.Addttblcarton_sizeRow((pdscarton_sizeDataSet.ttblcarton_sizeRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcarton_size.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcarton_size.Rows[0]) : null;
      }
  

      public CartonSize Update(CartonSize record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcarton_size.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcarton_size.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(CartonSize record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcarton_size.Newttblcarton_sizeRow();
            CartonSize.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcarton_size.Addttblcarton_sizeRow((pdscarton_sizeDataSet.ttblcarton_sizeRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new CartonSize() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  