//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsdepmst;

   public partial class DepmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsdepmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> depmstTableControlKey;
		
      private pdsdepmstDataSet dataSet;
        
      public DepmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsdepmstDataSet() { DataSetName = DataSetName };
            this.depmstTableControlKey = this.dataSet.ttbldepmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.depmstTableControlKey))
            {
               this.CreateTableControlParameters(this.depmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in DepmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Depmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poDepmstproxy = this.proxyAppObject.CreatePO_depmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poDepmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Depmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.depmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.depmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Depmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poDepmstproxy = this.proxyAppObject.CreatePO_depmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poDepmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Depmstproxy - After Call");
      }
   

      public Depmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Depmst depmst = null;
         if (row != null)
         {
             depmst = this.BuildFromRow(row);
         }
         return depmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(depmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbldepmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Depmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(depmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Depmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbldepmst.AsEnumerable().SingleOrDefault();
         Depmst depmst = null;
         if (row != null)
         {
             depmst = this.BuildFromRow(row);
         }
         return depmst;
      }
	  
	  

      public IEnumerable<Depmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbldepmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Depmst Get(string coNum, string whNum, int deptNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("depmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND depmst.wh_num = '{0}'", whNum);
         }
         if (deptNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND depmst.dept_num = {0}", deptNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Depmst BuildFromRow(DataRow row)
      {
         var returnRecord = Depmst.BuildDepmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Depmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Depmst record)
      {
         Depmst.UpdateRowFromDepmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Depmst Insert(Depmst record)
      {
         DataRow row = this.dataSet.ttbldepmst.NewttbldepmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbldepmst.AddttbldepmstRow((pdsdepmstDataSet.ttbldepmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttbldepmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldepmst.Rows[0]) : null;
      }
  

      public Depmst Update(Depmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbldepmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldepmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Depmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbldepmst.NewttbldepmstRow();
            Depmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttbldepmst.AddttbldepmstRow((pdsdepmstDataSet.ttbldepmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Depmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  