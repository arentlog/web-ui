//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsautodrpcfg;

   public partial class AutodrpcfgAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsautodrpcfg";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> autodrpcfgTableControlKey;
		
      private pdsautodrpcfgDataSet dataSet;
        
      public AutodrpcfgAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsautodrpcfgDataSet() { DataSetName = DataSetName };
            this.autodrpcfgTableControlKey = this.dataSet.ttblautodrpcfg.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.autodrpcfgTableControlKey))
            {
               this.CreateTableControlParameters(this.autodrpcfgTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in AutodrpcfgAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Autodrpcfgproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poAutodrpcfgproxy = this.proxyAppObject.CreatePO_autodrpcfgproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poAutodrpcfgproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Autodrpcfgproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.autodrpcfgTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.autodrpcfgTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Autodrpcfgproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poAutodrpcfgproxy = this.proxyAppObject.CreatePO_autodrpcfgproxy())
               {
                  this.SetRequiredContextParameters();
                  poAutodrpcfgproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Autodrpcfgproxy - After Call");
      }
   

      public Autodrpcfg GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Autodrpcfg autodrpcfg = null;
         if (row != null)
         {
             autodrpcfg = this.BuildFromRow(row);
         }
         return autodrpcfg;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(autodrpcfg) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblautodrpcfg.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Autodrpcfg> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(autodrpcfg)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Autodrpcfg Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblautodrpcfg.AsEnumerable().SingleOrDefault();
         Autodrpcfg autodrpcfg = null;
         if (row != null)
         {
             autodrpcfg = this.BuildFromRow(row);
         }
         return autodrpcfg;
      }
	  
	  

      public IEnumerable<Autodrpcfg> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblautodrpcfg.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Autodrpcfg Get(string coNum, string whNum, string printer, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("autodrpcfg.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND autodrpcfg.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(printer)) 
         {
             sb.AppendFormatWithEscape(" AND autodrpcfg.printer = '{0}'", printer);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Autodrpcfg BuildFromRow(DataRow row)
      {
         var returnRecord = Autodrpcfg.BuildAutodrpcfgFromRow(row);
         returnRecord = this.BuildExtraFromRow<Autodrpcfg>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Autodrpcfg record)
      {
         Autodrpcfg.UpdateRowFromAutodrpcfg(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Autodrpcfg Insert(Autodrpcfg record)
      {
         DataRow row = this.dataSet.ttblautodrpcfg.NewttblautodrpcfgRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblautodrpcfg.AddttblautodrpcfgRow((pdsautodrpcfgDataSet.ttblautodrpcfgRow)row);
         this.SaveChanges();
         return this.dataSet.ttblautodrpcfg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblautodrpcfg.Rows[0]) : null;
      }
  

      public Autodrpcfg Update(Autodrpcfg record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblautodrpcfg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblautodrpcfg.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Autodrpcfg record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblautodrpcfg.NewttblautodrpcfgRow();
            Autodrpcfg.BuildMinimalRow(ref row, record);
            this.dataSet.ttblautodrpcfg.AddttblautodrpcfgRow((pdsautodrpcfgDataSet.ttblautodrpcfgRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Autodrpcfg() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  