//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscmpmst;

   public partial class CmpmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscmpmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cmpmstTableControlKey;
		
      private pdscmpmstDataSet dataSet;
        
      public CmpmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscmpmstDataSet() { DataSetName = DataSetName };
            this.cmpmstTableControlKey = this.dataSet.ttblcmpmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cmpmstTableControlKey))
            {
               this.CreateTableControlParameters(this.cmpmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CmpmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Cmpmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCmpmstproxy = this.proxyAppObject.CreatePO_cmpmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCmpmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Cmpmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cmpmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cmpmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Cmpmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCmpmstproxy = this.proxyAppObject.CreatePO_cmpmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poCmpmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Cmpmstproxy - After Call");
      }
   

      public Cmpmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Cmpmst cmpmst = null;
         if (row != null)
         {
             cmpmst = this.BuildFromRow(row);
         }
         return cmpmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(cmpmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcmpmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Cmpmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(cmpmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Cmpmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcmpmst.AsEnumerable().SingleOrDefault();
         Cmpmst cmpmst = null;
         if (row != null)
         {
             cmpmst = this.BuildFromRow(row);
         }
         return cmpmst;
      }
	  
	  

      public IEnumerable<Cmpmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcmpmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Cmpmst Get(string coNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("cmpmst.co_num = '{0}'", coNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Cmpmst BuildFromRow(DataRow row)
      {
         var returnRecord = Cmpmst.BuildCmpmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Cmpmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Cmpmst record)
      {
         Cmpmst.UpdateRowFromCmpmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Cmpmst Insert(Cmpmst record)
      {
         DataRow row = this.dataSet.ttblcmpmst.NewttblcmpmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcmpmst.AddttblcmpmstRow((pdscmpmstDataSet.ttblcmpmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcmpmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmpmst.Rows[0]) : null;
      }
  

      public Cmpmst Update(Cmpmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcmpmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcmpmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Cmpmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcmpmst.NewttblcmpmstRow();
            Cmpmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcmpmst.AddttblcmpmstRow((pdscmpmstDataSet.ttblcmpmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Cmpmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  