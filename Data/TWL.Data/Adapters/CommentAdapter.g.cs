//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscomment;

   public partial class CommentAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscomment";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> commentTableControlKey;
		
      private pdscommentDataSet dataSet;
        
      public CommentAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscommentDataSet() { DataSetName = DataSetName };
            this.commentTableControlKey = this.dataSet.ttblcomment.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.commentTableControlKey))
            {
               this.CreateTableControlParameters(this.commentTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CommentAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Commentproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCommentproxy = this.proxyAppObject.CreatePO_commentproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCommentproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Commentproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.commentTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.commentTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Commentproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCommentproxy = this.proxyAppObject.CreatePO_commentproxy())
               {
                  this.SetRequiredContextParameters();
                  poCommentproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Commentproxy - After Call");
      }
   

      public Comment GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Comment comment = null;
         if (row != null)
         {
             comment = this.BuildFromRow(row);
         }
         return comment;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(comment) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcomment.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Comment> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(comment)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Comment Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcomment.AsEnumerable().SingleOrDefault();
         Comment comment = null;
         if (row != null)
         {
             comment = this.BuildFromRow(row);
         }
         return comment;
      }
	  
	  

      public IEnumerable<Comment> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcomment.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Comment Get(string type, int id, int line, int lineSequence, int commentLine, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(type)) 
         {
             sb.AppendFormatWithEscape("comment.type = '{0}'", type);
         }
         if (id != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND comment.id = {0}", id);
         }
         if (line != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND comment.line = {0}", line);
         }
         if (lineSequence != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND comment.line_sequence = {0}", lineSequence);
         }
         if (commentLine != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND comment.comment_line = {0}", commentLine);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Comment> GetListByCommentNum(int commentNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (commentNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape("comment.comment_num = {0}", commentNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Comment BuildFromRow(DataRow row)
      {
         var returnRecord = Comment.BuildCommentFromRow(row);
         returnRecord = this.BuildExtraFromRow<Comment>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Comment record)
      {
         Comment.UpdateRowFromComment(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Comment Insert(Comment record)
      {
         DataRow row = this.dataSet.ttblcomment.NewttblcommentRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcomment.AddttblcommentRow((pdscommentDataSet.ttblcommentRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcomment.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcomment.Rows[0]) : null;
      }
  

      public Comment Update(Comment record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcomment.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcomment.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Comment record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcomment.NewttblcommentRow();
            Comment.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcomment.AddttblcommentRow((pdscommentDataSet.ttblcommentRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Comment() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  