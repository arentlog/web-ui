//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdskitmst;

   public partial class KitmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdskitmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> kitmstTableControlKey;
		
      private pdskitmstDataSet dataSet;
        
      public KitmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdskitmstDataSet() { DataSetName = DataSetName };
            this.kitmstTableControlKey = this.dataSet.ttblkitmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.kitmstTableControlKey))
            {
               this.CreateTableControlParameters(this.kitmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in KitmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Kitmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poKitmstproxy = this.proxyAppObject.CreatePO_kitmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poKitmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Kitmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.kitmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.kitmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Kitmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poKitmstproxy = this.proxyAppObject.CreatePO_kitmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poKitmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Kitmstproxy - After Call");
      }
   

      public Kitmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Kitmst kitmst = null;
         if (row != null)
         {
             kitmst = this.BuildFromRow(row);
         }
         return kitmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(kitmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblkitmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Kitmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(kitmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Kitmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblkitmst.AsEnumerable().SingleOrDefault();
         Kitmst kitmst = null;
         if (row != null)
         {
             kitmst = this.BuildFromRow(row);
         }
         return kitmst;
      }
	  
	  

      public IEnumerable<Kitmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblkitmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Kitmst Get(string coNum, string whNum, string kitNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("kitmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND kitmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(kitNum)) 
         {
             sb.AppendFormatWithEscape(" AND kitmst.kit_num = '{0}'", kitNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Kitmst> GetListById(int id, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (id != int.MinValue) 
         {
             sb.AppendFormatWithEscape("kitmst.id = {0}", id);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Kitmst BuildFromRow(DataRow row)
      {
         var returnRecord = Kitmst.BuildKitmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Kitmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Kitmst record)
      {
         Kitmst.UpdateRowFromKitmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Kitmst Insert(Kitmst record)
      {
         DataRow row = this.dataSet.ttblkitmst.NewttblkitmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblkitmst.AddttblkitmstRow((pdskitmstDataSet.ttblkitmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblkitmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkitmst.Rows[0]) : null;
      }
  

      public Kitmst Update(Kitmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblkitmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkitmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Kitmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblkitmst.NewttblkitmstRow();
            Kitmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblkitmst.AddttblkitmstRow((pdskitmstDataSet.ttblkitmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Kitmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  