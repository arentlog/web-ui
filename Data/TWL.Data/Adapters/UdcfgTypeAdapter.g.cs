//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsudcfgType;

   public partial class UdcfgTypeAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsudcfg_type";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> udcfgTypeTableControlKey;
		
      private pdsudcfg_typeDataSet dataSet;
        
      public UdcfgTypeAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsudcfg_typeDataSet() { DataSetName = DataSetName };
            this.udcfgTypeTableControlKey = this.dataSet.ttbludcfg_type.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.udcfgTypeTableControlKey))
            {
               this.CreateTableControlParameters(this.udcfgTypeTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in UdcfgTypeAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Udcfg_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poUdcfg_typeproxy = this.proxyAppObject.CreatePO_udcfg_typeproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poUdcfg_typeproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Udcfg_typeproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.udcfgTypeTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.udcfgTypeTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Udcfg_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poUdcfg_typeproxy = this.proxyAppObject.CreatePO_udcfg_typeproxy())
               {
                  this.SetRequiredContextParameters();
                  poUdcfg_typeproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Udcfg_typeproxy - After Call");
      }
   

      public UdcfgType GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         UdcfgType udcfgType = null;
         if (row != null)
         {
             udcfgType = this.BuildFromRow(row);
         }
         return udcfgType;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(udcfg_type) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbludcfg_type.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<UdcfgType> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(udcfg_type)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected UdcfgType Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbludcfg_type.AsEnumerable().SingleOrDefault();
         UdcfgType udcfgType = null;
         if (row != null)
         {
             udcfgType = this.BuildFromRow(row);
         }
         return udcfgType;
      }
	  
	  

      public IEnumerable<UdcfgType> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbludcfg_type.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public UdcfgType Get(string coNum, string whNum, string udcType, bool udcActivefl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("udcfg_type.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND udcfg_type.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(udcType)) 
         {
             sb.AppendFormatWithEscape(" AND udcfg_type.udc_type = '{0}'", udcType);
         }
         sb.AppendFormatWithEscape(" AND udcfg_type.udc_activefl = {0}", udcActivefl);
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public UdcfgType BuildFromRow(DataRow row)
      {
         var returnRecord = UdcfgType.BuildUdcfgTypeFromRow(row);
         returnRecord = this.BuildExtraFromRow<UdcfgType>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, UdcfgType record)
      {
         UdcfgType.UpdateRowFromUdcfgType(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public UdcfgType Insert(UdcfgType record)
      {
         DataRow row = this.dataSet.ttbludcfg_type.Newttbludcfg_typeRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbludcfg_type.Addttbludcfg_typeRow((pdsudcfg_typeDataSet.ttbludcfg_typeRow)row);
         this.SaveChanges();
         return this.dataSet.ttbludcfg_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbludcfg_type.Rows[0]) : null;
      }
  

      public UdcfgType Update(UdcfgType record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbludcfg_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbludcfg_type.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(UdcfgType record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbludcfg_type.Newttbludcfg_typeRow();
            UdcfgType.BuildMinimalRow(ref row, record);
            this.dataSet.ttbludcfg_type.Addttbludcfg_typeRow((pdsudcfg_typeDataSet.ttbludcfg_typeRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new UdcfgType() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  