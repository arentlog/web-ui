//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsshfmst;

   public partial class ShfmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsshfmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> shfmstTableControlKey;
		
      private pdsshfmstDataSet dataSet;
        
      public ShfmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsshfmstDataSet() { DataSetName = DataSetName };
            this.shfmstTableControlKey = this.dataSet.ttblshfmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.shfmstTableControlKey))
            {
               this.CreateTableControlParameters(this.shfmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ShfmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Shfmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poShfmstproxy = this.proxyAppObject.CreatePO_shfmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poShfmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Shfmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.shfmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.shfmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Shfmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poShfmstproxy = this.proxyAppObject.CreatePO_shfmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poShfmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Shfmstproxy - After Call");
      }
   

      public Shfmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Shfmst shfmst = null;
         if (row != null)
         {
             shfmst = this.BuildFromRow(row);
         }
         return shfmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(shfmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblshfmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Shfmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(shfmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Shfmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblshfmst.AsEnumerable().SingleOrDefault();
         Shfmst shfmst = null;
         if (row != null)
         {
             shfmst = this.BuildFromRow(row);
         }
         return shfmst;
      }
	  
	  

      public IEnumerable<Shfmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblshfmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Shfmst Get(string coNum, string whNum, int shfNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("shfmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND shfmst.wh_num = '{0}'", whNum);
         }
         if (shfNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND shfmst.shf_num = {0}", shfNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Shfmst BuildFromRow(DataRow row)
      {
         var returnRecord = Shfmst.BuildShfmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Shfmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Shfmst record)
      {
         Shfmst.UpdateRowFromShfmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Shfmst Insert(Shfmst record)
      {
         DataRow row = this.dataSet.ttblshfmst.NewttblshfmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblshfmst.AddttblshfmstRow((pdsshfmstDataSet.ttblshfmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblshfmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblshfmst.Rows[0]) : null;
      }
  

      public Shfmst Update(Shfmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblshfmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblshfmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Shfmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblshfmst.NewttblshfmstRow();
            Shfmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblshfmst.AddttblshfmstRow((pdsshfmstDataSet.ttblshfmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Shfmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  