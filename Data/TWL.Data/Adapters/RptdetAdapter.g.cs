//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsrptdet;

   public partial class RptdetAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsrptdet";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> rptdetTableControlKey;
		
      private pdsrptdetDataSet dataSet;
        
      public RptdetAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsrptdetDataSet() { DataSetName = DataSetName };
            this.rptdetTableControlKey = this.dataSet.ttblrptdet.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.rptdetTableControlKey))
            {
               this.CreateTableControlParameters(this.rptdetTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in RptdetAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Rptdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poRptdetproxy = this.proxyAppObject.CreatePO_rptdetproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poRptdetproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Rptdetproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.rptdetTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.rptdetTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Rptdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRptdetproxy = this.proxyAppObject.CreatePO_rptdetproxy())
               {
                  this.SetRequiredContextParameters();
                  poRptdetproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Rptdetproxy - After Call");
      }
   

      public Rptdet GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Rptdet rptdet = null;
         if (row != null)
         {
             rptdet = this.BuildFromRow(row);
         }
         return rptdet;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(rptdet) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblrptdet.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Rptdet> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(rptdet)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Rptdet Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblrptdet.AsEnumerable().SingleOrDefault();
         Rptdet rptdet = null;
         if (row != null)
         {
             rptdet = this.BuildFromRow(row);
         }
         return rptdet;
      }
	  
	  

      public IEnumerable<Rptdet> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblrptdet.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Rptdet Get(string coNum, string whNum, int reportid, string reportname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rptdet.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rptdet.wh_num = '{0}'", whNum);
         }
         if (reportid != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND rptdet.reportid = {0}", reportid);
         }
         if (!string.IsNullOrEmpty(reportname)) 
         {
             sb.AppendFormatWithEscape(" AND rptdet.reportname = '{0}'", reportname);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Rptdet> GetListByReportNameIndex(string coNum, string whNum, string reportname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rptdet.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rptdet.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(reportname)) 
         {
             sb.AppendFormatWithEscape(" AND rptdet.reportname = '{0}'", reportname);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rptdet> GetListByReportTypeFindIndex(string reporttypeid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(reporttypeid)) 
         {
             sb.AppendFormatWithEscape("rptdet.reporttypeid = '{0}'", reporttypeid);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Rptdet BuildFromRow(DataRow row)
      {
         var returnRecord = Rptdet.BuildRptdetFromRow(row);
         returnRecord = this.BuildExtraFromRow<Rptdet>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Rptdet record)
      {
         Rptdet.UpdateRowFromRptdet(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Rptdet Insert(Rptdet record)
      {
         DataRow row = this.dataSet.ttblrptdet.NewttblrptdetRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblrptdet.AddttblrptdetRow((pdsrptdetDataSet.ttblrptdetRow)row);
         this.SaveChanges();
         return this.dataSet.ttblrptdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrptdet.Rows[0]) : null;
      }
  

      public Rptdet Update(Rptdet record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblrptdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrptdet.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Rptdet record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblrptdet.NewttblrptdetRow();
            Rptdet.BuildMinimalRow(ref row, record);
            this.dataSet.ttblrptdet.AddttblrptdetRow((pdsrptdetDataSet.ttblrptdetRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Rptdet() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  