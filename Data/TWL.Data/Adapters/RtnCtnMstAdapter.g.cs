//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsrtnCtnMst;

   public partial class RtnCtnMstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsrtn_ctn_mst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> rtnCtnMstTableControlKey;
		
      private pdsrtn_ctn_mstDataSet dataSet;
        
      public RtnCtnMstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsrtn_ctn_mstDataSet() { DataSetName = DataSetName };
            this.rtnCtnMstTableControlKey = this.dataSet.ttblrtn_ctn_mst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.rtnCtnMstTableControlKey))
            {
               this.CreateTableControlParameters(this.rtnCtnMstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in RtnCtnMstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Rtn_ctn_mstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poRtn_ctn_mstproxy = this.proxyAppObject.CreatePO_rtn_ctn_mstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poRtn_ctn_mstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Rtn_ctn_mstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.rtnCtnMstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.rtnCtnMstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Rtn_ctn_mstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRtn_ctn_mstproxy = this.proxyAppObject.CreatePO_rtn_ctn_mstproxy())
               {
                  this.SetRequiredContextParameters();
                  poRtn_ctn_mstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Rtn_ctn_mstproxy - After Call");
      }
   

      public RtnCtnMst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         RtnCtnMst rtnCtnMst = null;
         if (row != null)
         {
             rtnCtnMst = this.BuildFromRow(row);
         }
         return rtnCtnMst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(rtn_ctn_mst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblrtn_ctn_mst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<RtnCtnMst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(rtn_ctn_mst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected RtnCtnMst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblrtn_ctn_mst.AsEnumerable().SingleOrDefault();
         RtnCtnMst rtnCtnMst = null;
         if (row != null)
         {
             rtnCtnMst = this.BuildFromRow(row);
         }
         return rtnCtnMst;
      }
	  
	  

      public IEnumerable<RtnCtnMst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblrtn_ctn_mst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public RtnCtnMst Get(string coNum, string whNum, string cartonId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rtn_ctn_mst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtn_ctn_mst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(cartonId)) 
         {
             sb.AppendFormatWithEscape(" AND rtn_ctn_mst.carton_id = '{0}'", cartonId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<RtnCtnMst> GetListByCoWhCategoryStatus(string coNum, string whNum, string rtnCategory, string rowStatus, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rtn_ctn_mst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtn_ctn_mst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(rtnCategory)) 
         {
             sb.AppendFormatWithEscape(" AND rtn_ctn_mst.rtn_category = '{0}'", rtnCategory);
         }
         if (!string.IsNullOrEmpty(rowStatus)) 
         {
             sb.AppendFormatWithEscape(" AND rtn_ctn_mst.row_status = '{0}'", rowStatus);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<RtnCtnMst> GetListByNum(int cartonNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (cartonNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape("rtn_ctn_mst.carton_num = {0}", cartonNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public RtnCtnMst BuildFromRow(DataRow row)
      {
         var returnRecord = RtnCtnMst.BuildRtnCtnMstFromRow(row);
         returnRecord = this.BuildExtraFromRow<RtnCtnMst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, RtnCtnMst record)
      {
         RtnCtnMst.UpdateRowFromRtnCtnMst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public RtnCtnMst Insert(RtnCtnMst record)
      {
         DataRow row = this.dataSet.ttblrtn_ctn_mst.Newttblrtn_ctn_mstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblrtn_ctn_mst.Addttblrtn_ctn_mstRow((pdsrtn_ctn_mstDataSet.ttblrtn_ctn_mstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblrtn_ctn_mst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrtn_ctn_mst.Rows[0]) : null;
      }
  

      public RtnCtnMst Update(RtnCtnMst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblrtn_ctn_mst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrtn_ctn_mst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(RtnCtnMst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblrtn_ctn_mst.Newttblrtn_ctn_mstRow();
            RtnCtnMst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblrtn_ctn_mst.Addttblrtn_ctn_mstRow((pdsrtn_ctn_mstDataSet.ttblrtn_ctn_mstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new RtnCtnMst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  