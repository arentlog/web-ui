//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvendaddr;

   public partial class VendaddrAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvendaddr";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> vendaddrTableControlKey;
		
      private pdsvendaddrDataSet dataSet;
        
      public VendaddrAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvendaddrDataSet() { DataSetName = DataSetName };
            this.vendaddrTableControlKey = this.dataSet.ttblvendaddr.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.vendaddrTableControlKey))
            {
               this.CreateTableControlParameters(this.vendaddrTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VendaddrAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Vendaddrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVendaddrproxy = this.proxyAppObject.CreatePO_vendaddrproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVendaddrproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Vendaddrproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.vendaddrTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.vendaddrTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Vendaddrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVendaddrproxy = this.proxyAppObject.CreatePO_vendaddrproxy())
               {
                  this.SetRequiredContextParameters();
                  poVendaddrproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Vendaddrproxy - After Call");
      }
   

      public Vendaddr GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Vendaddr vendaddr = null;
         if (row != null)
         {
             vendaddr = this.BuildFromRow(row);
         }
         return vendaddr;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(vendaddr) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvendaddr.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Vendaddr> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(vendaddr)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Vendaddr Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvendaddr.AsEnumerable().SingleOrDefault();
         Vendaddr vendaddr = null;
         if (row != null)
         {
             vendaddr = this.BuildFromRow(row);
         }
         return vendaddr;
      }
	  
	  
	  public IEnumerable<Vendaddr> GetListByRowpointers(List<string> rowpointers, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("vendaddr.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("vendaddr.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvendaddr.AsEnumerable().SingleOrDefault();
      }

      public IEnumerable<Vendaddr> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvendaddr.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Vendaddr Get(string coNum, string whNum, string vendorId, int vendAddrNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("vendaddr.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND vendaddr.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(vendorId)) 
         {
             sb.AppendFormatWithEscape(" AND vendaddr.vendor_id = '{0}'", vendorId);
         }
         if (vendAddrNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND vendaddr.vend_addr_num = {0}", vendAddrNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Vendaddr> GetListByAddrNum(int vendAddrNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (vendAddrNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape("vendaddr.vend_addr_num = {0}", vendAddrNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Vendaddr GetByRowpointer(string rowpointer, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("vendaddr.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, 1, fldList);
      }

      public IEnumerable<Vendaddr> GetListByTransdttmz(DateTime? transdttmz, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (transdttmz != null) 
         {
             sb.AppendFormatWithEscape("vendaddr.transdttmz = '{0}'", transdttmz);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Vendaddr BuildFromRow(DataRow row)
      {
         var returnRecord = Vendaddr.BuildVendaddrFromRow(row);
         returnRecord = this.BuildExtraFromRow<Vendaddr>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Vendaddr record)
      {
         Vendaddr.UpdateRowFromVendaddr(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Vendaddr Insert(Vendaddr record)
      {
         DataRow row = this.dataSet.ttblvendaddr.NewttblvendaddrRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvendaddr.AddttblvendaddrRow((pdsvendaddrDataSet.ttblvendaddrRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvendaddr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvendaddr.Rows[0]) : null;
      }
  

      public Vendaddr Update(Vendaddr record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvendaddr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvendaddr.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Vendaddr record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvendaddr.NewttblvendaddrRow();
            Vendaddr.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvendaddr.AddttblvendaddrRow((pdsvendaddrDataSet.ttblvendaddrRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Vendaddr record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttblvendaddr.NewttblvendaddrRow();
            Vendaddr.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvendaddr.AddttblvendaddrRow((pdsvendaddrDataSet.ttblvendaddrRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Vendaddr() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Vendaddr() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  