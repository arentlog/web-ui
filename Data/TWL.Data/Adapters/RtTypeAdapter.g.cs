//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsrtType;

   public partial class RtTypeAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsrt_type";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> rtTypeTableControlKey;
		
      private pdsrt_typeDataSet dataSet;
        
      public RtTypeAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsrt_typeDataSet() { DataSetName = DataSetName };
            this.rtTypeTableControlKey = this.dataSet.ttblrt_type.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.rtTypeTableControlKey))
            {
               this.CreateTableControlParameters(this.rtTypeTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in RtTypeAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Rt_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poRt_typeproxy = this.proxyAppObject.CreatePO_rt_typeproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poRt_typeproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Rt_typeproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.rtTypeTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.rtTypeTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Rt_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRt_typeproxy = this.proxyAppObject.CreatePO_rt_typeproxy())
               {
                  this.SetRequiredContextParameters();
                  poRt_typeproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Rt_typeproxy - After Call");
      }
   

      public RtType GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         RtType rtType = null;
         if (row != null)
         {
             rtType = this.BuildFromRow(row);
         }
         return rtType;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(rt_type) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblrt_type.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<RtType> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(rt_type)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected RtType Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblrt_type.AsEnumerable().SingleOrDefault();
         RtType rtType = null;
         if (row != null)
         {
             rtType = this.BuildFromRow(row);
         }
         return rtType;
      }
	  
	  

      public IEnumerable<RtType> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblrt_type.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public RtType Get(string code, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(code)) 
         {
             sb.AppendFormatWithEscape("rt_type.code = '{0}'", code);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<RtType> GetListByName(string name, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape("rt_type.name = '{0}'", name);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public RtType BuildFromRow(DataRow row)
      {
         var returnRecord = RtType.BuildRtTypeFromRow(row);
         returnRecord = this.BuildExtraFromRow<RtType>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, RtType record)
      {
         RtType.UpdateRowFromRtType(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public RtType Insert(RtType record)
      {
         DataRow row = this.dataSet.ttblrt_type.Newttblrt_typeRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblrt_type.Addttblrt_typeRow((pdsrt_typeDataSet.ttblrt_typeRow)row);
         this.SaveChanges();
         return this.dataSet.ttblrt_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrt_type.Rows[0]) : null;
      }
  

      public RtType Update(RtType record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblrt_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrt_type.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(RtType record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblrt_type.Newttblrt_typeRow();
            RtType.BuildMinimalRow(ref row, record);
            this.dataSet.ttblrt_type.Addttblrt_typeRow((pdsrt_typeDataSet.ttblrt_typeRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new RtType() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  