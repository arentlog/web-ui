//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsparameters;

   public partial class ParametersAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsparameters";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> parametersTableControlKey;
		
      private pdsparametersDataSet dataSet;
        
      public ParametersAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsparametersDataSet() { DataSetName = DataSetName };
            this.parametersTableControlKey = this.dataSet.ttblparameters.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.parametersTableControlKey))
            {
               this.CreateTableControlParameters(this.parametersTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ParametersAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Parametersproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poParametersproxy = this.proxyAppObject.CreatePO_parametersproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poParametersproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Parametersproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.parametersTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.parametersTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Parametersproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poParametersproxy = this.proxyAppObject.CreatePO_parametersproxy())
               {
                  this.SetRequiredContextParameters();
                  poParametersproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Parametersproxy - After Call");
      }
   

      public Parameters GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Parameters parameters = null;
         if (row != null)
         {
             parameters = this.BuildFromRow(row);
         }
         return parameters;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(parameters) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblparameters.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Parameters> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(parameters)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Parameters Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblparameters.AsEnumerable().SingleOrDefault();
         Parameters parameters = null;
         if (row != null)
         {
             parameters = this.BuildFromRow(row);
         }
         return parameters;
      }
	  
	  

      public IEnumerable<Parameters> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblparameters.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Parameters Get(string coNum, string whNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("parameters.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND parameters.wh_num = '{0}'", whNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Parameters BuildFromRow(DataRow row)
      {
         var returnRecord = Parameters.BuildParametersFromRow(row);
         returnRecord = this.BuildExtraFromRow<Parameters>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Parameters record)
      {
         Parameters.UpdateRowFromParameters(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Parameters Insert(Parameters record)
      {
         DataRow row = this.dataSet.ttblparameters.NewttblparametersRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblparameters.AddttblparametersRow((pdsparametersDataSet.ttblparametersRow)row);
         this.SaveChanges();
         return this.dataSet.ttblparameters.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblparameters.Rows[0]) : null;
      }
  

      public Parameters Update(Parameters record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblparameters.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblparameters.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Parameters record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblparameters.NewttblparametersRow();
            Parameters.BuildMinimalRow(ref row, record);
            this.dataSet.ttblparameters.AddttblparametersRow((pdsparametersDataSet.ttblparametersRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Parameters() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  