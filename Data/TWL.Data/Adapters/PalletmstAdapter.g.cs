//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspalletmst;

   public partial class PalletmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspalletmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> palletmstTableControlKey;
		
      private pdspalletmstDataSet dataSet;
        
      public PalletmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspalletmstDataSet() { DataSetName = DataSetName };
            this.palletmstTableControlKey = this.dataSet.ttblpalletmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.palletmstTableControlKey))
            {
               this.CreateTableControlParameters(this.palletmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PalletmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Palletmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPalletmstproxy = this.proxyAppObject.CreatePO_palletmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPalletmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Palletmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.palletmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.palletmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Palletmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPalletmstproxy = this.proxyAppObject.CreatePO_palletmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poPalletmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Palletmstproxy - After Call");
      }
   

      public Palletmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Palletmst palletmst = null;
         if (row != null)
         {
             palletmst = this.BuildFromRow(row);
         }
         return palletmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(palletmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpalletmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Palletmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(palletmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Palletmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpalletmst.AsEnumerable().SingleOrDefault();
         Palletmst palletmst = null;
         if (row != null)
         {
             palletmst = this.BuildFromRow(row);
         }
         return palletmst;
      }
	  
	  

      public IEnumerable<Palletmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpalletmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Palletmst Get(string coNum, string whNum, string palletId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("palletmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(palletId)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.pallet_id = '{0}'", palletId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Palletmst> GetListByCoWhCarrierStat(string coNum, string whNum, string carrierId, string rowStatus, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("palletmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(carrierId)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.carrier_id = '{0}'", carrierId);
         }
         if (!string.IsNullOrEmpty(rowStatus)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.row_status = '{0}'", rowStatus);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Palletmst> GetListByCoWhCartidCartbinPallet(string coNum, string whNum, string cartId, string cartBin, string palletId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("palletmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(cartId)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.cart_id = '{0}'", cartId);
         }
         if (!string.IsNullOrEmpty(cartBin)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.cart_bin = '{0}'", cartBin);
         }
         if (!string.IsNullOrEmpty(palletId)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.pallet_id = '{0}'", palletId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Palletmst> GetListByCoWhCustStat(string coNum, string whNum, string custCode, string rowStatus, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("palletmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(custCode)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.cust_code = '{0}'", custCode);
         }
         if (!string.IsNullOrEmpty(rowStatus)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.row_status = '{0}'", rowStatus);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Palletmst> GetListByCoWhPalletType(string coNum, string whNum, string palletType, string rowStatus, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("palletmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(palletType)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.pallet_type = '{0}'", palletType);
         }
         if (!string.IsNullOrEmpty(rowStatus)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.row_status = '{0}'", rowStatus);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Palletmst> GetListByCoWhTracking(string coNum, string whNum, string trackingId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("palletmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(trackingId)) 
         {
             sb.AppendFormatWithEscape(" AND palletmst.tracking_id = '{0}'", trackingId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Palletmst> GetListByNum(int palletNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (palletNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape("palletmst.pallet_num = {0}", palletNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Palletmst BuildFromRow(DataRow row)
      {
         var returnRecord = Palletmst.BuildPalletmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Palletmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Palletmst record)
      {
         Palletmst.UpdateRowFromPalletmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Palletmst Insert(Palletmst record)
      {
         DataRow row = this.dataSet.ttblpalletmst.NewttblpalletmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpalletmst.AddttblpalletmstRow((pdspalletmstDataSet.ttblpalletmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpalletmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpalletmst.Rows[0]) : null;
      }
  

      public Palletmst Update(Palletmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpalletmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpalletmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Palletmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpalletmst.NewttblpalletmstRow();
            Palletmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpalletmst.AddttblpalletmstRow((pdspalletmstDataSet.ttblpalletmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Palletmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  