//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscommst;

   public partial class CommstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscommst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> commstTableControlKey;
		
      private pdscommstDataSet dataSet;
        
      public CommstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscommstDataSet() { DataSetName = DataSetName };
            this.commstTableControlKey = this.dataSet.ttblcommst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.commstTableControlKey))
            {
               this.CreateTableControlParameters(this.commstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CommstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Commstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCommstproxy = this.proxyAppObject.CreatePO_commstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCommstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Commstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.commstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.commstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Commstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCommstproxy = this.proxyAppObject.CreatePO_commstproxy())
               {
                  this.SetRequiredContextParameters();
                  poCommstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Commstproxy - After Call");
      }
   

      public Commst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Commst commst = null;
         if (row != null)
         {
             commst = this.BuildFromRow(row);
         }
         return commst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(commst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcommst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Commst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(commst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Commst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcommst.AsEnumerable().SingleOrDefault();
         Commst commst = null;
         if (row != null)
         {
             commst = this.BuildFromRow(row);
         }
         return commst;
      }
	  
	  

      public IEnumerable<Commst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcommst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Commst Get(string recordType, string version, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(recordType)) 
         {
             sb.AppendFormatWithEscape("commst.record_type = '{0}'", recordType);
         }
         if (!string.IsNullOrEmpty(version)) 
         {
             sb.AppendFormatWithEscape(" AND commst.version = '{0}'", version);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Commst> GetListByActiveUploadTransType(bool active, bool upload, string transType, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("commst.active = {0}", active);
         sb.AppendFormatWithEscape(" AND commst.upload = {0}", upload);
         if (!string.IsNullOrEmpty(transType)) 
         {
             sb.AppendFormatWithEscape(" AND commst.trans_type = '{0}'", transType);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Commst> GetListByVersionType(string version, string recordType, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(version)) 
         {
             sb.AppendFormatWithEscape("commst.version = '{0}'", version);
         }
         if (!string.IsNullOrEmpty(recordType)) 
         {
             sb.AppendFormatWithEscape(" AND commst.record_type = '{0}'", recordType);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Commst BuildFromRow(DataRow row)
      {
         var returnRecord = Commst.BuildCommstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Commst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Commst record)
      {
         Commst.UpdateRowFromCommst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Commst Insert(Commst record)
      {
         DataRow row = this.dataSet.ttblcommst.NewttblcommstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcommst.AddttblcommstRow((pdscommstDataSet.ttblcommstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcommst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcommst.Rows[0]) : null;
      }
  

      public Commst Update(Commst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcommst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcommst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Commst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcommst.NewttblcommstRow();
            Commst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcommst.AddttblcommstRow((pdscommstDataSet.ttblcommstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Commst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  