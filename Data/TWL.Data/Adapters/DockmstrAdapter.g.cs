//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsdockmstr;

   public partial class DockmstrAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsdockmstr";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> dockmstrTableControlKey;
		
      private pdsdockmstrDataSet dataSet;
        
      public DockmstrAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsdockmstrDataSet() { DataSetName = DataSetName };
            this.dockmstrTableControlKey = this.dataSet.ttbldockmstr.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.dockmstrTableControlKey))
            {
               this.CreateTableControlParameters(this.dockmstrTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in DockmstrAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Dockmstrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poDockmstrproxy = this.proxyAppObject.CreatePO_dockmstrproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poDockmstrproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Dockmstrproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.dockmstrTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.dockmstrTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Dockmstrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poDockmstrproxy = this.proxyAppObject.CreatePO_dockmstrproxy())
               {
                  this.SetRequiredContextParameters();
                  poDockmstrproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Dockmstrproxy - After Call");
      }
   

      public Dockmstr GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Dockmstr dockmstr = null;
         if (row != null)
         {
             dockmstr = this.BuildFromRow(row);
         }
         return dockmstr;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(dockmstr) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbldockmstr.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Dockmstr> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(dockmstr)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Dockmstr Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbldockmstr.AsEnumerable().SingleOrDefault();
         Dockmstr dockmstr = null;
         if (row != null)
         {
             dockmstr = this.BuildFromRow(row);
         }
         return dockmstr;
      }
	  
	  

      public IEnumerable<Dockmstr> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbldockmstr.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Dockmstr Get(string coNum, string whNum, string dockId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("dockmstr.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND dockmstr.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(dockId)) 
         {
             sb.AppendFormatWithEscape(" AND dockmstr.dock_id = '{0}'", dockId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Dockmstr BuildFromRow(DataRow row)
      {
         var returnRecord = Dockmstr.BuildDockmstrFromRow(row);
         returnRecord = this.BuildExtraFromRow<Dockmstr>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Dockmstr record)
      {
         Dockmstr.UpdateRowFromDockmstr(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Dockmstr Insert(Dockmstr record)
      {
         DataRow row = this.dataSet.ttbldockmstr.NewttbldockmstrRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbldockmstr.AddttbldockmstrRow((pdsdockmstrDataSet.ttbldockmstrRow)row);
         this.SaveChanges();
         return this.dataSet.ttbldockmstr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldockmstr.Rows[0]) : null;
      }
  

      public Dockmstr Update(Dockmstr record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbldockmstr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldockmstr.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Dockmstr record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbldockmstr.NewttbldockmstrRow();
            Dockmstr.BuildMinimalRow(ref row, record);
            this.dataSet.ttbldockmstr.AddttbldockmstrRow((pdsdockmstrDataSet.ttbldockmstrRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Dockmstr() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  