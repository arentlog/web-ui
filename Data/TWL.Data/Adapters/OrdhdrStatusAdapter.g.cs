//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsordhdrStatus;

   public partial class OrdhdrStatusAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsordhdr_status";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> ordhdrStatusTableControlKey;
		
      private pdsordhdr_statusDataSet dataSet;
        
      public OrdhdrStatusAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsordhdr_statusDataSet() { DataSetName = DataSetName };
            this.ordhdrStatusTableControlKey = this.dataSet.ttblordhdr_status.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.ordhdrStatusTableControlKey))
            {
               this.CreateTableControlParameters(this.ordhdrStatusTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OrdhdrStatusAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Ordhdr_statusproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOrdhdr_statusproxy = this.proxyAppObject.CreatePO_ordhdr_statusproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOrdhdr_statusproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Ordhdr_statusproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.ordhdrStatusTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.ordhdrStatusTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Ordhdr_statusproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOrdhdr_statusproxy = this.proxyAppObject.CreatePO_ordhdr_statusproxy())
               {
                  this.SetRequiredContextParameters();
                  poOrdhdr_statusproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Ordhdr_statusproxy - After Call");
      }
   

      public OrdhdrStatus GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         OrdhdrStatus ordhdrStatus = null;
         if (row != null)
         {
             ordhdrStatus = this.BuildFromRow(row);
         }
         return ordhdrStatus;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(ordhdr_status) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblordhdr_status.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<OrdhdrStatus> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(ordhdr_status)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected OrdhdrStatus Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblordhdr_status.AsEnumerable().SingleOrDefault();
         OrdhdrStatus ordhdrStatus = null;
         if (row != null)
         {
             ordhdrStatus = this.BuildFromRow(row);
         }
         return ordhdrStatus;
      }
	  
	  

      public IEnumerable<OrdhdrStatus> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblordhdr_status.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public OrdhdrStatus Get(string code, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(code)) 
         {
             sb.AppendFormatWithEscape("ordhdr_status.code = '{0}'", code);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<OrdhdrStatus> GetListByName(string name, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape("ordhdr_status.name = '{0}'", name);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<OrdhdrStatus> GetListBySequence(int sequence, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (sequence != int.MinValue) 
         {
             sb.AppendFormatWithEscape("ordhdr_status.sequence = {0}", sequence);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public OrdhdrStatus BuildFromRow(DataRow row)
      {
         var returnRecord = OrdhdrStatus.BuildOrdhdrStatusFromRow(row);
         returnRecord = this.BuildExtraFromRow<OrdhdrStatus>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, OrdhdrStatus record)
      {
         OrdhdrStatus.UpdateRowFromOrdhdrStatus(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public OrdhdrStatus Insert(OrdhdrStatus record)
      {
         DataRow row = this.dataSet.ttblordhdr_status.Newttblordhdr_statusRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblordhdr_status.Addttblordhdr_statusRow((pdsordhdr_statusDataSet.ttblordhdr_statusRow)row);
         this.SaveChanges();
         return this.dataSet.ttblordhdr_status.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblordhdr_status.Rows[0]) : null;
      }
  

      public OrdhdrStatus Update(OrdhdrStatus record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblordhdr_status.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblordhdr_status.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(OrdhdrStatus record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblordhdr_status.Newttblordhdr_statusRow();
            OrdhdrStatus.BuildMinimalRow(ref row, record);
            this.dataSet.ttblordhdr_status.Addttblordhdr_statusRow((pdsordhdr_statusDataSet.ttblordhdr_statusRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new OrdhdrStatus() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  