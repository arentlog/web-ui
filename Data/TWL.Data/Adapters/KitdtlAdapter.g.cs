//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdskitdtl;

   public partial class KitdtlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdskitdtl";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> kitdtlTableControlKey;
		
      private pdskitdtlDataSet dataSet;
        
      public KitdtlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdskitdtlDataSet() { DataSetName = DataSetName };
            this.kitdtlTableControlKey = this.dataSet.ttblkitdtl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.kitdtlTableControlKey))
            {
               this.CreateTableControlParameters(this.kitdtlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in KitdtlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Kitdtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poKitdtlproxy = this.proxyAppObject.CreatePO_kitdtlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poKitdtlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Kitdtlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.kitdtlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.kitdtlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Kitdtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poKitdtlproxy = this.proxyAppObject.CreatePO_kitdtlproxy())
               {
                  this.SetRequiredContextParameters();
                  poKitdtlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Kitdtlproxy - After Call");
      }
   

      public Kitdtl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Kitdtl kitdtl = null;
         if (row != null)
         {
             kitdtl = this.BuildFromRow(row);
         }
         return kitdtl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(kitdtl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblkitdtl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Kitdtl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(kitdtl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Kitdtl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblkitdtl.AsEnumerable().SingleOrDefault();
         Kitdtl kitdtl = null;
         if (row != null)
         {
             kitdtl = this.BuildFromRow(row);
         }
         return kitdtl;
      }
	  
	  

      public IEnumerable<Kitdtl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblkitdtl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Kitdtl Get(int id, string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (id != int.MinValue) 
         {
             sb.AppendFormatWithEscape("kitdtl.id = {0}", id);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND kitdtl.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Kitdtl> GetListByCoWhAbs(string coNum, string whNum, string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("kitdtl.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND kitdtl.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND kitdtl.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Kitdtl BuildFromRow(DataRow row)
      {
         var returnRecord = Kitdtl.BuildKitdtlFromRow(row);
         returnRecord = this.BuildExtraFromRow<Kitdtl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Kitdtl record)
      {
         Kitdtl.UpdateRowFromKitdtl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Kitdtl Insert(Kitdtl record)
      {
         DataRow row = this.dataSet.ttblkitdtl.NewttblkitdtlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblkitdtl.AddttblkitdtlRow((pdskitdtlDataSet.ttblkitdtlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblkitdtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkitdtl.Rows[0]) : null;
      }
  

      public Kitdtl Update(Kitdtl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblkitdtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblkitdtl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Kitdtl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblkitdtl.NewttblkitdtlRow();
            Kitdtl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblkitdtl.AddttblkitdtlRow((pdskitdtlDataSet.ttblkitdtlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Kitdtl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  