//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsudCalc;

   public partial class UdCalcAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsud_calc";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> udCalcTableControlKey;
		
      private pdsud_calcDataSet dataSet;
        
      public UdCalcAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsud_calcDataSet() { DataSetName = DataSetName };
            this.udCalcTableControlKey = this.dataSet.ttblud_calc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.udCalcTableControlKey))
            {
               this.CreateTableControlParameters(this.udCalcTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in UdCalcAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Ud_calcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poUd_calcproxy = this.proxyAppObject.CreatePO_ud_calcproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poUd_calcproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Ud_calcproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.udCalcTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.udCalcTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Ud_calcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poUd_calcproxy = this.proxyAppObject.CreatePO_ud_calcproxy())
               {
                  this.SetRequiredContextParameters();
                  poUd_calcproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Ud_calcproxy - After Call");
      }
   

      public UdCalc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         UdCalc udCalc = null;
         if (row != null)
         {
             udCalc = this.BuildFromRow(row);
         }
         return udCalc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(ud_calc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblud_calc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<UdCalc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(ud_calc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected UdCalc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblud_calc.AsEnumerable().SingleOrDefault();
         UdCalc udCalc = null;
         if (row != null)
         {
             udCalc = this.BuildFromRow(row);
         }
         return udCalc;
      }
	  
	  

      public IEnumerable<UdCalc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblud_calc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public UdCalc Get(string coNum, string whNum, string programName, string procName, string sectionName, string subsectionName, string udcType, string udcId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("ud_calc.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND ud_calc.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(programName)) 
         {
             sb.AppendFormatWithEscape(" AND ud_calc.program_name = '{0}'", programName);
         }
         if (!string.IsNullOrEmpty(procName)) 
         {
             sb.AppendFormatWithEscape(" AND ud_calc.proc_name = '{0}'", procName);
         }
         if (!string.IsNullOrEmpty(sectionName)) 
         {
             sb.AppendFormatWithEscape(" AND ud_calc.section_name = '{0}'", sectionName);
         }
         if (!string.IsNullOrEmpty(subsectionName)) 
         {
             sb.AppendFormatWithEscape(" AND ud_calc.subsection_name = '{0}'", subsectionName);
         }
         if (!string.IsNullOrEmpty(udcType)) 
         {
             sb.AppendFormatWithEscape(" AND ud_calc.udc_type = '{0}'", udcType);
         }
         if (!string.IsNullOrEmpty(udcId)) 
         {
             sb.AppendFormatWithEscape(" AND ud_calc.udc_id = '{0}'", udcId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public UdCalc BuildFromRow(DataRow row)
      {
         var returnRecord = UdCalc.BuildUdCalcFromRow(row);
         returnRecord = this.BuildExtraFromRow<UdCalc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, UdCalc record)
      {
         UdCalc.UpdateRowFromUdCalc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public UdCalc Insert(UdCalc record)
      {
         DataRow row = this.dataSet.ttblud_calc.Newttblud_calcRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblud_calc.Addttblud_calcRow((pdsud_calcDataSet.ttblud_calcRow)row);
         this.SaveChanges();
         return this.dataSet.ttblud_calc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblud_calc.Rows[0]) : null;
      }
  

      public UdCalc Update(UdCalc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblud_calc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblud_calc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(UdCalc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblud_calc.Newttblud_calcRow();
            UdCalc.BuildMinimalRow(ref row, record);
            this.dataSet.ttblud_calc.Addttblud_calcRow((pdsud_calcDataSet.ttblud_calcRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new UdCalc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  