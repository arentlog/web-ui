//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscartondtl;

   public partial class CartondtlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscartondtl";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cartondtlTableControlKey;
		
      private pdscartondtlDataSet dataSet;
        
      public CartondtlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscartondtlDataSet() { DataSetName = DataSetName };
            this.cartondtlTableControlKey = this.dataSet.ttblcartondtl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cartondtlTableControlKey))
            {
               this.CreateTableControlParameters(this.cartondtlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CartondtlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Cartondtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCartondtlproxy = this.proxyAppObject.CreatePO_cartondtlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCartondtlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Cartondtlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cartondtlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cartondtlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Cartondtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCartondtlproxy = this.proxyAppObject.CreatePO_cartondtlproxy())
               {
                  this.SetRequiredContextParameters();
                  poCartondtlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Cartondtlproxy - After Call");
      }
   

      public Cartondtl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Cartondtl cartondtl = null;
         if (row != null)
         {
             cartondtl = this.BuildFromRow(row);
         }
         return cartondtl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(cartondtl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcartondtl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Cartondtl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(cartondtl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Cartondtl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcartondtl.AsEnumerable().SingleOrDefault();
         Cartondtl cartondtl = null;
         if (row != null)
         {
             cartondtl = this.BuildFromRow(row);
         }
         return cartondtl;
      }
	  
	  

      public IEnumerable<Cartondtl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcartondtl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Cartondtl Get(int cartonNum, string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (cartonNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape("cartondtl.carton_num = {0}", cartonNum);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND cartondtl.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Cartondtl> GetListByAbsNum(string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape("cartondtl.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Cartondtl> GetListByOrderOrdsuf(string order, string orderSuffix, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(order)) 
         {
             sb.AppendFormatWithEscape("cartondtl.order = '{0}'", order);
         }
         if (!string.IsNullOrEmpty(orderSuffix)) 
         {
             sb.AppendFormatWithEscape(" AND cartondtl.order_suffix = '{0}'", orderSuffix);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Cartondtl> GetListByPickId(int pickId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (pickId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("cartondtl.pick_id = {0}", pickId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Cartondtl BuildFromRow(DataRow row)
      {
         var returnRecord = Cartondtl.BuildCartondtlFromRow(row);
         returnRecord = this.BuildExtraFromRow<Cartondtl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Cartondtl record)
      {
         Cartondtl.UpdateRowFromCartondtl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Cartondtl Insert(Cartondtl record)
      {
         DataRow row = this.dataSet.ttblcartondtl.NewttblcartondtlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcartondtl.AddttblcartondtlRow((pdscartondtlDataSet.ttblcartondtlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcartondtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcartondtl.Rows[0]) : null;
      }
  

      public Cartondtl Update(Cartondtl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcartondtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcartondtl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Cartondtl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcartondtl.NewttblcartondtlRow();
            Cartondtl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcartondtl.AddttblcartondtlRow((pdscartondtlDataSet.ttblcartondtlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Cartondtl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  