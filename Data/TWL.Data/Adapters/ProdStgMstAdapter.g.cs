//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsprodStgMst;

   public partial class ProdStgMstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsprod_stg_mst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> prodStgMstTableControlKey;
		
      private pdsprod_stg_mstDataSet dataSet;
        
      public ProdStgMstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsprod_stg_mstDataSet() { DataSetName = DataSetName };
            this.prodStgMstTableControlKey = this.dataSet.ttblprod_stg_mst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.prodStgMstTableControlKey))
            {
               this.CreateTableControlParameters(this.prodStgMstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ProdStgMstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Prod_stg_mstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poProd_stg_mstproxy = this.proxyAppObject.CreatePO_prod_stg_mstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poProd_stg_mstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Prod_stg_mstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.prodStgMstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.prodStgMstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Prod_stg_mstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poProd_stg_mstproxy = this.proxyAppObject.CreatePO_prod_stg_mstproxy())
               {
                  this.SetRequiredContextParameters();
                  poProd_stg_mstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Prod_stg_mstproxy - After Call");
      }
   

      public ProdStgMst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         ProdStgMst prodStgMst = null;
         if (row != null)
         {
             prodStgMst = this.BuildFromRow(row);
         }
         return prodStgMst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(prod_stg_mst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblprod_stg_mst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<ProdStgMst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(prod_stg_mst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected ProdStgMst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblprod_stg_mst.AsEnumerable().SingleOrDefault();
         ProdStgMst prodStgMst = null;
         if (row != null)
         {
             prodStgMst = this.BuildFromRow(row);
         }
         return prodStgMst;
      }
	  
	  

      public IEnumerable<ProdStgMst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblprod_stg_mst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public ProdStgMst Get(int id, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (id != int.MinValue) 
         {
             sb.AppendFormatWithEscape("prod_stg_mst.id = {0}", id);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<ProdStgMst> GetListByCoWhDate(string coNum, string whNum, string dateTime, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("prod_stg_mst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(dateTime)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.date_time = '{0}'", dateTime);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<ProdStgMst> GetListByCoWhDeptDate(string coNum, string whNum, int deptNum, string dateTime, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("prod_stg_mst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.wh_num = '{0}'", whNum);
         }
         if (deptNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.dept_num = {0}", deptNum);
         }
         if (!string.IsNullOrEmpty(dateTime)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.date_time = '{0}'", dateTime);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<ProdStgMst> GetListByCoWhDeptNameDate(string coNum, string whNum, int deptNum, string name, string dateTime, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("prod_stg_mst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.wh_num = '{0}'", whNum);
         }
         if (deptNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.dept_num = {0}", deptNum);
         }
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.name = '{0}'", name);
         }
         if (!string.IsNullOrEmpty(dateTime)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.date_time = '{0}'", dateTime);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<ProdStgMst> GetListByCoWhNameDate(string coNum, string whNum, string name, string dateTime, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("prod_stg_mst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.name = '{0}'", name);
         }
         if (!string.IsNullOrEmpty(dateTime)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.date_time = '{0}'", dateTime);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<ProdStgMst> GetListByCoWhNameLine(string coNum, string whNum, string name, int orderLine, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("prod_stg_mst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.name = '{0}'", name);
         }
         if (orderLine != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.order_line = {0}", orderLine);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<ProdStgMst> GetListByCoWhStgDate(string coNum, string whNum, string stagingStatus, string dateTime, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("prod_stg_mst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(stagingStatus)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.staging_status = '{0}'", stagingStatus);
         }
         if (!string.IsNullOrEmpty(dateTime)) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_mst.date_time = '{0}'", dateTime);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public ProdStgMst BuildFromRow(DataRow row)
      {
         var returnRecord = ProdStgMst.BuildProdStgMstFromRow(row);
         returnRecord = this.BuildExtraFromRow<ProdStgMst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, ProdStgMst record)
      {
         ProdStgMst.UpdateRowFromProdStgMst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public ProdStgMst Insert(ProdStgMst record)
      {
         DataRow row = this.dataSet.ttblprod_stg_mst.Newttblprod_stg_mstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblprod_stg_mst.Addttblprod_stg_mstRow((pdsprod_stg_mstDataSet.ttblprod_stg_mstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblprod_stg_mst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblprod_stg_mst.Rows[0]) : null;
      }
  

      public ProdStgMst Update(ProdStgMst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblprod_stg_mst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblprod_stg_mst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(ProdStgMst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblprod_stg_mst.Newttblprod_stg_mstRow();
            ProdStgMst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblprod_stg_mst.Addttblprod_stg_mstRow((pdsprod_stg_mstDataSet.ttblprod_stg_mstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new ProdStgMst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  