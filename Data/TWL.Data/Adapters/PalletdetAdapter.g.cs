//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspalletdet;

   public partial class PalletdetAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspalletdet";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> palletdetTableControlKey;
		
      private pdspalletdetDataSet dataSet;
        
      public PalletdetAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspalletdetDataSet() { DataSetName = DataSetName };
            this.palletdetTableControlKey = this.dataSet.ttblpalletdet.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.palletdetTableControlKey))
            {
               this.CreateTableControlParameters(this.palletdetTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PalletdetAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Palletdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPalletdetproxy = this.proxyAppObject.CreatePO_palletdetproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPalletdetproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Palletdetproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.palletdetTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.palletdetTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Palletdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPalletdetproxy = this.proxyAppObject.CreatePO_palletdetproxy())
               {
                  this.SetRequiredContextParameters();
                  poPalletdetproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Palletdetproxy - After Call");
      }
   

      public Palletdet GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Palletdet palletdet = null;
         if (row != null)
         {
             palletdet = this.BuildFromRow(row);
         }
         return palletdet;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(palletdet) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpalletdet.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Palletdet> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(palletdet)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Palletdet Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpalletdet.AsEnumerable().SingleOrDefault();
         Palletdet palletdet = null;
         if (row != null)
         {
             palletdet = this.BuildFromRow(row);
         }
         return palletdet;
      }
	  
	  

      public IEnumerable<Palletdet> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpalletdet.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Palletdet Get(int palletNum, string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (palletNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape("palletdet.pallet_num = {0}", palletNum);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND palletdet.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Palletdet> GetListByAbs(string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape("palletdet.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Palletdet> GetListByPickId(string pickId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(pickId)) 
         {
             sb.AppendFormatWithEscape("palletdet.pick_id = '{0}'", pickId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Palletdet BuildFromRow(DataRow row)
      {
         var returnRecord = Palletdet.BuildPalletdetFromRow(row);
         returnRecord = this.BuildExtraFromRow<Palletdet>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Palletdet record)
      {
         Palletdet.UpdateRowFromPalletdet(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Palletdet Insert(Palletdet record)
      {
         DataRow row = this.dataSet.ttblpalletdet.NewttblpalletdetRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpalletdet.AddttblpalletdetRow((pdspalletdetDataSet.ttblpalletdetRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpalletdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpalletdet.Rows[0]) : null;
      }
  

      public Palletdet Update(Palletdet record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpalletdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpalletdet.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Palletdet record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpalletdet.NewttblpalletdetRow();
            Palletdet.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpalletdet.AddttblpalletdetRow((pdspalletdetDataSet.ttblpalletdetRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Palletdet() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  