//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdswhZone;

   public partial class WhZoneAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdswh_zone";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> whZoneTableControlKey;
		
      private pdswh_zoneDataSet dataSet;
        
      public WhZoneAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdswh_zoneDataSet() { DataSetName = DataSetName };
            this.whZoneTableControlKey = this.dataSet.ttblwh_zone.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.whZoneTableControlKey))
            {
               this.CreateTableControlParameters(this.whZoneTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in WhZoneAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Wh_zoneproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poWh_zoneproxy = this.proxyAppObject.CreatePO_wh_zoneproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poWh_zoneproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Wh_zoneproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.whZoneTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.whZoneTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Wh_zoneproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWh_zoneproxy = this.proxyAppObject.CreatePO_wh_zoneproxy())
               {
                  this.SetRequiredContextParameters();
                  poWh_zoneproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Wh_zoneproxy - After Call");
      }
   

      public WhZone GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         WhZone whZone = null;
         if (row != null)
         {
             whZone = this.BuildFromRow(row);
         }
         return whZone;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(wh_zone) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwh_zone.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<WhZone> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(wh_zone)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected WhZone Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblwh_zone.AsEnumerable().SingleOrDefault();
         WhZone whZone = null;
         if (row != null)
         {
             whZone = this.BuildFromRow(row);
         }
         return whZone;
      }
	  
	  

      public IEnumerable<WhZone> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblwh_zone.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public WhZone Get(string coNum, string whNum, int pickSequence, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("wh_zone.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND wh_zone.wh_num = '{0}'", whNum);
         }
         if (pickSequence != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND wh_zone.pick_sequence = {0}", pickSequence);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<WhZone> GetListByCoWhName(string coNum, string whNum, string whZone, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("wh_zone.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND wh_zone.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(whZone)) 
         {
             sb.AppendFormatWithEscape(" AND wh_zone.wh_zone = '{0}'", whZone);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<WhZone> GetListByCoZone(string coNum, string whZone, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("wh_zone.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whZone)) 
         {
             sb.AppendFormatWithEscape(" AND wh_zone.wh_zone = '{0}'", whZone);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public WhZone BuildFromRow(DataRow row)
      {
         var returnRecord = WhZone.BuildWhZoneFromRow(row);
         returnRecord = this.BuildExtraFromRow<WhZone>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, WhZone record)
      {
         WhZone.UpdateRowFromWhZone(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public WhZone Insert(WhZone record)
      {
         DataRow row = this.dataSet.ttblwh_zone.Newttblwh_zoneRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblwh_zone.Addttblwh_zoneRow((pdswh_zoneDataSet.ttblwh_zoneRow)row);
         this.SaveChanges();
         return this.dataSet.ttblwh_zone.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwh_zone.Rows[0]) : null;
      }
  

      public WhZone Update(WhZone record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblwh_zone.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwh_zone.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(WhZone record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblwh_zone.Newttblwh_zoneRow();
            WhZone.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwh_zone.Addttblwh_zoneRow((pdswh_zoneDataSet.ttblwh_zoneRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new WhZone() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  