//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsrptmst;

   public partial class RptmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsrptmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> rptmstTableControlKey;
		
      private pdsrptmstDataSet dataSet;
        
      public RptmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsrptmstDataSet() { DataSetName = DataSetName };
            this.rptmstTableControlKey = this.dataSet.ttblrptmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.rptmstTableControlKey))
            {
               this.CreateTableControlParameters(this.rptmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in RptmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Rptmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poRptmstproxy = this.proxyAppObject.CreatePO_rptmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poRptmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Rptmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.rptmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.rptmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Rptmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRptmstproxy = this.proxyAppObject.CreatePO_rptmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poRptmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Rptmstproxy - After Call");
      }
   

      public Rptmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Rptmst rptmst = null;
         if (row != null)
         {
             rptmst = this.BuildFromRow(row);
         }
         return rptmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(rptmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblrptmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Rptmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(rptmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Rptmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblrptmst.AsEnumerable().SingleOrDefault();
         Rptmst rptmst = null;
         if (row != null)
         {
             rptmst = this.BuildFromRow(row);
         }
         return rptmst;
      }
	  
	  

      public IEnumerable<Rptmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblrptmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Rptmst Get(string reporttypeid, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(reporttypeid)) 
         {
             sb.AppendFormatWithEscape("rptmst.reporttypeid = '{0}'", reporttypeid);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Rptmst BuildFromRow(DataRow row)
      {
         var returnRecord = Rptmst.BuildRptmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Rptmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Rptmst record)
      {
         Rptmst.UpdateRowFromRptmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Rptmst Insert(Rptmst record)
      {
         DataRow row = this.dataSet.ttblrptmst.NewttblrptmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblrptmst.AddttblrptmstRow((pdsrptmstDataSet.ttblrptmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblrptmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrptmst.Rows[0]) : null;
      }
  

      public Rptmst Update(Rptmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblrptmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrptmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Rptmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblrptmst.NewttblrptmstRow();
            Rptmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblrptmst.AddttblrptmstRow((pdsrptmstDataSet.ttblrptmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Rptmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  