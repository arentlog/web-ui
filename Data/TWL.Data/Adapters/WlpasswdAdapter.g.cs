//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdswlpasswd;

   public partial class WlpasswdAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdswlpasswd";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> wlpasswdTableControlKey;
		
      private pdswlpasswdDataSet dataSet;
        
      public WlpasswdAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdswlpasswdDataSet() { DataSetName = DataSetName };
            this.wlpasswdTableControlKey = this.dataSet.ttblwlpasswd.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.wlpasswdTableControlKey))
            {
               this.CreateTableControlParameters(this.wlpasswdTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in WlpasswdAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Wlpasswdproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poWlpasswdproxy = this.proxyAppObject.CreatePO_wlpasswdproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poWlpasswdproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Wlpasswdproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.wlpasswdTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.wlpasswdTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Wlpasswdproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWlpasswdproxy = this.proxyAppObject.CreatePO_wlpasswdproxy())
               {
                  this.SetRequiredContextParameters();
                  poWlpasswdproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Wlpasswdproxy - After Call");
      }
   

      public Wlpasswd GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Wlpasswd wlpasswd = null;
         if (row != null)
         {
             wlpasswd = this.BuildFromRow(row);
         }
         return wlpasswd;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(wlpasswd) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwlpasswd.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Wlpasswd> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(wlpasswd)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Wlpasswd Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblwlpasswd.AsEnumerable().SingleOrDefault();
         Wlpasswd wlpasswd = null;
         if (row != null)
         {
             wlpasswd = this.BuildFromRow(row);
         }
         return wlpasswd;
      }
	  
	  

      public IEnumerable<Wlpasswd> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblwlpasswd.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Wlpasswd Get(string empNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(empNum)) 
         {
             sb.AppendFormatWithEscape("wlpasswd.emp_num = '{0}'", empNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Wlpasswd BuildFromRow(DataRow row)
      {
         var returnRecord = Wlpasswd.BuildWlpasswdFromRow(row);
         returnRecord = this.BuildExtraFromRow<Wlpasswd>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Wlpasswd record)
      {
         Wlpasswd.UpdateRowFromWlpasswd(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Wlpasswd Insert(Wlpasswd record)
      {
         DataRow row = this.dataSet.ttblwlpasswd.NewttblwlpasswdRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblwlpasswd.AddttblwlpasswdRow((pdswlpasswdDataSet.ttblwlpasswdRow)row);
         this.SaveChanges();
         return this.dataSet.ttblwlpasswd.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwlpasswd.Rows[0]) : null;
      }
  

      public Wlpasswd Update(Wlpasswd record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblwlpasswd.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwlpasswd.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Wlpasswd record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblwlpasswd.NewttblwlpasswdRow();
            Wlpasswd.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwlpasswd.AddttblwlpasswdRow((pdswlpasswdDataSet.ttblwlpasswdRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Wlpasswd() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  