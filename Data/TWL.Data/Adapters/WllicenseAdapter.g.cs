//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdswllicense;

   public partial class WllicenseAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdswllicense";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> wllicenseTableControlKey;
		
      private pdswllicenseDataSet dataSet;
        
      public WllicenseAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdswllicenseDataSet() { DataSetName = DataSetName };
            this.wllicenseTableControlKey = this.dataSet.ttblwllicense.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.wllicenseTableControlKey))
            {
               this.CreateTableControlParameters(this.wllicenseTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in WllicenseAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Wllicenseproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poWllicenseproxy = this.proxyAppObject.CreatePO_wllicenseproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poWllicenseproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Wllicenseproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.wllicenseTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.wllicenseTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Wllicenseproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poWllicenseproxy = this.proxyAppObject.CreatePO_wllicenseproxy())
               {
                  this.SetRequiredContextParameters();
                  poWllicenseproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Wllicenseproxy - After Call");
      }
   

      public Wllicense GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Wllicense wllicense = null;
         if (row != null)
         {
             wllicense = this.BuildFromRow(row);
         }
         return wllicense;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(wllicense) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblwllicense.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Wllicense> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(wllicense)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Wllicense Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblwllicense.AsEnumerable().SingleOrDefault();
         Wllicense wllicense = null;
         if (row != null)
         {
             wllicense = this.BuildFromRow(row);
         }
         return wllicense;
      }
	  
	  

      public IEnumerable<Wllicense> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblwllicense.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Wllicense Get(string appname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(appname)) 
         {
             sb.AppendFormatWithEscape("wllicense.appname = '{0}'", appname);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Wllicense BuildFromRow(DataRow row)
      {
         var returnRecord = Wllicense.BuildWllicenseFromRow(row);
         returnRecord = this.BuildExtraFromRow<Wllicense>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Wllicense record)
      {
         Wllicense.UpdateRowFromWllicense(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Wllicense Insert(Wllicense record)
      {
         DataRow row = this.dataSet.ttblwllicense.NewttblwllicenseRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblwllicense.AddttblwllicenseRow((pdswllicenseDataSet.ttblwllicenseRow)row);
         this.SaveChanges();
         return this.dataSet.ttblwllicense.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwllicense.Rows[0]) : null;
      }
  

      public Wllicense Update(Wllicense record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblwllicense.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblwllicense.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Wllicense record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblwllicense.NewttblwllicenseRow();
            Wllicense.BuildMinimalRow(ref row, record);
            this.dataSet.ttblwllicense.AddttblwllicenseRow((pdswllicenseDataSet.ttblwllicenseRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Wllicense() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  