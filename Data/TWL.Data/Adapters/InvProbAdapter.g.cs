//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsinvProb;

   public partial class InvProbAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsinv_prob";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> invProbTableControlKey;
		
      private pdsinv_probDataSet dataSet;
        
      public InvProbAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsinv_probDataSet() { DataSetName = DataSetName };
            this.invProbTableControlKey = this.dataSet.ttblinv_prob.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.invProbTableControlKey))
            {
               this.CreateTableControlParameters(this.invProbTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in InvProbAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Inv_probproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poInv_probproxy = this.proxyAppObject.CreatePO_inv_probproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poInv_probproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Inv_probproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.invProbTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.invProbTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Inv_probproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poInv_probproxy = this.proxyAppObject.CreatePO_inv_probproxy())
               {
                  this.SetRequiredContextParameters();
                  poInv_probproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Inv_probproxy - After Call");
      }
   

      public InvProb GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         InvProb invProb = null;
         if (row != null)
         {
             invProb = this.BuildFromRow(row);
         }
         return invProb;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(inv_prob) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblinv_prob.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<InvProb> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(inv_prob)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected InvProb Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblinv_prob.AsEnumerable().SingleOrDefault();
         InvProb invProb = null;
         if (row != null)
         {
             invProb = this.BuildFromRow(row);
         }
         return invProb;
      }
	  
	  

      public IEnumerable<InvProb> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblinv_prob.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public InvProb Get(string coNum, string whNum, string transType, int transNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("inv_prob.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(transType)) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.trans_type = '{0}'", transType);
         }
         if (transNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.trans_num = {0}", transNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<InvProb> GetListByCoWhAbs(string coNum, string whNum, string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("inv_prob.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<InvProb> GetListByCoWhBinAbsType(string coNum, string whNum, string binNum, string absNum, string transType, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("inv_prob.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(binNum)) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.bin_num = '{0}'", binNum);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.abs_num = '{0}'", absNum);
         }
         if (!string.IsNullOrEmpty(transType)) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.trans_type = '{0}'", transType);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<InvProb> GetListByCoWhTrans(string coNum, string whNum, int transNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("inv_prob.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.wh_num = '{0}'", whNum);
         }
         if (transNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND inv_prob.trans_num = {0}", transNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<InvProb> GetListById(int invProbId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (invProbId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("inv_prob.inv_prob_id = {0}", invProbId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public InvProb BuildFromRow(DataRow row)
      {
         var returnRecord = InvProb.BuildInvProbFromRow(row);
         returnRecord = this.BuildExtraFromRow<InvProb>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, InvProb record)
      {
         InvProb.UpdateRowFromInvProb(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public InvProb Insert(InvProb record)
      {
         DataRow row = this.dataSet.ttblinv_prob.Newttblinv_probRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblinv_prob.Addttblinv_probRow((pdsinv_probDataSet.ttblinv_probRow)row);
         this.SaveChanges();
         return this.dataSet.ttblinv_prob.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblinv_prob.Rows[0]) : null;
      }
  

      public InvProb Update(InvProb record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblinv_prob.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblinv_prob.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(InvProb record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblinv_prob.Newttblinv_probRow();
            InvProb.BuildMinimalRow(ref row, record);
            this.dataSet.ttblinv_prob.Addttblinv_probRow((pdsinv_probDataSet.ttblinv_probRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new InvProb() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  