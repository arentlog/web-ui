//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsorderType;

   public partial class OrderTypeAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsorder_type";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> orderTypeTableControlKey;
		
      private pdsorder_typeDataSet dataSet;
        
      public OrderTypeAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsorder_typeDataSet() { DataSetName = DataSetName };
            this.orderTypeTableControlKey = this.dataSet.ttblorder_type.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.orderTypeTableControlKey))
            {
               this.CreateTableControlParameters(this.orderTypeTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OrderTypeAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Order_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOrder_typeproxy = this.proxyAppObject.CreatePO_order_typeproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOrder_typeproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Order_typeproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.orderTypeTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.orderTypeTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Order_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOrder_typeproxy = this.proxyAppObject.CreatePO_order_typeproxy())
               {
                  this.SetRequiredContextParameters();
                  poOrder_typeproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Order_typeproxy - After Call");
      }
   

      public OrderType GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         OrderType orderType = null;
         if (row != null)
         {
             orderType = this.BuildFromRow(row);
         }
         return orderType;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(order_type) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblorder_type.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<OrderType> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(order_type)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected OrderType Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblorder_type.AsEnumerable().SingleOrDefault();
         OrderType orderType = null;
         if (row != null)
         {
             orderType = this.BuildFromRow(row);
         }
         return orderType;
      }
	  
	  

      public IEnumerable<OrderType> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblorder_type.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public OrderType Get(string code, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(code)) 
         {
             sb.AppendFormatWithEscape("order_type.code = '{0}'", code);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<OrderType> GetListByName(string name, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(name)) 
         {
             sb.AppendFormatWithEscape("order_type.name = '{0}'", name);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public OrderType BuildFromRow(DataRow row)
      {
         var returnRecord = OrderType.BuildOrderTypeFromRow(row);
         returnRecord = this.BuildExtraFromRow<OrderType>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, OrderType record)
      {
         OrderType.UpdateRowFromOrderType(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public OrderType Insert(OrderType record)
      {
         DataRow row = this.dataSet.ttblorder_type.Newttblorder_typeRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblorder_type.Addttblorder_typeRow((pdsorder_typeDataSet.ttblorder_typeRow)row);
         this.SaveChanges();
         return this.dataSet.ttblorder_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblorder_type.Rows[0]) : null;
      }
  

      public OrderType Update(OrderType record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblorder_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblorder_type.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(OrderType record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblorder_type.Newttblorder_typeRow();
            OrderType.BuildMinimalRow(ref row, record);
            this.dataSet.ttblorder_type.Addttblorder_typeRow((pdsorder_typeDataSet.ttblorder_typeRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new OrderType() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  