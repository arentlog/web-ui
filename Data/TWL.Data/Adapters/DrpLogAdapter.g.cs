//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsdrpLog;

   public partial class DrpLogAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsdrp_log";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> drpLogTableControlKey;
		
      private pdsdrp_logDataSet dataSet;
        
      public DrpLogAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsdrp_logDataSet() { DataSetName = DataSetName };
            this.drpLogTableControlKey = this.dataSet.ttbldrp_log.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.drpLogTableControlKey))
            {
               this.CreateTableControlParameters(this.drpLogTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in DrpLogAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Drp_logproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poDrp_logproxy = this.proxyAppObject.CreatePO_drp_logproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poDrp_logproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Drp_logproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.drpLogTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.drpLogTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Drp_logproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poDrp_logproxy = this.proxyAppObject.CreatePO_drp_logproxy())
               {
                  this.SetRequiredContextParameters();
                  poDrp_logproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Drp_logproxy - After Call");
      }
   

      public DrpLog GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         DrpLog drpLog = null;
         if (row != null)
         {
             drpLog = this.BuildFromRow(row);
         }
         return drpLog;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(drp_log) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbldrp_log.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<DrpLog> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(drp_log)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected DrpLog Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbldrp_log.AsEnumerable().SingleOrDefault();
         DrpLog drpLog = null;
         if (row != null)
         {
             drpLog = this.BuildFromRow(row);
         }
         return drpLog;
      }
	  
	  

      public IEnumerable<DrpLog> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbldrp_log.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public DrpLog Get(string coNum, string whNum, string ruleCode, int logSeq, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("drp_log.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND drp_log.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(ruleCode)) 
         {
             sb.AppendFormatWithEscape(" AND drp_log.rule_code = '{0}'", ruleCode);
         }
         if (logSeq != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND drp_log.log_seq = {0}", logSeq);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<DrpLog> GetListByCoWhSq(string coNum, string whNum, int logSeq, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("drp_log.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND drp_log.wh_num = '{0}'", whNum);
         }
         if (logSeq != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND drp_log.log_seq = {0}", logSeq);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public DrpLog BuildFromRow(DataRow row)
      {
         var returnRecord = DrpLog.BuildDrpLogFromRow(row);
         returnRecord = this.BuildExtraFromRow<DrpLog>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, DrpLog record)
      {
         DrpLog.UpdateRowFromDrpLog(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public DrpLog Insert(DrpLog record)
      {
         DataRow row = this.dataSet.ttbldrp_log.Newttbldrp_logRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbldrp_log.Addttbldrp_logRow((pdsdrp_logDataSet.ttbldrp_logRow)row);
         this.SaveChanges();
         return this.dataSet.ttbldrp_log.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldrp_log.Rows[0]) : null;
      }
  

      public DrpLog Update(DrpLog record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbldrp_log.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldrp_log.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(DrpLog record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbldrp_log.Newttbldrp_logRow();
            DrpLog.BuildMinimalRow(ref row, record);
            this.dataSet.ttbldrp_log.Addttbldrp_logRow((pdsdrp_logDataSet.ttbldrp_logRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new DrpLog() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  