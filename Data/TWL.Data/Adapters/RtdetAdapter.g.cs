//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsrtdet;

   public partial class RtdetAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsrtdet";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> rtdetTableControlKey;
		
      private pdsrtdetDataSet dataSet;
        
      public RtdetAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsrtdetDataSet() { DataSetName = DataSetName };
            this.rtdetTableControlKey = this.dataSet.ttblrtdet.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.rtdetTableControlKey))
            {
               this.CreateTableControlParameters(this.rtdetTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in RtdetAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Rtdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poRtdetproxy = this.proxyAppObject.CreatePO_rtdetproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poRtdetproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Rtdetproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.rtdetTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.rtdetTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Rtdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRtdetproxy = this.proxyAppObject.CreatePO_rtdetproxy())
               {
                  this.SetRequiredContextParameters();
                  poRtdetproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Rtdetproxy - After Call");
      }
   

      public Rtdet GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Rtdet rtdet = null;
         if (row != null)
         {
             rtdet = this.BuildFromRow(row);
         }
         return rtdet;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(rtdet) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblrtdet.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Rtdet> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(rtdet)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Rtdet Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblrtdet.AsEnumerable().SingleOrDefault();
         Rtdet rtdet = null;
         if (row != null)
         {
             rtdet = this.BuildFromRow(row);
         }
         return rtdet;
      }
	  
	  

      public IEnumerable<Rtdet> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblrtdet.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Rtdet Get(int rtId, int lineNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (rtId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("rtdet.rt_id = {0}", rtId);
         }
         if (lineNum != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.line_num = {0}", lineNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Rtdet> GetListByCoWhAbs(string coNum, string whNum, string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rtdet.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtdet> GetListByCoWhPoSufLineSeq(string coNum, string whNum, string poNumber, string poSuffix, int poLine, int lineSequence, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rtdet.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(poNumber)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.po_number = '{0}'", poNumber);
         }
         if (!string.IsNullOrEmpty(poSuffix)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.po_suffix = '{0}'", poSuffix);
         }
         if (poLine != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.po_line = {0}", poLine);
         }
         if (lineSequence != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.line_sequence = {0}", lineSequence);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtdet> GetListByCoWhStatusPoSufLineSeq(string coNum, string whNum, string rowStatus, string poNumber, string poSuffix, int poLine, int lineSequence, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rtdet.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(rowStatus)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.row_status = '{0}'", rowStatus);
         }
         if (!string.IsNullOrEmpty(poNumber)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.po_number = '{0}'", poNumber);
         }
         if (!string.IsNullOrEmpty(poSuffix)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.po_suffix = '{0}'", poSuffix);
         }
         if (poLine != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.po_line = {0}", poLine);
         }
         if (lineSequence != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.line_sequence = {0}", lineSequence);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtdet> GetListByCoWhVendorStatus(string coNum, string whNum, string vendorId, string rowStatus, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rtdet.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(vendorId)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.vendor_id = '{0}'", vendorId);
         }
         if (!string.IsNullOrEmpty(rowStatus)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.row_status = '{0}'", rowStatus);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtdet> GetListByIdAbs(int rtId, string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (rtId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("rtdet.rt_id = {0}", rtId);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtdet> GetListByIdDesc(int rtId, string itemDesc, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (rtId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("rtdet.rt_id = {0}", rtId);
         }
         if (!string.IsNullOrEmpty(itemDesc)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.item_desc = '{0}'", itemDesc);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtdet> GetListByIdItem(int rtId, string itemNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (rtId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("rtdet.rt_id = {0}", rtId);
         }
         if (!string.IsNullOrEmpty(itemNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtdet.item_num = '{0}'", itemNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Rtdet BuildFromRow(DataRow row)
      {
         var returnRecord = Rtdet.BuildRtdetFromRow(row);
         returnRecord = this.BuildExtraFromRow<Rtdet>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Rtdet record)
      {
         Rtdet.UpdateRowFromRtdet(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Rtdet Insert(Rtdet record)
      {
         DataRow row = this.dataSet.ttblrtdet.NewttblrtdetRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblrtdet.AddttblrtdetRow((pdsrtdetDataSet.ttblrtdetRow)row);
         this.SaveChanges();
         return this.dataSet.ttblrtdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrtdet.Rows[0]) : null;
      }
  

      public Rtdet Update(Rtdet record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblrtdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrtdet.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Rtdet record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblrtdet.NewttblrtdetRow();
            Rtdet.BuildMinimalRow(ref row, record);
            this.dataSet.ttblrtdet.AddttblrtdetRow((pdsrtdetDataSet.ttblrtdetRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Rtdet() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  