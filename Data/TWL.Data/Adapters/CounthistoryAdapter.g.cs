//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscounthistory;

   public partial class CounthistoryAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscounthistory";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> counthistoryTableControlKey;
		
      private pdscounthistoryDataSet dataSet;
        
      public CounthistoryAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscounthistoryDataSet() { DataSetName = DataSetName };
            this.counthistoryTableControlKey = this.dataSet.ttblcounthistory.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.counthistoryTableControlKey))
            {
               this.CreateTableControlParameters(this.counthistoryTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CounthistoryAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Counthistoryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCounthistoryproxy = this.proxyAppObject.CreatePO_counthistoryproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCounthistoryproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Counthistoryproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.counthistoryTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.counthistoryTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Counthistoryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCounthistoryproxy = this.proxyAppObject.CreatePO_counthistoryproxy())
               {
                  this.SetRequiredContextParameters();
                  poCounthistoryproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Counthistoryproxy - After Call");
      }
   

      public Counthistory GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Counthistory counthistory = null;
         if (row != null)
         {
             counthistory = this.BuildFromRow(row);
         }
         return counthistory;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(counthistory) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcounthistory.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Counthistory> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(counthistory)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Counthistory Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcounthistory.AsEnumerable().SingleOrDefault();
         Counthistory counthistory = null;
         if (row != null)
         {
             counthistory = this.BuildFromRow(row);
         }
         return counthistory;
      }
	  
	  

      public IEnumerable<Counthistory> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcounthistory.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Counthistory Get(string coNum, string whNum, string absNum, string countType, DateTime? countDate, string binNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("counthistory.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.abs_num = '{0}'", absNum);
         }
         if (!string.IsNullOrEmpty(countType)) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.count_type = '{0}'", countType);
         }
         if (countDate != null) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.count_date = '{0}'", countDate);
         }
         if (!string.IsNullOrEmpty(binNum)) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.bin_num = '{0}'", binNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Counthistory> GetListByCoWhBin(string coNum, string whNum, string binNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("counthistory.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(binNum)) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.bin_num = '{0}'", binNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Counthistory> GetListByCoWhDate(string coNum, string whNum, DateTime? countDate, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("counthistory.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.wh_num = '{0}'", whNum);
         }
         if (countDate != null) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.count_date = '{0}'", countDate);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Counthistory> GetListByCoWhType(string coNum, string whNum, string countType, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("counthistory.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(countType)) 
         {
             sb.AppendFormatWithEscape(" AND counthistory.count_type = '{0}'", countType);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Counthistory BuildFromRow(DataRow row)
      {
         var returnRecord = Counthistory.BuildCounthistoryFromRow(row);
         returnRecord = this.BuildExtraFromRow<Counthistory>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Counthistory record)
      {
         Counthistory.UpdateRowFromCounthistory(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Counthistory Insert(Counthistory record)
      {
         DataRow row = this.dataSet.ttblcounthistory.NewttblcounthistoryRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcounthistory.AddttblcounthistoryRow((pdscounthistoryDataSet.ttblcounthistoryRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcounthistory.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcounthistory.Rows[0]) : null;
      }
  

      public Counthistory Update(Counthistory record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcounthistory.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcounthistory.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Counthistory record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcounthistory.NewttblcounthistoryRow();
            Counthistory.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcounthistory.AddttblcounthistoryRow((pdscounthistoryDataSet.ttblcounthistoryRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Counthistory() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  