//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsdrpRules;

   public partial class DrpRulesAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsdrp_rules";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> drpRulesTableControlKey;
		
      private pdsdrp_rulesDataSet dataSet;
        
      public DrpRulesAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsdrp_rulesDataSet() { DataSetName = DataSetName };
            this.drpRulesTableControlKey = this.dataSet.ttbldrp_rules.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.drpRulesTableControlKey))
            {
               this.CreateTableControlParameters(this.drpRulesTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in DrpRulesAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Drp_rulesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poDrp_rulesproxy = this.proxyAppObject.CreatePO_drp_rulesproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poDrp_rulesproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Drp_rulesproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.drpRulesTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.drpRulesTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Drp_rulesproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poDrp_rulesproxy = this.proxyAppObject.CreatePO_drp_rulesproxy())
               {
                  this.SetRequiredContextParameters();
                  poDrp_rulesproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Drp_rulesproxy - After Call");
      }
   

      public DrpRules GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         DrpRules drpRules = null;
         if (row != null)
         {
             drpRules = this.BuildFromRow(row);
         }
         return drpRules;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(drp_rules) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbldrp_rules.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<DrpRules> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(drp_rules)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected DrpRules Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbldrp_rules.AsEnumerable().SingleOrDefault();
         DrpRules drpRules = null;
         if (row != null)
         {
             drpRules = this.BuildFromRow(row);
         }
         return drpRules;
      }
	  
	  

      public IEnumerable<DrpRules> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbldrp_rules.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public DrpRules Get(string coNum, string whNum, string ruleCode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("drp_rules.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND drp_rules.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(ruleCode)) 
         {
             sb.AppendFormatWithEscape(" AND drp_rules.rule_code = '{0}'", ruleCode);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<DrpRules> GetListByCoWhRunIdx(string coNum, string whNum, bool active, int processing, DateTime? nextrundt, int nextruntm, int priority, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("drp_rules.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND drp_rules.wh_num = '{0}'", whNum);
         }
         sb.AppendFormatWithEscape(" AND drp_rules.active = {0}", active);
         if (processing != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND drp_rules.processing = {0}", processing);
         }
         if (nextrundt != null) 
         {
             sb.AppendFormatWithEscape(" AND drp_rules.nextrundt = '{0}'", nextrundt);
         }
         if (nextruntm != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND drp_rules.nextruntm = {0}", nextruntm);
         }
         if (priority != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND drp_rules.priority = {0}", priority);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public DrpRules BuildFromRow(DataRow row)
      {
         var returnRecord = DrpRules.BuildDrpRulesFromRow(row);
         returnRecord = this.BuildExtraFromRow<DrpRules>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, DrpRules record)
      {
         DrpRules.UpdateRowFromDrpRules(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public DrpRules Insert(DrpRules record)
      {
         DataRow row = this.dataSet.ttbldrp_rules.Newttbldrp_rulesRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbldrp_rules.Addttbldrp_rulesRow((pdsdrp_rulesDataSet.ttbldrp_rulesRow)row);
         this.SaveChanges();
         return this.dataSet.ttbldrp_rules.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldrp_rules.Rows[0]) : null;
      }
  

      public DrpRules Update(DrpRules record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbldrp_rules.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbldrp_rules.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(DrpRules record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbldrp_rules.Newttbldrp_rulesRow();
            DrpRules.BuildMinimalRow(ref row, record);
            this.dataSet.ttbldrp_rules.Addttbldrp_rulesRow((pdsdrp_rulesDataSet.ttbldrp_rulesRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new DrpRules() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  