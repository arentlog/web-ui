//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdssysparDef;

   public partial class SysparDefAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssyspar_def";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sysparDefTableControlKey;
		
      private pdssyspar_defDataSet dataSet;
        
      public SysparDefAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssyspar_defDataSet() { DataSetName = DataSetName };
            this.sysparDefTableControlKey = this.dataSet.ttblsyspar_def.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sysparDefTableControlKey))
            {
               this.CreateTableControlParameters(this.sysparDefTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SysparDefAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Syspar_defproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSyspar_defproxy = this.proxyAppObject.CreatePO_syspar_defproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSyspar_defproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Syspar_defproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sysparDefTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sysparDefTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Syspar_defproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSyspar_defproxy = this.proxyAppObject.CreatePO_syspar_defproxy())
               {
                  this.SetRequiredContextParameters();
                  poSyspar_defproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Syspar_defproxy - After Call");
      }
   

      public SysparDef GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         SysparDef sysparDef = null;
         if (row != null)
         {
             sysparDef = this.BuildFromRow(row);
         }
         return sysparDef;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(syspar_def) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsyspar_def.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<SysparDef> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(syspar_def)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected SysparDef Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsyspar_def.AsEnumerable().SingleOrDefault();
         SysparDef sysparDef = null;
         if (row != null)
         {
             sysparDef = this.BuildFromRow(row);
         }
         return sysparDef;
      }
	  
	  

      public IEnumerable<SysparDef> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsyspar_def.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public SysparDef Get(int typeId, int parameterId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (typeId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("syspar_def.type_id = {0}", typeId);
         }
         if (parameterId != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND syspar_def.parameter_id = {0}", parameterId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<SysparDef> GetListById(int parameterId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (parameterId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("syspar_def.parameter_id = {0}", parameterId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<SysparDef> GetListByTypeid(int typeId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (typeId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("syspar_def.type_id = {0}", typeId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public SysparDef BuildFromRow(DataRow row)
      {
         var returnRecord = SysparDef.BuildSysparDefFromRow(row);
         returnRecord = this.BuildExtraFromRow<SysparDef>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, SysparDef record)
      {
         SysparDef.UpdateRowFromSysparDef(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public SysparDef Insert(SysparDef record)
      {
         DataRow row = this.dataSet.ttblsyspar_def.Newttblsyspar_defRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsyspar_def.Addttblsyspar_defRow((pdssyspar_defDataSet.ttblsyspar_defRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsyspar_def.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsyspar_def.Rows[0]) : null;
      }
  

      public SysparDef Update(SysparDef record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsyspar_def.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsyspar_def.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(SysparDef record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsyspar_def.Newttblsyspar_defRow();
            SysparDef.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsyspar_def.Addttblsyspar_defRow((pdssyspar_defDataSet.ttblsyspar_defRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new SysparDef() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  