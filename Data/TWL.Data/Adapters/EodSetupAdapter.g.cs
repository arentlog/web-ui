//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdseodSetup;

   public partial class EodSetupAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdseod_setup";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> eodSetupTableControlKey;
		
      private pdseod_setupDataSet dataSet;
        
      public EodSetupAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdseod_setupDataSet() { DataSetName = DataSetName };
            this.eodSetupTableControlKey = this.dataSet.ttbleod_setup.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.eodSetupTableControlKey))
            {
               this.CreateTableControlParameters(this.eodSetupTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in EodSetupAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Eod_setupproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poEod_setupproxy = this.proxyAppObject.CreatePO_eod_setupproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poEod_setupproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Eod_setupproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.eodSetupTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.eodSetupTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Eod_setupproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poEod_setupproxy = this.proxyAppObject.CreatePO_eod_setupproxy())
               {
                  this.SetRequiredContextParameters();
                  poEod_setupproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Eod_setupproxy - After Call");
      }
   

      public EodSetup GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         EodSetup eodSetup = null;
         if (row != null)
         {
             eodSetup = this.BuildFromRow(row);
         }
         return eodSetup;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(eod_setup) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbleod_setup.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<EodSetup> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(eod_setup)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected EodSetup Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbleod_setup.AsEnumerable().SingleOrDefault();
         EodSetup eodSetup = null;
         if (row != null)
         {
             eodSetup = this.BuildFromRow(row);
         }
         return eodSetup;
      }
	  
	  

      public IEnumerable<EodSetup> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbleod_setup.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public EodSetup Get(string coNum, string whNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("eod_setup.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND eod_setup.wh_num = '{0}'", whNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public EodSetup BuildFromRow(DataRow row)
      {
         var returnRecord = EodSetup.BuildEodSetupFromRow(row);
         returnRecord = this.BuildExtraFromRow<EodSetup>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, EodSetup record)
      {
         EodSetup.UpdateRowFromEodSetup(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public EodSetup Insert(EodSetup record)
      {
         DataRow row = this.dataSet.ttbleod_setup.Newttbleod_setupRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbleod_setup.Addttbleod_setupRow((pdseod_setupDataSet.ttbleod_setupRow)row);
         this.SaveChanges();
         return this.dataSet.ttbleod_setup.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbleod_setup.Rows[0]) : null;
      }
  

      public EodSetup Update(EodSetup record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbleod_setup.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbleod_setup.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(EodSetup record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbleod_setup.Newttbleod_setupRow();
            EodSetup.BuildMinimalRow(ref row, record);
            this.dataSet.ttbleod_setup.Addttbleod_setupRow((pdseod_setupDataSet.ttbleod_setupRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new EodSetup() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  