//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdstransType;

   public partial class TransTypeAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdstrans_type";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> transTypeTableControlKey;
		
      private pdstrans_typeDataSet dataSet;
        
      public TransTypeAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdstrans_typeDataSet() { DataSetName = DataSetName };
            this.transTypeTableControlKey = this.dataSet.ttbltrans_type.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.transTypeTableControlKey))
            {
               this.CreateTableControlParameters(this.transTypeTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in TransTypeAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Trans_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poTrans_typeproxy = this.proxyAppObject.CreatePO_trans_typeproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poTrans_typeproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Trans_typeproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.transTypeTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.transTypeTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Trans_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poTrans_typeproxy = this.proxyAppObject.CreatePO_trans_typeproxy())
               {
                  this.SetRequiredContextParameters();
                  poTrans_typeproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Trans_typeproxy - After Call");
      }
   

      public TransType GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         TransType transType = null;
         if (row != null)
         {
             transType = this.BuildFromRow(row);
         }
         return transType;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(trans_type) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltrans_type.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<TransType> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(trans_type)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected TransType Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbltrans_type.AsEnumerable().SingleOrDefault();
         TransType transType = null;
         if (row != null)
         {
             transType = this.BuildFromRow(row);
         }
         return transType;
      }
	  
	  

      public IEnumerable<TransType> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbltrans_type.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public TransType Get(string transType, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(transType)) 
         {
             sb.AppendFormatWithEscape("trans_type.trans_type = '{0}'", transType);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public TransType BuildFromRow(DataRow row)
      {
         var returnRecord = TransType.BuildTransTypeFromRow(row);
         returnRecord = this.BuildExtraFromRow<TransType>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, TransType record)
      {
         TransType.UpdateRowFromTransType(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public TransType Insert(TransType record)
      {
         DataRow row = this.dataSet.ttbltrans_type.Newttbltrans_typeRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbltrans_type.Addttbltrans_typeRow((pdstrans_typeDataSet.ttbltrans_typeRow)row);
         this.SaveChanges();
         return this.dataSet.ttbltrans_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltrans_type.Rows[0]) : null;
      }
  

      public TransType Update(TransType record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbltrans_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltrans_type.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(TransType record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbltrans_type.Newttbltrans_typeRow();
            TransType.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltrans_type.Addttbltrans_typeRow((pdstrans_typeDataSet.ttbltrans_typeRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new TransType() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  