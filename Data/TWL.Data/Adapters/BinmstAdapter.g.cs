//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsbinmst;

   public partial class BinmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbinmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> binmstTableControlKey;
		
      private pdsbinmstDataSet dataSet;
        
      public BinmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbinmstDataSet() { DataSetName = DataSetName };
            this.binmstTableControlKey = this.dataSet.ttblbinmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.binmstTableControlKey))
            {
               this.CreateTableControlParameters(this.binmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BinmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Binmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBinmstproxy = this.proxyAppObject.CreatePO_binmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBinmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Binmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.binmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.binmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Binmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBinmstproxy = this.proxyAppObject.CreatePO_binmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poBinmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Binmstproxy - After Call");
      }
   

      public Binmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Binmst binmst = null;
         if (row != null)
         {
             binmst = this.BuildFromRow(row);
         }
         return binmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(binmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbinmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Binmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(binmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Binmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbinmst.AsEnumerable().SingleOrDefault();
         Binmst binmst = null;
         if (row != null)
         {
             binmst = this.BuildFromRow(row);
         }
         return binmst;
      }
	  
	  

      public IEnumerable<Binmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbinmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Binmst Get(string coNum, string whNum, string binNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("binmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(binNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.bin_num = '{0}'", binNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Binmst> GetListByCoWhAbc(string coNum, string whNum, string abc, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("binmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(abc)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.abc = '{0}'", abc);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Binmst> GetListByCoWhAbcPending(string coNum, string whNum, string abcPending, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("binmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(abcPending)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.abc_pending = '{0}'", abcPending);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Binmst> GetListByCoWhPickseqBin(string coNum, string whNum, int pickSequence, string binNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("binmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_num = '{0}'", whNum);
         }
         if (pickSequence != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND binmst.pick_sequence = {0}", pickSequence);
         }
         if (!string.IsNullOrEmpty(binNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.bin_num = '{0}'", binNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Binmst> GetListByCoWhPrimAbsType(string coNum, string whNum, bool primPick, string absNum, string primPickType, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("binmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_num = '{0}'", whNum);
         }
         sb.AppendFormatWithEscape(" AND binmst.prim_pick = {0}", primPick);
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.abs_num = '{0}'", absNum);
         }
         if (!string.IsNullOrEmpty(primPickType)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.prim_pick_type = '{0}'", primPickType);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Binmst> GetListByCoWhPutgrp(string coNum, string whNum, string putawayGroup, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("binmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(putawayGroup)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.putaway_group = '{0}'", putawayGroup);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Binmst> GetListByCoWhZoneAbcCubeBin(string coNum, string whNum, string whZone, string abc, decimal cube, string binNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("binmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(whZone)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_zone = '{0}'", whZone);
         }
         if (!string.IsNullOrEmpty(abc)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.abc = '{0}'", abc);
         }
         if (cube != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND binmst.cube = {0}", cube);
         }
         if (!string.IsNullOrEmpty(binNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.bin_num = '{0}'", binNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Binmst> GetListByCoWhZoneAisleBin(string coNum, string whNum, string whZone, int aisle, string binNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("binmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(whZone)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.wh_zone = '{0}'", whZone);
         }
         if (aisle != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND binmst.aisle = {0}", aisle);
         }
         if (!string.IsNullOrEmpty(binNum)) 
         {
             sb.AppendFormatWithEscape(" AND binmst.bin_num = '{0}'", binNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Binmst BuildFromRow(DataRow row)
      {
         var returnRecord = Binmst.BuildBinmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Binmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Binmst record)
      {
         Binmst.UpdateRowFromBinmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Binmst Insert(Binmst record)
      {
         DataRow row = this.dataSet.ttblbinmst.NewttblbinmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbinmst.AddttblbinmstRow((pdsbinmstDataSet.ttblbinmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbinmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbinmst.Rows[0]) : null;
      }
  

      public Binmst Update(Binmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbinmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbinmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Binmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbinmst.NewttblbinmstRow();
            Binmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbinmst.AddttblbinmstRow((pdsbinmstDataSet.ttblbinmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Binmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  