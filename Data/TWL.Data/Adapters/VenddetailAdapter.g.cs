//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsvenddetail;

   public partial class VenddetailAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsvenddetail";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> venddetailTableControlKey;
		
      private pdsvenddetailDataSet dataSet;
        
      public VenddetailAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsvenddetailDataSet() { DataSetName = DataSetName };
            this.venddetailTableControlKey = this.dataSet.ttblvenddetail.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.venddetailTableControlKey))
            {
               this.CreateTableControlParameters(this.venddetailTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in VenddetailAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Venddetailproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poVenddetailproxy = this.proxyAppObject.CreatePO_venddetailproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poVenddetailproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Venddetailproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.venddetailTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.venddetailTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Venddetailproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poVenddetailproxy = this.proxyAppObject.CreatePO_venddetailproxy())
               {
                  this.SetRequiredContextParameters();
                  poVenddetailproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Venddetailproxy - After Call");
      }
   

      public Venddetail GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Venddetail venddetail = null;
         if (row != null)
         {
             venddetail = this.BuildFromRow(row);
         }
         return venddetail;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(venddetail) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblvenddetail.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Venddetail> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(venddetail)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Venddetail Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblvenddetail.AsEnumerable().SingleOrDefault();
         Venddetail venddetail = null;
         if (row != null)
         {
             venddetail = this.BuildFromRow(row);
         }
         return venddetail;
      }
	  
	  

      public IEnumerable<Venddetail> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblvenddetail.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Venddetail Get(string coNum, string whNum, string vendorId, string vendItem, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("venddetail.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND venddetail.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(vendorId)) 
         {
             sb.AppendFormatWithEscape(" AND venddetail.vendor_id = '{0}'", vendorId);
         }
         if (!string.IsNullOrEmpty(vendItem)) 
         {
             sb.AppendFormatWithEscape(" AND venddetail.vend_item = '{0}'", vendItem);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Venddetail> GetListByCoWhAbsType(string coNum, string whNum, string absNum, string refType, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("venddetail.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND venddetail.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND venddetail.abs_num = '{0}'", absNum);
         }
         if (!string.IsNullOrEmpty(refType)) 
         {
             sb.AppendFormatWithEscape(" AND venddetail.ref_type = '{0}'", refType);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Venddetail> GetListByCoWhVenditem(string coNum, string whNum, string vendItem, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("venddetail.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND venddetail.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(vendItem)) 
         {
             sb.AppendFormatWithEscape(" AND venddetail.vend_item = '{0}'", vendItem);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Venddetail BuildFromRow(DataRow row)
      {
         var returnRecord = Venddetail.BuildVenddetailFromRow(row);
         returnRecord = this.BuildExtraFromRow<Venddetail>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Venddetail record)
      {
         Venddetail.UpdateRowFromVenddetail(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Venddetail Insert(Venddetail record)
      {
         DataRow row = this.dataSet.ttblvenddetail.NewttblvenddetailRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblvenddetail.AddttblvenddetailRow((pdsvenddetailDataSet.ttblvenddetailRow)row);
         this.SaveChanges();
         return this.dataSet.ttblvenddetail.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvenddetail.Rows[0]) : null;
      }
  

      public Venddetail Update(Venddetail record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblvenddetail.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblvenddetail.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Venddetail record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblvenddetail.NewttblvenddetailRow();
            Venddetail.BuildMinimalRow(ref row, record);
            this.dataSet.ttblvenddetail.AddttblvenddetailRow((pdsvenddetailDataSet.ttblvenddetailRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Venddetail() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  