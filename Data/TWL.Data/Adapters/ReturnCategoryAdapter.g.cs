//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsreturnCategory;

   public partial class ReturnCategoryAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsreturn_category";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> returnCategoryTableControlKey;
		
      private pdsreturn_categoryDataSet dataSet;
        
      public ReturnCategoryAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsreturn_categoryDataSet() { DataSetName = DataSetName };
            this.returnCategoryTableControlKey = this.dataSet.ttblreturn_category.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.returnCategoryTableControlKey))
            {
               this.CreateTableControlParameters(this.returnCategoryTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ReturnCategoryAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Return_categoryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poReturn_categoryproxy = this.proxyAppObject.CreatePO_return_categoryproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poReturn_categoryproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Return_categoryproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.returnCategoryTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.returnCategoryTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Return_categoryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poReturn_categoryproxy = this.proxyAppObject.CreatePO_return_categoryproxy())
               {
                  this.SetRequiredContextParameters();
                  poReturn_categoryproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Return_categoryproxy - After Call");
      }
   

      public ReturnCategory GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         ReturnCategory returnCategory = null;
         if (row != null)
         {
             returnCategory = this.BuildFromRow(row);
         }
         return returnCategory;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(return_category) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblreturn_category.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<ReturnCategory> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(return_category)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected ReturnCategory Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblreturn_category.AsEnumerable().SingleOrDefault();
         ReturnCategory returnCategory = null;
         if (row != null)
         {
             returnCategory = this.BuildFromRow(row);
         }
         return returnCategory;
      }
	  
	  

      public IEnumerable<ReturnCategory> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblreturn_category.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public ReturnCategory Get(string coNum, string whNum, string rtnCategory, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("return_category.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND return_category.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(rtnCategory)) 
         {
             sb.AppendFormatWithEscape(" AND return_category.rtn_category = '{0}'", rtnCategory);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public ReturnCategory BuildFromRow(DataRow row)
      {
         var returnRecord = ReturnCategory.BuildReturnCategoryFromRow(row);
         returnRecord = this.BuildExtraFromRow<ReturnCategory>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, ReturnCategory record)
      {
         ReturnCategory.UpdateRowFromReturnCategory(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public ReturnCategory Insert(ReturnCategory record)
      {
         DataRow row = this.dataSet.ttblreturn_category.Newttblreturn_categoryRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblreturn_category.Addttblreturn_categoryRow((pdsreturn_categoryDataSet.ttblreturn_categoryRow)row);
         this.SaveChanges();
         return this.dataSet.ttblreturn_category.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblreturn_category.Rows[0]) : null;
      }
  

      public ReturnCategory Update(ReturnCategory record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblreturn_category.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblreturn_category.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(ReturnCategory record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblreturn_category.Newttblreturn_categoryRow();
            ReturnCategory.BuildMinimalRow(ref row, record);
            this.dataSet.ttblreturn_category.Addttblreturn_categoryRow((pdsreturn_categoryDataSet.ttblreturn_categoryRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new ReturnCategory() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  