//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdstaskEmp;

   public partial class TaskEmpAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdstask_emp";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> taskEmpTableControlKey;
		
      private pdstask_empDataSet dataSet;
        
      public TaskEmpAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdstask_empDataSet() { DataSetName = DataSetName };
            this.taskEmpTableControlKey = this.dataSet.ttbltask_emp.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.taskEmpTableControlKey))
            {
               this.CreateTableControlParameters(this.taskEmpTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in TaskEmpAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Task_empproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poTask_empproxy = this.proxyAppObject.CreatePO_task_empproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poTask_empproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Task_empproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.taskEmpTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.taskEmpTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Task_empproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poTask_empproxy = this.proxyAppObject.CreatePO_task_empproxy())
               {
                  this.SetRequiredContextParameters();
                  poTask_empproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Task_empproxy - After Call");
      }
   

      public TaskEmp GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         TaskEmp taskEmp = null;
         if (row != null)
         {
             taskEmp = this.BuildFromRow(row);
         }
         return taskEmp;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(task_emp) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltask_emp.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<TaskEmp> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(task_emp)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected TaskEmp Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbltask_emp.AsEnumerable().SingleOrDefault();
         TaskEmp taskEmp = null;
         if (row != null)
         {
             taskEmp = this.BuildFromRow(row);
         }
         return taskEmp;
      }
	  
	  

      public IEnumerable<TaskEmp> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbltask_emp.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public TaskEmp Get(int taskId, string empNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (taskId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("task_emp.task_id = {0}", taskId);
         }
         if (!string.IsNullOrEmpty(empNum)) 
         {
             sb.AppendFormatWithEscape(" AND task_emp.emp_num = '{0}'", empNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<TaskEmp> GetListByEmpTask(string empNum, int taskId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(empNum)) 
         {
             sb.AppendFormatWithEscape("task_emp.emp_num = '{0}'", empNum);
         }
         if (taskId != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND task_emp.task_id = {0}", taskId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public TaskEmp BuildFromRow(DataRow row)
      {
         var returnRecord = TaskEmp.BuildTaskEmpFromRow(row);
         returnRecord = this.BuildExtraFromRow<TaskEmp>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, TaskEmp record)
      {
         TaskEmp.UpdateRowFromTaskEmp(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public TaskEmp Insert(TaskEmp record)
      {
         DataRow row = this.dataSet.ttbltask_emp.Newttbltask_empRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbltask_emp.Addttbltask_empRow((pdstask_empDataSet.ttbltask_empRow)row);
         this.SaveChanges();
         return this.dataSet.ttbltask_emp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltask_emp.Rows[0]) : null;
      }
  

      public TaskEmp Update(TaskEmp record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbltask_emp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltask_emp.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(TaskEmp record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbltask_emp.Newttbltask_empRow();
            TaskEmp.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltask_emp.Addttbltask_empRow((pdstask_empDataSet.ttbltask_empRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new TaskEmp() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  