//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsinvAdj;

   public partial class InvAdjAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsinv_adj";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> invAdjTableControlKey;
		
      private pdsinv_adjDataSet dataSet;
        
      public InvAdjAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsinv_adjDataSet() { DataSetName = DataSetName };
            this.invAdjTableControlKey = this.dataSet.ttblinv_adj.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.invAdjTableControlKey))
            {
               this.CreateTableControlParameters(this.invAdjTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in InvAdjAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Inv_adjproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poInv_adjproxy = this.proxyAppObject.CreatePO_inv_adjproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poInv_adjproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Inv_adjproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.invAdjTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.invAdjTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Inv_adjproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poInv_adjproxy = this.proxyAppObject.CreatePO_inv_adjproxy())
               {
                  this.SetRequiredContextParameters();
                  poInv_adjproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Inv_adjproxy - After Call");
      }
   

      public InvAdj GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         InvAdj invAdj = null;
         if (row != null)
         {
             invAdj = this.BuildFromRow(row);
         }
         return invAdj;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(inv_adj) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblinv_adj.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<InvAdj> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(inv_adj)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected InvAdj Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblinv_adj.AsEnumerable().SingleOrDefault();
         InvAdj invAdj = null;
         if (row != null)
         {
             invAdj = this.BuildFromRow(row);
         }
         return invAdj;
      }
	  
	  

      public IEnumerable<InvAdj> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblinv_adj.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public InvAdj Get(string coNum, string whNum, string adjCode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("inv_adj.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND inv_adj.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(adjCode)) 
         {
             sb.AppendFormatWithEscape(" AND inv_adj.adj_code = '{0}'", adjCode);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public InvAdj BuildFromRow(DataRow row)
      {
         var returnRecord = InvAdj.BuildInvAdjFromRow(row);
         returnRecord = this.BuildExtraFromRow<InvAdj>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, InvAdj record)
      {
         InvAdj.UpdateRowFromInvAdj(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public InvAdj Insert(InvAdj record)
      {
         DataRow row = this.dataSet.ttblinv_adj.Newttblinv_adjRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblinv_adj.Addttblinv_adjRow((pdsinv_adjDataSet.ttblinv_adjRow)row);
         this.SaveChanges();
         return this.dataSet.ttblinv_adj.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblinv_adj.Rows[0]) : null;
      }
  

      public InvAdj Update(InvAdj record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblinv_adj.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblinv_adj.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(InvAdj record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblinv_adj.Newttblinv_adjRow();
            InvAdj.BuildMinimalRow(ref row, record);
            this.dataSet.ttblinv_adj.Addttblinv_adjRow((pdsinv_adjDataSet.ttblinv_adjRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new InvAdj() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  