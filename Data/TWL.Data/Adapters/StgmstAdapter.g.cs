//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsstgmst;

   public partial class StgmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsstgmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> stgmstTableControlKey;
		
      private pdsstgmstDataSet dataSet;
        
      public StgmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsstgmstDataSet() { DataSetName = DataSetName };
            this.stgmstTableControlKey = this.dataSet.ttblstgmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.stgmstTableControlKey))
            {
               this.CreateTableControlParameters(this.stgmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in StgmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Stgmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poStgmstproxy = this.proxyAppObject.CreatePO_stgmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poStgmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Stgmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.stgmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.stgmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Stgmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poStgmstproxy = this.proxyAppObject.CreatePO_stgmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poStgmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Stgmstproxy - After Call");
      }
   

      public Stgmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Stgmst stgmst = null;
         if (row != null)
         {
             stgmst = this.BuildFromRow(row);
         }
         return stgmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(stgmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblstgmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Stgmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(stgmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Stgmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblstgmst.AsEnumerable().SingleOrDefault();
         Stgmst stgmst = null;
         if (row != null)
         {
             stgmst = this.BuildFromRow(row);
         }
         return stgmst;
      }
	  
	  

      public IEnumerable<Stgmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblstgmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Stgmst Get(string coNum, string whNum, string containerId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("stgmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND stgmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(containerId)) 
         {
             sb.AppendFormatWithEscape(" AND stgmst.container_id = '{0}'", containerId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Stgmst> GetListByCoWhBinOrdSuf(string coNum, string whNum, string binNum, string order, string orderSuffix, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("stgmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND stgmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(binNum)) 
         {
             sb.AppendFormatWithEscape(" AND stgmst.bin_num = '{0}'", binNum);
         }
         if (!string.IsNullOrEmpty(order)) 
         {
             sb.AppendFormatWithEscape(" AND stgmst.order = '{0}'", order);
         }
         if (!string.IsNullOrEmpty(orderSuffix)) 
         {
             sb.AppendFormatWithEscape(" AND stgmst.order_suffix = '{0}'", orderSuffix);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Stgmst BuildFromRow(DataRow row)
      {
         var returnRecord = Stgmst.BuildStgmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Stgmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Stgmst record)
      {
         Stgmst.UpdateRowFromStgmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Stgmst Insert(Stgmst record)
      {
         DataRow row = this.dataSet.ttblstgmst.NewttblstgmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblstgmst.AddttblstgmstRow((pdsstgmstDataSet.ttblstgmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblstgmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblstgmst.Rows[0]) : null;
      }
  

      public Stgmst Update(Stgmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblstgmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblstgmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Stgmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblstgmst.NewttblstgmstRow();
            Stgmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblstgmst.AddttblstgmstRow((pdsstgmstDataSet.ttblstgmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Stgmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  