//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsrtmst;

   public partial class RtmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsrtmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> rtmstTableControlKey;
		
      private pdsrtmstDataSet dataSet;
        
      public RtmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsrtmstDataSet() { DataSetName = DataSetName };
            this.rtmstTableControlKey = this.dataSet.ttblrtmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.rtmstTableControlKey))
            {
               this.CreateTableControlParameters(this.rtmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in RtmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Rtmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poRtmstproxy = this.proxyAppObject.CreatePO_rtmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poRtmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Rtmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.rtmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.rtmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Rtmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRtmstproxy = this.proxyAppObject.CreatePO_rtmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poRtmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Rtmstproxy - After Call");
      }
   

      public Rtmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Rtmst rtmst = null;
         if (row != null)
         {
             rtmst = this.BuildFromRow(row);
         }
         return rtmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(rtmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblrtmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Rtmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(rtmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Rtmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblrtmst.AsEnumerable().SingleOrDefault();
         Rtmst rtmst = null;
         if (row != null)
         {
             rtmst = this.BuildFromRow(row);
         }
         return rtmst;
      }
	  
	  

      public IEnumerable<Rtmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblrtmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Rtmst Get(string coNum, string whNum, string rtNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rtmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(rtNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtmst.rt_num = '{0}'", rtNum);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Rtmst> GetListByCargocontrol(string cargoControl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(cargoControl)) 
         {
             sb.AppendFormatWithEscape("rtmst.cargo_control = '{0}'", cargoControl);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtmst> GetListByClearance(bool clearanceRequired, string clearanceCode, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("rtmst.clearance_required = {0}", clearanceRequired);
         if (!string.IsNullOrEmpty(clearanceCode)) 
         {
             sb.AppendFormatWithEscape(" AND rtmst.clearance_code = '{0}'", clearanceCode);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtmst> GetListByCoWhDelivery(string coNum, string whNum, DateTime? delivery, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rtmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtmst.wh_num = '{0}'", whNum);
         }
         if (delivery != null) 
         {
             sb.AppendFormatWithEscape(" AND rtmst.delivery = '{0}'", delivery);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtmst> GetListByCoWhStatusType(string coNum, string whNum, string rowStatus, string type, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("rtmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND rtmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(rowStatus)) 
         {
             sb.AppendFormatWithEscape(" AND rtmst.row_status = '{0}'", rowStatus);
         }
         if (!string.IsNullOrEmpty(type)) 
         {
             sb.AppendFormatWithEscape(" AND rtmst.type = '{0}'", type);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtmst> GetListByOrderSuf(string order, string orderSuffix, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(order)) 
         {
             sb.AppendFormatWithEscape("rtmst.order = '{0}'", order);
         }
         if (!string.IsNullOrEmpty(orderSuffix)) 
         {
             sb.AppendFormatWithEscape(" AND rtmst.order_suffix = '{0}'", orderSuffix);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtmst> GetListByReleaseid(string releaseId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(releaseId)) 
         {
             sb.AppendFormatWithEscape("rtmst.release_id = '{0}'", releaseId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rtmst> GetListByRtId(int rtId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (rtId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("rtmst.rt_id = {0}", rtId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Rtmst BuildFromRow(DataRow row)
      {
         var returnRecord = Rtmst.BuildRtmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Rtmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Rtmst record)
      {
         Rtmst.UpdateRowFromRtmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Rtmst Insert(Rtmst record)
      {
         DataRow row = this.dataSet.ttblrtmst.NewttblrtmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblrtmst.AddttblrtmstRow((pdsrtmstDataSet.ttblrtmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblrtmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrtmst.Rows[0]) : null;
      }
  

      public Rtmst Update(Rtmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblrtmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrtmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Rtmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblrtmst.NewttblrtmstRow();
            Rtmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblrtmst.AddttblrtmstRow((pdsrtmstDataSet.ttblrtmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Rtmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  