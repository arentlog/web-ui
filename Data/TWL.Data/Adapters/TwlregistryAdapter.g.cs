//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdstwlregistry;

   public partial class TwlregistryAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdstwlregistry";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> twlregistryTableControlKey;
		
      private pdstwlregistryDataSet dataSet;
        
      public TwlregistryAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdstwlregistryDataSet() { DataSetName = DataSetName };
            this.twlregistryTableControlKey = this.dataSet.ttbltwlregistry.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.twlregistryTableControlKey))
            {
               this.CreateTableControlParameters(this.twlregistryTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in TwlregistryAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Twlregistryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poTwlregistryproxy = this.proxyAppObject.CreatePO_twlregistryproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poTwlregistryproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Twlregistryproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.twlregistryTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.twlregistryTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Twlregistryproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poTwlregistryproxy = this.proxyAppObject.CreatePO_twlregistryproxy())
               {
                  this.SetRequiredContextParameters();
                  poTwlregistryproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Twlregistryproxy - After Call");
      }
   

      public Twlregistry GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Twlregistry twlregistry = null;
         if (row != null)
         {
             twlregistry = this.BuildFromRow(row);
         }
         return twlregistry;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(twlregistry) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbltwlregistry.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Twlregistry> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(twlregistry)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Twlregistry Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbltwlregistry.AsEnumerable().SingleOrDefault();
         Twlregistry twlregistry = null;
         if (row != null)
         {
             twlregistry = this.BuildFromRow(row);
         }
         return twlregistry;
      }
	  
	  

      public IEnumerable<Twlregistry> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbltwlregistry.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Twlregistry Get(string coNum, string whNum, string empNum, string function, string section, string subsection, string @object, string keyname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("twlregistry.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND twlregistry.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(empNum)) 
         {
             sb.AppendFormatWithEscape(" AND twlregistry.emp_num = '{0}'", empNum);
         }
         if (!string.IsNullOrEmpty(function)) 
         {
             sb.AppendFormatWithEscape(" AND twlregistry.function = '{0}'", function);
         }
         if (!string.IsNullOrEmpty(section)) 
         {
             sb.AppendFormatWithEscape(" AND twlregistry.section = '{0}'", section);
         }
         if (!string.IsNullOrEmpty(subsection)) 
         {
             sb.AppendFormatWithEscape(" AND twlregistry.subsection = '{0}'", subsection);
         }
         if (!string.IsNullOrEmpty(@object)) 
         {
             sb.AppendFormatWithEscape(" AND twlregistry.object = '{0}'", @object);
         }
         if (!string.IsNullOrEmpty(keyname)) 
         {
             sb.AppendFormatWithEscape(" AND twlregistry.keyname = '{0}'", keyname);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Twlregistry BuildFromRow(DataRow row)
      {
         var returnRecord = Twlregistry.BuildTwlregistryFromRow(row);
         returnRecord = this.BuildExtraFromRow<Twlregistry>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Twlregistry record)
      {
         Twlregistry.UpdateRowFromTwlregistry(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Twlregistry Insert(Twlregistry record)
      {
         DataRow row = this.dataSet.ttbltwlregistry.NewttbltwlregistryRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbltwlregistry.AddttbltwlregistryRow((pdstwlregistryDataSet.ttbltwlregistryRow)row);
         this.SaveChanges();
         return this.dataSet.ttbltwlregistry.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltwlregistry.Rows[0]) : null;
      }
  

      public Twlregistry Update(Twlregistry record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbltwlregistry.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbltwlregistry.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Twlregistry record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbltwlregistry.NewttbltwlregistryRow();
            Twlregistry.BuildMinimalRow(ref row, record);
            this.dataSet.ttbltwlregistry.AddttbltwlregistryRow((pdstwlregistryDataSet.ttbltwlregistryRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Twlregistry() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  