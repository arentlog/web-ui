//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsseqControl;

   public partial class SeqControlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsseq_control";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> seqControlTableControlKey;
		
      private pdsseq_controlDataSet dataSet;
        
      public SeqControlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsseq_controlDataSet() { DataSetName = DataSetName };
            this.seqControlTableControlKey = this.dataSet.ttblseq_control.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.seqControlTableControlKey))
            {
               this.CreateTableControlParameters(this.seqControlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SeqControlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Seq_controlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSeq_controlproxy = this.proxyAppObject.CreatePO_seq_controlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSeq_controlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Seq_controlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.seqControlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.seqControlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Seq_controlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSeq_controlproxy = this.proxyAppObject.CreatePO_seq_controlproxy())
               {
                  this.SetRequiredContextParameters();
                  poSeq_controlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Seq_controlproxy - After Call");
      }
   

      public SeqControl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         SeqControl seqControl = null;
         if (row != null)
         {
             seqControl = this.BuildFromRow(row);
         }
         return seqControl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(seq_control) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblseq_control.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<SeqControl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(seq_control)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected SeqControl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblseq_control.AsEnumerable().SingleOrDefault();
         SeqControl seqControl = null;
         if (row != null)
         {
             seqControl = this.BuildFromRow(row);
         }
         return seqControl;
      }
	  
	  

      public IEnumerable<SeqControl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblseq_control.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public SeqControl Get(string coNum, string whNum, string scType, string scTable, string scId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("seq_control.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND seq_control.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(scType)) 
         {
             sb.AppendFormatWithEscape(" AND seq_control.sc_type = '{0}'", scType);
         }
         if (!string.IsNullOrEmpty(scTable)) 
         {
             sb.AppendFormatWithEscape(" AND seq_control.sc_table = '{0}'", scTable);
         }
         if (!string.IsNullOrEmpty(scId)) 
         {
             sb.AppendFormatWithEscape(" AND seq_control.sc_id = '{0}'", scId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public SeqControl BuildFromRow(DataRow row)
      {
         var returnRecord = SeqControl.BuildSeqControlFromRow(row);
         returnRecord = this.BuildExtraFromRow<SeqControl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, SeqControl record)
      {
         SeqControl.UpdateRowFromSeqControl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public SeqControl Insert(SeqControl record)
      {
         DataRow row = this.dataSet.ttblseq_control.Newttblseq_controlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblseq_control.Addttblseq_controlRow((pdsseq_controlDataSet.ttblseq_controlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblseq_control.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblseq_control.Rows[0]) : null;
      }
  

      public SeqControl Update(SeqControl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblseq_control.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblseq_control.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(SeqControl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblseq_control.Newttblseq_controlRow();
            SeqControl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblseq_control.Addttblseq_controlRow((pdsseq_controlDataSet.ttblseq_controlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new SeqControl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  