//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsbarcodedtl;

   public partial class BarcodedtlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbarcodedtl";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> barcodedtlTableControlKey;
		
      private pdsbarcodedtlDataSet dataSet;
        
      public BarcodedtlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbarcodedtlDataSet() { DataSetName = DataSetName };
            this.barcodedtlTableControlKey = this.dataSet.ttblbarcodedtl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.barcodedtlTableControlKey))
            {
               this.CreateTableControlParameters(this.barcodedtlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BarcodedtlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Barcodedtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBarcodedtlproxy = this.proxyAppObject.CreatePO_barcodedtlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBarcodedtlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Barcodedtlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.barcodedtlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.barcodedtlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Barcodedtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBarcodedtlproxy = this.proxyAppObject.CreatePO_barcodedtlproxy())
               {
                  this.SetRequiredContextParameters();
                  poBarcodedtlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Barcodedtlproxy - After Call");
      }
   

      public Barcodedtl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Barcodedtl barcodedtl = null;
         if (row != null)
         {
             barcodedtl = this.BuildFromRow(row);
         }
         return barcodedtl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(barcodedtl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbarcodedtl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Barcodedtl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(barcodedtl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Barcodedtl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbarcodedtl.AsEnumerable().SingleOrDefault();
         Barcodedtl barcodedtl = null;
         if (row != null)
         {
             barcodedtl = this.BuildFromRow(row);
         }
         return barcodedtl;
      }
	  
	  

      public IEnumerable<Barcodedtl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbarcodedtl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Barcodedtl Get(string coNum, string whNum, string vendorId, string barcodeId, string attributeName, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("barcodedtl.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND barcodedtl.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(vendorId)) 
         {
             sb.AppendFormatWithEscape(" AND barcodedtl.vendor_id = '{0}'", vendorId);
         }
         if (!string.IsNullOrEmpty(barcodeId)) 
         {
             sb.AppendFormatWithEscape(" AND barcodedtl.barcode_id = '{0}'", barcodeId);
         }
         if (!string.IsNullOrEmpty(attributeName)) 
         {
             sb.AppendFormatWithEscape(" AND barcodedtl.attribute_name = '{0}'", attributeName);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Barcodedtl> GetListByBarCodeIdAttrName(string barcodeId, string attributeName, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(barcodeId)) 
         {
             sb.AppendFormatWithEscape("barcodedtl.barcode_id = '{0}'", barcodeId);
         }
         if (!string.IsNullOrEmpty(attributeName)) 
         {
             sb.AppendFormatWithEscape(" AND barcodedtl.attribute_name = '{0}'", attributeName);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Barcodedtl BuildFromRow(DataRow row)
      {
         var returnRecord = Barcodedtl.BuildBarcodedtlFromRow(row);
         returnRecord = this.BuildExtraFromRow<Barcodedtl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Barcodedtl record)
      {
         Barcodedtl.UpdateRowFromBarcodedtl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Barcodedtl Insert(Barcodedtl record)
      {
         DataRow row = this.dataSet.ttblbarcodedtl.NewttblbarcodedtlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbarcodedtl.AddttblbarcodedtlRow((pdsbarcodedtlDataSet.ttblbarcodedtlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbarcodedtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbarcodedtl.Rows[0]) : null;
      }
  

      public Barcodedtl Update(Barcodedtl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbarcodedtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbarcodedtl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Barcodedtl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbarcodedtl.NewttblbarcodedtlRow();
            Barcodedtl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbarcodedtl.AddttblbarcodedtlRow((pdsbarcodedtlDataSet.ttblbarcodedtlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Barcodedtl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  