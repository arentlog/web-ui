//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsprodStgDtl;

   public partial class ProdStgDtlAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsprod_stg_dtl";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> prodStgDtlTableControlKey;
		
      private pdsprod_stg_dtlDataSet dataSet;
        
      public ProdStgDtlAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsprod_stg_dtlDataSet() { DataSetName = DataSetName };
            this.prodStgDtlTableControlKey = this.dataSet.ttblprod_stg_dtl.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.prodStgDtlTableControlKey))
            {
               this.CreateTableControlParameters(this.prodStgDtlTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ProdStgDtlAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Prod_stg_dtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poProd_stg_dtlproxy = this.proxyAppObject.CreatePO_prod_stg_dtlproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poProd_stg_dtlproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Prod_stg_dtlproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.prodStgDtlTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.prodStgDtlTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Prod_stg_dtlproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poProd_stg_dtlproxy = this.proxyAppObject.CreatePO_prod_stg_dtlproxy())
               {
                  this.SetRequiredContextParameters();
                  poProd_stg_dtlproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Prod_stg_dtlproxy - After Call");
      }
   

      public ProdStgDtl GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         ProdStgDtl prodStgDtl = null;
         if (row != null)
         {
             prodStgDtl = this.BuildFromRow(row);
         }
         return prodStgDtl;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(prod_stg_dtl) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblprod_stg_dtl.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<ProdStgDtl> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(prod_stg_dtl)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected ProdStgDtl Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblprod_stg_dtl.AsEnumerable().SingleOrDefault();
         ProdStgDtl prodStgDtl = null;
         if (row != null)
         {
             prodStgDtl = this.BuildFromRow(row);
         }
         return prodStgDtl;
      }
	  
	  

      public IEnumerable<ProdStgDtl> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblprod_stg_dtl.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public ProdStgDtl Get(int id, int line, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (id != int.MinValue) 
         {
             sb.AppendFormatWithEscape("prod_stg_dtl.id = {0}", id);
         }
         if (line != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND prod_stg_dtl.line = {0}", line);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public ProdStgDtl BuildFromRow(DataRow row)
      {
         var returnRecord = ProdStgDtl.BuildProdStgDtlFromRow(row);
         returnRecord = this.BuildExtraFromRow<ProdStgDtl>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, ProdStgDtl record)
      {
         ProdStgDtl.UpdateRowFromProdStgDtl(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public ProdStgDtl Insert(ProdStgDtl record)
      {
         DataRow row = this.dataSet.ttblprod_stg_dtl.Newttblprod_stg_dtlRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblprod_stg_dtl.Addttblprod_stg_dtlRow((pdsprod_stg_dtlDataSet.ttblprod_stg_dtlRow)row);
         this.SaveChanges();
         return this.dataSet.ttblprod_stg_dtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblprod_stg_dtl.Rows[0]) : null;
      }
  

      public ProdStgDtl Update(ProdStgDtl record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblprod_stg_dtl.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblprod_stg_dtl.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(ProdStgDtl record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblprod_stg_dtl.Newttblprod_stg_dtlRow();
            ProdStgDtl.BuildMinimalRow(ref row, record);
            this.dataSet.ttblprod_stg_dtl.Addttblprod_stg_dtlRow((pdsprod_stg_dtlDataSet.ttblprod_stg_dtlRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new ProdStgDtl() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  