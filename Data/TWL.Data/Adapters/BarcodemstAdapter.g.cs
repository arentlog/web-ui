//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsbarcodemst;

   public partial class BarcodemstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbarcodemst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> barcodemstTableControlKey;
		
      private pdsbarcodemstDataSet dataSet;
        
      public BarcodemstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbarcodemstDataSet() { DataSetName = DataSetName };
            this.barcodemstTableControlKey = this.dataSet.ttblbarcodemst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.barcodemstTableControlKey))
            {
               this.CreateTableControlParameters(this.barcodemstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BarcodemstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Barcodemstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBarcodemstproxy = this.proxyAppObject.CreatePO_barcodemstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBarcodemstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Barcodemstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.barcodemstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.barcodemstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Barcodemstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBarcodemstproxy = this.proxyAppObject.CreatePO_barcodemstproxy())
               {
                  this.SetRequiredContextParameters();
                  poBarcodemstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Barcodemstproxy - After Call");
      }
   

      public Barcodemst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Barcodemst barcodemst = null;
         if (row != null)
         {
             barcodemst = this.BuildFromRow(row);
         }
         return barcodemst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(barcodemst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbarcodemst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Barcodemst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(barcodemst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Barcodemst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbarcodemst.AsEnumerable().SingleOrDefault();
         Barcodemst barcodemst = null;
         if (row != null)
         {
             barcodemst = this.BuildFromRow(row);
         }
         return barcodemst;
      }
	  
	  

      public IEnumerable<Barcodemst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbarcodemst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Barcodemst Get(string coNum, string whNum, string vendorId, string barcodeId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("barcodemst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND barcodemst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(vendorId)) 
         {
             sb.AppendFormatWithEscape(" AND barcodemst.vendor_id = '{0}'", vendorId);
         }
         if (!string.IsNullOrEmpty(barcodeId)) 
         {
             sb.AppendFormatWithEscape(" AND barcodemst.barcode_id = '{0}'", barcodeId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Barcodemst> GetListByBarcodeID(string barcodeId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(barcodeId)) 
         {
             sb.AppendFormatWithEscape("barcodemst.barcode_id = '{0}'", barcodeId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Barcodemst> GetListByLength(int barcodeLength, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (barcodeLength != int.MinValue) 
         {
             sb.AppendFormatWithEscape("barcodemst.barcode_length = {0}", barcodeLength);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Barcodemst> GetListByVendor(string vendorId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(vendorId)) 
         {
             sb.AppendFormatWithEscape("barcodemst.vendor_id = '{0}'", vendorId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Barcodemst BuildFromRow(DataRow row)
      {
         var returnRecord = Barcodemst.BuildBarcodemstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Barcodemst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Barcodemst record)
      {
         Barcodemst.UpdateRowFromBarcodemst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Barcodemst Insert(Barcodemst record)
      {
         DataRow row = this.dataSet.ttblbarcodemst.NewttblbarcodemstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbarcodemst.AddttblbarcodemstRow((pdsbarcodemstDataSet.ttblbarcodemstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbarcodemst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbarcodemst.Rows[0]) : null;
      }
  

      public Barcodemst Update(Barcodemst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbarcodemst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbarcodemst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Barcodemst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbarcodemst.NewttblbarcodemstRow();
            Barcodemst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbarcodemst.AddttblbarcodemstRow((pdsbarcodemstDataSet.ttblbarcodemstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Barcodemst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  