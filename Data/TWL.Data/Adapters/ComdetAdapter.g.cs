//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdscomdet;

   public partial class ComdetAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscomdet";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> comdetTableControlKey;
		
      private pdscomdetDataSet dataSet;
        
      public ComdetAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscomdetDataSet() { DataSetName = DataSetName };
            this.comdetTableControlKey = this.dataSet.ttblcomdet.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.comdetTableControlKey))
            {
               this.CreateTableControlParameters(this.comdetTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ComdetAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Comdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poComdetproxy = this.proxyAppObject.CreatePO_comdetproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poComdetproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Comdetproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.comdetTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.comdetTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Comdetproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poComdetproxy = this.proxyAppObject.CreatePO_comdetproxy())
               {
                  this.SetRequiredContextParameters();
                  poComdetproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Comdetproxy - After Call");
      }
   

      public Comdet GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Comdet comdet = null;
         if (row != null)
         {
             comdet = this.BuildFromRow(row);
         }
         return comdet;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(comdet) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcomdet.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Comdet> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(comdet)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Comdet Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcomdet.AsEnumerable().SingleOrDefault();
         Comdet comdet = null;
         if (row != null)
         {
             comdet = this.BuildFromRow(row);
         }
         return comdet;
      }
	  
	  

      public IEnumerable<Comdet> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcomdet.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Comdet Get(string version, string recordType, int fieldStart, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(version)) 
         {
             sb.AppendFormatWithEscape("comdet.version = '{0}'", version);
         }
         if (!string.IsNullOrEmpty(recordType)) 
         {
             sb.AppendFormatWithEscape(" AND comdet.record_type = '{0}'", recordType);
         }
         if (fieldStart != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND comdet.field_start = {0}", fieldStart);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Comdet> GetListByVersionTypeActiveReplyStart(string version, string recordType, bool active, bool addInReply, int fieldStart, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(version)) 
         {
             sb.AppendFormatWithEscape("comdet.version = '{0}'", version);
         }
         if (!string.IsNullOrEmpty(recordType)) 
         {
             sb.AppendFormatWithEscape(" AND comdet.record_type = '{0}'", recordType);
         }
         sb.AppendFormatWithEscape(" AND comdet.active = {0}", active);
         sb.AppendFormatWithEscape(" AND comdet.add_in_reply = {0}", addInReply);
         if (fieldStart != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND comdet.field_start = {0}", fieldStart);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Comdet> GetListByVersionTypeName(string version, string recordType, string fieldName, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(version)) 
         {
             sb.AppendFormatWithEscape("comdet.version = '{0}'", version);
         }
         if (!string.IsNullOrEmpty(recordType)) 
         {
             sb.AppendFormatWithEscape(" AND comdet.record_type = '{0}'", recordType);
         }
         if (!string.IsNullOrEmpty(fieldName)) 
         {
             sb.AppendFormatWithEscape(" AND comdet.field_name = '{0}'", fieldName);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Comdet BuildFromRow(DataRow row)
      {
         var returnRecord = Comdet.BuildComdetFromRow(row);
         returnRecord = this.BuildExtraFromRow<Comdet>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Comdet record)
      {
         Comdet.UpdateRowFromComdet(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Comdet Insert(Comdet record)
      {
         DataRow row = this.dataSet.ttblcomdet.NewttblcomdetRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcomdet.AddttblcomdetRow((pdscomdetDataSet.ttblcomdetRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcomdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcomdet.Rows[0]) : null;
      }
  

      public Comdet Update(Comdet record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcomdet.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcomdet.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Comdet record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcomdet.NewttblcomdetRow();
            Comdet.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcomdet.AddttblcomdetRow((pdscomdetDataSet.ttblcomdetRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Comdet() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  