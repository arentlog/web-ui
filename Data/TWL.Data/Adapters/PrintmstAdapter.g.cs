//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsprintmst;

   public partial class PrintmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsprintmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> printmstTableControlKey;
		
      private pdsprintmstDataSet dataSet;
        
      public PrintmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsprintmstDataSet() { DataSetName = DataSetName };
            this.printmstTableControlKey = this.dataSet.ttblprintmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.printmstTableControlKey))
            {
               this.CreateTableControlParameters(this.printmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PrintmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Printmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPrintmstproxy = this.proxyAppObject.CreatePO_printmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPrintmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Printmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.printmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.printmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Printmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPrintmstproxy = this.proxyAppObject.CreatePO_printmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poPrintmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Printmstproxy - After Call");
      }
   

      public Printmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Printmst printmst = null;
         if (row != null)
         {
             printmst = this.BuildFromRow(row);
         }
         return printmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(printmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblprintmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Printmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(printmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Printmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblprintmst.AsEnumerable().SingleOrDefault();
         Printmst printmst = null;
         if (row != null)
         {
             printmst = this.BuildFromRow(row);
         }
         return printmst;
      }
	  
	  

      public IEnumerable<Printmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblprintmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Printmst Get(string coNum, string whNum, string printerId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("printmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND printmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(printerId)) 
         {
             sb.AppendFormatWithEscape(" AND printmst.printer_id = '{0}'", printerId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Printmst BuildFromRow(DataRow row)
      {
         var returnRecord = Printmst.BuildPrintmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Printmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Printmst record)
      {
         Printmst.UpdateRowFromPrintmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Printmst Insert(Printmst record)
      {
         DataRow row = this.dataSet.ttblprintmst.NewttblprintmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblprintmst.AddttblprintmstRow((pdsprintmstDataSet.ttblprintmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblprintmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblprintmst.Rows[0]) : null;
      }
  

      public Printmst Update(Printmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblprintmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblprintmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Printmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblprintmst.NewttblprintmstRow();
            Printmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblprintmst.AddttblprintmstRow((pdsprintmstDataSet.ttblprintmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Printmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  