//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsudcalcType;

   public partial class UdcalcTypeAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsudcalc_type";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> udcalcTypeTableControlKey;
		
      private pdsudcalc_typeDataSet dataSet;
        
      public UdcalcTypeAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsudcalc_typeDataSet() { DataSetName = DataSetName };
            this.udcalcTypeTableControlKey = this.dataSet.ttbludcalc_type.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.udcalcTypeTableControlKey))
            {
               this.CreateTableControlParameters(this.udcalcTypeTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in UdcalcTypeAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Udcalc_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poUdcalc_typeproxy = this.proxyAppObject.CreatePO_udcalc_typeproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poUdcalc_typeproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Udcalc_typeproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.udcalcTypeTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.udcalcTypeTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Udcalc_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poUdcalc_typeproxy = this.proxyAppObject.CreatePO_udcalc_typeproxy())
               {
                  this.SetRequiredContextParameters();
                  poUdcalc_typeproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Udcalc_typeproxy - After Call");
      }
   

      public UdcalcType GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         UdcalcType udcalcType = null;
         if (row != null)
         {
             udcalcType = this.BuildFromRow(row);
         }
         return udcalcType;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(udcalc_type) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbludcalc_type.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<UdcalcType> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(udcalc_type)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected UdcalcType Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbludcalc_type.AsEnumerable().SingleOrDefault();
         UdcalcType udcalcType = null;
         if (row != null)
         {
             udcalcType = this.BuildFromRow(row);
         }
         return udcalcType;
      }
	  
	  

      public IEnumerable<UdcalcType> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbludcalc_type.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public UdcalcType Get(string coNum, string whNum, string udcType, bool udcActivefl, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("udcalc_type.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND udcalc_type.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(udcType)) 
         {
             sb.AppendFormatWithEscape(" AND udcalc_type.udc_type = '{0}'", udcType);
         }
         sb.AppendFormatWithEscape(" AND udcalc_type.udc_activefl = {0}", udcActivefl);
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public UdcalcType BuildFromRow(DataRow row)
      {
         var returnRecord = UdcalcType.BuildUdcalcTypeFromRow(row);
         returnRecord = this.BuildExtraFromRow<UdcalcType>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, UdcalcType record)
      {
         UdcalcType.UpdateRowFromUdcalcType(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public UdcalcType Insert(UdcalcType record)
      {
         DataRow row = this.dataSet.ttbludcalc_type.Newttbludcalc_typeRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbludcalc_type.Addttbludcalc_typeRow((pdsudcalc_typeDataSet.ttbludcalc_typeRow)row);
         this.SaveChanges();
         return this.dataSet.ttbludcalc_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbludcalc_type.Rows[0]) : null;
      }
  

      public UdcalcType Update(UdcalcType record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbludcalc_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbludcalc_type.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(UdcalcType record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbludcalc_type.Newttbludcalc_typeRow();
            UdcalcType.BuildMinimalRow(ref row, record);
            this.dataSet.ttbludcalc_type.Addttbludcalc_typeRow((pdsudcalc_typeDataSet.ttbludcalc_typeRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new UdcalcType() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  