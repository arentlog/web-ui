//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdslabelmst;

   public partial class LabelmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdslabelmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> labelmstTableControlKey;
		
      private pdslabelmstDataSet dataSet;
        
      public LabelmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdslabelmstDataSet() { DataSetName = DataSetName };
            this.labelmstTableControlKey = this.dataSet.ttbllabelmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.labelmstTableControlKey))
            {
               this.CreateTableControlParameters(this.labelmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in LabelmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Labelmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poLabelmstproxy = this.proxyAppObject.CreatePO_labelmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poLabelmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Labelmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.labelmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.labelmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Labelmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poLabelmstproxy = this.proxyAppObject.CreatePO_labelmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poLabelmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Labelmstproxy - After Call");
      }
   

      public Labelmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Labelmst labelmst = null;
         if (row != null)
         {
             labelmst = this.BuildFromRow(row);
         }
         return labelmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(labelmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbllabelmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Labelmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(labelmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Labelmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbllabelmst.AsEnumerable().SingleOrDefault();
         Labelmst labelmst = null;
         if (row != null)
         {
             labelmst = this.BuildFromRow(row);
         }
         return labelmst;
      }
	  
	  

      public IEnumerable<Labelmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbllabelmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Labelmst Get(string coNum, string whNum, string sysName, string programName, string procName, string labelId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("labelmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND labelmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(sysName)) 
         {
             sb.AppendFormatWithEscape(" AND labelmst.sys_name = '{0}'", sysName);
         }
         if (!string.IsNullOrEmpty(programName)) 
         {
             sb.AppendFormatWithEscape(" AND labelmst.program_name = '{0}'", programName);
         }
         if (!string.IsNullOrEmpty(procName)) 
         {
             sb.AppendFormatWithEscape(" AND labelmst.proc_name = '{0}'", procName);
         }
         if (!string.IsNullOrEmpty(labelId)) 
         {
             sb.AppendFormatWithEscape(" AND labelmst.label_id = '{0}'", labelId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Labelmst BuildFromRow(DataRow row)
      {
         var returnRecord = Labelmst.BuildLabelmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Labelmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Labelmst record)
      {
         Labelmst.UpdateRowFromLabelmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Labelmst Insert(Labelmst record)
      {
         DataRow row = this.dataSet.ttbllabelmst.NewttbllabelmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbllabelmst.AddttbllabelmstRow((pdslabelmstDataSet.ttbllabelmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttbllabelmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbllabelmst.Rows[0]) : null;
      }
  

      public Labelmst Update(Labelmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbllabelmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbllabelmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Labelmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbllabelmst.NewttbllabelmstRow();
            Labelmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttbllabelmst.AddttbllabelmstRow((pdslabelmstDataSet.ttbllabelmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Labelmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  