//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdscycleCnt;

   public partial class CycleCntAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdscycle_cnt";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> cycleCntTableControlKey;
		
      private pdscycle_cntDataSet dataSet;
        
      public CycleCntAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdscycle_cntDataSet() { DataSetName = DataSetName };
            this.cycleCntTableControlKey = this.dataSet.ttblcycle_cnt.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.cycleCntTableControlKey))
            {
               this.CreateTableControlParameters(this.cycleCntTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in CycleCntAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Cycle_cntproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poCycle_cntproxy = this.proxyAppObject.CreatePO_cycle_cntproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poCycle_cntproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Cycle_cntproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.cycleCntTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.cycleCntTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Cycle_cntproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poCycle_cntproxy = this.proxyAppObject.CreatePO_cycle_cntproxy())
               {
                  this.SetRequiredContextParameters();
                  poCycle_cntproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Cycle_cntproxy - After Call");
      }
   

      public CycleCnt GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         CycleCnt cycleCnt = null;
         if (row != null)
         {
             cycleCnt = this.BuildFromRow(row);
         }
         return cycleCnt;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(cycle_cnt) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblcycle_cnt.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<CycleCnt> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(cycle_cnt)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected CycleCnt Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblcycle_cnt.AsEnumerable().SingleOrDefault();
         CycleCnt cycleCnt = null;
         if (row != null)
         {
             cycleCnt = this.BuildFromRow(row);
         }
         return cycleCnt;
      }
	  
	  

      public IEnumerable<CycleCnt> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblcycle_cnt.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public CycleCnt Get(int id, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (id != int.MinValue) 
         {
             sb.AppendFormatWithEscape("cycle_cnt.id = {0}", id);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<CycleCnt> GetListByCoWhTypeString(string coNum, string whNum, string cycleType, string cycleString, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("cycle_cnt.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND cycle_cnt.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(cycleType)) 
         {
             sb.AppendFormatWithEscape(" AND cycle_cnt.cycle_type = '{0}'", cycleType);
         }
         if (!string.IsNullOrEmpty(cycleString)) 
         {
             sb.AppendFormatWithEscape(" AND cycle_cnt.cycle_string = '{0}'", cycleString);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public CycleCnt BuildFromRow(DataRow row)
      {
         var returnRecord = CycleCnt.BuildCycleCntFromRow(row);
         returnRecord = this.BuildExtraFromRow<CycleCnt>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, CycleCnt record)
      {
         CycleCnt.UpdateRowFromCycleCnt(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public CycleCnt Insert(CycleCnt record)
      {
         DataRow row = this.dataSet.ttblcycle_cnt.Newttblcycle_cntRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblcycle_cnt.Addttblcycle_cntRow((pdscycle_cntDataSet.ttblcycle_cntRow)row);
         this.SaveChanges();
         return this.dataSet.ttblcycle_cnt.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcycle_cnt.Rows[0]) : null;
      }
  

      public CycleCnt Update(CycleCnt record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblcycle_cnt.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblcycle_cnt.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(CycleCnt record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblcycle_cnt.Newttblcycle_cntRow();
            CycleCnt.BuildMinimalRow(ref row, record);
            this.dataSet.ttblcycle_cnt.Addttblcycle_cntRow((pdscycle_cntDataSet.ttblcycle_cntRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new CycleCnt() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  