//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdsbinSize;

   public partial class BinSizeAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbin_size";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> binSizeTableControlKey;
		
      private pdsbin_sizeDataSet dataSet;
        
      public BinSizeAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbin_sizeDataSet() { DataSetName = DataSetName };
            this.binSizeTableControlKey = this.dataSet.ttblbin_size.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.binSizeTableControlKey))
            {
               this.CreateTableControlParameters(this.binSizeTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BinSizeAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Bin_sizeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBin_sizeproxy = this.proxyAppObject.CreatePO_bin_sizeproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBin_sizeproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Bin_sizeproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.binSizeTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.binSizeTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Bin_sizeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBin_sizeproxy = this.proxyAppObject.CreatePO_bin_sizeproxy())
               {
                  this.SetRequiredContextParameters();
                  poBin_sizeproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Bin_sizeproxy - After Call");
      }
   

      public BinSize GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         BinSize binSize = null;
         if (row != null)
         {
             binSize = this.BuildFromRow(row);
         }
         return binSize;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(bin_size) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbin_size.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<BinSize> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(bin_size)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected BinSize Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbin_size.AsEnumerable().SingleOrDefault();
         BinSize binSize = null;
         if (row != null)
         {
             binSize = this.BuildFromRow(row);
         }
         return binSize;
      }
	  
	  

      public IEnumerable<BinSize> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbin_size.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public BinSize Get(string coNum, string whNum, string locType, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("bin_size.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND bin_size.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(locType)) 
         {
             sb.AppendFormatWithEscape(" AND bin_size.loc_type = '{0}'", locType);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public BinSize BuildFromRow(DataRow row)
      {
         var returnRecord = BinSize.BuildBinSizeFromRow(row);
         returnRecord = this.BuildExtraFromRow<BinSize>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, BinSize record)
      {
         BinSize.UpdateRowFromBinSize(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public BinSize Insert(BinSize record)
      {
         DataRow row = this.dataSet.ttblbin_size.Newttblbin_sizeRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbin_size.Addttblbin_sizeRow((pdsbin_sizeDataSet.ttblbin_sizeRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbin_size.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbin_size.Rows[0]) : null;
      }
  

      public BinSize Update(BinSize record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbin_size.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbin_size.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(BinSize record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbin_size.Newttblbin_sizeRow();
            BinSize.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbin_size.Addttblbin_sizeRow((pdsbin_sizeDataSet.ttblbin_sizeRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new BinSize() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  