//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.PdssysparType;

   public partial class SysparTypeAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdssyspar_type";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> sysparTypeTableControlKey;
		
      private pdssyspar_typeDataSet dataSet;
        
      public SysparTypeAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdssyspar_typeDataSet() { DataSetName = DataSetName };
            this.sysparTypeTableControlKey = this.dataSet.ttblsyspar_type.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.sysparTypeTableControlKey))
            {
               this.CreateTableControlParameters(this.sysparTypeTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in SysparTypeAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Syspar_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poSyspar_typeproxy = this.proxyAppObject.CreatePO_syspar_typeproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poSyspar_typeproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Syspar_typeproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.sysparTypeTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.sysparTypeTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Syspar_typeproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poSyspar_typeproxy = this.proxyAppObject.CreatePO_syspar_typeproxy())
               {
                  this.SetRequiredContextParameters();
                  poSyspar_typeproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Syspar_typeproxy - After Call");
      }
   

      public SysparType GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         SysparType sysparType = null;
         if (row != null)
         {
             sysparType = this.BuildFromRow(row);
         }
         return sysparType;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(syspar_type) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblsyspar_type.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<SysparType> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(syspar_type)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected SysparType Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblsyspar_type.AsEnumerable().SingleOrDefault();
         SysparType sysparType = null;
         if (row != null)
         {
             sysparType = this.BuildFromRow(row);
         }
         return sysparType;
      }
	  
	  

      public IEnumerable<SysparType> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblsyspar_type.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public SysparType Get(int typeId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (typeId != int.MinValue) 
         {
             sb.AppendFormatWithEscape("syspar_type.type_id = {0}", typeId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<SysparType> GetListByName(string typeName, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(typeName)) 
         {
             sb.AppendFormatWithEscape("syspar_type.type_name = '{0}'", typeName);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public SysparType BuildFromRow(DataRow row)
      {
         var returnRecord = SysparType.BuildSysparTypeFromRow(row);
         returnRecord = this.BuildExtraFromRow<SysparType>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, SysparType record)
      {
         SysparType.UpdateRowFromSysparType(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public SysparType Insert(SysparType record)
      {
         DataRow row = this.dataSet.ttblsyspar_type.Newttblsyspar_typeRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblsyspar_type.Addttblsyspar_typeRow((pdssyspar_typeDataSet.ttblsyspar_typeRow)row);
         this.SaveChanges();
         return this.dataSet.ttblsyspar_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsyspar_type.Rows[0]) : null;
      }
  

      public SysparType Update(SysparType record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblsyspar_type.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblsyspar_type.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(SysparType record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblsyspar_type.Newttblsyspar_typeRow();
            SysparType.BuildMinimalRow(ref row, record);
            this.dataSet.ttblsyspar_type.Addttblsyspar_typeRow((pdssyspar_typeDataSet.ttblsyspar_typeRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new SysparType() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  