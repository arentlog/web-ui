//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsbcswmst;

   public partial class BcswmstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbcswmst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> bcswmstTableControlKey;
		
      private pdsbcswmstDataSet dataSet;
        
      public BcswmstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbcswmstDataSet() { DataSetName = DataSetName };
            this.bcswmstTableControlKey = this.dataSet.ttblbcswmst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.bcswmstTableControlKey))
            {
               this.CreateTableControlParameters(this.bcswmstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BcswmstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Bcswmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBcswmstproxy = this.proxyAppObject.CreatePO_bcswmstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBcswmstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Bcswmstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.bcswmstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.bcswmstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Bcswmstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBcswmstproxy = this.proxyAppObject.CreatePO_bcswmstproxy())
               {
                  this.SetRequiredContextParameters();
                  poBcswmstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Bcswmstproxy - After Call");
      }
   

      public Bcswmst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Bcswmst bcswmst = null;
         if (row != null)
         {
             bcswmst = this.BuildFromRow(row);
         }
         return bcswmst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(bcswmst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbcswmst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Bcswmst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(bcswmst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Bcswmst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbcswmst.AsEnumerable().SingleOrDefault();
         Bcswmst bcswmst = null;
         if (row != null)
         {
             bcswmst = this.BuildFromRow(row);
         }
         return bcswmst;
      }
	  
	  

      public IEnumerable<Bcswmst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbcswmst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Bcswmst Get(string coNum, string whNum, string sysName, string appId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("bcswmst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND bcswmst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(sysName)) 
         {
             sb.AppendFormatWithEscape(" AND bcswmst.sys_name = '{0}'", sysName);
         }
         if (!string.IsNullOrEmpty(appId)) 
         {
             sb.AppendFormatWithEscape(" AND bcswmst.app_id = '{0}'", appId);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Bcswmst BuildFromRow(DataRow row)
      {
         var returnRecord = Bcswmst.BuildBcswmstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Bcswmst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Bcswmst record)
      {
         Bcswmst.UpdateRowFromBcswmst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Bcswmst Insert(Bcswmst record)
      {
         DataRow row = this.dataSet.ttblbcswmst.NewttblbcswmstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbcswmst.AddttblbcswmstRow((pdsbcswmstDataSet.ttblbcswmstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbcswmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbcswmst.Rows[0]) : null;
      }
  

      public Bcswmst Update(Bcswmst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbcswmst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbcswmst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Bcswmst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbcswmst.NewttblbcswmstRow();
            Bcswmst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbcswmst.AddttblbcswmstRow((pdsbcswmstDataSet.ttblbcswmstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Bcswmst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  