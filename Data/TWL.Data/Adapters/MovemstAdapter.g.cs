//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.TWL.Data.Adapters
{
   using com.infor.sxproxy.twlproxy;
   using com.infor.sxproxy.twlproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsmovemst;

   public partial class MovemstAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsmovemst";
      private TWLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> movemstTableControlKey;
		
      private pdsmovemstDataSet dataSet;
        
      public MovemstAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new TWLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsmovemstDataSet() { DataSetName = DataSetName };
            this.movemstTableControlKey = this.dataSet.ttblmovemst.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.movemstTableControlKey))
            {
               this.CreateTableControlParameters(this.movemstTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in MovemstAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Movemstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poMovemstproxy = this.proxyAppObject.CreatePO_movemstproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poMovemstproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Movemstproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.movemstTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.movemstTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Movemstproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poMovemstproxy = this.proxyAppObject.CreatePO_movemstproxy())
               {
                  this.SetRequiredContextParameters();
                  poMovemstproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Movemstproxy - After Call");
      }
   

      public Movemst GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Movemst movemst = null;
         if (row != null)
         {
             movemst = this.BuildFromRow(row);
         }
         return movemst;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(movemst) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblmovemst.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Movemst> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(movemst)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Movemst Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblmovemst.AsEnumerable().SingleOrDefault();
         Movemst movemst = null;
         if (row != null)
         {
             movemst = this.BuildFromRow(row);
         }
         return movemst;
      }
	  
	  

      public IEnumerable<Movemst> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblmovemst.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Movemst Get(string coNum, string whNum, string binFrom, string absNum, string rowStatus, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("movemst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(binFrom)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.bin_from = '{0}'", binFrom);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.abs_num = '{0}'", absNum);
         }
         if (!string.IsNullOrEmpty(rowStatus)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.row_status = '{0}'", rowStatus);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Movemst> GetListByBatchItemFromTo(int batch, string absNum, string binFrom, string binTo, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (batch != int.MinValue) 
         {
             sb.AppendFormatWithEscape("movemst.batch = {0}", batch);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.abs_num = '{0}'", absNum);
         }
         if (!string.IsNullOrEmpty(binFrom)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.bin_from = '{0}'", binFrom);
         }
         if (!string.IsNullOrEmpty(binTo)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.bin_to = '{0}'", binTo);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Movemst> GetListByCoWhAbs(string coNum, string whNum, string absNum, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("movemst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.abs_num = '{0}'", absNum);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Movemst> GetListByCoWhOrderSuffix(string coNum, string whNum, string order, string orderSuffix, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("movemst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(order)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.order = '{0}'", order);
         }
         if (!string.IsNullOrEmpty(orderSuffix)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.order_suffix = '{0}'", orderSuffix);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Movemst> GetListByCoWhTask(string coNum, string whNum, int taskId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("movemst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.wh_num = '{0}'", whNum);
         }
         if (taskId != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND movemst.task_id = {0}", taskId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Movemst> GetListByCoWhToAbsStatus(string coNum, string whNum, string binTo, string absNum, string rowStatus, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("movemst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(binTo)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.bin_to = '{0}'", binTo);
         }
         if (!string.IsNullOrEmpty(absNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.abs_num = '{0}'", absNum);
         }
         if (!string.IsNullOrEmpty(rowStatus)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.row_status = '{0}'", rowStatus);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Movemst> GetListByCoWhTruck(string coNum, string whNum, string truckId, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(coNum)) 
         {
             sb.AppendFormatWithEscape("movemst.co_num = '{0}'", coNum);
         }
         if (!string.IsNullOrEmpty(whNum)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.wh_num = '{0}'", whNum);
         }
         if (!string.IsNullOrEmpty(truckId)) 
         {
             sb.AppendFormatWithEscape(" AND movemst.truck_id = '{0}'", truckId);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Movemst> GetListById(int id, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (id != int.MinValue) 
         {
             sb.AppendFormatWithEscape("movemst.id = {0}", id);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Movemst BuildFromRow(DataRow row)
      {
         var returnRecord = Movemst.BuildMovemstFromRow(row);
         returnRecord = this.BuildExtraFromRow<Movemst>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Movemst record)
      {
         Movemst.UpdateRowFromMovemst(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Movemst Insert(Movemst record)
      {
         DataRow row = this.dataSet.ttblmovemst.NewttblmovemstRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblmovemst.AddttblmovemstRow((pdsmovemstDataSet.ttblmovemstRow)row);
         this.SaveChanges();
         return this.dataSet.ttblmovemst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblmovemst.Rows[0]) : null;
      }
  

      public Movemst Update(Movemst record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblmovemst.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblmovemst.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Movemst record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblmovemst.NewttblmovemstRow();
            Movemst.BuildMinimalRow(ref row, record);
            this.dataSet.ttblmovemst.AddttblmovemstRow((pdsmovemstDataSet.ttblmovemstRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Movemst() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  