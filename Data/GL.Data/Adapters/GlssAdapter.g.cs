//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.GL.Data.Adapters
{
   using com.infor.sxproxy.glproxy;
   using com.infor.sxproxy.glproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsglss;

   public partial class GlssAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsglss";
      private GLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> glssTableControlKey;
		
      private pdsglssDataSet dataSet;
        
      public GlssAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new GLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsglssDataSet() { DataSetName = DataSetName };
            this.glssTableControlKey = this.dataSet.ttblglss.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.glssTableControlKey))
            {
               this.CreateTableControlParameters(this.glssTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in GlssAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Glssproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poGlssproxy = this.proxyAppObject.CreatePO_glssproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poGlssproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Glssproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.glssTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.glssTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Glssproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poGlssproxy = this.proxyAppObject.CreatePO_glssproxy())
               {
                  this.SetRequiredContextParameters();
                  poGlssproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Glssproxy - After Call");
      }
   

      public Glss GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Glss glss = null;
         if (row != null)
         {
             glss = this.BuildFromRow(row);
         }
         return glss;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(glss) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblglss.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Glss> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(glss)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Glss Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblglss.AsEnumerable().SingleOrDefault();
         Glss glss = null;
         if (row != null)
         {
             glss = this.BuildFromRow(row);
         }
         return glss;
      }
	  
	  
      public IEnumerable<Glss> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("glss.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Glss> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblglss.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Glss Get(int cono, string subledger, string key1, string key2, string key3, string key4, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("glss.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(subledger)) 
         {
             sb.AppendFormatWithEscape(" AND glss.Subledger = '{0}'", subledger);
         }
         if (!string.IsNullOrEmpty(key1)) 
         {
             sb.AppendFormatWithEscape(" AND glss.key1 = '{0}'", key1);
         }
         if (!string.IsNullOrEmpty(key2)) 
         {
             sb.AppendFormatWithEscape(" AND glss.key2 = '{0}'", key2);
         }
         if (!string.IsNullOrEmpty(key3)) 
         {
             sb.AppendFormatWithEscape(" AND glss.key3 = '{0}'", key3);
         }
         if (!string.IsNullOrEmpty(key4)) 
         {
             sb.AppendFormatWithEscape(" AND glss.key4 = '{0}'", key4);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glss.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Glss BuildFromRow(DataRow row)
      {
         var returnRecord = Glss.BuildGlssFromRow(row);
         returnRecord = this.BuildExtraFromRow<Glss>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Glss record)
      {
         Glss.UpdateRowFromGlss(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Glss Insert(Glss record)
      {
         DataRow row = this.dataSet.ttblglss.NewttblglssRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblglss.AddttblglssRow((pdsglssDataSet.ttblglssRow)row);
         this.SaveChanges();
         return this.dataSet.ttblglss.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglss.Rows[0]) : null;
      }
  

      public Glss Update(Glss record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblglss.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglss.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Glss record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblglss.NewttblglssRow();
            Glss.BuildMinimalRow(ref row, record);
            this.dataSet.ttblglss.AddttblglssRow((pdsglssDataSet.ttblglssRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Glss() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  