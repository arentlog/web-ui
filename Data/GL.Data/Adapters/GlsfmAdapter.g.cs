//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.GL.Data.Adapters
{
   using com.infor.sxproxy.glproxy;
   using com.infor.sxproxy.glproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsglsfm;
   using Models.Pdsglsfmlookup;
   using Models.Complex;

   public partial class GlsfmAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsglsfm";
      private GLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> glsfmTableControlKey;
		
      private pdsglsfmDataSet dataSet;
        
      public GlsfmAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new GLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsglsfmDataSet() { DataSetName = DataSetName };
            this.glsfmTableControlKey = this.dataSet.ttblglsfm.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.glsfmTableControlKey))
            {
               this.CreateTableControlParameters(this.glsfmTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in GlsfmAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Glsfmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poGlsfmproxy = this.proxyAppObject.CreatePO_glsfmproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poGlsfmproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Glsfmproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.glsfmTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.glsfmTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Glsfmproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poGlsfmproxy = this.proxyAppObject.CreatePO_glsfmproxy())
               {
                  this.SetRequiredContextParameters();
                  poGlsfmproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Glsfmproxy - After Call");
      }
   

      public Glsfm GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Glsfm glsfm = null;
         if (row != null)
         {
             glsfm = this.BuildFromRow(row);
         }
         return glsfm;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(glsfm) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblglsfm.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Glsfm> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(glsfm)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Glsfm Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblglsfm.AsEnumerable().SingleOrDefault();
         Glsfm glsfm = null;
         if (row != null)
         {
             glsfm = this.BuildFromRow(row);
         }
         return glsfm;
      }
	  
	  

      public IEnumerable<Glsfm> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblglsfm.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Glsfm Get(string memoryloc, int percal, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(memoryloc)) 
         {
             sb.AppendFormatWithEscape("glsfm.memoryloc = '{0}'", memoryloc);
         }
         if (percal != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glsfm.percal = {0}", percal);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Glsfm BuildFromRow(DataRow row)
      {
         var returnRecord = Glsfm.BuildGlsfmFromRow(row);
         returnRecord = this.BuildExtraFromRow<Glsfm>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Glsfm record)
      {
         Glsfm.UpdateRowFromGlsfm(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Glsfm Insert(Glsfm record)
      {
         DataRow row = this.dataSet.ttblglsfm.NewttblglsfmRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblglsfm.AddttblglsfmRow((pdsglsfmDataSet.ttblglsfmRow)row);
         this.SaveChanges();
         return this.dataSet.ttblglsfm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglsfm.Rows[0]) : null;
      }
  

      public Glsfm Update(Glsfm record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblglsfm.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglsfm.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Glsfm record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblglsfm.NewttblglsfmRow();
            Glsfm.BuildMinimalRow(ref row, record);
            this.dataSet.ttblglsfm.AddttblglsfmRow((pdsglsfmDataSet.ttblglsfmRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Glsfm() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public GlsfmLookupResponseAPI Lookup(Glsfmlookupcriteria glsfmlookupcriteria)
      {   
         var result = new GlsfmLookupResponseAPI();
         
         var pdsglsfmlookup = new pdsglsfmlookupDataSet();
            
         DataRow ttblglsfmlookupcriteriaCriteria = pdsglsfmlookup.ttblglsfmlookupcriteria.NewttblglsfmlookupcriteriaRow();
         Glsfmlookupcriteria.UpdateRowFromGlsfmlookupcriteria(ref ttblglsfmlookupcriteriaCriteria, glsfmlookupcriteria);
         pdsglsfmlookup.ttblglsfmlookupcriteria.AddttblglsfmlookupcriteriaRow((pdsglsfmlookupDataSet.ttblglsfmlookupcriteriaRow)ttblglsfmlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Glsfm - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poGlsfmproxy = this.proxyAppObject.CreatePO_glsfmproxy())
               {
                   this.SetRequiredContextParameters();
                   poGlsfmproxy.Lookup(ref pdsContext, ref pdsglsfmlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Glsfm - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsglsfmlookup); 
    
         foreach (DataRow row in pdsglsfmlookup.ttblglsfmlookupresults)
         {
            result.glsfmlookupresults.Add(Glsfmlookupresults.BuildGlsfmlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  