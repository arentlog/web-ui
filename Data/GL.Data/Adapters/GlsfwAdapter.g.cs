//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.GL.Data.Adapters
{
   using com.infor.sxproxy.glproxy;
   using com.infor.sxproxy.glproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsglsfw;

   public partial class GlsfwAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsglsfw";
      private GLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> glsfwTableControlKey;
		
      private pdsglsfwDataSet dataSet;
        
      public GlsfwAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new GLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsglsfwDataSet() { DataSetName = DataSetName };
            this.glsfwTableControlKey = this.dataSet.ttblglsfw.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.glsfwTableControlKey))
            {
               this.CreateTableControlParameters(this.glsfwTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in GlsfwAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Glsfwproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poGlsfwproxy = this.proxyAppObject.CreatePO_glsfwproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poGlsfwproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Glsfwproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.glsfwTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.glsfwTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Glsfwproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poGlsfwproxy = this.proxyAppObject.CreatePO_glsfwproxy())
               {
                  this.SetRequiredContextParameters();
                  poGlsfwproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Glsfwproxy - After Call");
      }
   

      public Glsfw GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Glsfw glsfw = null;
         if (row != null)
         {
             glsfw = this.BuildFromRow(row);
         }
         return glsfw;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(glsfw) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblglsfw.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Glsfw> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(glsfw)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Glsfw Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblglsfw.AsEnumerable().SingleOrDefault();
         Glsfw glsfw = null;
         if (row != null)
         {
             glsfw = this.BuildFromRow(row);
         }
         return glsfw;
      }
	  
	  
      public IEnumerable<Glsfw> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("glsfw.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Glsfw> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblglsfw.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Glsfw Get(int runcono, string oper2, string reportnm, string sortorder, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (runcono != int.MinValue) 
         {
             sb.AppendFormatWithEscape("glsfw.runcono = {0}", runcono);
         }
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND glsfw.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND glsfw.reportnm = '{0}'", reportnm);
         }
         if (!string.IsNullOrEmpty(sortorder)) 
         {
             sb.AppendFormatWithEscape(" AND glsfw.sortorder = '{0}'", sortorder);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Glsfw BuildFromRow(DataRow row)
      {
         var returnRecord = Glsfw.BuildGlsfwFromRow(row);
         returnRecord = this.BuildExtraFromRow<Glsfw>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Glsfw record)
      {
         Glsfw.UpdateRowFromGlsfw(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Glsfw Insert(Glsfw record)
      {
         DataRow row = this.dataSet.ttblglsfw.NewttblglsfwRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblglsfw.AddttblglsfwRow((pdsglsfwDataSet.ttblglsfwRow)row);
         this.SaveChanges();
         return this.dataSet.ttblglsfw.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglsfw.Rows[0]) : null;
      }
  

      public Glsfw Update(Glsfw record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblglsfw.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglsfw.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Glsfw record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblglsfw.NewttblglsfwRow();
            Glsfw.BuildMinimalRow(ref row, record);
            this.dataSet.ttblglsfw.AddttblglsfwRow((pdsglsfwDataSet.ttblglsfwRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Glsfw() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  