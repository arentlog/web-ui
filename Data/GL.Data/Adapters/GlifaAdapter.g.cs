//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.GL.Data.Adapters
{
   using com.infor.sxproxy.glproxy;
   using com.infor.sxproxy.glproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsglifa;

   public partial class GlifaAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsglifa";
      private GLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> glifaTableControlKey;
		
      private pdsglifaDataSet dataSet;
        
      public GlifaAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new GLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsglifaDataSet() { DataSetName = DataSetName };
            this.glifaTableControlKey = this.dataSet.ttblglifa.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.glifaTableControlKey))
            {
               this.CreateTableControlParameters(this.glifaTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in GlifaAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Glifaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poGlifaproxy = this.proxyAppObject.CreatePO_glifaproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poGlifaproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Glifaproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.glifaTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.glifaTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Glifaproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poGlifaproxy = this.proxyAppObject.CreatePO_glifaproxy())
               {
                  this.SetRequiredContextParameters();
                  poGlifaproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Glifaproxy - After Call");
      }
   

      public Glifa GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Glifa glifa = null;
         if (row != null)
         {
             glifa = this.BuildFromRow(row);
         }
         return glifa;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(glifa) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblglifa.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Glifa> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(glifa)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Glifa Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblglifa.AsEnumerable().SingleOrDefault();
         Glifa glifa = null;
         if (row != null)
         {
             glifa = this.BuildFromRow(row);
         }
         return glifa;
      }
	  
	  
      public IEnumerable<Glifa> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("glifa.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Glifa> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblglifa.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Glifa Get(int cono, string groupnm, string reportnm, int seqlineno, int columnno, int gldivno, int gldeptno, int glacctno, int glsubno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("glifa.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(groupnm)) 
         {
             sb.AppendFormatWithEscape(" AND glifa.groupnm = '{0}'", groupnm);
         }
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND glifa.reportnm = '{0}'", reportnm);
         }
         if (seqlineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glifa.seqlineno = {0}", seqlineno);
         }
         if (columnno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glifa.columnno = {0}", columnno);
         }
         if (gldivno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glifa.gldivno = {0}", gldivno);
         }
         if (gldeptno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glifa.gldeptno = {0}", gldeptno);
         }
         if (glacctno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glifa.glacctno = {0}", glacctno);
         }
         if (glsubno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glifa.glsubno = {0}", glsubno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Glifa BuildFromRow(DataRow row)
      {
         var returnRecord = Glifa.BuildGlifaFromRow(row);
         returnRecord = this.BuildExtraFromRow<Glifa>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Glifa record)
      {
         Glifa.UpdateRowFromGlifa(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Glifa Insert(Glifa record)
      {
         DataRow row = this.dataSet.ttblglifa.NewttblglifaRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblglifa.AddttblglifaRow((pdsglifaDataSet.ttblglifaRow)row);
         this.SaveChanges();
         return this.dataSet.ttblglifa.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglifa.Rows[0]) : null;
      }
  

      public Glifa Update(Glifa record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblglifa.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglifa.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Glifa record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblglifa.NewttblglifaRow();
            Glifa.BuildMinimalRow(ref row, record);
            this.dataSet.ttblglifa.AddttblglifaRow((pdsglifaDataSet.ttblglifaRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Glifa() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  