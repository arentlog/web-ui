//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.GL.Data.Adapters
{
   using com.infor.sxproxy.glproxy;
   using com.infor.sxproxy.glproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsglsb;
   using Models.Pdsglbdgaccountlookup;
   using Models.Complex;

   public partial class GlsbAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsglsb";
      private GLProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> glsbTableControlKey;
		
      private pdsglsbDataSet dataSet;
        
      public GlsbAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new GLProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsglsbDataSet() { DataSetName = DataSetName };
            this.glsbTableControlKey = this.dataSet.ttblglsb.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.glsbTableControlKey))
            {
               this.CreateTableControlParameters(this.glsbTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in GlsbAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Glsbproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poGlsbproxy = this.proxyAppObject.CreatePO_glsbproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poGlsbproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Glsbproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.glsbTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.glsbTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Glsbproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poGlsbproxy = this.proxyAppObject.CreatePO_glsbproxy())
               {
                  this.SetRequiredContextParameters();
                  poGlsbproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Glsbproxy - After Call");
      }
   

      public Glsb GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Glsb glsb = null;
         if (row != null)
         {
             glsb = this.BuildFromRow(row);
         }
         return glsb;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(glsb) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblglsb.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Glsb> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(glsb)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Glsb Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblglsb.AsEnumerable().SingleOrDefault();
         Glsb glsb = null;
         if (row != null)
         {
             glsb = this.BuildFromRow(row);
         }
         return glsb;
      }
	  
	  
      public IEnumerable<Glsb> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("glsb.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Glsb> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblglsb.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Glsb Get(int cono, int yr, int gldivno, int gldeptno, int glacctno, int glsubno, int revno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("glsb.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (yr != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glsb.yr = {0}", yr);
         }
         if (gldivno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glsb.gldivno = {0}", gldivno);
         }
         if (gldeptno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glsb.gldeptno = {0}", gldeptno);
         }
         if (glacctno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glsb.glacctno = {0}", glacctno);
         }
         if (glsubno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glsb.glsubno = {0}", glsubno);
         }
         if (revno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND glsb.revno = {0}", revno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Glsb BuildFromRow(DataRow row)
      {
         var returnRecord = Glsb.BuildGlsbFromRow(row);
         returnRecord = this.BuildExtraFromRow<Glsb>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Glsb record)
      {
         Glsb.UpdateRowFromGlsb(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Glsb Insert(Glsb record)
      {
         DataRow row = this.dataSet.ttblglsb.NewttblglsbRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblglsb.AddttblglsbRow((pdsglsbDataSet.ttblglsbRow)row);
         this.SaveChanges();
         return this.dataSet.ttblglsb.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglsb.Rows[0]) : null;
      }
  

      public Glsb Update(Glsb record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblglsb.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblglsb.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Glsb record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblglsb.NewttblglsbRow();
            Glsb.BuildMinimalRow(ref row, record);
            this.dataSet.ttblglsb.AddttblglsbRow((pdsglsbDataSet.ttblglsbRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Glsb() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public GlsbLookupResponseAPI Lookup(Glbdgaccountlookupcriteria glbdgaccountlookupcriteria)
      {   
         var result = new GlsbLookupResponseAPI();
         
         var pdsglbdgaccountlookup = new pdsglbdgaccountlookupDataSet();
            
         DataRow ttblglbdgaccountlookupcriteriaCriteria = pdsglbdgaccountlookup.ttblglbdgaccountlookupcriteria.NewttblglbdgaccountlookupcriteriaRow();
         Glbdgaccountlookupcriteria.UpdateRowFromGlbdgaccountlookupcriteria(ref ttblglbdgaccountlookupcriteriaCriteria, glbdgaccountlookupcriteria);
         pdsglbdgaccountlookup.ttblglbdgaccountlookupcriteria.AddttblglbdgaccountlookupcriteriaRow((pdsglbdgaccountlookupDataSet.ttblglbdgaccountlookupcriteriaRow)ttblglbdgaccountlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Glsb - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poGlsbproxy = this.proxyAppObject.CreatePO_glsbproxy())
               {
                   this.SetRequiredContextParameters();
                   poGlsbproxy.Lookup(ref pdsContext, ref pdsglbdgaccountlookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Glsb - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsglbdgaccountlookup); 
    
         foreach (DataRow row in pdsglbdgaccountlookup.ttblglbdgaccountlookupresults)
         {
            result.glbdgaccountlookupresults.Add(Glbdgaccountlookupresults.BuildGlbdgaccountlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  