//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.RS.Data.Adapters
{
   using com.infor.sxproxy.rsproxy;
   using com.infor.sxproxy.rsproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsrssj;

   public partial class RssjAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsrssj";
      private RSProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> rssjTableControlKey;
		
      private pdsrssjDataSet dataSet;
        
      public RssjAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new RSProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsrssjDataSet() { DataSetName = DataSetName };
            this.rssjTableControlKey = this.dataSet.ttblrssj.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.rssjTableControlKey))
            {
               this.CreateTableControlParameters(this.rssjTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in RssjAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Rssjproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poRssjproxy = this.proxyAppObject.CreatePO_rssjproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poRssjproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Rssjproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.rssjTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.rssjTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Rssjproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRssjproxy = this.proxyAppObject.CreatePO_rssjproxy())
               {
                  this.SetRequiredContextParameters();
                  poRssjproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Rssjproxy - After Call");
      }
   

      public Rssj GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Rssj rssj = null;
         if (row != null)
         {
             rssj = this.BuildFromRow(row);
         }
         return rssj;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(rssj) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblrssj.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Rssj> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(rssj)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Rssj Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblrssj.AsEnumerable().SingleOrDefault();
         Rssj rssj = null;
         if (row != null)
         {
             rssj = this.BuildFromRow(row);
         }
         return rssj;
      }
	  
	  

      public IEnumerable<Rssj> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblrssj.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Rssj Get(string groupnm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(groupnm)) 
         {
             sb.AppendFormatWithEscape("rssj.groupnm = '{0}'", groupnm);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Rssj BuildFromRow(DataRow row)
      {
         var returnRecord = Rssj.BuildRssjFromRow(row);
         returnRecord = this.BuildExtraFromRow<Rssj>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Rssj record)
      {
         Rssj.UpdateRowFromRssj(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Rssj Insert(Rssj record)
      {
         DataRow row = this.dataSet.ttblrssj.NewttblrssjRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblrssj.AddttblrssjRow((pdsrssjDataSet.ttblrssjRow)row);
         this.SaveChanges();
         return this.dataSet.ttblrssj.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrssj.Rows[0]) : null;
      }
  

      public Rssj Update(Rssj record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblrssj.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrssj.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Rssj record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblrssj.NewttblrssjRow();
            Rssj.BuildMinimalRow(ref row, record);
            this.dataSet.ttblrssj.AddttblrssjRow((pdsrssjDataSet.ttblrssjRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Rssj() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  