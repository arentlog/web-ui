//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.RS.Data.Adapters
{
   using com.infor.sxproxy.rsproxy;
   using com.infor.sxproxy.rsproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsrssq;
   using Models.Pdsrsqueuelookup;
   using Models.Complex;

   public partial class RssqAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsrssq";
      private RSProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> rssqTableControlKey;
		
      private pdsrssqDataSet dataSet;
        
      public RssqAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new RSProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsrssqDataSet() { DataSetName = DataSetName };
            this.rssqTableControlKey = this.dataSet.ttblrssq.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.rssqTableControlKey))
            {
               this.CreateTableControlParameters(this.rssqTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in RssqAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Rssqproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poRssqproxy = this.proxyAppObject.CreatePO_rssqproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poRssqproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Rssqproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.rssqTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.rssqTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Rssqproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRssqproxy = this.proxyAppObject.CreatePO_rssqproxy())
               {
                  this.SetRequiredContextParameters();
                  poRssqproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Rssqproxy - After Call");
      }
   

      public Rssq GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Rssq rssq = null;
         if (row != null)
         {
             rssq = this.BuildFromRow(row);
         }
         return rssq;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(rssq) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblrssq.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Rssq> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(rssq)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Rssq Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblrssq.AsEnumerable().SingleOrDefault();
         Rssq rssq = null;
         if (row != null)
         {
             rssq = this.BuildFromRow(row);
         }
         return rssq;
      }
	  
	  

      public IEnumerable<Rssq> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblrssq.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Rssq Get(string queuenm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(queuenm)) 
         {
             sb.AppendFormatWithEscape("rssq.queuenm = '{0}'", queuenm);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Rssq> GetListByPriority(int queuepri, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (queuepri != int.MinValue) 
         {
             sb.AppendFormatWithEscape("rssq.queuepri = {0}", queuepri);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Rssq> GetListByStatus(bool statustype, int queuepri, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("rssq.statustype = {0}", statustype);
         if (queuepri != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND rssq.queuepri = {0}", queuepri);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Rssq BuildFromRow(DataRow row)
      {
         var returnRecord = Rssq.BuildRssqFromRow(row);
         returnRecord = this.BuildExtraFromRow<Rssq>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Rssq record)
      {
         Rssq.UpdateRowFromRssq(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Rssq Insert(Rssq record)
      {
         DataRow row = this.dataSet.ttblrssq.NewttblrssqRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblrssq.AddttblrssqRow((pdsrssqDataSet.ttblrssqRow)row);
         this.SaveChanges();
         return this.dataSet.ttblrssq.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrssq.Rows[0]) : null;
      }
  

      public Rssq Update(Rssq record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblrssq.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblrssq.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Rssq record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblrssq.NewttblrssqRow();
            Rssq.BuildMinimalRow(ref row, record);
            this.dataSet.ttblrssq.AddttblrssqRow((pdsrssqDataSet.ttblrssqRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Rssq() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public RssqLookupResponseAPI Lookup(Rsqueuelookupcriteria rsqueuelookupcriteria)
      {   
         var result = new RssqLookupResponseAPI();
         
         var pdsrsqueuelookup = new pdsrsqueuelookupDataSet();
            
         DataRow ttblrsqueuelookupcriteriaCriteria = pdsrsqueuelookup.ttblrsqueuelookupcriteria.NewttblrsqueuelookupcriteriaRow();
         Rsqueuelookupcriteria.UpdateRowFromRsqueuelookupcriteria(ref ttblrsqueuelookupcriteriaCriteria, rsqueuelookupcriteria);
         pdsrsqueuelookup.ttblrsqueuelookupcriteria.AddttblrsqueuelookupcriteriaRow((pdsrsqueuelookupDataSet.ttblrsqueuelookupcriteriaRow)ttblrsqueuelookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Rssq - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poRssqproxy = this.proxyAppObject.CreatePO_rssqproxy())
               {
                   this.SetRequiredContextParameters();
                   poRssqproxy.Lookup(ref pdsContext, ref pdsrsqueuelookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Rssq - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsrsqueuelookup); 
    
         foreach (DataRow row in pdsrsqueuelookup.ttblrsqueuelookupresults)
         {
            result.rsqueuelookupresults.Add(Rsqueuelookupresults.BuildRsqueuelookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  