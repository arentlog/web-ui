//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.BI.Data.Adapters
{
   using com.infor.sxproxy.biproxy;
   using com.infor.sxproxy.biproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsbirbg;

   public partial class BirbgAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbirbg";
      private BIProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> birbgTableControlKey;
		
      private pdsbirbgDataSet dataSet;
        
      public BirbgAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new BIProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbirbgDataSet() { DataSetName = DataSetName };
            this.birbgTableControlKey = this.dataSet.ttblbirbg.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.birbgTableControlKey))
            {
               this.CreateTableControlParameters(this.birbgTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BirbgAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Birbgproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBirbgproxy = this.proxyAppObject.CreatePO_birbgproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBirbgproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Birbgproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.birbgTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.birbgTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Birbgproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBirbgproxy = this.proxyAppObject.CreatePO_birbgproxy())
               {
                  this.SetRequiredContextParameters();
                  poBirbgproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Birbgproxy - After Call");
      }
   

      public Birbg GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Birbg birbg = null;
         if (row != null)
         {
             birbg = this.BuildFromRow(row);
         }
         return birbg;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(birbg) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbirbg.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Birbg> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(birbg)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Birbg Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbirbg.AsEnumerable().SingleOrDefault();
         Birbg birbg = null;
         if (row != null)
         {
             birbg = this.BuildFromRow(row);
         }
         return birbg;
      }
	  
	  
      public IEnumerable<Birbg> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("birbg.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Birbg> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbirbg.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Birbg Get(int cono, string kpigroup, int gaugeno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("birbg.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(kpigroup)) 
         {
             sb.AppendFormatWithEscape(" AND birbg.kpigroup = '{0}'", kpigroup);
         }
         if (gaugeno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND birbg.gaugeno = {0}", gaugeno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Birbg BuildFromRow(DataRow row)
      {
         var returnRecord = Birbg.BuildBirbgFromRow(row);
         returnRecord = this.BuildExtraFromRow<Birbg>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Birbg record)
      {
         Birbg.UpdateRowFromBirbg(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Birbg Insert(Birbg record)
      {
         DataRow row = this.dataSet.ttblbirbg.NewttblbirbgRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbirbg.AddttblbirbgRow((pdsbirbgDataSet.ttblbirbgRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbirbg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbirbg.Rows[0]) : null;
      }
  

      public Birbg Update(Birbg record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbirbg.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbirbg.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Birbg record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbirbg.NewttblbirbgRow();
            Birbg.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbirbg.AddttblbirbgRow((pdsbirbgDataSet.ttblbirbgRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Birbg() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  