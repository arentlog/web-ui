//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.BI.Data.Adapters
{
   using com.infor.sxproxy.biproxy;
   using com.infor.sxproxy.biproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsbirbd;

   public partial class BirbdAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsbirbd";
      private BIProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> birbdTableControlKey;
		
      private pdsbirbdDataSet dataSet;
        
      public BirbdAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new BIProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsbirbdDataSet() { DataSetName = DataSetName };
            this.birbdTableControlKey = this.dataSet.ttblbirbd.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.birbdTableControlKey))
            {
               this.CreateTableControlParameters(this.birbdTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in BirbdAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Birbdproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poBirbdproxy = this.proxyAppObject.CreatePO_birbdproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poBirbdproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Birbdproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.birbdTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.birbdTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Birbdproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poBirbdproxy = this.proxyAppObject.CreatePO_birbdproxy())
               {
                  this.SetRequiredContextParameters();
                  poBirbdproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Birbdproxy - After Call");
      }
   

      public Birbd GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Birbd birbd = null;
         if (row != null)
         {
             birbd = this.BuildFromRow(row);
         }
         return birbd;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(birbd) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblbirbd.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Birbd> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(birbd)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Birbd Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblbirbd.AsEnumerable().SingleOrDefault();
         Birbd birbd = null;
         if (row != null)
         {
             birbd = this.BuildFromRow(row);
         }
         return birbd;
      }
	  
	  
      public IEnumerable<Birbd> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("birbd.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Birbd> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblbirbd.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Birbd Get(int cono, string kpigroup, int gaugeno, int pagenum, string slicerdata, string row1data, string row2data, string row3data, string col1data, string col2data, string col3data, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("birbd.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(kpigroup)) 
         {
             sb.AppendFormatWithEscape(" AND birbd.kpigroup = '{0}'", kpigroup);
         }
         if (gaugeno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND birbd.gaugeno = {0}", gaugeno);
         }
         if (pagenum != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND birbd.pagenum = {0}", pagenum);
         }
         if (!string.IsNullOrEmpty(slicerdata)) 
         {
             sb.AppendFormatWithEscape(" AND birbd.slicerdata = '{0}'", slicerdata);
         }
         if (!string.IsNullOrEmpty(row1data)) 
         {
             sb.AppendFormatWithEscape(" AND birbd.row1data = '{0}'", row1data);
         }
         if (!string.IsNullOrEmpty(row2data)) 
         {
             sb.AppendFormatWithEscape(" AND birbd.row2data = '{0}'", row2data);
         }
         if (!string.IsNullOrEmpty(row3data)) 
         {
             sb.AppendFormatWithEscape(" AND birbd.row3data = '{0}'", row3data);
         }
         if (!string.IsNullOrEmpty(col1data)) 
         {
             sb.AppendFormatWithEscape(" AND birbd.col1data = '{0}'", col1data);
         }
         if (!string.IsNullOrEmpty(col2data)) 
         {
             sb.AppendFormatWithEscape(" AND birbd.col2data = '{0}'", col2data);
         }
         if (!string.IsNullOrEmpty(col3data)) 
         {
             sb.AppendFormatWithEscape(" AND birbd.col3data = '{0}'", col3data);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Birbd BuildFromRow(DataRow row)
      {
         var returnRecord = Birbd.BuildBirbdFromRow(row);
         returnRecord = this.BuildExtraFromRow<Birbd>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Birbd record)
      {
         Birbd.UpdateRowFromBirbd(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Birbd Insert(Birbd record)
      {
         DataRow row = this.dataSet.ttblbirbd.NewttblbirbdRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblbirbd.AddttblbirbdRow((pdsbirbdDataSet.ttblbirbdRow)row);
         this.SaveChanges();
         return this.dataSet.ttblbirbd.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbirbd.Rows[0]) : null;
      }
  

      public Birbd Update(Birbd record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblbirbd.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblbirbd.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Birbd record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblbirbd.NewttblbirbdRow();
            Birbd.BuildMinimalRow(ref row, record);
            this.dataSet.ttblbirbd.AddttblbirbdRow((pdsbirbdDataSet.ttblbirbdRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Birbd() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  