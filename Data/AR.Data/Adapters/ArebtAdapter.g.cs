//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.AR.Data.Adapters
{
   using com.infor.sxproxy.arproxy;
   using com.infor.sxproxy.arproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsarebt;

   public partial class ArebtAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsarebt";
      private ARProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> arebtTableControlKey;
		
      private pdsarebtDataSet dataSet;
        
      public ArebtAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ARProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsarebtDataSet() { DataSetName = DataSetName };
            this.arebtTableControlKey = this.dataSet.ttblarebt.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.arebtTableControlKey))
            {
               this.CreateTableControlParameters(this.arebtTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ArebtAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Arebtproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poArebtproxy = this.proxyAppObject.CreatePO_arebtproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poArebtproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Arebtproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.arebtTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.arebtTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Arebtproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poArebtproxy = this.proxyAppObject.CreatePO_arebtproxy())
               {
                  this.SetRequiredContextParameters();
                  poArebtproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Arebtproxy - After Call");
      }
   

      public Arebt GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Arebt arebt = null;
         if (row != null)
         {
             arebt = this.BuildFromRow(row);
         }
         return arebt;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(arebt) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblarebt.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Arebt> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(arebt)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Arebt Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblarebt.AsEnumerable().SingleOrDefault();
         Arebt arebt = null;
         if (row != null)
         {
             arebt = this.BuildFromRow(row);
         }
         return arebt;
      }
	  
	  
      public IEnumerable<Arebt> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("arebt.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arebt> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblarebt.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Arebt Get(int cono, string batchnm, int setno, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arebt.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(batchnm)) 
         {
             sb.AppendFormatWithEscape(" AND arebt.batchnm = '{0}'", batchnm);
         }
         if (setno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arebt.setno = {0}", setno);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arebt.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Arebt> GetListByInvno(int cono, decimal custno, int invno, int invsuf, int transcd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arebt.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arebt.custno = {0}", custno);
         }
         if (invno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arebt.invno = {0}", invno);
         }
         if (invsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arebt.invsuf = {0}", invsuf);
         }
         if (transcd != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arebt.transcd = {0}", transcd);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Arebt BuildFromRow(DataRow row)
      {
         var returnRecord = Arebt.BuildArebtFromRow(row);
         returnRecord = this.BuildExtraFromRow<Arebt>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Arebt record)
      {
         Arebt.UpdateRowFromArebt(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Arebt Insert(Arebt record)
      {
         DataRow row = this.dataSet.ttblarebt.NewttblarebtRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblarebt.AddttblarebtRow((pdsarebtDataSet.ttblarebtRow)row);
         this.SaveChanges();
         return this.dataSet.ttblarebt.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarebt.Rows[0]) : null;
      }
  

      public Arebt Update(Arebt record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblarebt.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarebt.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Arebt record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblarebt.NewttblarebtRow();
            Arebt.BuildMinimalRow(ref row, record);
            this.dataSet.ttblarebt.AddttblarebtRow((pdsarebtDataSet.ttblarebtRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Arebt() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  