//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.AR.Data.Adapters
{
   using com.infor.sxproxy.arproxy;
   using com.infor.sxproxy.arproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsarbsid;

   public partial class ArbsidAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsarbsid";
      private ARProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> arbsidTableControlKey;
		
      private pdsarbsidDataSet dataSet;
        
      public ArbsidAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ARProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsarbsidDataSet() { DataSetName = DataSetName };
            this.arbsidTableControlKey = this.dataSet.ttblarbsid.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.arbsidTableControlKey))
            {
               this.CreateTableControlParameters(this.arbsidTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ArbsidAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Arbsidproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poArbsidproxy = this.proxyAppObject.CreatePO_arbsidproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poArbsidproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Arbsidproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.arbsidTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.arbsidTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Arbsidproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poArbsidproxy = this.proxyAppObject.CreatePO_arbsidproxy())
               {
                  this.SetRequiredContextParameters();
                  poArbsidproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Arbsidproxy - After Call");
      }
   

      public Arbsid GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Arbsid arbsid = null;
         if (row != null)
         {
             arbsid = this.BuildFromRow(row);
         }
         return arbsid;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(arbsid) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblarbsid.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Arbsid> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(arbsid)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Arbsid Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblarbsid.AsEnumerable().SingleOrDefault();
         Arbsid arbsid = null;
         if (row != null)
         {
             arbsid = this.BuildFromRow(row);
         }
         return arbsid;
      }
	  
	  

      public IEnumerable<Arbsid> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblarbsid.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Arbsid Get(string imptype, int recno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(imptype)) 
         {
             sb.AppendFormatWithEscape("arbsid.imptype = '{0}'", imptype);
         }
         if (recno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbsid.recno = {0}", recno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Arbsid BuildFromRow(DataRow row)
      {
         var returnRecord = Arbsid.BuildArbsidFromRow(row);
         returnRecord = this.BuildExtraFromRow<Arbsid>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Arbsid record)
      {
         Arbsid.UpdateRowFromArbsid(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Arbsid Insert(Arbsid record)
      {
         DataRow row = this.dataSet.ttblarbsid.NewttblarbsidRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblarbsid.AddttblarbsidRow((pdsarbsidDataSet.ttblarbsidRow)row);
         this.SaveChanges();
         return this.dataSet.ttblarbsid.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarbsid.Rows[0]) : null;
      }
  

      public Arbsid Update(Arbsid record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblarbsid.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarbsid.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Arbsid record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblarbsid.NewttblarbsidRow();
            Arbsid.BuildMinimalRow(ref row, record);
            this.dataSet.ttblarbsid.AddttblarbsidRow((pdsarbsidDataSet.ttblarbsidRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Arbsid() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  