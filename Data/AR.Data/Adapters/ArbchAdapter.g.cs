//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.AR.Data.Adapters
{
   using com.infor.sxproxy.arproxy;
   using com.infor.sxproxy.arproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsarbch;
   using Models.Pdsarbchchecklookup;
   using Models.Complex;

   public partial class ArbchAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsarbch";
      private ARProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> arbchTableControlKey;
		
      private pdsarbchDataSet dataSet;
        
      public ArbchAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ARProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsarbchDataSet() { DataSetName = DataSetName };
            this.arbchTableControlKey = this.dataSet.ttblarbch.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.arbchTableControlKey))
            {
               this.CreateTableControlParameters(this.arbchTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ArbchAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Arbchproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poArbchproxy = this.proxyAppObject.CreatePO_arbchproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poArbchproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Arbchproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.arbchTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.arbchTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Arbchproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poArbchproxy = this.proxyAppObject.CreatePO_arbchproxy())
               {
                  this.SetRequiredContextParameters();
                  poArbchproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Arbchproxy - After Call");
      }
   

      public Arbch GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Arbch arbch = null;
         if (row != null)
         {
             arbch = this.BuildFromRow(row);
         }
         return arbch;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(arbch) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblarbch.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Arbch> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(arbch)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Arbch Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblarbch.AsEnumerable().SingleOrDefault();
         Arbch arbch = null;
         if (row != null)
         {
             arbch = this.BuildFromRow(row);
         }
         return arbch;
      }
	  
	  
      public IEnumerable<Arbch> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("arbch.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arbch> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblarbch.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Arbch Get(int cono, string batch, decimal custno, int checkno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arbch.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(batch)) 
         {
             sb.AppendFormatWithEscape(" AND arbch.batch = '{0}'", batch);
         }
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbch.custno = {0}", custno);
         }
         if (checkno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbch.checkno = {0}", checkno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Arbch> GetListByRecvdt(int cono, DateTime? recvdt, decimal custno, int checkno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arbch.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (recvdt != null) 
         {
             sb.AppendFormatWithEscape(" AND arbch.recvdt = '{0}'", recvdt);
         }
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbch.custno = {0}", custno);
         }
         if (checkno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbch.checkno = {0}", checkno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arbch> GetListBySeqno(int cono, string batch, int checkseq, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arbch.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(batch)) 
         {
             sb.AppendFormatWithEscape(" AND arbch.batch = '{0}'", batch);
         }
         if (checkseq != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbch.checkseq = {0}", checkseq);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arbch> GetListByXmit(int cono, string transmission, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arbch.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(transmission)) 
         {
             sb.AppendFormatWithEscape(" AND arbch.transmission = '{0}'", transmission);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Arbch BuildFromRow(DataRow row)
      {
         var returnRecord = Arbch.BuildArbchFromRow(row);
         returnRecord = this.BuildExtraFromRow<Arbch>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Arbch record)
      {
         Arbch.UpdateRowFromArbch(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Arbch Insert(Arbch record)
      {
         DataRow row = this.dataSet.ttblarbch.NewttblarbchRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblarbch.AddttblarbchRow((pdsarbchDataSet.ttblarbchRow)row);
         this.SaveChanges();
         return this.dataSet.ttblarbch.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarbch.Rows[0]) : null;
      }
  

      public Arbch Update(Arbch record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblarbch.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarbch.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Arbch record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblarbch.NewttblarbchRow();
            Arbch.BuildMinimalRow(ref row, record);
            this.dataSet.ttblarbch.AddttblarbchRow((pdsarbchDataSet.ttblarbchRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Arbch() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      public ArbchLookupResponseAPI Lookup(Arbchlookupcriteria arbchlookupcriteria)
      {   
         var result = new ArbchLookupResponseAPI();
         
         var pdsarbchchecklookup = new pdsarbchchecklookupDataSet();
            
         DataRow ttblarbchlookupcriteriaCriteria = pdsarbchchecklookup.ttblarbchlookupcriteria.NewttblarbchlookupcriteriaRow();
         Arbchlookupcriteria.UpdateRowFromArbchlookupcriteria(ref ttblarbchlookupcriteriaCriteria, arbchlookupcriteria);
         pdsarbchchecklookup.ttblarbchlookupcriteria.AddttblarbchlookupcriteriaRow((pdsarbchchecklookupDataSet.ttblarbchlookupcriteriaRow)ttblarbchlookupcriteriaCriteria);
            
         string cErrorMessage = string.Empty;
         bool lMoreRecords = false;
   
         
         NLogLoggerP.Trace("Lookup - Arbch - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poArbchproxy = this.proxyAppObject.CreatePO_arbchproxy())
               {
                   this.SetRequiredContextParameters();
                   poArbchproxy.Lookup(ref pdsContext, ref pdsarbchchecklookup, out cErrorMessage, out lMoreRecords);
               }
            });
         NLogLoggerP.Info("Lookup - Arbch - After Call");

         
         this.ReportErrors(cErrorMessage);
         this.ReportErrors(this.pdsContext);
         this.ReportErrors(pdsarbchchecklookup); 
    
         foreach (DataRow row in pdsarbchchecklookup.ttblarbchlookupresults)
         {
            result.arbchlookupresults.Add(Arbchlookupresults.BuildArbchlookupresultsFromRow(row));
         }     
         result.lMoreRecords = lMoreRecords;
          
         return result;
          
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  