//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.AR.Data.Adapters
{
   using com.infor.sxproxy.arproxy;
   using com.infor.sxproxy.arproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsarett;

   public partial class ArettAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsarett";
      private ARProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> arettTableControlKey;
		
      private pdsarettDataSet dataSet;
        
      public ArettAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ARProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsarettDataSet() { DataSetName = DataSetName };
            this.arettTableControlKey = this.dataSet.ttblarett.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.arettTableControlKey))
            {
               this.CreateTableControlParameters(this.arettTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ArettAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Arettproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poArettproxy = this.proxyAppObject.CreatePO_arettproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poArettproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Arettproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.arettTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.arettTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Arettproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poArettproxy = this.proxyAppObject.CreatePO_arettproxy())
               {
                  this.SetRequiredContextParameters();
                  poArettproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Arettproxy - After Call");
      }
   

      public Arett GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Arett arett = null;
         if (row != null)
         {
             arett = this.BuildFromRow(row);
         }
         return arett;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(arett) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblarett.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Arett> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(arett)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Arett Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblarett.AsEnumerable().SingleOrDefault();
         Arett arett = null;
         if (row != null)
         {
             arett = this.BuildFromRow(row);
         }
         return arett;
      }
	  
	  
      public IEnumerable<Arett> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("arett.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arett> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblarett.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Arett Get(int cono, int recty, int taxgroup, string statecd, string countycd, string citycd, string othercd, string taxexemptcd, int orderno, int ordersuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arett.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (recty != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.recty = {0}", recty);
         }
         if (taxgroup != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.taxgroup = {0}", taxgroup);
         }
         if (!string.IsNullOrEmpty(statecd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.statecd = '{0}'", statecd);
         }
         if (!string.IsNullOrEmpty(countycd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.countycd = '{0}'", countycd);
         }
         if (!string.IsNullOrEmpty(citycd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.citycd = '{0}'", citycd);
         }
         if (!string.IsNullOrEmpty(othercd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.othercd = '{0}'", othercd);
         }
         if (!string.IsNullOrEmpty(taxexemptcd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.taxexemptcd = '{0}'", taxexemptcd);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.ordersuf = {0}", ordersuf);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Arett> GetListByInvdt(int cono, int recty, int taxgroup, string statecd, string countycd, string citycd, string othercd, DateTime? invdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arett.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (recty != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.recty = {0}", recty);
         }
         if (taxgroup != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.taxgroup = {0}", taxgroup);
         }
         if (!string.IsNullOrEmpty(statecd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.statecd = '{0}'", statecd);
         }
         if (!string.IsNullOrEmpty(countycd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.countycd = '{0}'", countycd);
         }
         if (!string.IsNullOrEmpty(citycd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.citycd = '{0}'", citycd);
         }
         if (!string.IsNullOrEmpty(othercd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.othercd = '{0}'", othercd);
         }
         if (invdt != null) 
         {
             sb.AppendFormatWithEscape(" AND arett.invdt = '{0}'", invdt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arett> GetListByOrderno(int cono, int orderno, int ordersuf, int recty, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arett.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.ordersuf = {0}", ordersuf);
         }
         if (recty != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.recty = {0}", recty);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arett> GetListByPaiddt(int cono, int recty, int taxgroup, string statecd, string countycd, string citycd, string othercd, DateTime? paiddt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arett.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (recty != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.recty = {0}", recty);
         }
         if (taxgroup != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arett.taxgroup = {0}", taxgroup);
         }
         if (!string.IsNullOrEmpty(statecd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.statecd = '{0}'", statecd);
         }
         if (!string.IsNullOrEmpty(countycd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.countycd = '{0}'", countycd);
         }
         if (!string.IsNullOrEmpty(citycd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.citycd = '{0}'", citycd);
         }
         if (!string.IsNullOrEmpty(othercd)) 
         {
             sb.AppendFormatWithEscape(" AND arett.othercd = '{0}'", othercd);
         }
         if (paiddt != null) 
         {
             sb.AppendFormatWithEscape(" AND arett.paiddt = '{0}'", paiddt);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arett> GetListByTransdttmz(DateTime? transdttmz, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         if (transdttmz != null) 
         {
             sb.AppendFormatWithEscape("arett.transdttmz = '{0}'", transdttmz);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Arett BuildFromRow(DataRow row)
      {
         var returnRecord = Arett.BuildArettFromRow(row);
         returnRecord = this.BuildExtraFromRow<Arett>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Arett record)
      {
         Arett.UpdateRowFromArett(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Arett Insert(Arett record)
      {
         DataRow row = this.dataSet.ttblarett.NewttblarettRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblarett.AddttblarettRow((pdsarettDataSet.ttblarettRow)row);
         this.SaveChanges();
         return this.dataSet.ttblarett.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarett.Rows[0]) : null;
      }
  

      public Arett Update(Arett record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblarett.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarett.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Arett record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblarett.NewttblarettRow();
            Arett.BuildMinimalRow(ref row, record);
            this.dataSet.ttblarett.AddttblarettRow((pdsarettDataSet.ttblarettRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Arett() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  