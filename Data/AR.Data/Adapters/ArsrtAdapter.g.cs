//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.AR.Data.Adapters
{
   using com.infor.sxproxy.arproxy;
   using com.infor.sxproxy.arproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsarsrt;

   public partial class ArsrtAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsarsrt";
      private ARProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> arsrtTableControlKey;
		
      private pdsarsrtDataSet dataSet;
        
      public ArsrtAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ARProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsarsrtDataSet() { DataSetName = DataSetName };
            this.arsrtTableControlKey = this.dataSet.ttblarsrt.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.arsrtTableControlKey))
            {
               this.CreateTableControlParameters(this.arsrtTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ArsrtAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Arsrtproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poArsrtproxy = this.proxyAppObject.CreatePO_arsrtproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poArsrtproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Arsrtproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.arsrtTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.arsrtTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Arsrtproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poArsrtproxy = this.proxyAppObject.CreatePO_arsrtproxy())
               {
                  this.SetRequiredContextParameters();
                  poArsrtproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Arsrtproxy - After Call");
      }
   

      public Arsrt GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Arsrt arsrt = null;
         if (row != null)
         {
             arsrt = this.BuildFromRow(row);
         }
         return arsrt;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(arsrt) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblarsrt.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Arsrt> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(arsrt)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Arsrt Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblarsrt.AsEnumerable().SingleOrDefault();
         Arsrt arsrt = null;
         if (row != null)
         {
             arsrt = this.BuildFromRow(row);
         }
         return arsrt;
      }
	  
	  
      public IEnumerable<Arsrt> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("arsrt.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arsrt> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblarsrt.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Arsrt Get(int cono, decimal custno, string shipto, string rebatety, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arsrt.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arsrt.custno = {0}", custno);
         }
         if (!string.IsNullOrEmpty(shipto)) 
         {
             sb.AppendFormatWithEscape(" AND arsrt.shipto = '{0}'", shipto);
         }
         if (!string.IsNullOrEmpty(rebatety)) 
         {
             sb.AppendFormatWithEscape(" AND arsrt.rebatety = '{0}'", rebatety);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Arsrt> GetListByRebatety(int cono, string rebatety, decimal custno, string shipto, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arsrt.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(rebatety)) 
         {
             sb.AppendFormatWithEscape(" AND arsrt.rebatety = '{0}'", rebatety);
         }
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arsrt.custno = {0}", custno);
         }
         if (!string.IsNullOrEmpty(shipto)) 
         {
             sb.AppendFormatWithEscape(" AND arsrt.shipto = '{0}'", shipto);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Arsrt BuildFromRow(DataRow row)
      {
         var returnRecord = Arsrt.BuildArsrtFromRow(row);
         returnRecord = this.BuildExtraFromRow<Arsrt>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Arsrt record)
      {
         Arsrt.UpdateRowFromArsrt(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Arsrt Insert(Arsrt record)
      {
         DataRow row = this.dataSet.ttblarsrt.NewttblarsrtRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblarsrt.AddttblarsrtRow((pdsarsrtDataSet.ttblarsrtRow)row);
         this.SaveChanges();
         return this.dataSet.ttblarsrt.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarsrt.Rows[0]) : null;
      }
  

      public Arsrt Update(Arsrt record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblarsrt.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarsrt.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Arsrt record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblarsrt.NewttblarsrtRow();
            Arsrt.BuildMinimalRow(ref row, record);
            this.dataSet.ttblarsrt.AddttblarsrtRow((pdsarsrtDataSet.ttblarsrtRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Arsrt() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  