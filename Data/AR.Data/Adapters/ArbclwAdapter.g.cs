//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.AR.Data.Adapters
{
   using com.infor.sxproxy.arproxy;
   using com.infor.sxproxy.arproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsarbclw;

   public partial class ArbclwAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsarbclw";
      private ARProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> arbclwTableControlKey;
		
      private pdsarbclwDataSet dataSet;
        
      public ArbclwAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new ARProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsarbclwDataSet() { DataSetName = DataSetName };
            this.arbclwTableControlKey = this.dataSet.ttblarbclw.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.arbclwTableControlKey))
            {
               this.CreateTableControlParameters(this.arbclwTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in ArbclwAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Arbclwproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poArbclwproxy = this.proxyAppObject.CreatePO_arbclwproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poArbclwproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Arbclwproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.arbclwTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.arbclwTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Arbclwproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poArbclwproxy = this.proxyAppObject.CreatePO_arbclwproxy())
               {
                  this.SetRequiredContextParameters();
                  poArbclwproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Arbclwproxy - After Call");
      }
   

      public Arbclw GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Arbclw arbclw = null;
         if (row != null)
         {
             arbclw = this.BuildFromRow(row);
         }
         return arbclw;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(arbclw) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblarbclw.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Arbclw> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(arbclw)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Arbclw Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblarbclw.AsEnumerable().SingleOrDefault();
         Arbclw arbclw = null;
         if (row != null)
         {
             arbclw = this.BuildFromRow(row);
         }
         return arbclw;
      }
	  
	  
      public IEnumerable<Arbclw> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("arbclw.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Arbclw> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblarbclw.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Arbclw Get(int cono, decimal custno, int checkno, int invno, int invsuf, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arbclw.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.custno = {0}", custno);
         }
         if (checkno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.checkno = {0}", checkno);
         }
         if (invno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.invno = {0}", invno);
         }
         if (invsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.invsuf = {0}", invsuf);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Arbclw> GetListByArbclw(int cono, string batch, decimal custno, int checkno, int invno, int invsuf, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("arbclw.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(batch)) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.batch = '{0}'", batch);
         }
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.custno = {0}", custno);
         }
         if (checkno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.checkno = {0}", checkno);
         }
         if (invno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.invno = {0}", invno);
         }
         if (invsuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.invsuf = {0}", invsuf);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND arbclw.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Arbclw BuildFromRow(DataRow row)
      {
         var returnRecord = Arbclw.BuildArbclwFromRow(row);
         returnRecord = this.BuildExtraFromRow<Arbclw>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Arbclw record)
      {
         Arbclw.UpdateRowFromArbclw(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Arbclw Insert(Arbclw record)
      {
         DataRow row = this.dataSet.ttblarbclw.NewttblarbclwRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblarbclw.AddttblarbclwRow((pdsarbclwDataSet.ttblarbclwRow)row);
         this.SaveChanges();
         return this.dataSet.ttblarbclw.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarbclw.Rows[0]) : null;
      }
  

      public Arbclw Update(Arbclw record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblarbclw.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblarbclw.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Arbclw record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblarbclw.NewttblarbclwRow();
            Arbclw.BuildMinimalRow(ref row, record);
            this.dataSet.ttblarbclw.AddttblarbclwRow((pdsarbclwDataSet.ttblarbclwRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Arbclw() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  