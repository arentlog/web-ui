//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PD.Data.Adapters
{
   using com.infor.sxproxy.pdproxy;
   using com.infor.sxproxy.pdproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspdsra;

   public partial class PdsraAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspdsra";
      private PDProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pdsraTableControlKey;
		
      private pdspdsraDataSet dataSet;
        
      public PdsraAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PDProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspdsraDataSet() { DataSetName = DataSetName };
            this.pdsraTableControlKey = this.dataSet.ttblpdsra.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pdsraTableControlKey))
            {
               this.CreateTableControlParameters(this.pdsraTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PdsraAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pdsraproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPdsraproxy = this.proxyAppObject.CreatePO_pdsraproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPdsraproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pdsraproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pdsraTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pdsraTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pdsraproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPdsraproxy = this.proxyAppObject.CreatePO_pdsraproxy())
               {
                  this.SetRequiredContextParameters();
                  poPdsraproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pdsraproxy - After Call");
      }
   

      public Pdsra GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Pdsra pdsra = null;
         if (row != null)
         {
             pdsra = this.BuildFromRow(row);
         }
         return pdsra;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pdsra) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpdsra.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Pdsra> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pdsra)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Pdsra Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpdsra.AsEnumerable().SingleOrDefault();
         Pdsra pdsra = null;
         if (row != null)
         {
             pdsra = this.BuildFromRow(row);
         }
         return pdsra;
      }
	  
	  
	  public IEnumerable<Pdsra> GetListByRowpointers(List<string> rowpointers, string fldList)
      {
         var where = new StringBuilder();
         if (rowpointers != null && rowpointers.Count > 0)
         {
           foreach (var rowpointer in rowpointers)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("pdsra.rowpointer='{0}'", rowpointer);
           }
         }
         return this.GetList(where.ToString(), rowpointers.Count, fldList);
      }
	  
	  private DataRow GetRowByRowpointer(string rowpointer, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("pdsra.rowpointer = '{0}'", rowpointer);
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpdsra.AsEnumerable().SingleOrDefault();
      }
      public IEnumerable<Pdsra> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pdsra.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Pdsra> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpdsra.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Pdsra Get(int cono, int methodno, int vendno, string altprodgrp, string custrebty, string whse, DateTime? startdt, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pdsra.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (methodno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pdsra.methodno = {0}", methodno);
         }
         if (vendno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pdsra.vendno = {0}", vendno);
         }
         if (!string.IsNullOrEmpty(altprodgrp)) 
         {
             sb.AppendFormatWithEscape(" AND pdsra.altprodgrp = '{0}'", altprodgrp);
         }
         if (!string.IsNullOrEmpty(custrebty)) 
         {
             sb.AppendFormatWithEscape(" AND pdsra.custrebty = '{0}'", custrebty);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND pdsra.whse = '{0}'", whse);
         }
         if (startdt != null) 
         {
             sb.AppendFormatWithEscape(" AND pdsra.startdt = '{0}'", startdt);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Pdsra> GetListByAltrebrecno(int cono, int altrebrecno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pdsra.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (altrebrecno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pdsra.altrebrecno = {0}", altrebrecno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Pdsra GetByRowpointer(string rowpointer, string fldList)
      {   
         var sb = new StringBuilder();
         if (!string.IsNullOrEmpty(rowpointer)) 
         {
             sb.AppendFormatWithEscape("pdsra.rowpointer = '{0}'", rowpointer);
         }
         var where = sb.ToString();
         return this.Fetch(where, 1, fldList);
      }

      public Pdsra BuildFromRow(DataRow row)
      {
         var returnRecord = Pdsra.BuildPdsraFromRow(row);
         returnRecord = this.BuildExtraFromRow<Pdsra>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Pdsra record)
      {
         Pdsra.UpdateRowFromPdsra(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Pdsra Insert(Pdsra record)
      {
         DataRow row = this.dataSet.ttblpdsra.NewttblpdsraRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpdsra.AddttblpdsraRow((pdspdsraDataSet.ttblpdsraRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpdsra.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpdsra.Rows[0]) : null;
      }
  

      public Pdsra Update(Pdsra record)
      {
        var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpdsra.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpdsra.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Pdsra record)
      {
         var row = this.GetRowByRowpointer(record.rowpointer, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpdsra.NewttblpdsraRow();
            Pdsra.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpdsra.AddttblpdsraRow((pdspdsraDataSet.ttblpdsraRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }
 
      private void DeleteUseRowID(Pdsra record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row == null)
         {
            row = this.dataSet.ttblpdsra.NewttblpdsraRow();
            Pdsra.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpdsra.AddttblpdsraRow((pdspdsraDataSet.ttblpdsraRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Pdsra() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.DeleteUseRowID(rec);
            }
         }
      }
	  
 
      public void DeleteListByRowPointers(List<string> rowpointers)
      {
         if (rowpointers != null)
         {
            var recList = rowpointers.Select(selectRowpointer => new Pdsra() { rowpointer = selectRowpointer }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  