//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.PD.Data.Adapters
{
   using com.infor.sxproxy.pdproxy;
   using com.infor.sxproxy.pdproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdspdsvtr;

   public partial class PdsvtrAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdspdsvtr";
      private PDProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> pdsvtrTableControlKey;
		
      private pdspdsvtrDataSet dataSet;
        
      public PdsvtrAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new PDProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdspdsvtrDataSet() { DataSetName = DataSetName };
            this.pdsvtrTableControlKey = this.dataSet.ttblpdsvtr.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.pdsvtrTableControlKey))
            {
               this.CreateTableControlParameters(this.pdsvtrTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in PdsvtrAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Pdsvtrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poPdsvtrproxy = this.proxyAppObject.CreatePO_pdsvtrproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poPdsvtrproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Pdsvtrproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.pdsvtrTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.pdsvtrTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Pdsvtrproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poPdsvtrproxy = this.proxyAppObject.CreatePO_pdsvtrproxy())
               {
                  this.SetRequiredContextParameters();
                  poPdsvtrproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Pdsvtrproxy - After Call");
      }
   

      public Pdsvtr GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Pdsvtr pdsvtr = null;
         if (row != null)
         {
             pdsvtr = this.BuildFromRow(row);
         }
         return pdsvtr;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(pdsvtr) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttblpdsvtr.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Pdsvtr> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(pdsvtr)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Pdsvtr Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttblpdsvtr.AsEnumerable().SingleOrDefault();
         Pdsvtr pdsvtr = null;
         if (row != null)
         {
             pdsvtr = this.BuildFromRow(row);
         }
         return pdsvtr;
      }
	  
	  
      public IEnumerable<Pdsvtr> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("pdsvtr.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Pdsvtr> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttblpdsvtr.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Pdsvtr Get(int cono, string typecd, int orderno, int ordersuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pdsvtr.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(typecd)) 
         {
             sb.AppendFormatWithEscape(" AND pdsvtr.typecd = '{0}'", typecd);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pdsvtr.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pdsvtr.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pdsvtr.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Pdsvtr> GetListByProd(int cono, string prod, string typecd, int orderno, int ordersuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pdsvtr.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND pdsvtr.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(typecd)) 
         {
             sb.AppendFormatWithEscape(" AND pdsvtr.typecd = '{0}'", typecd);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pdsvtr.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND pdsvtr.ordersuf = {0}", ordersuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Pdsvtr> GetListByTypecd(int cono, string typecd, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("pdsvtr.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(typecd)) 
         {
             sb.AppendFormatWithEscape(" AND pdsvtr.typecd = '{0}'", typecd);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Pdsvtr BuildFromRow(DataRow row)
      {
         var returnRecord = Pdsvtr.BuildPdsvtrFromRow(row);
         returnRecord = this.BuildExtraFromRow<Pdsvtr>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Pdsvtr record)
      {
         Pdsvtr.UpdateRowFromPdsvtr(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Pdsvtr Insert(Pdsvtr record)
      {
         DataRow row = this.dataSet.ttblpdsvtr.NewttblpdsvtrRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttblpdsvtr.AddttblpdsvtrRow((pdspdsvtrDataSet.ttblpdsvtrRow)row);
         this.SaveChanges();
         return this.dataSet.ttblpdsvtr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpdsvtr.Rows[0]) : null;
      }
  

      public Pdsvtr Update(Pdsvtr record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttblpdsvtr.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttblpdsvtr.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Pdsvtr record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttblpdsvtr.NewttblpdsvtrRow();
            Pdsvtr.BuildMinimalRow(ref row, record);
            this.dataSet.ttblpdsvtr.AddttblpdsvtrRow((pdspdsvtrDataSet.ttblpdsvtrRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Pdsvtr() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  