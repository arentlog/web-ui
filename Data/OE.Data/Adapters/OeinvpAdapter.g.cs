//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OE.Data.Adapters
{
   using com.infor.sxproxy.oeproxy;
   using com.infor.sxproxy.oeproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoeinvp;

   public partial class OeinvpAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoeinvp";
      private OEProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> oeinvpTableControlKey;
		
      private pdsoeinvpDataSet dataSet;
        
      public OeinvpAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OEProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsoeinvpDataSet() { DataSetName = DataSetName };
            this.oeinvpTableControlKey = this.dataSet.ttbloeinvp.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.oeinvpTableControlKey))
            {
               this.CreateTableControlParameters(this.oeinvpTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OeinvpAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Oeinvpproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOeinvpproxy = this.proxyAppObject.CreatePO_oeinvpproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOeinvpproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Oeinvpproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.oeinvpTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.oeinvpTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Oeinvpproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOeinvpproxy = this.proxyAppObject.CreatePO_oeinvpproxy())
               {
                  this.SetRequiredContextParameters();
                  poOeinvpproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Oeinvpproxy - After Call");
      }
   

      public Oeinvp GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oeinvp oeinvp = null;
         if (row != null)
         {
             oeinvp = this.BuildFromRow(row);
         }
         return oeinvp;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oeinvp) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloeinvp.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oeinvp> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oeinvp)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oeinvp Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloeinvp.AsEnumerable().SingleOrDefault();
         Oeinvp oeinvp = null;
         if (row != null)
         {
             oeinvp = this.BuildFromRow(row);
         }
         return oeinvp;
      }
	  
	  
      public IEnumerable<Oeinvp> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oeinvp.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oeinvp> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloeinvp.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oeinvp Get(int cono, string oper2, string reportnm, string etype, int orderno, int ordersuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeinvp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND oeinvp.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND oeinvp.reportnm = '{0}'", reportnm);
         }
         if (!string.IsNullOrEmpty(etype)) 
         {
             sb.AppendFormatWithEscape(" AND oeinvp.etype = '{0}'", etype);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeinvp.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeinvp.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeinvp.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Oeinvp BuildFromRow(DataRow row)
      {
         var returnRecord = Oeinvp.BuildOeinvpFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oeinvp>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oeinvp record)
      {
         Oeinvp.UpdateRowFromOeinvp(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oeinvp Insert(Oeinvp record)
      {
         DataRow row = this.dataSet.ttbloeinvp.NewttbloeinvpRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloeinvp.AddttbloeinvpRow((pdsoeinvpDataSet.ttbloeinvpRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloeinvp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeinvp.Rows[0]) : null;
      }
  

      public Oeinvp Update(Oeinvp record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloeinvp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeinvp.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oeinvp record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloeinvp.NewttbloeinvpRow();
            Oeinvp.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloeinvp.AddttbloeinvpRow((pdsoeinvpDataSet.ttbloeinvpRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oeinvp() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  