//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OE.Data.Adapters
{
   using com.infor.sxproxy.oeproxy;
   using com.infor.sxproxy.oeproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoeelextra;

   public partial class OeelextraAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoeelextra";
      private OEProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> oeelextraTableControlKey;
		
      private pdsoeelextraDataSet dataSet;
        
      public OeelextraAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OEProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsoeelextraDataSet() { DataSetName = DataSetName };
            this.oeelextraTableControlKey = this.dataSet.ttbloeelextra.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.oeelextraTableControlKey))
            {
               this.CreateTableControlParameters(this.oeelextraTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OeelextraAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Oeelextraproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOeelextraproxy = this.proxyAppObject.CreatePO_oeelextraproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOeelextraproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Oeelextraproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.oeelextraTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.oeelextraTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Oeelextraproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOeelextraproxy = this.proxyAppObject.CreatePO_oeelextraproxy())
               {
                  this.SetRequiredContextParameters();
                  poOeelextraproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Oeelextraproxy - After Call");
      }
   

      public Oeelextra GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oeelextra oeelextra = null;
         if (row != null)
         {
             oeelextra = this.BuildFromRow(row);
         }
         return oeelextra;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oeelextra) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloeelextra.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oeelextra> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oeelextra)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oeelextra Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloeelextra.AsEnumerable().SingleOrDefault();
         Oeelextra oeelextra = null;
         if (row != null)
         {
             oeelextra = this.BuildFromRow(row);
         }
         return oeelextra;
      }
	  
	  
      public IEnumerable<Oeelextra> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oeelextra.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oeelextra> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloeelextra.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oeelextra Get(int cono, int orderno, int ordersuf, int lineno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeelextra.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelextra.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelextra.ordersuf = {0}", ordersuf);
         }
         if (lineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelextra.lineno = {0}", lineno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Oeelextra BuildFromRow(DataRow row)
      {
         var returnRecord = Oeelextra.BuildOeelextraFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oeelextra>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oeelextra record)
      {
         Oeelextra.UpdateRowFromOeelextra(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oeelextra Insert(Oeelextra record)
      {
         DataRow row = this.dataSet.ttbloeelextra.NewttbloeelextraRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloeelextra.AddttbloeelextraRow((pdsoeelextraDataSet.ttbloeelextraRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloeelextra.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeelextra.Rows[0]) : null;
      }
  

      public Oeelextra Update(Oeelextra record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloeelextra.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeelextra.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oeelextra record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloeelextra.NewttbloeelextraRow();
            Oeelextra.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloeelextra.AddttbloeelextraRow((pdsoeelextraDataSet.ttbloeelextraRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oeelextra() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  