//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OE.Data.Adapters
{
   using com.infor.sxproxy.oeproxy;
   using com.infor.sxproxy.oeproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoeehp;

   public partial class OeehpAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoeehp";
      private OEProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> oeehpTableControlKey;
		
      private pdsoeehpDataSet dataSet;
        
      public OeehpAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OEProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsoeehpDataSet() { DataSetName = DataSetName };
            this.oeehpTableControlKey = this.dataSet.ttbloeehp.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.oeehpTableControlKey))
            {
               this.CreateTableControlParameters(this.oeehpTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OeehpAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Oeehpproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOeehpproxy = this.proxyAppObject.CreatePO_oeehpproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOeehpproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Oeehpproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.oeehpTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.oeehpTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Oeehpproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOeehpproxy = this.proxyAppObject.CreatePO_oeehpproxy())
               {
                  this.SetRequiredContextParameters();
                  poOeehpproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Oeehpproxy - After Call");
      }
   

      public Oeehp GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oeehp oeehp = null;
         if (row != null)
         {
             oeehp = this.BuildFromRow(row);
         }
         return oeehp;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oeehp) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloeehp.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oeehp> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oeehp)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oeehp Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloeehp.AsEnumerable().SingleOrDefault();
         Oeehp oeehp = null;
         if (row != null)
         {
             oeehp = this.BuildFromRow(row);
         }
         return oeehp;
      }
	  
	  
      public IEnumerable<Oeehp> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oeehp.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oeehp> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloeehp.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oeehp Get(int cono, string orderty, int orderno, int ordersuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeehp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(orderty)) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.orderty = '{0}'", orderty);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.ordersuf = {0}", ordersuf);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Oeehp> GetListByStatus(int cono, string statusty, int orderno, int ordersuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeehp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(statusty)) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.statusty = '{0}'", statusty);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.ordersuf = {0}", ordersuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oeehp> GetListByTracker(int cono, string trackerno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeehp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(trackerno)) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.trackerno = '{0}'", trackerno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oeehp> GetListByWhse(int cono, string whse, string statusty, string shipviaty, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeehp.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(statusty)) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.statusty = '{0}'", statusty);
         }
         if (!string.IsNullOrEmpty(shipviaty)) 
         {
             sb.AppendFormatWithEscape(" AND oeehp.shipviaty = '{0}'", shipviaty);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Oeehp BuildFromRow(DataRow row)
      {
         var returnRecord = Oeehp.BuildOeehpFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oeehp>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oeehp record)
      {
         Oeehp.UpdateRowFromOeehp(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oeehp Insert(Oeehp record)
      {
         DataRow row = this.dataSet.ttbloeehp.NewttbloeehpRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloeehp.AddttbloeehpRow((pdsoeehpDataSet.ttbloeehpRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloeehp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeehp.Rows[0]) : null;
      }
  

      public Oeehp Update(Oeehp record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloeehp.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeehp.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oeehp record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloeehp.NewttbloeehpRow();
            Oeehp.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloeehp.AddttbloeehpRow((pdsoeehpDataSet.ttbloeehpRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oeehp() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  