//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OE.Data.Adapters
{
   using com.infor.sxproxy.oeproxy;
   using com.infor.sxproxy.oeproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoerbt;

   public partial class OerbtAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoerbt";
      private OEProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> oerbtTableControlKey;
		
      private pdsoerbtDataSet dataSet;
        
      public OerbtAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OEProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsoerbtDataSet() { DataSetName = DataSetName };
            this.oerbtTableControlKey = this.dataSet.ttbloerbt.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.oerbtTableControlKey))
            {
               this.CreateTableControlParameters(this.oerbtTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OerbtAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Oerbtproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOerbtproxy = this.proxyAppObject.CreatePO_oerbtproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOerbtproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Oerbtproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.oerbtTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.oerbtTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Oerbtproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOerbtproxy = this.proxyAppObject.CreatePO_oerbtproxy())
               {
                  this.SetRequiredContextParameters();
                  poOerbtproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Oerbtproxy - After Call");
      }
   

      public Oerbt GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oerbt oerbt = null;
         if (row != null)
         {
             oerbt = this.BuildFromRow(row);
         }
         return oerbt;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oerbt) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloerbt.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oerbt> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oerbt)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oerbt Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloerbt.AsEnumerable().SingleOrDefault();
         Oerbt oerbt = null;
         if (row != null)
         {
             oerbt = this.BuildFromRow(row);
         }
         return oerbt;
      }
	  
	  
      public IEnumerable<Oerbt> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oerbt.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oerbt> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloerbt.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oerbt Get(int cono, string type, string filedt, string filetm, decimal custno, int orderno, int ordersuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oerbt.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(type)) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.type = '{0}'", type);
         }
         if (!string.IsNullOrEmpty(filedt)) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.filedt = '{0}'", filedt);
         }
         if (!string.IsNullOrEmpty(filetm)) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.filetm = '{0}'", filetm);
         }
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.custno = {0}", custno);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.ordersuf = {0}", ordersuf);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Oerbt> GetListByInvoice(int cono, string type, string filedt, string filetm, int orderno, int ordersuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oerbt.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(type)) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.type = '{0}'", type);
         }
         if (!string.IsNullOrEmpty(filedt)) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.filedt = '{0}'", filedt);
         }
         if (!string.IsNullOrEmpty(filetm)) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.filetm = '{0}'", filetm);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.ordersuf = {0}", ordersuf);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oerbt> GetListByOrder(int cono, string type, int orderno, int ordersuf, string filedt, string filetm, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oerbt.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(type)) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.type = '{0}'", type);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.ordersuf = {0}", ordersuf);
         }
         if (!string.IsNullOrEmpty(filedt)) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.filedt = '{0}'", filedt);
         }
         if (!string.IsNullOrEmpty(filetm)) 
         {
             sb.AppendFormatWithEscape(" AND oerbt.filetm = '{0}'", filetm);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Oerbt BuildFromRow(DataRow row)
      {
         var returnRecord = Oerbt.BuildOerbtFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oerbt>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oerbt record)
      {
         Oerbt.UpdateRowFromOerbt(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oerbt Insert(Oerbt record)
      {
         DataRow row = this.dataSet.ttbloerbt.NewttbloerbtRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloerbt.AddttbloerbtRow((pdsoerbtDataSet.ttbloerbtRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloerbt.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloerbt.Rows[0]) : null;
      }
  

      public Oerbt Update(Oerbt record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloerbt.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloerbt.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oerbt record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloerbt.NewttbloerbtRow();
            Oerbt.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloerbt.AddttbloerbtRow((pdsoerbtDataSet.ttbloerbtRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oerbt() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  