//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OE.Data.Adapters
{
   using com.infor.sxproxy.oeproxy;
   using com.infor.sxproxy.oeproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoeehcx;

   public partial class OeehcxAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoeehcx";
      private OEProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> oeehcxTableControlKey;
		
      private pdsoeehcxDataSet dataSet;
        
      public OeehcxAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OEProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsoeehcxDataSet() { DataSetName = DataSetName };
            this.oeehcxTableControlKey = this.dataSet.ttbloeehcx.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.oeehcxTableControlKey))
            {
               this.CreateTableControlParameters(this.oeehcxTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OeehcxAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Oeehcxproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOeehcxproxy = this.proxyAppObject.CreatePO_oeehcxproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOeehcxproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Oeehcxproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.oeehcxTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.oeehcxTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Oeehcxproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOeehcxproxy = this.proxyAppObject.CreatePO_oeehcxproxy())
               {
                  this.SetRequiredContextParameters();
                  poOeehcxproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Oeehcxproxy - After Call");
      }
   

      public Oeehcx GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oeehcx oeehcx = null;
         if (row != null)
         {
             oeehcx = this.BuildFromRow(row);
         }
         return oeehcx;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oeehcx) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloeehcx.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oeehcx> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oeehcx)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oeehcx Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloeehcx.AsEnumerable().SingleOrDefault();
         Oeehcx oeehcx = null;
         if (row != null)
         {
             oeehcx = this.BuildFromRow(row);
         }
         return oeehcx;
      }
	  
	  
      public IEnumerable<Oeehcx> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oeehcx.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oeehcx> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloeehcx.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oeehcx Get(int cono, int orderno, int ordersuf, string certifiedtype, string certifiedname, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeehcx.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehcx.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehcx.ordersuf = {0}", ordersuf);
         }
         if (!string.IsNullOrEmpty(certifiedtype)) 
         {
             sb.AppendFormatWithEscape(" AND oeehcx.certifiedtype = '{0}'", certifiedtype);
         }
         if (!string.IsNullOrEmpty(certifiedname)) 
         {
             sb.AppendFormatWithEscape(" AND oeehcx.certifiedname = '{0}'", certifiedname);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Oeehcx BuildFromRow(DataRow row)
      {
         var returnRecord = Oeehcx.BuildOeehcxFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oeehcx>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oeehcx record)
      {
         Oeehcx.UpdateRowFromOeehcx(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oeehcx Insert(Oeehcx record)
      {
         DataRow row = this.dataSet.ttbloeehcx.NewttbloeehcxRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloeehcx.AddttbloeehcxRow((pdsoeehcxDataSet.ttbloeehcxRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloeehcx.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeehcx.Rows[0]) : null;
      }
  

      public Oeehcx Update(Oeehcx record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloeehcx.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeehcx.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oeehcx record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloeehcx.NewttbloeehcxRow();
            Oeehcx.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloeehcx.AddttbloeehcxRow((pdsoeehcxDataSet.ttbloeehcxRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oeehcx() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  