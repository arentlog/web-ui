//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OE.Data.Adapters
{
   using com.infor.sxproxy.oeproxy;
   using com.infor.sxproxy.oeproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoeehextra;

   public partial class OeehextraAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoeehextra";
      private OEProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> oeehextraTableControlKey;
		
      private pdsoeehextraDataSet dataSet;
        
      public OeehextraAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OEProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsoeehextraDataSet() { DataSetName = DataSetName };
            this.oeehextraTableControlKey = this.dataSet.ttbloeehextra.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.oeehextraTableControlKey))
            {
               this.CreateTableControlParameters(this.oeehextraTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OeehextraAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Oeehextraproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOeehextraproxy = this.proxyAppObject.CreatePO_oeehextraproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOeehextraproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Oeehextraproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.oeehextraTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.oeehextraTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Oeehextraproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOeehextraproxy = this.proxyAppObject.CreatePO_oeehextraproxy())
               {
                  this.SetRequiredContextParameters();
                  poOeehextraproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Oeehextraproxy - After Call");
      }
   

      public Oeehextra GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oeehextra oeehextra = null;
         if (row != null)
         {
             oeehextra = this.BuildFromRow(row);
         }
         return oeehextra;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oeehextra) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloeehextra.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oeehextra> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oeehextra)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oeehextra Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloeehextra.AsEnumerable().SingleOrDefault();
         Oeehextra oeehextra = null;
         if (row != null)
         {
             oeehextra = this.BuildFromRow(row);
         }
         return oeehextra;
      }
	  
	  
      public IEnumerable<Oeehextra> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oeehextra.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oeehextra> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloeehextra.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oeehextra Get(int cono, int orderno, int ordersuf, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeehextra.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehextra.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehextra.ordersuf = {0}", ordersuf);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Oeehextra BuildFromRow(DataRow row)
      {
         var returnRecord = Oeehextra.BuildOeehextraFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oeehextra>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oeehextra record)
      {
         Oeehextra.UpdateRowFromOeehextra(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oeehextra Insert(Oeehextra record)
      {
         DataRow row = this.dataSet.ttbloeehextra.NewttbloeehextraRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloeehextra.AddttbloeehextraRow((pdsoeehextraDataSet.ttbloeehextraRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloeehextra.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeehextra.Rows[0]) : null;
      }
  

      public Oeehextra Update(Oeehextra record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloeehextra.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeehextra.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oeehextra record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloeehextra.NewttbloeehextraRow();
            Oeehextra.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloeehextra.AddttbloeehextraRow((pdsoeehextraDataSet.ttbloeehextraRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oeehextra() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  