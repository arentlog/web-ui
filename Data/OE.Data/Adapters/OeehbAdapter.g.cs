//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OE.Data.Adapters
{
   using com.infor.sxproxy.oeproxy;
   using com.infor.sxproxy.oeproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoeehb;

   public partial class OeehbAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoeehb";
      private OEProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> oeehbTableControlKey;
		
      private pdsoeehbDataSet dataSet;
        
      public OeehbAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OEProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsoeehbDataSet() { DataSetName = DataSetName };
            this.oeehbTableControlKey = this.dataSet.ttbloeehb.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.oeehbTableControlKey))
            {
               this.CreateTableControlParameters(this.oeehbTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OeehbAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Oeehbproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOeehbproxy = this.proxyAppObject.CreatePO_oeehbproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOeehbproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Oeehbproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.oeehbTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.oeehbTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Oeehbproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOeehbproxy = this.proxyAppObject.CreatePO_oeehbproxy())
               {
                  this.SetRequiredContextParameters();
                  poOeehbproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Oeehbproxy - After Call");
      }
   

      public Oeehb GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oeehb oeehb = null;
         if (row != null)
         {
             oeehb = this.BuildFromRow(row);
         }
         return oeehb;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oeehb) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloeehb.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oeehb> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oeehb)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oeehb Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloeehb.AsEnumerable().SingleOrDefault();
         Oeehb oeehb = null;
         if (row != null)
         {
             oeehb = this.BuildFromRow(row);
         }
         return oeehb;
      }
	  
	  
      public IEnumerable<Oeehb> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oeehb.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oeehb> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloeehb.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oeehb Get(int cono, string batchnm, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeehb.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(batchnm)) 
         {
             sb.AppendFormatWithEscape(" AND oeehb.batchnm = '{0}'", batchnm);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehb.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Oeehb> GetListByCustno(int cono, decimal custno, int orderno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeehb.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (custno != decimal.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehb.custno = {0}", custno);
         }
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeehb.orderno = {0}", orderno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Oeehb BuildFromRow(DataRow row)
      {
         var returnRecord = Oeehb.BuildOeehbFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oeehb>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oeehb record)
      {
         Oeehb.UpdateRowFromOeehb(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oeehb Insert(Oeehb record)
      {
         DataRow row = this.dataSet.ttbloeehb.NewttbloeehbRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloeehb.AddttbloeehbRow((pdsoeehbDataSet.ttbloeehbRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloeehb.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeehb.Rows[0]) : null;
      }
  

      public Oeehb Update(Oeehb record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloeehb.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeehb.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oeehb record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloeehb.NewttbloeehbRow();
            Oeehb.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloeehb.AddttbloeehbRow((pdsoeehbDataSet.ttbloeehbRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oeehb() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  