//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OE.Data.Adapters
{
   using com.infor.sxproxy.oeproxy;
   using com.infor.sxproxy.oeproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoepick;

   public partial class OepickAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoepick";
      private OEProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> oepickTableControlKey;
		
      private pdsoepickDataSet dataSet;
        
      public OepickAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OEProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsoepickDataSet() { DataSetName = DataSetName };
            this.oepickTableControlKey = this.dataSet.ttbloepick.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.oepickTableControlKey))
            {
               this.CreateTableControlParameters(this.oepickTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OepickAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Oepickproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOepickproxy = this.proxyAppObject.CreatePO_oepickproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOepickproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Oepickproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.oepickTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.oepickTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Oepickproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOepickproxy = this.proxyAppObject.CreatePO_oepickproxy())
               {
                  this.SetRequiredContextParameters();
                  poOepickproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Oepickproxy - After Call");
      }
   

      public Oepick GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oepick oepick = null;
         if (row != null)
         {
             oepick = this.BuildFromRow(row);
         }
         return oepick;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oepick) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloepick.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oepick> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oepick)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oepick Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloepick.AsEnumerable().SingleOrDefault();
         Oepick oepick = null;
         if (row != null)
         {
             oepick = this.BuildFromRow(row);
         }
         return oepick;
      }
	  
	  
      public IEnumerable<Oepick> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oepick.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oepick> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloepick.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oepick Get(int cono, string oper2, string reportnm, string whse, string prod, string serlottype, string serlotno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oepick.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (!string.IsNullOrEmpty(oper2)) 
         {
             sb.AppendFormatWithEscape(" AND oepick.oper2 = '{0}'", oper2);
         }
         if (!string.IsNullOrEmpty(reportnm)) 
         {
             sb.AppendFormatWithEscape(" AND oepick.reportnm = '{0}'", reportnm);
         }
         if (!string.IsNullOrEmpty(whse)) 
         {
             sb.AppendFormatWithEscape(" AND oepick.whse = '{0}'", whse);
         }
         if (!string.IsNullOrEmpty(prod)) 
         {
             sb.AppendFormatWithEscape(" AND oepick.prod = '{0}'", prod);
         }
         if (!string.IsNullOrEmpty(serlottype)) 
         {
             sb.AppendFormatWithEscape(" AND oepick.serlottype = '{0}'", serlottype);
         }
         if (!string.IsNullOrEmpty(serlotno)) 
         {
             sb.AppendFormatWithEscape(" AND oepick.serlotno = '{0}'", serlotno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public Oepick BuildFromRow(DataRow row)
      {
         var returnRecord = Oepick.BuildOepickFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oepick>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oepick record)
      {
         Oepick.UpdateRowFromOepick(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oepick Insert(Oepick record)
      {
         DataRow row = this.dataSet.ttbloepick.NewttbloepickRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloepick.AddttbloepickRow((pdsoepickDataSet.ttbloepickRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloepick.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloepick.Rows[0]) : null;
      }
  

      public Oepick Update(Oepick record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloepick.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloepick.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oepick record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloepick.NewttbloepickRow();
            Oepick.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloepick.AddttbloepickRow((pdsoepickDataSet.ttbloepickRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oepick() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  