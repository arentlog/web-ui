//------------------------------------------------------------------------------
// 
//     This code was generated by a tool.
//     Template version $Rev: 21496 $
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
//
//     (c) Infor Global Solutions 2018
// 
//------------------------------------------------------------------------------

#pragma warning disable 1591
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using NLog;
using ServiceInterfaceClient.BaseClasses;
using ServiceInterfaceClient.Extensions;
using ServiceInterfaceClient.Helpers;
using ServiceInterfaceClient.Progress;
    
namespace Infor.Sxe.OE.Data.Adapters
{
   using com.infor.sxproxy.oeproxy;
   using com.infor.sxproxy.oeproxy.StrongTypesNS;
    
   using Models.PdsContext;
   using Models.Pdsoeelc;

   public partial class OeelcAdapter : AdapterBase<pdsContextDataSet>
   {
      private const string DataSetName = "pdsoeelc";
      private OEProxyAppObject proxyAppObject;
      private readonly Tuple<string, string> oeelcTableControlKey;
		
      private pdsoeelcDataSet dataSet;
        
      public OeelcAdapter(IProgressConnection connection) : base(connection)
      {
         try 
         {
            this.proxyAppObject = new OEProxyAppObject(connection.Connection);
            this.pdsContext = new pdsContextDataSet();
            this.dataSet = new pdsoeelcDataSet() { DataSetName = DataSetName };
            this.oeelcTableControlKey = this.dataSet.ttbloeelc.GetTableControlParametersKey();
            
            if (!this.tempTableControlParameters.ContainsKey(this.oeelcTableControlKey))
            {
               this.CreateTableControlParameters(this.oeelcTableControlKey);
            }
            this.OnCreated();
         }
         catch (Exception ex)
         {
            NLogLogger.ErrorException("Failed in adapter ", ex);
            ErrorReportingHelper.ReportProgramErrors($"Error in OeelcAdapter constructor - {ex.Message}");
         }
      }
  
      partial void OnCreated();
    
      protected void FetchWhere(string where, int batchsize, string fldList)
      {
         NLogLoggerP.Trace("Fetchwhere - Oeelcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               this.SetAndFetch(where, batchsize == 0 ? DefaultFetchWhereRowCount : batchsize, fldList);
               using (var poOeelcproxy = this.proxyAppObject.CreatePO_oeelcproxy())
               {
                  var cErrorMessage = string.Empty;
                  this.SetRequiredContextParameters();
                  poOeelcproxy.FetchWhere(ref this.pdsContext, out this.dataSet, out cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("Fetchwhere - Oeelcproxy - After Call");
      }
      
      private void SetAndFetch(string where, int batchsize, string fldList)
      {
         MappedDiagnosticsContext.Set("where", where);
         this.SetFetchWhereParameters(this.oeelcTableControlKey, where, batchsize, fldList);
         this.SetTableParametersOnContext(this.oeelcTableControlKey, true);
      }
  
      protected void SaveChanges()
      {
         NLogLoggerP.Trace("SaveChanges - Oeelcproxy - Before Call");
         StopwatchUtil.Time(
         () =>
            {
               using (var poOeelcproxy = this.proxyAppObject.CreatePO_oeelcproxy())
               {
                  this.SetRequiredContextParameters();
                  poOeelcproxy.SaveChanges(ref this.pdsContext, ref this.dataSet, out string cErrorMessage);
                  ErrorReportingHelper.ReportErrors(cErrorMessage);
                  this.ReportErrors(this.pdsContext);
                  this.ReportErrors(this.dataSet);
               }
            });
         NLogLoggerP.Info("SaveChanges - Oeelcproxy - After Call");
      }
   

      public Oeelc GetByRowId(string rowId, string fldList)
      {
         var row = this.GetRowByRowId(rowId, fldList);
         Oeelc oeelc = null;
         if (row != null)
         {
             oeelc = this.BuildFromRow(row);
         }
         return oeelc;
      }
   
      private DataRow GetRowByRowId(string rowId, string fldList)
      {
         var where = new StringBuilder();
         where.AppendFormat("rowid(oeelc) = to-rowid('0x{0}')", rowId.ToClauseString());
         this.FetchWhere(where.ToString(), 1, fldList);
         return this.dataSet.ttbloeelc.AsEnumerable().SingleOrDefault();
      }
        
      public IEnumerable<Oeelc> GetListByRowIdList(List<string> rowIds, int batchsize, string fldList)
      {
         var where = new StringBuilder();
         if (rowIds != null && rowIds.Count > 0)
         {
           foreach (var rowId in rowIds)
           {
              if (where.ToString().Length > 0)
              {
                 where.Append(" OR ");
              }
              where.AppendFormat("rowid(oeelc)=to-rowid('0x{0}')", rowId.ToClauseString());
           }
         }
         return this.GetList(where.ToString(), batchsize, fldList);
      }

      protected Oeelc Fetch(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         var row = this.dataSet.ttbloeelc.AsEnumerable().SingleOrDefault();
         Oeelc oeelc = null;
         if (row != null)
         {
             oeelc = this.BuildFromRow(row);
         }
         return oeelc;
      }
	  
	  
      public IEnumerable<Oeelc> GetListAllByCono(int cono, int batchsize, string fldList)
      {   
         cono = cono == 0 ? this.connection.CompanyNumber : cono;
         var sb = new StringBuilder();
         sb.AppendFormat("oeelc.cono = {0}", cono);
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public IEnumerable<Oeelc> GetList(string where, int batchsize, string fldList)
      {
         this.FetchWhere(where, batchsize, fldList);
         return (from DataRow row in this.dataSet.ttbloeelc.Rows where row != null select this.BuildFromRow(row)).ToList();
      }

      public Oeelc Get(int cono, int orderno, int ordersuf, int dspllineno, string linetype, int seqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeelc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelc.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelc.ordersuf = {0}", ordersuf);
         }
         if (dspllineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelc.dspllineno = {0}", dspllineno);
         }
         if (!string.IsNullOrEmpty(linetype)) 
         {
             sb.AppendFormatWithEscape(" AND oeelc.linetype = '{0}'", linetype);
         }
         if (seqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelc.seqno = {0}", seqno);
         }
         var where = sb.ToString();
         return this.Fetch(where, batchsize, fldList);
      }
  
      public IEnumerable<Oeelc> GetListByIntseqno(int cono, int orderno, int ordersuf, int dspllineno, int intseqno, int batchsize, string fldList)
      {   
         var sb = new StringBuilder();
         sb.AppendFormatWithEscape("oeelc.cono = {0}", cono == 0 ? this.connection.CompanyNumber : cono);
         if (orderno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelc.orderno = {0}", orderno);
         }
         if (ordersuf != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelc.ordersuf = {0}", ordersuf);
         }
         if (dspllineno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelc.dspllineno = {0}", dspllineno);
         }
         if (intseqno != int.MinValue) 
         {
             sb.AppendFormatWithEscape(" AND oeelc.intseqno = {0}", intseqno);
         }
         var where = sb.ToString();
         return this.GetList(where, batchsize, fldList);
      }

      public Oeelc BuildFromRow(DataRow row)
      {
         var returnRecord = Oeelc.BuildOeelcFromRow(row);
         returnRecord = this.BuildExtraFromRow<Oeelc>(returnRecord, row);
         return returnRecord;
      }
      
      public void UpdateToRow(ref DataRow row, Oeelc record)
      {
         Oeelc.UpdateRowFromOeelc(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
      }
      
      public Oeelc Insert(Oeelc record)
      {
         DataRow row = this.dataSet.ttbloeelc.NewttbloeelcRow();
         this.UpdateToRow(ref row, record);
         this.ExtraUpdateToRow(ref row, record);
         this.dataSet.ttbloeelc.AddttbloeelcRow((pdsoeelcDataSet.ttbloeelcRow)row);
         this.SaveChanges();
         return this.dataSet.ttbloeelc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeelc.Rows[0]) : null;
      }
  

      public Oeelc Update(Oeelc record)
      {
        var row = this.GetRowByRowId(record.rowID, string.Empty);
         if (row != null)
         {
            this.UpdateToRow(ref row, record);
            this.ExtraUpdateToRow(ref row, record);
            this.SaveChanges();
            return this.dataSet.ttbloeelc.Rows.Count > 0 ? this.BuildFromRow(this.dataSet.ttbloeelc.Rows[0]) : null;
         }
         ErrorReportingHelper.ReportErrors("global.update.doesnotexist", 421);
         return null;
      }
  
      public void Delete(Oeelc record)
      {
         var row = this.GetRowByRowId(record.rowID, string.Empty);         
         if (row == null)
         {
            row = this.dataSet.ttbloeelc.NewttbloeelcRow();
            Oeelc.BuildMinimalRow(ref row, record);
            this.dataSet.ttbloeelc.AddttbloeelcRow((pdsoeelcDataSet.ttbloeelcRow)row);
         }
         row.Delete();
         this.SaveChanges();
      }

      public void DeleteListByRowIds(List<string> rowIds)
      {
         if (rowIds != null)
         {
            var recList = rowIds.Select(selectRowId => new Oeelc() { rowID = selectRowId }).ToList();
            foreach (var rec in recList)
            {
               this.Delete(rec);
            }
         }
      }
	  

      protected override void Dispose(bool disposing)
      {
         if (this.disposed) { return; }
         if (!disposing)
         {
            return;
         }	
         this.proxyAppObject?.Dispose();

         this.dataSet?.Dispose();
         base.Dispose(true);
      }
  
   }
}
#pragma warning restore 1591
  